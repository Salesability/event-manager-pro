# Eval: 0086-atlantic-dealer-import (chunk-end, shipped to prod)

**Plan:** docs/chunks/0086-atlantic-dealer-import/plan.md
**Branch:** main (`4e5781e` → `9fc62b6`, all 6 phases)
**Date:** 2026-06-19

---

## Verdict: PASS with warnings — shipped to prod

All 6 phases complete and live on prod. The mid-chunk prod-overlap probe pivoted the
dedup from a broken address-key to an owner-vetted reconciliation worksheet (D8); the
import was re-validated end-to-end on sandbox and then run against prod with verified
counts + idempotency.

**Static checks:** tsc PASS · tests **1182 pass / 2 skip** (serial) PASS · lint **0 new** (the ~253 raw problems are the pre-existing `.claude/worktrees/` stale-copy noise)
**Browser smoke:** PASS (this session) — `/dealerships` list (imported prospects render) + `/dealerships/[id]` detail (province, status, no console errors)
**Codex review:** the runtime/app code (src/) is byte-unchanged since [eval-2026-06-19-1359](eval-2026-06-19-1359.md) (PASS-with-warnings, 0 High/0 Med/2 Low). The delta since is **scripts-only** (`atlantic-reconcile.mjs`, the `import-atlantic-dealers.ts` rewire) + data/docs — validated empirically against real prod (see below) rather than re-reviewed.

---

## What shipped

- **Schema:** `dealers.phone` / `.manufacturer` / `.notes` (nullable, migration `0041`) — applied sandbox + **prod** (DB `0040`→`0041`).
- **QBO push:** `mapDealerToCustomer` prefers `dealers.phone` for `PrimaryPhone`; `loadDealer` projects the new cols (+4 unit tests). No behavior change when phone is null.
- **Import:** `scripts/import-atlantic-dealers.ts` + pure mapper `src/features/dealers/atlantic-import.ts` (13 CI tests) + committed `scripts/data/atlantic-dealers.json` (281 rows + drop-list).
- **Dedup pivot (D8):** prod overlap was large + messy (67 name-matches; dealer groups share phones/postal across brand rooftops; BD list is city-only). Built `scripts/atlantic-reconcile.mjs` → owner-vetted `scripts/data/atlantic-reconciliation.csv` (**188 import-new · 86 skip-existing**); the import honors `suggested_action` (never guesses).

## Live validation

- **Sandbox** (reset between approaches): worksheet-driven real run = 188 dealers / 302 contacts / 312 links; **re-run = 0 inserts** (idempotent); spot-checks pass.
- **Prod** (2026-06-19): dry-run = 188 import-new / 86 skip / 0 unvetted / 0 already-by-name+addr; real run = **188 prospect dealers + 294 contacts + 312 links**; **0 active / 0 QB-linked** (no QB writes); total prod dealers **333 = 145 + 188 (no dups)**; province NB:62/NL:40/NS:71/PE:15; O'Regan's 12 brand rooftops in / 2 Hyundai skipped; Central Nova Hyundai+Subaru in; read-only re-run dry-run = 0 dealers would-insert.

---

## Action Required

### Must Fix / Should Fix
- None. Chunk shipped + verified on prod.

### Consider (parked follow-ups)
1. **0086-a** — no DB partial-unique on dealers `lower(name)+lower(coalesce(address,''))`; dedup is application-level (intent non-goal). Un-park if dealer-create becomes concurrent / web-intake.
2. **0086-b** — contact dedup reuses any active identifier even on an archived contact (matches 0085 app-wide). Revisit app-wide, not per-import.
3. **0086-c** — the **86 skip-existing** prod dealers did NOT receive the BD GM/SM contacts (chose skip-not-enrich, D8). A future enrichment pass could add those contacts to the existing dealers.
4. **0086-d** — prod QBO `DisplayName` overlap was never folded into the worksheet (prod QBO token expired 2026-06-17). Optional 3rd confirming signal; not blocking (the app-dealer overlap was authoritative and those dealers are already QBO-linked).

---

## Coverage Caveats

- Codex was not re-run on the final diff; the runtime code was unchanged since the prior eval and the scripts were validated against real prod. The prior eval's Codex pass had a synthesis-truncation (reconstructed from log).
- The prod re-run idempotency was confirmed via a **read-only dry-run** (0 dealers would-insert); the dry-run reports a small name-only contact count it can't dedup without real dealer ids — the sandbox **real** re-run proved that resolves to 0 inserts.
