# Active chunk

**Plan:** _None — pick a new plan._

_(0082-quote-msa-decouple built + closed 2026-06-15 — see **Last chunk** below; **code-only + migration `0040` (DROP `quotes.msa_id`), sandbox-applied, NOT yet on prod**. 0081-button-consolidation deployed to prod 2026-06-15, rev `-00021-skx`. 0079-quote-document-library stays deferred in `future/`.)_

**Last chunk:** `0082-quote-msa-decouple` **built + closed 2026-06-15** → [`closed/`](closed/0082-quote-msa-decouple/plan.md). Broke the quote out of the MSA/BoldSign envelope (unwinds the 0055/0061 bundle). The **MSA now e-signs on its own BoldSign envelope** (MSA pages only), sent from the per-dealer panel on `/dealerships/[id]` (**admin-only** — eval-tightened the `createMsaDraft`/`sendMsaEnvelope` capability `msa:edit` → `admin:access` to match); signing flips **only** the MSA (deleted `acceptBundledQuote` + `markQuoteAcceptedViaEnvelope` + `combineQuoteAndMsa`/`src/lib/pdf/merge.ts`). **Every quote accepts via a new staff "Customer decision" Accept/Decline card** (`quote-status-actions.tsx`) on `/quotes/[id]`, gated: a `sent` quote needs the dealer to have a **current (unexpired) active MSA** (`acceptQuote` checks `status='active' AND expires_at >= now()`). Dropped `quotes.msa_id` FK+index (**migration `0040`**, sandbox-applied). 5 phases: P1 decision gate (`decision.md` D1–D5) → P2 MSA-only envelope `a719f66` → P3 accept control+gate `ac1aec6` → P4 remove coupling + drop column `4edc75c` → P5 wiki `df41510`; in-cycle eval fixes `72c2231` (admin-only cap + expiry-aware gate). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0082-quote-msa-decouple/eval-2026-06-15-1945.md)): static clean (tsc, **0 new lint**, full suite green bar the parked pooler flake), browser smoke **6/6** (both accept-gate paths + the relocated MSA send action), Codex **3 High / 0 Med / 0 Low — 2 fixed in-cycle, 1 parked (0082-a)**. Committed directly to `main`. **Sandbox-only — migration `0040` + code NOT on prod.** Wiki: rewrote [commercial-spine.md](../wiki/commercial-spine.md) ("The MSA envelope (standalone)" + "Accepting a Quote") + [data-model.md](../wiki/data-model.md) (dropped `quotes.msa_id`).

_(0081-button-consolidation built + closed 2026-06-15 — see further below; **✅ deployed to prod 2026-06-15, rev `-00021-skx`** (code-only, no migration; incl. the create-action consistency follow-up). 0079-quote-document-library stays deferred in `future/` until the owner makes the two product calls — see **Future** below.)_

---

**Last QBO milestone:** the bidirectional + tax-source effort (0070/0071/0073/0074/**0075**) is **functionally complete + live-verified** on the CA sandbox (Estimate `182`: HST ON 13% → $2260; **ON's rate is now QB-sourced** via 0075 name-match + adoption), and **all of it is now deployed to prod** (rev `-00013-fc9`, DB `0035`, 2026-06-10). Remaining **prod activation** (not build, owner steps): (1) **Connect** prod QBO at `/admin/quickbooks` with the real Salesability production QBO admin login (prod `quickbooks_connection` is empty); (2) confirm the prod company's **tax model** (manual vs Automated Sales Tax — 0074 assumes manual); (3) **map each operating province → its QB tax code on `/admin/lookups`** (0076 — single or group code; the old "Pull tax codes" heuristic is retired), then push a quote. 0075/0076 (QB as tax-rate source + the mapping UI) are **built + closed** — see Last chunk. **Owner setup pending in QB:** add Quebec (+ any GST+PST provinces) so they have codes to map, and fix the wrong per-customer default codes (two NB dealers point at NS codes — see the 0076 session probe).

**Last chunk:** `0081-button-consolidation` **built + closed 2026-06-15** → [`closed/`](closed/0081-button-consolidation/plan.md). Consolidated the app's buttons onto the shared **Catalyst `Button`** and standardized the primary-action color. **Brand blue (`color="brand"`) is now the one primary color** (green retired from primary; semantic `<Badge color="green">` kept) — swapped 7 green CTAs → brand. Migrated ~50 raw `<button>` / duplicated `submitClass`/`buttonClass`-style constants across ~25 files onto `Button`. Extended `button.tsx` with a **`compact`** size (dense admin rows) + a soft-red **`destructive`** variant (pale fill + soft red border + dark text — owner inspiration, the Supabase "Delete project" look). Intentional exceptions left raw (filter pills, field-label text links, icon/text-link removes, CSV-download+Print toolbars, bespoke auth-page buttons, component primitives) — see [decision.md](closed/0081-button-consolidation/decision.md). 5 phases: P1 green→brand `c69c11f` → P2 foundation+constants `5d20fc8` → P3 inline buttons `dd88395` (P4 decisions rode in P3, doc-only). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0081-button-consolidation/eval-2026-06-15-1643.md)): static clean (tsc, 1136 serial, **0 new lint**), browser smoke **7/7 routes PASS** (both server-page submit buttons render at runtime, 0 console errors), Codex **0 High / 1 Medium / 0 Low — the Medium (RSC boundary) is a verified false positive, 0 parked**. Committed directly to `main`. **Code-only, no migration. ✅ Deployed to prod 2026-06-15** (rev `event-manager-pro-00021-skx`, image `:20260615-215156` — base 0081 first shipped at `-00020-n54`; same-day follow-up `6859439` promoted "+ Add Person"/"+ Add Dealer" → brand-blue primary; domain healthy; see **Last deploy** below). Wiki: new "Buttons (the one primitive)" section in [layout.md](../wiki/layout.md). Follow-up parked: danger Callout panel (≈0082).

**Prior chunk:** `0080-remove-quote-tax-override` **built + closed 2026-06-15** → [`closed/`](closed/0080-remove-quote-tax-override/plan.md). Removed the **per-quote manual tax override** (owner: *"QB manages tax, I do not think we need an override"*). QuickBooks owns the province rate (0075/0076) and the QBO Estimate push already rejected overridden quotes, so the override was redundant + a footgun. The composer **Tax field is now display-only** (auto, QB-sourced province rate + the `auto · X%` pill; no Override link/input); `computePickedTotals` lost its `override` opt, `parseTaxOverride`/`setQuoteTax` deleted, `createQuote`/`setQuoteInputs`/`setQuoteDealer` recompute auto tax + now clear any stale `tax_override` on save. **Column kept (Option A, expand→contract — no migration):** `quotes.tax_override` retained-but-unused (preserves pre-0080 history + the QB-push defensive guard; droppable later) — [decision.md](closed/0080-remove-quote-tax-override/decision.md). 5 phases: P1 decision gate (keep-the-column) → P2 composer `9f8ff43` → P3 server+pricing `df92f16` → P4 schema comment `60bed1d` → P5 wiki `53a6978`; in-cycle eval fix `2c893f9` (clear stale historical override on re-save + stale-doc fixes). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0080-remove-quote-tax-override/eval-2026-06-15-0934.md)): static clean (tsc, 1136 tests serial, **0 new lint**), browser smoke PASS (Tax field has no Override link), Codex **0 High / 1 Med / 1 Low — both fixed in-cycle, 0 parked**. Committed directly to `main`. **Code-only, NO migration — NOT yet on prod** (deploy when ready). **Deferred:** the prod data-impact count (how many prod quotes carry a historical override) + a future contract migration to DROP the column.

**Prior chunk:** `0078-quote-attachments` **built + closed 2026-06-12** → [`closed/`](closed/0078-quote-attachments/plan.md). Local-file **quote attachments**: a coach uploads supporting paperwork (forms, banking info, waivers) on the **Send Quote** dialog's new **Documents** section; `sendQuote` fetches each file from GCS and appends it to the outgoing email next to the quote PDF. New `quote_attachments` table (migration **`0038`**, `quote_id` CASCADE + snapshot cols + RLS), two `quote:edit`-gated Server Actions `uploadQuoteAttachment` / `removeQuoteAttachment`, a shared caps/MIME module (`src/features/quotes/attachments.ts` — PDF/PNG/JPG/WEBP/docx/xlsx, ≤10 MB/file, ≤20 MB total), and the **fail-closed-before-the-transition** size guard. Two new `audit_action` enum values (migration **`0039`**). The set **persists** (a re-send re-attaches without re-uploading) and the `quote.sent` audit denorms what went out. Builds the **spine 0079 reuses** (the deferred document-library adds a nullable `document_id` FK). 4 phases: P1 schema `42d8259` → P2 upload UI + actions `bbb03be` → P3 sendQuote wiring `49cba17` → P4 integration tests + wiki `106d5b8`; in-cycle eval fixes `59c01a0` (terminal-quote remove guard + filename control-char strip). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0078-quote-attachments/eval-2026-06-12-2043.md)): static clean (tsc, tests 1152, **0 new lint** — the raw 147-error count is a pre-existing stale-worktree artifact), browser smoke PASS (Documents section + Choose-Files render in the Re-send dialog on `/quotes/11`), Codex **1 High + 4 Med + 2 Low — 2 fixed in-cycle, 5 parked as 0078 (a)–(e)** (all UI-gated/single-admin concurrency or hardening nuances). Committed directly to `main`. **Sandbox-only — migrations `0038`+`0039` + code NOT on prod.**

**Prior chunk:** `0077-calendar-distribution` **built + closed 2026-06-12** → [`closed/`](closed/0077-calendar-distribution/plan.md). Google Calendar projection: a booked campaign auto-projects into the **coach + dealer** personal calendars (guest invites) + a shared colour-by-coach team calendar — **one-way**, app stays source of truth, **best-effort** (a Google failure never blocks the booking). Keyless DWD auth (`src/lib/google/calendar.ts` — signJwt impersonation, no key file); pure mapper `mapCampaignToGcalEvent` (customer-safe event — **no** ops-field leak, all-day EXCLUSIVE end, coach colour derived `(id%11)+1`); status-driven `reconcileCampaignCalendar` (`src/features/schedule/calendar-sync.ts`) wired into create/update/cancel + a `resyncCampaign` recovery action + an event-detail **Calendar** badge. Schema migration **`0037`** (`campaigns.gcal_event_id` partial-unique + `gcal_sync_status` enum + `gcal_synced_at`) **applied to sandbox** ([decision.md §7](closed/0077-calendar-distribution/decision.md)). 6 phases (P0 owner-setup + P1 client done pre-session; P2 mapper `dc733e0` → P3 schema `dc255a5` → P4 wiring `1529c2f` → P5 tests+smoke+wiki `ae6bb45`); in-cycle eval fixes `ba4d3cb` (orphan cleanup + stale-link 404 self-heal + never-throw + `missing` outcome) + `3aa1346` (CAS-guard the stale-link clear). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0077-calendar-distribution/eval-2026-06-12-1605.md)): static clean (tsc, 0 new lint, tests green in isolation — full-parallel hits the parked 0073 pooler-cap flake), browser smoke `/calendar` renders clean (event-detail dialog not a11y-clickable — coverage caveat), Codex **1 High + 2 Med + 1 Low + 1 fix-introduced High — ALL fixed in-cycle, final pass clean (0 parked)**. Committed directly to `main`. **Sandbox-only — migration `0037` + code NOT on prod.** **Post-close hardening (2026-06-12):** live keyless+DWD round-trip **PASSED** (organizer = calendar display name **"EventPro"** — the owner's CHOSEN brand, no rename; "take it off the list" 2026-06-12); `sendUpdates` now gated on `APP_ENV` so only prod emails real guests (`9277f60`); prod `GOOGLE_CALENDAR_*` wired into `deploy.sh` (prod-only, keyless — `e18fbb4`). **✅ DEPLOYED to prod 2026-06-12** (rev `-00016-srt`, image `:20260612-215737`; migration `0037` applied first — see **Last deploy**). The feature is **ARMED** on prod (organizer "EventPro"; real invites on the next campaign mutation). **Only owner step left:** share the calendar read-only to staff (overlay only — doesn't gate guest invites). See [go-live §4a](../wiki/go-live-accounts.md).

**Prior chunk:** `0076-quickbooks-tax-code-mapping` **built + closed 2026-06-11** → [`closed/`](closed/0076-quickbooks-tax-code-mapping/plan.md). Replaces 0075's fragile auto-apply name heuristic with an **explicit in-app province → QB-tax-code mapping** on `/admin/lookups`: a per-province dropdown of live QBO codes (**single or group** — QC's GST+QST = 14.975%, BC's GST+PST; rate adopted via the group-aware `resolveCodeRatePct`), `assignProvinceTaxCode` sets the code + adopts its rate, a **"Refresh rates"** action (`refreshTaxRates`) re-syncs mapped provinces' rates only (compare-and-set, never re-maps). The 0075 matcher is **demoted to a dropdown suggestion**; the **auto-apply "Pull tax codes" button is retired** (it mis-mapped real prod data: NS stale `HST NS` 15% vs `HST NS 2025` 14%; the shared `HST Atlantic 15%` code). **Row model** (keep all 13 seeded rows; managed ⇔ `quickbooks_tax_code_id` non-null; **no migration**) — [decision.md](closed/0076-quickbooks-tax-code-mapping/decision.md). 6 phases: P1 gate `52f712d` → P2 view-model `ad90cc9` → P3 mapping UI+assign `bb51dcb` → P4 refresh+retire `f83399d` → P5 guard message `7c2c5d7` → P6 wiki `41d2f26`; in-cycle eval fixes `45b4b60` (lint) + `c4a07b5` (reject unresolvable-rate map + refresh compare-and-set). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0076-quickbooks-tax-code-mapping/eval-2026-06-11-1014.md)): static clean (1109 serial, tsc, 0 new lint), browser smoke 2/2 (mapping UI on `/admin/lookups`; Pull-tax-codes gone from `/admin/quickbooks`), Codex **1 High / 1 Medium — both fixed in-cycle (re-review confirmed)** / 2 Low parked (**0076-a**, **0076-b**). Committed directly to `main`. **Sandbox-only — code NOT on prod; no migration.** **Pending verify:** a live group-code (QC) Estimate push (needs QC set up in the QB company first).

**Prior chunk:** `0075-quickbooks-tax-rate-source` **built + closed 2026-06-10** → [`closed/`](closed/0075-quickbooks-tax-rate-source/plan.md). Makes **QuickBooks the source of truth for province tax rates** (the tax-rate analogue of 0071 making QB the item master). The admin **"Pull tax codes"** sync now **name-matches** each province to the QBO `TaxCode` whose name identifies it ("HST ON" → ON; `resolveProvinceLinksByName` in `src/lib/quickbooks/tax-sync.ts`, replacing 0074's match-by-rate) and **adopts that code's rate** into `tax_rates.rate`; unmatched/ambiguous provinces keep their app rate, flagged unmanaged (`quickbooks_tax_code_id` null — managed is **inferred**, no new column/migration). The **in-app tax-rate editor was removed** (deleted `tax-rates/{actions,tax-rates-admin,tax-rate-schema*}` + the `/admin/lookups` render + the gate-matrix row; `lookup:edit` capability + `queries.ts` quote-path kept). **Phase 1 was a research/decision gate** → owner chose **name heuristic** (auto, no manual map), **editor removed entirely** (not read-only), **per-province override deferred** ([decision.md](closed/0075-quickbooks-tax-rate-source/decision.md)). 5 phases: P1 gate `e883e4a` → P2 matcher `1de4bb1` → P3 rate adoption `4262b5f` → P4 editor removal `c562cbd` → P5 wiki `05ff72c`; in-cycle eval fixes `e98862b` (case-sensitive ON + name-first ambiguity + empty-read guard) + `aecbd9e` (trailing-token name anchor). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0075-quickbooks-tax-rate-source/eval-2026-06-10-1557.md)): static clean (1092 serial, tsc, 0 new lint), browser smoke 3/3 (editor gone on `/admin/lookups`; "Tax rates" + Pull-tax-codes on `/admin/quickbooks`), Codex **1 High / 2 Medium / 1 Low — all fixed in-cycle**; 1 speculative Low parked (**0075-a**). **ON rate-adoption live-verified on the CA sandbox** (rolled-back DB integration test). Committed directly to `main`. **Sandbox-only — code NOT on prod; no migration. Multi-province adoption deferred to prod** (CA sandbox has only Ontario).

**Prior chunk:** `0074-quickbooks-tax-alignment` **built + closed 2026-06-10** → [`closed/`](closed/0074-quickbooks-tax-alignment/plan.md). Makes a pushed quote→Estimate carry **correct tax**: admin **"Pull tax codes"** maps each province's rate → a QBO `TaxCode.Id` (`tax_rates.quickbooks_tax_code_id`, by rate, unambiguous/1:1-only — `src/lib/quickbooks/tax-sync.ts`), and the Estimate push sets `TxnTaxDetail.TxnTaxCodeRef` so **QBO computes the tax** (replacing the dropped `TotalTax` override that 0073's smoke exposed). Pre-flight fails closed on an unmapped taxed province, a manual `tax_override`, or **rate drift** (quote snapshot tax ≠ current province rate). **Phase 1 was a research gate** — owner created a **CA sandbox `9341457252668239`** ([[project_qbo_realms]]); a probe found it uses **manual sales tax (not AST)** so codes are honored, txn-level `TxnTaxCodeRef`, "HST ON" 13% ([decision.md](closed/0074-quickbooks-tax-alignment/decision.md)). 6 phases: research → `788ea03`→ wait, code commits `3bb47b4` (client tax reads) → `4ae5708` (tax-sync + migration `0035` + Pull-tax-codes action/button) → `19e0c5f` (push wiring) → `fac9d26` (rate-drift guard) → `9507372` (tests); in-cycle eval fix `963ee2e` (province-collision → ambiguous, not mislinked). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0074-quickbooks-tax-alignment/eval-2026-06-10-1259.md)): static clean (1095 tests serially, tsc, 0 new lint), browser smoke 1/1 (Pull-tax-codes button), Codex **1 High (fixed in-cycle) / 1 Medium (parked 0074-a) / 1 Low (intentional)**. Committed directly to `main`. **Sandbox-only — migration `0035` + code NOT on prod.**

**Prior chunk:** `0073-quote-estimate-push` **built + closed 2026-06-10** → [`closed/`](closed/0073-quote-estimate-push/plan.md). **Slice 3 (final build slice)** — admin-only **"Push to QuickBooks"** on `/quotes/[id]` pushes a `sent`/`accepted` quote → QBO **Estimate** (`src/lib/quickbooks/quote-push.ts` + `client.ts` Estimate helpers): `CustomerRef` ← `dealers.quickbooks_id` (0070), each line `ItemRef` ← `service_items.quickbooks_id` (0071), computed `tax` as a `TxnTaxDetail` override; **pre-flight check** fails closed unless the dealer + every line SKU are QBO-linked. Linked quote (`quotes.quickbooks_estimate_id` set) → update (read-before-write SyncToken); unlinked → create + guarded backfill (idempotent). 5 phases: `0f4a1df` (schema `quickbooks_estimate_id` + migration `0034`) → `788ea03` (Estimate client helpers) → `d2cc9c7` (`quote-push.ts` preflight+map+core) → `9a589fb` (Server Action + gate row + quote-page button) → `25090fd` (integration suite); in-cycle eval fix `308d5a8` (Amount = unit×qty for QBO line-consistency). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0073-quote-estimate-push/eval-2026-06-10-0911.md)): static clean (1067 tests serially, tsc, 0 new lint), browser smoke 1/1 (button renders on `/quotes/3`), Codex **0 High / 2 Medium — 1 fixed in-cycle, 1 parked as 0073 follow-up (a)**. Committed directly to `main`. **Sandbox-only — migration `0034` + code NOT on prod; a prod Estimate push is additionally gated on the prod `service_items` being QBO-linked.**

**Earlier chunk:** `0072-service-items-readonly-list` **built + closed 2026-06-09** → [`closed/`](closed/0072-service-items-readonly-list/plan.md). Small follow-up to 0071 — restores the catalog-view UI the 0071 CRUD-removal dropped. Adds a **read-only "Service items" list** on `/admin/quickbooks` (code · label · price · QB-linked? · archived?) via `loadServiceItemsForAdmin` + `service-items-list.tsx`, rendered above the QBO change-set and **independent of the QBO connection**. Pure additive read-only UI — **no schema/migration/Server Action/gate-matrix row**. 2 phases: `5a83d84` (loader + component + render test) → `3b45b34` (page wire); plus a drive-by `2535934` (made 0071's `item-sync` archive assertion resilient to shared sandbox state). Chunk-end `/eval` **clean PASS** ([eval](closed/0072-service-items-readonly-list/eval-2026-06-09-1900.md)): static clean (1047 tests, tsc, 0 new lint), browser smoke 1/1 (list renders "14 items · 14 linked"), **Codex 0 findings**. Committed directly to `main`. ✅ **Deployed to prod 2026-06-09** (rev `event-manager-pro-00012-fks`, code-only — no migration). The read-only catalog viewer is now live on prod `/admin/quickbooks`.

**Earlier chunk:** `0071-quickbooks-item-pull` **built + closed 2026-06-09** → [`closed/`](closed/0071-quickbooks-item-pull/plan.md). **Slice 2 — QuickBooks is the item master.** `service_items` is now a **read-through mirror** of the connected QBO company's Items (new `service_items.quickbooks_id`, migration `0033`, sandbox only); an admin **"Pull items"** on `/admin/quickbooks` makes the catalog match QBO: **create** new · **overwrite** linked `label`/`price`/`description` from QBO · **archive** QBO-removed · **hard-delete** legacy unlinked rows. **In-app item CRUD removed** (deleted `services/actions.ts` + `service-schema.ts` + `services-admin.tsx` + the `/admin/lookups` editor + 3 gate-matrix rows). History-safe (`quote_line_items` snapshots + `set null` FK). 5 phases: `8192b9a` (schema) → `194bcd0` (`fetchItems`+`item-sync.ts`) → `bad1005` (remove CRUD) → `aa6d0be` (admin change-set + `pullItemsFromQuickbooks` action) → `426df00` (integration suite); in-cycle eval hardening `7f86715`. Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0071-quickbooks-item-pull/eval-2026-06-09-1429.md)): static clean (1045 tests, tsc, 0 new lint), browser smoke 2/2 (Items change-set + Pull button on `/admin/quickbooks`; editor gone on `/admin/lookups`), Codex **1 High + 1 Medium + 1 Low — Medium+Low fixed in-cycle, High partially fixed in-cycle (zero-syncable destructive guard) + residual parked as 0071 follow-up (a)**. **Committed directly to `main`** (no feature branch this time) — `main` not pushed to `origin`. ✅ **Deployed to prod 2026-06-09** (rev `event-manager-pro-00011-nvm`; prod DB → `0033`). **The `quickbooks_id` link is the `ItemRef` for Slice 3.**

**Prior chunk:** `0070-quickbooks-dealer-push` **built + closed 2026-06-09** → [`closed/`](closed/0070-quickbooks-dealer-push/plan.md). **Slice 1 of the bidirectional QuickBooks effort** (owner: "can we make it bidirectional"). Adds an admin **"Push to QuickBooks"** button on `/dealerships/[id]` that pushes a dealer → **QBO Customer**: linked (`quickbooks_id` set) → **update** with a freshly-read `SyncToken` (read-before-write); unlinked → **create** + **backfill** the returned `Id` (guarded `UPDATE … WHERE quickbooks_id IS NULL`). New `src/lib/quickbooks/dealer-push.ts` (`mapDealerToCustomer` inverse map + `pushDealerToQuickbooks` core) + `client.ts` write helpers (`createCustomer`/`updateCustomer`/`fetchCustomerById` + `QboDuplicateNameError`) — the existing `com.intuit.quickbooks.accounting` scope already permits writes. Server Action admin-gated + gate-matrix row. 4 phases: `3cbcc10` (client helpers) → `763844e` (dealer-push module) → `8329203` (action + dealer-page button) → `b31e73c` (integration + smoke fixture); in-cycle lint fix `a7d3cc9` (safeParse). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0070-quickbooks-dealer-push/eval-2026-06-09-1211.md)): static clean (1056 tests, tsc, 0 new lint), browser smoke 1/1 (fixture dealer → QB section + Push button render against the live sandbox connection), Codex **0 High / 3 Medium + 2 Low — all parked as 0070 follow-ups (a)–(e) below** / 1 Low (lint) fixed in-cycle. **No schema/migration** — reuses `dealers.quickbooks_id` (0069). **Merged to `main`** (fast-forward to `dc65da9`, 2026-06-09); `main` not pushed to `origin`. ✅ **Deployed to prod 2026-06-09** (rev `event-manager-pro-00011-nvm`) — the "Push to QuickBooks" button is now live on prod and writes a real Customer to the prod QBO company; **the write path has not been exercised on prod yet** (no dealer pushed). **Follow-on slices** (intent.md): (2) one-way item pull (QBO `Item` → `service_items`), (3) Quotes → QBO **Estimates** push, + a tax-alignment pull (QBO rates → app).

**Prior chunk:** `0069-quickbooks-dealer-sync` **built + closed 2026-06-08** → [`closed/`](closed/0069-quickbooks-dealer-sync/plan.md). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0069-quickbooks-dealer-sync/eval-2026-06-08-1703.md)): static clean (1035 tests, tsc, 0 new lint on touched files), browser smoke 1/1 (connected change-set table, 26 sandbox customers all "Create"), Codex **0 High / 4 Medium (all parked as 0069 follow-ups a–d below) / 1 Low fixed in-cycle** (`f96c140` — `decodeSyncSummary` digit guard). Migration **`0032`** (`dealers.quickbooks_id` + partial unique index) applied to **sandbox** only — ships to prod when prod migrations run. **On the same unmerged `0068-quickbooks-oauth-viewer` branch — 0068 + 0069 merge to `main` together.** Original scope follows. Follow-up to 0068: adds `dealers.quickbooks_id` (nullable + **unique partial index**, `WHERE quickbooks_id IS NOT NULL`) and **pivots `/admin/quickbooks` from a passive customer list into a sync surface** — for each QB customer it shows the computed change against our DB (**Create / Link → #N / Already linked / Skip**), and a deliberate **"Sync dealers"** button applies it through one env-agnostic path: match-by-`quickbooks_id` → match-by-`lower(name)+lower(address)` & **backfill the QB ID** → else insert; never clobbers local `name`/`address`/`province` (province backfills only when null). **Sandbox** inserts dealers fresh; **prod** backfills QB IDs onto the dealers the 0060 import seeded. The change set is computed **read-only** on page load (`computeDealerSyncPlan`); the button calls `applyDealerSync`. Reuses 0068's `fetchCustomers`/`getValidAccessToken` + the 0060 import's `mapCustomer`/`findOrCreateDealer` logic (extracted into `src/lib/quickbooks/dealer-sync.ts`). Out of scope: write-back to QB, contact sync, webhooks, prod OAuth keys. **Builds on the unmerged `0068-quickbooks-oauth-viewer` branch** (see Prior chunk).

**Prod baseline:** ✅ 0065 → 0069 + 0070 + 0071 + 0072 + 0073 + 0074 + 0075 + 0076 + 0077 + 0078 + 0080 + **0081** all live on prod (**0081: 2026-06-15, current rev `event-manager-pro-00021-skx`, image `:20260615-215156`, us-east4** — buttons consolidated onto Catalyst `Button`, brand-blue primary (incl. all top-of-list "+ Add X"/"New X" create actions), soft-red destructive; 0081 base first shipped at `-00020-n54`; **code-only, NO migration**; prior 0080 at `-00019-ngh`/`:20260615-170912` — per-quote tax override removed, Tax field display-only; 0080 core first shipped at `-00018-dhp`, 0078 prior at `-00017-8pr`); **prod DB at migration `0039`** (0080 added no migration; 0038 `quote_attachments` table+RLS + 0039 `audit_action` enum applied 2026-06-13; 0037 calendar columns 2026-06-12; 0036 RLS applied 2026-06-11; 0076 added no migration; 0034 `quotes.quickbooks_estimate_id` + 0035 `tax_rates.quickbooks_tax_code_id` applied 2026-06-10; 0033 `service_items.quickbooks_id` 2026-06-09; 0031/0032 2026-06-08). ⚠️ **0071 is live → the in-app `service_items` editor is GONE on prod and the "Pull items" button is armed. DO NOT click "Pull items" on prod until the prod QBO company holds the real Salesability SKUs — it hard-deletes the live catalog and replaces it with QBO's items** (+ the parked partial-read follow-up (a) should land first). ✅ **0076 is live → the province → QB-tax-code MAPPING is managed on `/admin/lookups`** (per-province dropdown of live QBO codes — single or group; rate adopted from the mapped code); the old auto-apply **"Pull tax codes" button is retired**. Prod `tax_rates` are **all currently unmanaged** (no `quickbooks_tax_code_id` set) — map each operating province on `/admin/lookups`; a taxed quote in an unmapped province fails the push pre-flight until mapped. ⚠️ **0073/0074 are live → "Push to QuickBooks" on `/quotes/[id]` pushes a quote → QBO Estimate with per-line `TaxCodeRef`. Before the first real push, confirm the prod QBO company's tax model (manual vs Automated Sales Tax — 0074 assumes manual, like the CA sandbox).** 0070's "Push to QuickBooks" (dealer→QBO Customer) is also live + writes to the prod QBO company. The **Send Test MSA** tool is live at `/admin/send-test-msa` — use it to verify prod BoldSign (real `isSandbox=false` send). ✅ **QB feature now WIRED on prod:** `deploy.sh` mounts `quickbooks-client-id`/`-client-secret`/`-token-enc-key` secrets + bakes `QBO_ENV=production` (rev `-00010-759`), so `/admin/quickbooks` shows **Connect** (production Intuit keys). ✅ **PROD QBO IS CONNECTED (2026-06-11) — realm `193514766730959`** (the real Salesability Canadian company; verified via read-only probes this session: 10 tax codes incl. "HST ON"/"HST NS"/"HST NS 2025"/"HST Atlantic 15%", 146 customers, **manual sales tax / not AST**). **Probe findings (owner action in QB):** no Quebec/BC/SK/MB codes set up yet; NS has a **stale `HST NS` 15%** alongside the current `HST NS 2025` 14%; **3 customers have wrong default tax codes** (see `scripts/qbo-customer-tax-audit.mjs` + the session's cleanup task — affects QB-native invoicing, not app pushes). ⚠️ The buttons are live (0069 "Sync dealers", 0070 "Push to QuickBooks", 0071 "Pull items") — **DO NOT click "Pull items" until the prod QBO company holds the real Salesability SKUs** (it hard-deletes the catalog). None has been clicked on prod yet.

**Active sub-plan:** _None._

**Last deploy:** 2026-06-15 — **PRODUCTION** (rev `event-manager-pro-00021-skx`, image `:20260615-215156`, us-east4) — **0081 button consolidation + same-day create-action consistency follow-up** (`6859439` — promoted "+ Add Person"/"+ Add Dealer" from `outline` → brand-blue primary so all top-of-list create actions match). The 0081 base shipped at `-00020-n54`: migrated ~50 raw/duplicated buttons across ~25 files onto the shared Catalyst `Button`; **brand blue is now the one primary-action color** (green retired → semantic `<Badge color="green">` only); added a `compact` size + soft-red `destructive` variant to `button.tsx`. **Code-only — NO migration** (prod DB stays at `0039`). `GCP_REGION=us-east4 DEPLOY_CONFIRM=production ./deploy.sh` (Cloud Build SUCCESS 2m9s). Verified: running rev `-00021-skx` serving 100%, domain `https://eventpro.salesability.ca` healthy (307 → `/login` 200). **Live on prod:** all primary CTAs (incl. every "+ Add X"/"New X" create action) are brand blue; destructive buttons (Cancel Campaign, lookup/availability archive ✕) are the soft-red treatment; no behavior change (pure UI refactor). _(Same-day prior prod revs: `-00020-n54` = first 0081 ship (2m1s); `-00019-ngh` = 0080.)_

**Prior deploy:** 2026-06-15 — **PRODUCTION** (rev `event-manager-pro-00019-ngh`, image `:20260615-170912`, us-east4) — **0080 (removed the per-quote tax override) + both tax-hint copy fixes** (`687fb37` no-province spacing; `fc935e9` context-aware hint — "Select a dealer…" on a new quote vs "This dealer has no province set…" when a dealer's picked). **Code-only — NO migration** (0080 kept `quotes.tax_override` retained-but-unused; prod DB stays at `0039`). `GCP_REGION=us-east4 DEPLOY_CONFIRM=production ./deploy.sh` (Cloud Build SUCCESS 1m42s). Verified: running rev `-00019-ngh` serving 100%, domain `https://eventpro.salesability.ca` healthy (307 → `/login` 200). **Live on prod:** the composer **Tax field is display-only** (auto, QB-sourced province rate + `auto · X%` pill — no Override link/input); a re-saved pre-0080 overridden quote clears its stale `tax_override` + recomputes auto. _(Same-day prior prod revs: `-00018-dhp` = 0080 core; `-00017-8pr` (2026-06-13) = 0078 attachments.)_

**Older deploy:** 2026-06-13 — **PRODUCTION** (rev `event-manager-pro-00017-8pr`, image `:20260613-032230`, us-east4) — **first prod ship of 0078 quote attachments**. Migrations **`0038`** (`quote_attachments` table + RLS) + **`0039`** (`audit_action` enum gains `quote.attachment_added`/`_removed`) applied to the prod DB FIRST via `pnpm db:migrate:prod` (session pooler `aws-1-us-east-1…:5432`) → table + RLS (2 policies) + 11 columns + both enum values verified; **prod DB now at `0039`**. Then `GCP_REGION=us-east4 DEPLOY_CONFIRM=production ./deploy.sh` (Cloud Build SUCCESS 2m15s). Verified: running revision `-00017-8pr` serving 100%, domain `https://eventpro.salesability.ca` healthy (307 → `/login` 200). **The feature is LIVE on prod:** the Send Quote dialog now carries a **Documents** section — a coach can attach local files (PDF / images / Office, ≤10 MB/file, ≤20 MB total) that ride alongside the quote PDF in the outgoing email; reuses the existing `GCS_BUCKET` + Resend wiring (no new secrets/env). The 5 parked follow-ups (0078 a–e) remain unaddressed but are UI-gated/single-admin/low-probability. _(Same-day prior was 2026-06-12 rev -00016-srt.)_

**Earlier deploy:** 2026-06-12 — **PRODUCTION** (rev `event-manager-pro-00016-srt`, image `:20260612-215737`, us-east4) — **first prod ship of 0077 Google Calendar distribution**. Migration **`0037`** (`campaigns.gcal_event_id` partial-unique + `gcal_sync_status` enum + `gcal_synced_at`) applied to the prod DB FIRST via `pnpm db:migrate:prod` (session pooler `aws-1-us-east-1…:5432`) → columns + index + enum verified. Then `GCP_REGION=us-east4 DEPLOY_CONFIRM=production ./deploy.sh` (Cloud Build SUCCESS 1m48s). Verified on the running revision: `APP_ENV=production` + `GOOGLE_CALENDAR_SA_EMAIL`/`_ID`/`_SUBJECT` set (the prod-only block in `deploy.sh`, **keyless** — prod runtime SA `1094204863648-compute@` holds `tokenCreator` on `eventpro-calendar`, no key/secret), domain `https://eventpro.salesability.ca` healthy (307). ⚠️ **The calendar feature is now ARMED on prod:** a booked-campaign create/edit/cancel projects to the **EventPro** calendar (organizer = "EventPro", the owner's chosen brand) + emails **real** coach/dealer invites (`sendUpdates='all'` in prod). **Existing campaigns do NOT auto-project** — only the next mutation (or a manual Re-sync) of each projects it, so no immediate blast of the ~42 existing bookings. **Still owner-pending:** share the calendar read-only to staff (for the overlay). Live keyless+DWD round-trip PASSED (smoke, 2026-06-12). _(Same-day prior was 2026-06-11.)_

**Oldest deploy:** 2026-06-11 — **PRODUCTION** (rev `event-manager-pro-00015-csq`, image `:20260611-155150`, us-east4) — **0076 follow-up `916e638`**: dropped the misleading "(suggested)" dropdown hint on `/admin/lookups` (it confidently suggested the stale `HST NS` 15% for NS). Code-only, no migration (prod DB stays at `0035`); Cloud Build SUCCESS 1m45s; rev + image flipped on us-east4, domain healthy (307 → `/login`). _(Same day, immediately prior: rev `-00014-7b9`, image `:20260611-152312` — shipped **0076** itself (the mapping UI + retired the auto-apply "Pull tax codes" button), code-only, no migration; that deploy needed an explicit confirm gate — the auto-mode classifier blocks a prod deploy off a question. Before that: rev `-00013-fc9`, image `:20260610-203035` — **0073 + 0074 + 0075** with migrations `0034`/`0035` applied first.)_

**Earliest deploy:** rev `event-manager-pro-00011-nvm`, image `:20260609-193604` — shipped **0070 + 0071** (dealer push + QBO item-master). Migration **`0033`** (`service_items.quickbooks_id`) applied to the prod DB FIRST via `pnpm db:migrate:prod` (session pooler `aws-1-us-east-1…:5432`) → prod DB at `0033`, column + partial index verified. Then `GCP_REGION=us-east4 DEPLOY_CONFIRM=production ./deploy.sh` (Cloud Build SUCCESS 1m42s). Verified on the running revision: `APP_ENV=production` + `QBO_ENV=production`, secret mounts (`database-url-production`, `quickbooks-client-id`/`-client-secret`/`-token-enc-key`, BoldSign, Resend, Supabase), domain **https://eventpro.salesability.ca** healthy (307), BoldSign `api-ca.boldsign.com` production mode. Note: re-auth was needed mid-deploy (`gcloud auth login`) — the prod token had expired. _(Immediately prior: rev `…-00010-759`, image `:20260608-220351` — QBO secrets wired + `QBO_ENV=production`; rev `…-00009-c7m` — first ship of 0068 + 0069, prod DB → `0032`.)_ **Deploy prod with `GCP_REGION=us-east4 DEPLOY_CONFIRM=production ./deploy.sh`** (REGION defaults to us-east4 in-script; keep passing it explicitly) and verify the running image hash flipped on us-east4. (See [[project-prod-gcp]] memory.)

**Future:**
- **[`future/0079-quote-document-library/`](future/0079-quote-document-library/plan.md)** — the reusable "from the system" document library (admin-managed library + send-dialog picker), additive on top of 0078's attachment spine. ✅ **0078 shipped 2026-06-12** (the spine is built — a nullable `document_id` FK on `quote_attachments` is 0079's additive migration). **Un-defer trigger (remaining):** the owner makes the two product calls — banking-info delivery (attachment vs short-lived signed link) + library scope (global vs per-client).
- **[`future/0058-production-sheet-and-date-range/`](future/0058-production-sheet-and-date-range/plan.md)** — the shareable Google Sheet (Phases 2–4). Phase 1 (1/2/3-month date-range filter) already shipped to `main` (`9b15e29`); only the Sheet work is deferred. **Un-defer trigger:** owner is ready to set up Google Cloud (enable Sheets/Drive API + provision credentials) and has chosen the auth approach (service account vs user OAuth).

**Parked:**
- **0082-a — `sendMsaEnvelope` concurrent double-send** (Codex High, pre-existing — [eval](closed/0082-quote-msa-decouple/eval-2026-06-15-1945.md)) · the external BoldSign `sendSignatureRequest` call precedes the guarded `provider_document_id` persist, so two concurrent callers can post two envelopes (only one id saved; the orphan's signed-webhook 404s). Predates 0082 (the decouple didn't touch the ordering); after the eval's admin-only retighten it fires from an admin confirm dialog that disables while pending — low probability. Fix shape: reserve a send slot in the DB (or row/advisory lock) before calling BoldSign. **Un-park trigger:** an orphaned-envelope report, or the next hardening of the MSA send path.
- **danger-callout component (≈0082, follow-up to 0081)** · the app has no shared inline danger/warning callout (the pink box + red icon badge + heading + subtext from the 0081 owner inspiration image) and has ~8 duplicated ad-hoc red error panels (`border-red-200 bg-red-50 text-red-800`-style) across login / quotes / msa / quickbooks. A shared `Callout color="red"` would unify them and host the soft-red destructive action. **Scoped out of 0081** (owner: "button only now" 2026-06-15). Un-park trigger: 0081 ships, or the next time a destructive/error panel needs touching.
- **0078 follow-up (a): in-flight upload can be omitted from the sent email but persist on the sent quote** (Codex High, [eval](closed/0078-quote-attachments/eval-2026-06-12-2043.md)) — a slow `uploadQuoteAttachment` racing a direct `sendQuote` call: the send snapshots attachments before the upload's insert, emails PDF-only, then the row lands on the now-`sent` quote. **Mitigated:** the dialog disables Send while uploading, and a re-send includes the persisted file (self-healing) — reachable only by bypassing the UI. Fix shape: re-read attachments inside/after the guarded transition, or bump `quotes.updatedAt` on attachment insert so the send optimistic-lock covers it. **Un-park trigger:** a report of a persisted-but-unsent attachment, or next hardening of the send path.
- **0078 follow-up (b): attachment changes not covered by the send optimistic lock** (Codex Medium, same eval) — removing an attachment between `sendQuote`'s snapshot and its guarded UPDATE can still email the removed doc; the lock only checks `quotes.updatedAt`. Same UI gate + narrow race as (a). Fix shape: same as (a) — make attachment mutations bump `quotes.updatedAt`. **Un-park trigger:** with (a).
- **0078 follow-up (c): parallel uploads can bypass the 20 MB total cap** (Codex Medium, same eval) — the upload cap check is read-then-write without a quote lock, so concurrent direct uploads each see `existingTotal=0`. The send-time guard still fails closed (nothing over-size goes out); only DB/GCS growth is unbounded. UI uploads sequentially. Fix shape: do the cap check + insert in a `quotes`-row-locked transaction. **Un-park trigger:** an over-cap storage report, or a prod arming of the feature.
- **0078 follow-up (d): server MIME validation trusts caller `File.type` only** (Codex Medium, same eval) — no extension/magic-byte check; the same value is stored + emailed as `contentType`. The file is emailed (not executed/served) and the uploader is a trusted authenticated coach, so it's a defensible v1 limit. Fix shape: sniff magic bytes (or validate extension↔MIME) before `putObject`. **Un-park trigger:** a malformed-attachment report, or before opening upload to a less-trusted role.
- **0078 follow-up (e): GCS object orphaned if the DB insert throws after `putObject`** (Codex Low, same eval) — the upload writes to GCS before inserting the row, with no compensating delete on insert failure. Retention is keep-forever anyway (orphan = harmless storage); `recordAudit` is best-effort (never throws). Fix shape: wrap insert in try/catch → best-effort `deleteObject` on failure. **Un-park trigger:** an orphaned-object report, or next hardening of the upload path.
- ~~**0077 follow-up (a): reconcile always sends `sendUpdates='all'` — non-prod envs would email real invites**~~ — **RESOLVED 2026-06-12** (`9277f60`): added `calendarSendUpdates()` (mirrors boldsign's `isProductionEnv`) — `sendUpdates='all'` only when `APP_ENV==='production'`, else `'none'`, threaded through every reconcile create/patch/delete call. The projection still runs in non-prod (events created/patched/deleted) but guests aren't emailed. Integration tests assert both env branches. So non-prod is now safe to arm. (Surfaced by the 2026-06-12 live smoke, not Codex.) **Live keyless+DWD round-trip PASSED 2026-06-12** (organizer = calendar displayName "EventPro" — the owner's chosen brand, no rename).
- **0076 follow-up (a): `loadTaxMappingAdmin` collapses a QBO error to "disconnected"** (Codex Low, [eval](closed/0076-quickbooks-tax-code-mapping/eval-2026-06-11-1014.md)) — any QBO/token/fetch failure falls back to the read-only "Connect QuickBooks" view even when a connection exists but QBO errored, hiding drift/broken-link diagnostics + misdirecting remediation. The fallback is **safe** (the rest of `/admin/lookups` stays up). Fix shape: a distinct `connected: true, error: string` state rendering "Couldn't load QuickBooks tax codes" while keeping local rows visible. **Un-park trigger:** a transient-QBO-error report on the mapping page.
- **0076 follow-up (b): `refreshTaxRates` reports planned writes, not rows actually updated** (Codex Low, same eval) — the action returns `updated: writes.length`, but the compare-and-set `.where()` can legitimately match 0 rows when a province was concurrently re-mapped → the toast count overcounts. DB state stays correct (the CAS correctly skips the stale write); only the reported number is optimistic. Fix shape: count `.returning()` rows from each CAS update and report that. **Un-park trigger:** next touch of the refresh path, or a wrong-count report.
- **0075 follow-up (a): compact/decorated QB tax-code names miss the name match** (Codex Low — speculative, [eval](closed/0075-quickbooks-tax-rate-source/eval-2026-06-10-1557.md)) — the matcher anchors the province code to the trailing whitespace token ("HST ON" → ON) + the full name, so non-standard names like "HSTON", "PEI", or "HST ON (13%)" fall through to **unmatched/flagged** (keeps the app rate — fail-safe, by design). Fix shape: the deferred per-province manual override (decision 3) resolves these; or broaden the heuristic if a real company uses such names. **Un-park trigger:** a real connected company has a province-naming code the trailing-token heuristic misses.
- **0074 follow-up (a): stale tax code on a taxed→untaxed Estimate re-push** (Codex Medium, [eval](closed/0074-quickbooks-tax-alignment/eval-2026-06-10-1259.md)) — `mapQuoteToEstimate` omits `TxnTaxDetail` when a quote is untaxed, but `updateEstimate` always sends `sparse:true`, so a re-push of a now-untaxed quote onto an Estimate that already has a `TxnTaxCodeRef` can leave the old code → QBO keeps taxing. Rare transition; create + taxed-update paths are correct. Fix shape: on an untaxed update, explicitly clear the Estimate's tax (full update, or the right `TxnTaxDetail`/`GlobalTaxCalculation` to zero it) — verify the clear mechanism on the CA sandbox. **Un-park with the live CA Estimate-with-tax smoke.**
- ~~**0073 follow-up (a): sparse-update line semantics**~~ — **RESOLVED 2026-06-10** by the live re-push smoke: QBO's sparse `updateEstimate` replaces the `Line` collection correctly (Estimate #1001 stayed 2 lines on re-push, SyncToken read-before-write held). See [eval addendum](closed/0073-quote-estimate-push/eval-2026-06-10-0911.md). Residual minor (test-infra): the DB integration suite flakes under default parallelism on the sandbox session-pooler 15-client cap (4 DB test files + dev server) — run serially (`vitest --no-file-parallelism`) or point integration tests at the direct connection. **Un-park:** if test flakiness becomes annoying.
- **0071 follow-up (a): partial multi-page item read can destructively sync** (Codex High residual, [eval](closed/0071-quickbooks-item-pull/eval-2026-06-09-1429.md)) — `fetchItems` stops at the first short page, so a transient HTTP-200 empty *later* page (page 1 full, page 2 silently empty) returns an incomplete-but-non-empty set; the pull then archives the "missing" linked rows + purges. The in-cycle zero-syncable guard (`7f86715`) covers the empty / all-non-syncable cases but **not** this partial-multi-page case. Fix shape: validate `fetchItems` pagination (detect an unexpectedly-short later page / use QBO `totalCount`) and/or a "this pull would archive+purge most of the linked catalog → require explicit confirm" threshold. **Un-park trigger: before enabling a prod item pull** (sandbox-only + admin-initiated today).
- **0070 follow-up (a): linked double-submit stale `SyncToken` 400** (Codex Medium, [eval](closed/0070-quickbooks-dealer-push/eval-2026-06-09-1211.md)) — two concurrent pushes of the same *linked* dealer both read token N; the first rotates QBO to N+1, the second posts N → stale-object 400. Read-before-write narrows but doesn't close the window; rare for on-demand single-admin use. Fix shape: catch the stale-token error from `updateCustomer`, re-fetch the SyncToken, retry once. **Un-park trigger:** a concurrent-push report, or next hardening of the push path.
- **0070 follow-up (b): create-before-guard orphan QBO Customer + misleading `created` report on a lost race** (Codex Medium, same eval) — the QBO Customer is created before the `quickbooks_id IS NULL` backfill guard; if a concurrent push/sync links the dealer in between, the new Customer is orphaned (no local link) yet the call still returns `created`. Orphan-on-lost-race is by-design per intent; the misleading report is the new nuance. Fix shape: inspect `.returning()`; on zero rows report non-`created` + optionally deactivate the orphan QBO Customer. **Un-park trigger:** an orphaned-customer report, or next hardening of the push path.
- **0070 follow-up (c): sparse update can't clear fields removed in-app** (Codex Medium, same eval) — clearing a dealer's address/province/email/phone locally won't clear it in QBO (a sparse update leaves omitted fields as-is). Defensible for a "push to populate" v1; whether the app should wipe QBO fields is a **product decision**. Fix shape: send QBO clear-semantics for null/empty locals on update + clearing tests. **Un-park trigger:** owner wants in-app field clears to propagate to QBO.
- **0070 follow-up (d): missing `SyncToken` guessed as `'0'`** (Codex Low, same eval) — `current.SyncToken ?? '0'` guesses if a Customer read omits the token (hypothetical — QBO always returns it). Fix shape: throw when `current.SyncToken == null` rather than guessing. **Un-park trigger:** any stale-token 400 noise on update.
- **0070 follow-up (e): duplicate-name detection only matches string `code: "6240"`** (Codex Low, same eval) — `e.code === '6240'` could miss a numeric code; the `/\b6240\b/` text regex runs only on JSON-parse failure. QBO returns string Fault codes today. Fix shape: `String(e.code) === '6240'` (+ scan Fault `Detail`). **Un-park trigger:** a duplicate-name push surfacing as a generic 500 instead of the friendly path.
- **0069 follow-up (a): duplicate-`create` for two QB customers sharing name+address** (Codex Medium, [eval](closed/0069-quickbooks-dealer-sync/eval-2026-06-08-1703.md)) — two QBO customers with identical `lower(name)+lower(address)` and no existing dealer both insert (distinct QB IDs → no conflict), creating duplicate dealers. **By-design today** per intent ("we don't try to be clever about near-duplicates"); rare (QBO IDs unique). Fix shape: track an in-memory name+address set during apply, collapse later same-key customers to `skip`. **Un-park trigger:** real QBO data produces same-name+address dupes, or a prod sync is enabled.
- **0069 follow-up (b): name-only match when both addresses are empty** (Codex Medium, same eval) — `nameAddrKey` normalizes a null address to `''`, so an address-less dealer links to an address-less same-name QB customer (name-only match). **Inherited from the 0060 importer's `lower(coalesce(address,''))` semantics** — not a new regression. Fix shape: require a non-blank formatted address on both sides before `link` (else `create` / manual-review). **Un-park trigger:** enabling a prod sync where address-less dealers exist, or a wrong-link report.
- **0069 follow-up (c): duplicate *existing* dealers → nondeterministic link/collision** (Codex Medium, same eval) — `classifyDealerSyncPlan`'s `byNameAddr` map is last-wins; if two existing dealers share name+address (one linked to a different QB ID, one unlinked), a customer can `link` to the unlinked one instead of `skip-collision`. Requires a rare DB state (the unique index is on `quickbooks_id`, not name+address). Fix shape: store arrays per key; `skip-collision` if any same-key dealer holds a different QB ID; skip-ambiguous if multiple unlinked match. **Un-park trigger:** next touch of the classifier, or a duplicate-dealer collision report.
- **0069 follow-up (d): archived dealers participate in sync matching** (Codex Medium, same eval) — `loadExistingDealers` reads all dealers (no `archivedAt IS NULL` filter) and the guarded link update doesn't exclude archived rows, so an archived dealer can be silently linked while the rest of the app excludes it. **Inherited from 0060** (also unfiltered). Fix shape: decide skip / revive / create-new for an archived name+address match, then filter `loadExistingDealers` + the update accordingly. **Un-park trigger:** archived dealers exist in a synced company, or the prod sync is enabled.
- ~~**0068 follow-up (a): owner end-to-end OAuth round-trip**~~ — **DONE 2026-06-08**: owner added sandbox `QBO_CLIENT_ID`/`QBO_CLIENT_SECRET` + `QBO_TOKEN_ENC_KEY` to `.env.local`, restarted dev, and the in-app **Connect** OAuth round-trip succeeded against the sandbox company (one-time Intuit consent — expected for authorization-code grant; silent refresh thereafter). The unconfigured-state guard (amber hint + hidden Connect button, post-close fix `640110a`) is also live. _(Open: confirm the customer table renders for the connected company — owner reported connect works.)_
- ~~**0068 follow-up (b): deploy.sh secret-wiring for QBO**~~ — **DONE 2026-06-08** (`291d223`): `deploy.sh` now conditionally mounts the `quickbooks-client-id`/`-client-secret`/`-token-enc-key` Secret Manager secrets (only when present in the target project — prod has them, stage stays dormant) + bakes `QBO_ENV` (default `production`). Owner pre-created the three secrets in `eventpro-498313`; redeployed to rev `-00010-759` with `QBO_ENV=production`. ~~Still owner-pending: register the prod redirect URI + production-API approval, then Connect.~~ ✅ **DONE 2026-06-09** — prod redirect URI registered + production-API approval landed; production QBO company **connected** and a live **read of customers** verified on prod (see **Prod baseline** above).
- **0065 follow-up (a): rate-snapshot TOCTOU** (Codex Medium from [`closed/0065-dealer-province-tax/eval-2026-06-04-1631.md`](closed/0065-dealer-province-tax/eval-2026-06-04-1631.md)) — `dealerTaxRatePct` reads the dealer's province→rate on the **app pool** before/outside the quote tx's `FOR UPDATE` dealer lock, so a concurrent `updateDealer` province change can snapshot a stale `tax_pct` onto a now-different-province quote. **Low-probability + self-healing** (next picker save / send re-derives). Fix shape: read the rate **inside** the quote tx from the `FOR UPDATE`-locked dealer's province. **Un-park trigger:** next touch of the quote write path, or any report of a wrong tax rate on a quote.
- **0060 follow-up (living sync): QBO webhooks/CDC + contact sync** — `0060-quickbooks-integration` **closed 2026-06-12** → [`closed/`](closed/0060-quickbooks-integration/intent.md). The one-time QBO Customers → `dealers` seed (its only build slice) **executed on prod 2026-06-05** (137 dealers · 133 staff links · 0 reused; importer `scripts/import-from-quickbooks.ts`, all committed) and every later slice 0060 named shipped as its own closed chunk: in-app OAuth (**0068**), dealer sync + `dealers.quickbooks_id` link — which replaced the planned `external_account_links` table (**0069**), dealer push (**0070**), item pull (**0071**), Estimate push (**0073**), tax align/source/map (**0074/0075/0076**); **prod QBO is connected** (realm `193514766730959`, 2026-06-11). The only **unbuilt** remnant is a *living* sync (QBO webhooks / CDC polling) + **contact/people** sync (0069 links companies only). Background in [`closed/0060-…/intent.md`](closed/0060-quickbooks-integration/intent.md) + [`research.md`](closed/0060-quickbooks-integration/research.md). **Un-park trigger:** owner wants a living QBO connection or contact-level sync.
- **Change-request batch (2026-05-21, from owner email)** — scaffolded from the owner's email. **`0056-booking-inline-add-dealer-coach`** (closed 2026-05-21) + **`0055-quote-msa-one-document`** (closed 2026-05-22 — live BoldSign smoke GREEN; fixed webhook auth-gate `097893c` + anchor DPI scaling `d06082b`) both shipped → [`closed/0055-quote-msa-one-document/`](closed/0055-quote-msa-one-document/plan.md). Remaining, in priority order:
  - ✅ **[`closed/0057-travel-estimated-label/`](closed/0057-travel-estimated-label/plan.md)** (closed 2026-05-22) — "Travel ($)" → "Estimated Travel" in composer + catalog (migration `0022` + seed). PDF label is catalog-driven (not hardcoded in `render-quote.ts` as the plan first guessed). PASS-with-caveats; Codex caught that `0022`'s journal `when` was earlier than `0021`'s (would have silently skipped the migration) — fixed `3404fae`. **Deploy step:** run `pnpm db:migrate` to apply `0022` to the shared DB.
  - ⏸ **[`future/0058-production-sheet-and-date-range/`](future/0058-production-sheet-and-date-range/plan.md)** — Phase 1 (date-range filter) **shipped `9b15e29`**; the Google Sheet (Phases 2–4) **deferred to `future/`** 2026-05-22 (see **Future:** above for the un-defer trigger).
  - ✅ **[`closed/0059-reports-inline-edit/`](closed/0059-reports-inline-edit/plan.md)** (closed 2026-05-22, 5/5) — admin-only inline billing-figure edits on `/reports` via a `billing_adjustments` overlay table (`0023` + RLS). Owner decisions: table model · Records/SMS-Email/Letters/BDC quantities · admin-only. PASS-with-caveats ([`eval`](closed/0059-reports-inline-edit/eval-2026-05-22-1524.md)) — Codex affirmed the SQL pivot (no fan-out) before its runtime truncated the synthesis; browser smoke deferred (needs `0023` applied). **Deploy step:** `pnpm db:migrate`.
- ~~`0053-quote-line-items-table`~~ — **superseded + closed 2026-06-01** → folded into [`0062-quote-line-item-picker`](closed/0062-quote-line-item-picker/plan.md), which absorbed its `quote_line_items` table migration as the picker's backing store. Both now in [`closed/`](closed/0053-quote-line-items-table/plan.md).
- 0061 follow-up (a): **`sendMsaEnvelope` links the quote AFTER the external BoldSign post** (Codex High from [`closed/0061-move-msa-action-to-quote/eval-2026-05-29-1418.md`](closed/0061-move-msa-action-to-quote/eval-2026-05-29-1418.md)) — the `quotes.msa_id` link UPDATE (`src/features/msa/actions.ts:383`) runs after `sendSignatureRequest` and ignores its result, so if the quote changes status (or is linked by another path) between preload and link, BoldSign holds a valid envelope but `acceptBundledQuote` later finds no row → the signed MSA activates while the bundled quote never auto-accepts. **Pre-existing** — predates 0061 (which only widened the guard draft→draft|sent); same family as **0041 follow-up (a)** (concurrent-send race). Fix shape: guarded `UPDATE … RETURNING` (`draft|sent` + `msa_id IS NULL`) BEFORE rendering/upload/BoldSign, abort if no row claimed. Un-park trigger: hardening the MSA send path, or any report of a signed-but-not-accepted bundled quote.
- 0059 follow-up (a): **`msa:edit` capability-pairing gap** (surfaced during 0059's eval, pre-existing) — `pnpm check:capability-pairing` reports `msa:edit` as server-gated (`src/features/msa/actions.ts:120,189`) with no UI affordance (`<Can capability="msa:edit">` / `useCan('msa:edit')`) pointing at it. Predates 0059 (confirmed against baseline). Fix shape: add a `<Can>`/`useCan('msa:edit')` to the MSA send affordance, OR add `// expected: server-only` to the action lines if the MSA send button is gated some other way. Un-park trigger: next touch of the MSA send UI, or a CI gate on `check:capability-pairing`.
- 0051 follow-up (a): **`constantTimeEquals` UTF-8 byte-length check** (Codex Low from [`closed/0051-dropbox-sign-to-boldsign/eval-2026-05-15-1644.md`](closed/0051-dropbox-sign-to-boldsign/eval-2026-05-15-1644.md)) — `src/lib/boldsign/webhook-verify.ts:52` compares JavaScript string length (UTF-16 code units) before calling `timingSafeEqual(Buffer.from(..., 'utf8'))`. A 64-char non-ASCII signature candidate has the same string length as the hex digest but a longer UTF-8 byte length, so `timingSafeEqual` throws instead of returning the 401 verification error. No signature bypass — malformed-input handling / availability. Fix shape: compare `Buffer.byteLength(a, 'utf8') !== Buffer.byteLength(b, 'utf8')` before `timingSafeEqual`. Un-park trigger: any sign of malformed-header production noise, or natural pickup when next touching `webhook-verify.ts`.
- 0051 follow-up (b): **Plan-doc grep-gate hygiene** (Codex Low from [`closed/0051-dropbox-sign-to-boldsign/eval-2026-05-15-1644.md`](closed/0051-dropbox-sign-to-boldsign/eval-2026-05-15-1644.md)) — Phase 7's "zero `dropbox` references in `src/`" claim is contradicted by two intentional exceptions: `.env.example:53` chunk-slug comment and `src/features/msa/template-version.ts:11` historical comment. Cosmetic chunk-doc only. Fix shape: refine the grep claim to acknowledge the two intentional exceptions, or rephrase the comments to non-grep-matchable spellings.
- 0050 follow-up (a): **TokenPill surrogate-pair truncation** (Codex Low from [`closed/0050-grid-pattern-resend/eval-2026-05-14-1059.md`](closed/0050-grid-pattern-resend/eval-2026-05-14-1059.md)) — `src/components/ui/token-pill.tsx:30`'s `value.slice(0, maxChars)` truncates by UTF-16 code units; a future caller passing a non-ASCII opaque value with an astral character on the boundary would split mid-surrogate and render a replacement character. No consumer today (the primitive landed ahead of any opaque-id surface). Fix shape: switch to code-point-aware slicing via `Array.from(value).slice(0, maxChars).join('')` or `Intl.Segmenter`. Un-park trigger: first surface adopts `<TokenPill>` with a non-ASCII value path.
- 0048 follow-up (a): **DST fall-back collision in `quoteDisplayName`** (Codex Low from [`closed/0048-quote-timestamp-naming/eval-2026-05-14-0859.md`](closed/0048-quote-timestamp-naming/eval-2026-05-14-0859.md)) — `src/features/quotes/display-name.ts` produces the same `quote-YYYYMMDD-HHmm` for two distinct UTC instants an hour apart during the autumn DST fall-back window (e.g. `2026-11-01T05:30:00Z` and `2026-11-01T06:30:00Z` both render as `quote-20261101-0130` in America/Toronto); `quoteDownloadFilename` likewise collides. Server-side persistence (GCS path `quotes/{quoteId}/{revision}.pdf` — id-keyed) is unaffected; collision is presentation-only and lasts one hour per year. Fix shape options: (i) include seconds in the display name (`quote-YYYYMMDD-HHmmss`); (ii) include a short id-tail suffix (`quote-…-<id-tail>`); (iii) emit the local-time offset to disambiguate (`quote-…-0400` vs `-0500`). Un-park trigger: a coach/customer reports two quotes sharing a display name in the November fall-back window, OR the next chunk that touches quote-identity display.
- 0049 follow-up (a): **Tabs rebuild on Headless UI `<TabGroup>`** (Codex Medium from [`closed/0049-migrate-to-catalyst/eval-2026-05-13-2316.md`](closed/0049-migrate-to-catalyst/eval-2026-05-13-2316.md)) — Phase 4d's custom `src/components/catalyst/tabs.tsx` is context-based and lacks the WAI-ARIA tabs contract: no Arrow/Home/End keyboard navigation, no `aria-controls`/`aria-labelledby` panel↔tab linkage, panels not connected to tabs by id. One callsite: `src/features/reports/reports-tabs.tsx`. Fix shape: replace the context-based implementation with Headless UI `<TabGroup>`/`<TabList>`/`<Tab>`/`<TabPanels>`/`<TabPanel>` wrapped to preserve the existing value-based `value`/`onValueChange` API (TabGroup is index-based natively, so a wrapper bridge is needed). Un-park trigger: any user complaint about `/reports` keyboard nav, OR natural pickup when scaffolding the next accessibility chunk.
- ~~0043 follow-up (a): **Composer-actions lift**~~ — shipped in-cycle on the 0043 feature branch at `5b63693` after user pushback ("action buttons are still on the bottom of the page"). Composer actions moved into a sticky toolbar at `top-16` inside the composer body (sits OUTSIDE the disabled fieldset so Preview/Close stay clickable on read-only quotes); `sticky` removed from the page-level `<PageHeader>` on `/quotes/new` + `/quotes/[id]` so the toolbar parks alone under the 64px `AppHeader` without double-stacking. Same commit batch also addressed user pushback on the `/quotes` row label (`View` → `Edit`, commit `57f3c17`) — the destination IS the composer, so the label should describe the operation, not the surface type; `docs/wiki/layout.md` View-xor-Edit rule refined accordingly.
- 0043 follow-up (b): **Row-action sweep into non-`*-columns.tsx` surfaces** (Codex Medium from [`closed/0043-portal-shell-conventions/eval-2026-05-13-1730.md`](closed/0043-portal-shell-conventions/eval-2026-05-13-1730.md)) — three callsites still render hand-rolled per-row actions outside the lint rule's path scope: the nested Quotes table on `/dealerships/[id]:225`, `src/features/services/services-admin.tsx:132`, `src/features/schedule/availability-admin.tsx:191`. Fix shape: port these to `<RowActions>` (preferred — same vocabulary win), or widen the lint rule's `files` glob to cover them.
- 0043 follow-up (c): **Status-pill sweep into calendar/people** (Codex Medium from [`closed/0043-portal-shell-conventions/eval-2026-05-13-1730.md`](closed/0043-portal-shell-conventions/eval-2026-05-13-1730.md)) — `src/app/(app)/calendar/event-detail.tsx:106,204` still renders campaign status via local `statusBadgeClass()`; `src/features/people/people-columns.tsx:169` renders lifecycle status with a hand-rolled rounded span. Fix shape: add `<CampaignStatusBadge status>` raw-enum variant + `<PersonLifecycleBadge>`, replace the inline chrome.
- 0043 follow-up (d): **Retire `STATUS_PILL_CLS`** (Codex Low from [`closed/0043-portal-shell-conventions/eval-2026-05-13-1730.md`](closed/0043-portal-shell-conventions/eval-2026-05-13-1730.md)) — `src/features/quotes/status-display.ts:13`'s map is no longer used by any swept callsite but is still exported + tested. Fix shape: delete `STATUS_PILL_CLS` + its test block, leaving `displayStatusKey()` as the shared helper.
- 0043 follow-up (e): **Lint vocabulary parity with labels map** (Codex Low from [`closed/0043-portal-shell-conventions/eval-2026-05-13-1730.md`](closed/0043-portal-shell-conventions/eval-2026-05-13-1730.md)) — `eslint-plugins/no-inline-row-action-label.mjs:20`'s `FORBIDDEN` set omits `Quote` even though `ROW_ACTION_LABELS.quote = 'Quote'`. Fix shape: derive the forbidden set from the label map directly (single source) or add `Quote` to the static set + RuleTester coverage.
- 0046 follow-up (a): **Outbox-style degraded-send retry** (Codex High from [`closed/0046-quote-mutable-after-send/eval-2026-05-13-1636.md`](closed/0046-quote-mutable-after-send/eval-2026-05-13-1636.md)) — when `sendQuote`'s guarded UPDATE commits but the subsequent GCS overwrite or email send fails, `sent_at` (and the derived validity window) has advanced without a delivered PDF / audit row; a manual Re-send recovers but a phantom-fresh validity window can mislead `acceptQuote` in the interim. Fix shape: introduce an `outbox` / `send_state` flag (`delivered | pending_pdf | pending_email`) that gates `acceptQuote` until delivery is confirmed, or hold the `sent_at` advance until both side effects succeed. Same family as the pre-0026 degraded-send retry gap (closed/0041 follow-up (a) covers the related concurrent-send race). Un-park trigger: shipping it now reduces a quiet-state risk; otherwise revisit when adding admin repair surfaces.
- 0046 follow-up (b): **ConfirmSendDialog hydration drift hypothesis** (Codex Low from [`closed/0046-quote-mutable-after-send/eval-2026-05-13-1636.md`](closed/0046-quote-mutable-after-send/eval-2026-05-13-1636.md)) — `formatValidUntil` runs `new Date(Date.now() + …)` in the dialog body while `open={false}`. Codex couldn't verify whether Base UI mounts hidden dialog content during SSR; practical risk is low (Radix unmounts hidden dialogs by default). Fix shape: compute the new validity date on dialog open via `useState`/`useEffect` rather than every render. Un-park trigger: a hydration warning surfaces in dev console for this dialog.
- 0044 follow-up (a): **MSA bundled-quote validity-deadline mismatch** (Codex Medium from [`closed/0044-quote-expiry-enforcement/eval-2026-05-13-1116.md`](closed/0044-quote-expiry-enforcement/eval-2026-05-13-1116.md)) — `sendMsaEnvelope` (`src/features/msa/actions.ts:285-292`) renders the bundled draft quote with `validUntilDate = today + quoteValidDays`, but the later `sendQuote` flip re-renders from a fresh `sentAt`, so the external MSA envelope PDF and the eventual sent quote PDF can disagree on "Valid until". Out of 0044's stated scope (closed/0041 owns the MSA bundle flow). Fix shape: persist a `quote_issued_at` timestamp the bundle and the send both read from, OR flip the bundled quote's lifecycle to `sent` inside the MSA envelope flow (which would also need to address MSA-vs-Quote acceptance ordering — closed/0041 deferred that to a separate "concurrent MSA+Quote send" follow-up).
- 0044 follow-up (b): **Expired filter pill on `/quotes`** (Codex Low from [`closed/0044-quote-expiry-enforcement/eval-2026-05-13-1116.md`](closed/0044-quote-expiry-enforcement/eval-2026-05-13-1116.md)) — expired quotes show the amber "expired" pill but still live under the "Sent" filter bucket (since the underlying row is `status='sent'`). Adding a dedicated Expired filter pill makes more sense if/when Option A (full enum + sweep) is promoted, since Option B's derived state can't be filtered server-side without re-fetching every row. Today the pill shows but the filter counts/pills don't separate expired from sent.
- 0045 follow-up (a): **`createPerson` / `updatePerson` appAccess + empty-email guard scope-creep** (Codex Medium from [`closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md`](closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md)) — `src/features/people/actions.ts:560` only enforces "email required when granting app access" on the `current.userId == null` (off→on) branch; an admin re-submitting `appAccess=1` with `email=''` against an already-linked person bypasses the guard, then `swapPrimaryIdentifier(..., '')` archives the primary email identifier while the auth-side sign-in email is preserved. Pre-existing bug, surfaced during 0045's eval but predates the chunk. Fix shape: hoist the email-required check out of the `userId == null` conditional so it fires on every appAccess=1 path, OR refuse `email=''` at the schema layer when `appAccess === '1'` (cross-field `.superRefine` on `personFormSchema`).
- 0045 follow-up (b): **`safeparse-required` ESLint rule policy hardening** (Codex Medium from [`closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md`](closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md)) — the rule accepts any reachable `safeParse` call regardless of (i) whether the argument is `Object.fromEntries(formData)`, (ii) whether it runs before mutation, or (iii) whether it sits in a dead branch. An adversarial action could insert/update from raw `formData`, then call `someSchema.safeParse({})` later and pass lint. Fix shape: tighten `eslint-plugins/safeparse-required.mjs` to assert the safeParse argument starts with `Object.fromEntries(...)` AND no mutation call (`db.update`/`db.insert`/`db.delete`) appears before the first `safeParse` in the action body. Adds RuleTester test coverage at the same time.
- 0041 follow-ups (deferred from chunk-end eval 2026-05-12 — see [`closed/0041-msa-send-bundled-envelope/eval-2026-05-12-2229.md`](closed/0041-msa-send-bundled-envelope/eval-2026-05-12-2229.md)): (a) **concurrent-send race on `sendMsaEnvelope`** (Codex High) — two callers can both post envelopes before the DB-claim lands; loser's webhook has no MSA row to correlate to. Fix shape: claim the send before external side effects (a `send_attempted_at` schema add + guarded UPDATE, or per-MSA advisory lock); paired naturally with (c) below; (b) **shared env-normalize helper** (Codex Medium) — `dropbox-sign/client.ts:83` uses raw `process.env.APP_ENV !== 'production'` while `email/send.ts` trims+lowercases; could test-mode a production deploy and still flip MSA rows to `active`; (c) **resend-envelope button** on the pending-MSA panel state (Codex Medium) — when `pending && !dropboxSignDocumentId`, the existing-MSA guard refuses a second `createMsaDraft` but `sendMsaEnvelope` is already idempotent — just needs UI; pairs naturally with (a); (d) **coach-scoped first-draft-Quote picker** (Codex Medium) — `firstDraftQuoteIdForDealer` is dealer-scoped, could pick another coach's draft in multi-coach dealers; better solved by an explicit Quote-picker UI than a `createdById` filter; (e) ~~**live BoldSign sandbox smoke**~~ **✅ RESOLVED 2026-05-22** (during 0055 close-out) — full envelope-post + webhook-signed round-trip exercised on Cloud Run (MSA #7→active, quote #3→accepted, signed PDF in GCS). The 0051 Codex Medium is answered: BoldSign does **not** need auto-detect — it accepts explicitly-placed `Initial`/`Signature` FormFields with `bounds`; the catch was a coordinate-units bug (BoldSign uses 96-DPI px, fixed by the `96/72` scale in `d06082b`), not field detection. (a)–(d) remain open.
- 0033 follow-up (carry-forward): retire hand-rolled validators (`parseId`/`field`/`parseLookupLabel`/`parseCampaignInput`/`validateContactInputs`) by writing per-action Zod schemas; replaces the `formDataSchema = z.instanceof(FormData)` passthrough; revisit when 0016 booking-intake lands so the public-facing form drives the schema-first conversion. **Superseded 2026-05-13 by `0045-form-schema-as-contract`** — covers the same retirement (Phase 7) plus the broader schema-sharing doctrine across all forms.
- **0025-quote-to-payment umbrella — closed 2026-06-12** → [`closed/`](closed/0025-quote-to-payment/plan.md). The Quote → Contract → Invoice → Payment loop (Phase 7 of 0004-port-migration), per owner: **7.1 Quote** (PDF+email) ✅ `closed/0026`+`0035`; **7.2 Contract/MSA e-sign** ✅ live on prod via **BoldSign** (`closed/0037/0041/0055/0061/0067` — the plan's Dropbox Sign was swapped out, 0051); **7.3 Invoice** ✅ **done via the QuickBooks Estimate push** (`closed/0073` — accepted quote → QBO Estimate; the planned in-app Stripe invoice was superseded by the QBO billing backend `0060`→`0076`); **7.4 Payment** — **off the roadmap at present** (no in-app Stripe / payment-status flip; the campaign status enum has no `paid`). Build-debt if ever resumed: 7.2 MSA-renewal automation + cancellation-fee invoicing. **Un-park trigger:** owner wants in-app card payment (7.4) or MSA-renewal/cancellation-fee automation.
- CSV-injection mitigation + print-overlay leak (Codex medium + lows from 5.6) → not yet scaffolded; pull a small chunk if/when revisited.
- 0018 follow-ups (not blockers): `createUser` partial-success after auth-side success (Codex Medium); test-mock predicate-blindness (Codex Low). Not yet scaffolded; small chunk if/when revisited.
- 0019 follow-ups (not blockers): email rate-limit / replay-prevention (Codex High parked from 0011 — the v1 0026 design dropped the public accept route, but rate-limit still load-bearing for any future public surface from `0016` web intake or a v2 quote-confirmation page); auth-callback Host-header tightening (Codex Medium from Phase 7); MFA enablement (0019 Phase 6 parked); secrets-boundary smoke test (0019 Phase 5 parked); `adoptOrphanAuthUser` audit emission (Codex Low from Phase 4); `actorRole` denorm precision once a non-admin path gains an audited action (Codex Low from Phase 4).
- 0023 follow-ups (not blockers): dealer-role grant audit emission for `createDealer`/`updateDealer`/Phase 2 backfill (Codex Medium from Phase 4 — needs a SELECT-then-conditional helper to distinguish granted/un-archived/no-op); production data triage (re-run `scripts/backfill-dealer-role.ts --dry-run` against prod); dealer + admin/coach hybrid confirm dialog (plan's open question, deferred to v2); schedule-actions vitest fixture for cleaner wiring tests; real form-submission test that would catch React-19-migration regressions like the `4a4afbd` wire-format drop.
- 0024 follow-ups (not blockers): Radix Form adoption (Phase 4 deferred — revisit alongside the `0016` booking-intake form so both forms' validation surfaces migrate in one pass); bundle-size baseline measurement on the people-admin route once a build-comparison harness exists; Combobox/Select interactive flows verified by code-trace + Codex source-review only — a Playwright-style test that drives ref-less popovers would catch any future regression.
- 0026 follow-ups (deferred from Phase 5, 2026-05-12 — each chunk-sized): (a) **degraded-send retry path** — small schema add (`emailed_at` on `quotes`) + a repair Server Action that re-renders, re-uploads, and re-emails a Quote when the guarded `draft → sent` UPDATE succeeded but GCS upload or Resend delivery failed; the degraded-state error already surfaces "admin repair required" so the gap is loud, not silent; (b) **split `quote:send` from `quote:edit`** (Codex Medium 4 from Phase 3 eval 2026-05-12) — match the irreversible nature of the document-emitting transition with an admin-only `quote:send` capability; touches the capability matrix, role assignments, action gates, and tests; (c) **staff accept/decline UI affordance** — `acceptQuote` / `declineQuote` Server Actions exist with prospect→active promotion + audit emission, but no UI button surface today (coaches phone/reply-to-email to confirm and run the action via CLI or future UI); paired naturally with (a) for an "admin repair" surface; (d) **live-send dev smoke** once GCS bucket + Resend creds are wired in a sandboxed environment — create campaign → create quote → send → verify PDF attachment in inbox → run staff accept → verify status flip + audit row + dealer promotion. Each of these is small enough to ship in a half-day chunk.

**Future:**
- [`future/0016-book-your-event-intake/plan.md`](future/0016-book-your-event-intake/plan.md) — public-form + Squarespace-cutover work, deferred to v2 on 2026-05-11. v1 funnel uses manual in-app intake (existing `+ Book Event` modal + 0035 P2 inline-create dealer). Un-defer trigger: prospect volume justifies replacing manual-entry-from-Shannon, OR marketing-site CTA is ready to point at the app. See `docs/chunks/future/` and CLAUDE.md `### Deferring a chunk`.

**Status:** No active plan — pick the next chunk from **Parked:** above. (0066 deliberately did **not** fold in the 0043 follow-up (b) row-action port — kept the chunk scoped to the unit-flatten.) The 0050 sweep already subsumed the nested-Quotes-table piece of 0043 follow-up (b) (`/dealerships/[id]:225` is now `<DealerQuotesPanel>` on `<DataTable>`); the remaining 0043 (b) callsites are `src/features/services/services-admin.tsx:132` + `src/features/schedule/availability-admin.tsx:191` (both hand-rolled per-row actions outside the lint rule's `*-columns.tsx` glob).

---

This file is the pointer to the plan currently being worked on. It exists because saying "the plan" or "read the plan" is ambiguous when `docs/chunks/` has many folders. See the **Tracking the active chunk** section in `CLAUDE.md` for the convention.

## History

For the raw "when was X active" — `git log -- docs/chunks/CURRENT.md` shows every transition as a separate commit (since this file is tracked as of `bc450ad`). Reach for that first if you just need sequencing.

- **2026-06-15** — **Built + closed `0082-quote-msa-decouple`** (Phases 2–5 via `/build`; Phase 1 was pre-done). Active **Plan** → `_None — pick a new plan._`. The MSA now signs on its own BoldSign envelope (admin-only, from the dealer page); every quote accepts via a new staff Accept/Decline card gated on a current active MSA; `quotes.msa_id` dropped (migration `0040`, sandbox). Commits P2 `a719f66` → P3 `ac1aec6` → P4 `4edc75c` → P5 wiki `df41510`; **paused once** in P3 (no Accept/Decline UI existed — owner chose Accept+Decline, D5). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0082-quote-msa-decouple/eval-2026-06-15-1945.md)): static clean (tsc, 0 new lint, full suite green bar the parked pooler flake), browser smoke 6/6, Codex 3 High/0 Med/0 Low → **2 fixed in-cycle `72c2231`** (admin-only MSA-send cap + expiry-aware accept gate), **1 parked (0082-a)**. Committed directly to `main`. **Sandbox-only — migration `0040` + code NOT on prod.** Folder → `closed/`; wiki/log/CURRENT 0082 links re-pointed to `closed/`.
- **2026-06-15** — **`0082` Phase 1 (decision gate) resolved** ([decision.md](closed/0082-quote-msa-decouple/decision.md), 4 owner calls via AskUserQuestion). **D1** drop `quotes.msaId` (contract migration, sequenced after writers/readers removed — Phase 4) · **D2** "Send MSA for signature" relocates onto the existing per-dealer MSA panel `/dealerships/[id]:143-195` (off the quote composer) · **D3** accept-gate = require an **active** MSA to accept ANY quote (stricter than the literal first-only rule; also re-blocks on MSA expiry) · **D4** no cross-dealer MSA list (per-dealer panel suffices). Also corrected an intent inaccuracy: there is **no** customer self-serve click-accept — acceptance is staff-recorded via `acceptQuote` (`actions.ts:1261`); that becomes the single surviving accept route. Plan Phase 1 → Done (20%); Phases 2–5 Pending.
- **2026-06-15** — Scaffolded **`0082-quote-msa-decouple`** (`/plan`) and set as active **Plan** (switched from `_None_` after 0081 closed). Owner ask: *"break the quote out of the MSA/BoldSign workflow."* Explored the coupling first (the first-deal `combineQuoteAndMsa` merge bundles quote+MSA into one BoldSign doc; signing flips both; `quotes.msaId` link; webhook auto-accepts the bundled quote; quote re-send blocked while MSA pending). Confirmed the target end-state via AskUserQuestion → **Option A: MSA solo; quote email+accept** (MSA signs on its own envelope, quotes always email-send + click-accept and never touch BoldSign, "MSA before first quote-accept" kept as an explicit gate). Matches [[project_msa_structure]]. Wrote `intent.md` + `plan.md` (5 phases; Phase 1 = decision gate: `quotes.msaId` keep/drop · MSA send-action home · accept-gate location). All Pending; no code yet.
- **2026-06-15** — **Deployed `0081` create-action follow-up to PRODUCTION** (rev `event-manager-pro-00021-skx`, image `:20260615-215156`, us-east4; Cloud Build SUCCESS 2m9s). `6859439` — "+ Add Person"/"+ Add Dealer" promoted outline → brand-blue primary so all top-of-list create actions match. Code-only, no migration; verified rev 100% + domain healthy.
- **2026-06-15** — **Deployed `0081` to PRODUCTION** (rev `event-manager-pro-00020-n54`, image `:20260615-210357`, us-east4; Cloud Build SUCCESS 2m1s). Code-only, no migration (prod DB stays at `0039`). `GCP_REGION=us-east4 DEPLOY_CONFIRM=production ./deploy.sh`; verified rev serving 100% + domain healthy (307→`/login` 200). Brand-blue primaries + soft-red destructive buttons now live on prod.
- **2026-06-15** — **Built + closed `0081-button-consolidation`** (5 phases via `/build`). Brand blue is now the one primary color (green retired → status-`<Badge>` only); ~50 raw/duplicated buttons consolidated onto Catalyst `Button`; added `compact` size + soft-red `destructive` variant to `button.tsx`. Active **Plan** → `_None — pick a new plan._`. Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0081-button-consolidation/eval-2026-06-15-1643.md)): static clean (tsc, 1136 serial, 0 new lint), browser smoke 7/7 PASS, Codex 0H/1M/0L — the Medium (RSC boundary) **verified false positive, 0 parked**. Commits P1 `c69c11f` → P2 `5d20fc8` → P3 `dd88395`. Code-only, no migration — NOT on prod. Wiki: new "Buttons" section in `layout.md`. Folder → `closed/`. Follow-up parked: danger Callout (≈0082).
- **2026-06-15** — **Scaffolded `0081-button-consolidation`** (`/plan`). Switched away from `_None_` (0080 had just closed). New Active **Plan** → `0081-button-consolidation`. Standalone chunk (no umbrella). Goal: consolidate ~50 raw `<button>` / duplicated `submitClass`/`buttonClass` constants across ~25 files onto the shared Catalyst `Button`, and make **brand blue** the single primary color (green retired from primary, `<Badge color="green">` status kept). Decisions locked with owner via analysis (consolidate + brand-blue-primary); two open questions parked into Phase 4 (destructive treatment, compact scale). 5 phases, all Pending.
- **2026-06-15** — **Built + closed `0080-remove-quote-tax-override`** (5 phases via `/build`). Phase 1 decision gate resolved → **keep the `tax_override` column** (Option A, no migration). Active **Plan** → `_None — pick a new plan._`. Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0080-remove-quote-tax-override/eval-2026-06-15-0934.md)): static clean (tsc, 1136 tests serial, 0 new lint), browser smoke PASS (no Override link), Codex 0H/1M/1L → **both fixed in-cycle `2c893f9`** (clear stale historical override on re-save + stale-doc fixes), **0 parked**. Commits P2 `9f8ff43` → P3 `df92f16` → P4 `60bed1d` → P5 `53a6978`. **Code-only, no migration — NOT on prod yet.** Folder → `closed/`; `log.md` decision link re-pointed. Deferred: prod override-count + a future column-drop migration.
- **2026-06-15** — Scaffolded **`0080-remove-quote-tax-override`** and set as active **Plan** (switched from `_None_`). Owner: *"QB manages tax, I do not think we need an override for tax."* Confirmed the per-quote `tax_override` is redundant with the QB-sourced auto rate (0075/0076) AND a footgun — `quote-push.ts:97` already rejects overridden quotes from the QBO Estimate push. Wrote `intent.md` + `plan.md` (5 phases; Phase 1 = decision gate). **Open call:** keep vs drop the `quotes.tax_override` column + how to treat existing overridden quotes — **sandbox has 5 (3 sent + 2 accepted); prod count is blocked on `gcloud auth login`** (token expired). _(Same session: deployed 0078 to prod rev `-00017-8pr`; fixed the no-province tax-hint spacing `687fb37`.)_
- **2026-06-12** — **Built + closed `0078-quote-attachments`** (4 phases via `/build`, all green). Active **Plan** → `_None — pick a new plan._` (no queued next-up; 0079 stays in `future/` pending the owner's two product calls). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0078-quote-attachments/eval-2026-06-12-2043.md)): static clean (tsc, 1152 tests, **0 new lint** — the raw 147-error count is a pre-existing stale `.claude/worktrees/` artifact, not 0078), browser smoke PASS (Documents section renders in the Re-send dialog), Codex 1H/4M/2L → **2 fixed in-cycle `59c01a0`** (terminal-remove guard + filename control-char strip), **5 parked as 0078 (a)–(e)** above (UI-gated/single-admin concurrency + hardening). Commits P1 `42d8259` → P2 `bbb03be` → P3 `49cba17` → P4 `106d5b8`. Migrations `0038` (table+RLS) + `0039` (audit enum) applied to **sandbox**; NOT on prod. Folder → `closed/`; `future/0079` links re-pointed to `closed/`.
- **2026-06-12** — **Split the quote-attachments work into two chunks** (owner: *"for v1 should we have the two chunks?"*). The two sources share one delivery spine (`quote_attachments` → GCS → `sendEmail` attachments array), so the dependency order is forced: spine first, library additive on top. **0078 rescoped to v1 = the attachment spine + "from local hard drive" upload** (lowest-risk slice, no blocked product decisions); intent/plan rewritten (5 phases → 4; dropped the library admin phase). **Scaffolded `0079-quote-document-library` into [`future/`](future/0079-quote-document-library/plan.md)** — the reusable "from the system" library (admin surface + send-dialog picker), additive, deferred. **Un-defer trigger:** 0078 ships + the owner makes the two library-only product calls (banking-info delivery sensitivity: attachment vs signed link; library scope: global vs per-client). 0078 stays the active Plan.
- **2026-06-12** — Scaffolded `0078-quote-attachments` and set as active **Plan** (switched from `_None_`). Owner ask: attach additional documents (forms, banking info) to a quote when it's sent — sourced from a reusable in-app library ("from the system") and/or uploaded from local disk. Explored first: the email layer **already** supports N attachments (`SendAttachment[]`, `send.ts:4`); storage is **GCS** (`src/lib/storage/gcs.ts`), not Supabase; the single quote-PDF attachment is assembled at `actions.ts:1064`; **no** file-upload UI and **no** document-library data model exist yet (both net-new). Wrote `intent.md` + `plan.md`. _(Same session: split into 0078 v1 + 0079 — see the entry above.)_
- **2026-06-12** — `0077` **post-close hardening + go-live prep** (Plan stays `_None_`). Live keyless+DWD round-trip **PASSED** against real Google. Owner decided the dealer-facing calendar name stays **"EventPro"** ("EventPro was requested") — the "SaleDay Events" rename recommendation is **declined/removed** from the docs (decision §3 resolved; event titles keep "SaleDay Event", so organizer/title intentionally differ). Resolved parked **0077-a**: `sendUpdates` gated on `APP_ENV` (`9277f60`) so only prod emails real guests. Wired prod `GOOGLE_CALENDAR_*` into `deploy.sh` (prod-only, keyless — `e18fbb4`). **DEPLOYED to prod 2026-06-12** at the owner's go: `pnpm db:migrate:prod` (0037) → `GCP_REGION=us-east4 DEPLOY_CONFIRM=production ./deploy.sh` → rev `event-manager-pro-00016-srt`, image `:20260612-215737`, GOOGLE_CALENDAR_* verified on the revision, domain healthy. Feature **ARMED** (organizer "EventPro"; real invites on next campaign mutation; existing bookings don't auto-project). Only owner step left: share the calendar read-only (overlay, not invite-gating).

- **2026-06-12** — `0077-calendar-distribution` **built + closed** → `closed/`. Switched **Plan** off `0077` to `_None — pick a new plan_`. Drove Phases 2–5 in one `/build` (P0/P1 were done pre-session): P2 mapper `dc733e0` → P3 schema `dc255a5` (migration `0037` applied to sandbox) → P4 reconcile wiring + `resyncCampaign` + event-detail badge `1529c2f` → P5 integration suite + live smoke script + wiki `ae6bb45`. Two owner design calls mid-build (AskUserQuestion): **coach colour = auto-derive `(id%11)+1`, no column**; **sync signal = a `gcal_sync_status` column** (decision §7). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0077-calendar-distribution/eval-2026-06-12-1605.md)): all Codex findings (1 High + 2 Med + 1 Low + 1 fix-introduced High) **fixed in-cycle** (`ba4d3cb`, `3aa1346`), final Codex pass clean — **0 parked follow-ups**. Sandbox-only; owner activation (calendar rename + read-only share + 3 env vars) pending before the live path runs. No new memory needed.
- **2026-06-12** — `0077` **Phase 0 complete — keyless pipeline smoke PASSED.** Finished the owner steps (DWD authorized for `calendar.events`; `tokenCreator` granted to runtime SA + `admin@`; EventPro Calendar created on `shannon@`, ID `c_eb45…@group.calendar.google.com`) and ran a keyless smoke from the session: built a DWD assertion via IAM `signJwt` (no key), exchanged for a `shannon@` token, created + deleted a test event on the calendar — full round-trip ✓. **Design correction from the smoke:** events on the secondary calendar report **organizer = the calendar** (`displayName`, `c_…@group` email), with `shannon@` only as **creator**. So the dealer-facing brand is the **calendar's display name**, not the impersonated user — which **moots the entire shannon@-now/events@-later/seat-cost/rebrand analysis** (decision §3 updated): the organizer is free, person-independent, and renameable. **One open owner decision:** rename the calendar's dealer-visible name "EventPro" → "SaleDay Events" (matches the "SaleDay Event" titles). Next: Phase 1 build (`src/lib/google/calendar.ts`).
- **2026-06-12** — `0077` Phase 0 progress + two refinements. **Keyless auth (Path 2):** org policy `iam.disableServiceAccountKeyCreation` blocks downloadable SA keys, so dropped the planned `google-calendar-sa-key` secret and switched to runtime-SA-impersonates-the-calendar-SA via IAM `signJwt` (decision.md §4a). **Renamed the SA** `saleday-calendar` → **`eventpro-calendar@eventpro-498313.iam.gserviceaccount.com`** (new Client ID **`101571815389036082153`**; the calendar overlay is now **"EventPro Calendar"** — technical/internal naming, while dealer-facing event titles stay "SaleDay Event"). Done in GCP: Calendar API enabled, `eventpro-calendar` SA created, runtime SA identified (`1094204863648-compute@developer.gserviceaccount.com`). **Pending owner steps:** the runtime-SA `tokenCreator` grant on `eventpro-calendar` (classifier-blocked for the agent — owner runs the `!` command), DWD console authorization of Client ID `101571815389036082153` for `calendar.events`, and creating the EventPro Calendar on `shannon@`. Updated intent/plan/decision + the Active block.
- **2026-06-12** — Scaffolded `0077-calendar-distribution` and set as active **Plan** (switched from `_None_`). First non-QBO chunk in a while: Google Calendar API distribution so booked campaigns project from the app into coaches' + dealers' real calendars + a shared read-only **SaleDay Events** team calendar. Came out of a long design conversation (the trigger: Shannon runs the schedule off her *personal* calendar — the "ST Personal" manual invite screenshot). Key calls captured in [decision.md](closed/0077-calendar-distribution/decision.md): app = hub / calendar = one-way projection; **Google API over `.ics`** *because `shannon@` is already licensed → $0 + warmer branding + real-time overlay/RSVP*; **organizer = `shannon@` now, config-driven** for a cheap later `events@` rebrand (old events stay, new switch, no migration); **service account + DWD impersonation** (attendees require it); one clean customer-safe event (ops fields stay app-only); best-effort sync. Wrote `intent.md` + `plan.md` (6 phases, Phase 0 = owner setup) + `decision.md`. **Phase 0 started this session** — SA `saleday-calendar` provisioning begun in `eventpro-498313` (key → Secret Manager `google-calendar-sa-key`), **blocked on `gcloud auth login` reauth**; DWD console authorization (admin@salesability.ca) + calendar creation pending. No app code yet. Schema (Phase 3) → `db-conventions`.
- **2026-06-12** — **Closed `0025-quote-to-payment`** (umbrella epic) → [`closed/`](closed/0025-quote-to-payment/plan.md). The Quote→Contract→Invoice→Payment loop (Phase 7 of 0004-port-migration). Per owner: **7.1 Quote** ✅ (`0026`/`0035`), **7.2 Contract/MSA e-sign** ✅ live on prod via BoldSign (`0037/0041/0055/0061/0067`; Dropbox Sign swapped out in `0051`), **7.3 Invoice** ✅ **done via the QuickBooks Estimate push** (`0073`; the planned in-app Stripe invoice was superseded by the QBO billing backend `0060`→`0076`), **7.4 Payment off the roadmap at present** (no in-app Stripe; campaign status enum has no `paid`). The stale 0% tracker never reflected reality — 7.1/7.2 shipped long ago, 7.3 re-routed to QBO. No `/eval` (umbrella tracker; its leaf chunks were each gated on close). Swept live cross-refs (`closed/0004/0026/0035/0039` plans, `wiki/log.md` link, this file); historical eval/log/History prose mentions left as point-in-time record. Remaining build-debt (7.2 MSA-renewal + cancellation-fee invoicing, 7.4 payment) carried as an un-park trigger on the rewritten status bullet. Active Plan unchanged (`_None_`).
- **2026-06-12** — **Closed `0060-quickbooks-integration`** → [`closed/`](closed/0060-quickbooks-integration/intent.md). The one-time QBO Customers → `dealers` seed (0060's only build slice) **executed on prod 2026-06-05** (137 dealers · 133 staff links; importer `scripts/import-from-quickbooks.ts`, 3 commits `b35826f`→`1cc65a1`→`f454ed8` — the dedup edit **is** committed; the stale "uncommitted" Parked note was wrong). Every later slice 0060 named shipped as its own closed chunk — in-app OAuth (**0068**), dealer sync + `dealers.quickbooks_id` link superseding the planned `external_account_links` table (**0069**), dealer push (**0070**), item pull (**0071**), Estimate push (**0073**), tax align/source/map (**0074/0075/0076**) — and **prod QBO is connected** (realm `193514766730959`, 2026-06-11). No `/eval` (no leaf-plan build loop; the live prod import + the shipped follow-on chunks are the gate). Swept cross-refs (`go-live-accounts.md`, `0068`, `0069`, this file). The 0060 **Parked** line is rewritten as a *living-sync / contact-sync* follow-up pointer (the one unbuilt remnant). Active Plan unchanged (`_None_`).

- **2026-06-11** — **Built `0076-quickbooks-tax-code-mapping` end-to-end + closed** (auto-close on PASS-with-warnings). Replaces 0075's auto-apply name heuristic with an **explicit province → QB-tax-code mapping UI on `/admin/lookups`** (per-province dropdown, single + **group** codes, rate adopted via `resolveCodeRatePct`; `assignProvinceTaxCode` + a compare-and-set `refreshTaxRates`); the **"Pull tax codes" button is retired**; the matcher survives as a suggestion. Keep-13-rows row model, no migration ([decision.md](closed/0076-quickbooks-tax-code-mapping/decision.md)). 6 phases on `main`: `52f712d` → `ad90cc9` → `bb51dcb` → `f83399d` → `7c2c5d7` → `41d2f26`; in-cycle eval fixes `45b4b60` (lint) + `c4a07b5` (reject unresolvable-rate map + refresh CAS). `/eval` **PASS-with-warnings** ([eval](closed/0076-quickbooks-tax-code-mapping/eval-2026-06-11-1014.md)): static clean (1109 serial), smoke 2/2, Codex **1 High / 1 Medium fixed in-cycle (re-review confirmed)** / 2 Low parked (**0076-a**, **0076-b**). **Sandbox-only; no migration.** Live group-code (QC) push verify deferred (needs QC set up in QB). Active Plan → `_None_`.

- **2026-06-11** — Scaffolded `0076-quickbooks-tax-code-mapping` and set as active **Plan** (switched from `_None_`). Follow-up to 0075: replace the fragile auto-apply name heuristic with an **explicit in-app province → QB-tax-code mapping** on `/admin/lookups` (single + **group** codes; rate adopted via `resolveCodeRatePct`), retire the "Pull tax codes" button, add a non-re-mapping "Refresh rates" sync, and guard quotes in unmapped provinces. Driven by hands-on prod probing this session (realm `193514766730959`, manual tax): the heuristic mis-maps NS (stale `HST NS` 15% vs `HST NS 2025` 14%) and can't map Atlantic/group provinces; per-customer `DefaultTaxCodeRef` exists but the live data is wrong → centralized province→code map is the key. QB supports Quebec via a GST+QST group code (non-compound since 2013 → flat 14.975% is correct); the push stamps the per-line `TaxCodeRef` and QB posts GST/QST to separate GL accounts at invoice. Wrote `intent.md` (row-model open Q + GL rationale) + `plan.md` (6 phases, Phase 1 a decision gate). No code yet; interim `scripts/0075-tax-map.mjs` override unblocks pushes meanwhile. Schema (if any) → `db-conventions`.

- **2026-06-10** — **Built `0075-quickbooks-tax-rate-source` end-to-end + closed** (auto-close on PASS-with-warnings). QuickBooks is now the **source of truth for province tax rates**: Phase 1 was a research/decision gate (owner chose **name heuristic** auto-sync, **editor removed entirely**, **per-province override deferred** — [decision.md](closed/0075-quickbooks-tax-rate-source/decision.md)); then the "Pull tax codes" sync **name-matches** each province → QBO `TaxCode` ("HST ON" → ON) and **adopts its rate** into `tax_rates.rate`, the in-app tax-rate editor was removed, and managed is inferred from `quickbooks_tax_code_id` (**no migration**). 5 phases on `main`: `e883e4a` (P1 gate) → `1de4bb1` (P2 matcher) → `4262b5f` (P3 rate adoption) → `c562cbd` (P4 editor removal) → `05ff72c` (P5 wiki); in-cycle eval fixes `e98862b` + `aecbd9e`. `/eval` **PASS-with-warnings** ([eval](closed/0075-quickbooks-tax-rate-source/eval-2026-06-10-1557.md)): static clean (1092 serial), smoke 3/3, Codex **1 High / 2 Medium / 1 Low — all fixed in-cycle**, 1 speculative Low parked (**0075-a**). **ON adoption live-verified on the CA sandbox**; multi-province deferred to prod. **Sandbox-only — not on prod, no migration.** Active Plan → `_None_`.
- **2026-06-10** — Scaffolded `0075-quickbooks-tax-rate-source` and set as active **Plan** (switched from `_None_`). Owner decided **QuickBooks should be the source of truth for tax rates** (vs validate-only/hybrid). Extends 0074: pull QB's rate → adopt into `tax_rates.rate`, match by **jurisdiction** (not rate), make the rate editor **read-only** (0071-style QB-as-master). Wrote `intent.md` (crux: jurisdiction-matching strategy — manual/heuristic/hybrid — a Phase-1 gate; + the CA-sandbox-ON-only blocker) + `plan.md` (5 phases). No code yet. Schema touch (if any) → `db-conventions`.
- **2026-06-10** — **0074 live CA smoke ✅ + line-level `TaxCodeRef` fix.** After re-linking the CA sandbox (reset stale US ids → re-sync dealers + hand-link a CA item), the first push of an ON quote hit QBO **error 6000** ("every transaction needs a GST/HST rate") — the **txn-level** `TxnTaxCodeRef` (Phase-1 guess) is rejected by QBO Canada. Fixed `8cead78` (per-line `SalesItemLineDetail.TaxCodeRef`; no `TxnTaxDetail`) + docs `5205d00`. Re-push **succeeded**: CA Estimate `182` = Subtotal $2000 · HST (ON) 13% $260 · Total $2260, `quickbooks_estimate_id` backfilled. **Tax path verified end-to-end on real QBO.** Eval addendum: [eval](closed/0074-quickbooks-tax-alignment/eval-2026-06-10-1259.md). The whole bidirectional QBO effort (0070/0071/0073/0074) is now live-verified on sandbox.
- **2026-06-10** — **Built `0074-quickbooks-tax-alignment` end-to-end + closed** (auto-close on PASS-with-warnings). The QBO tax-alignment slice — a pushed quote→Estimate now carries correct tax via a province→QBO-`TaxCode` map (`tax_rates.quickbooks_tax_code_id`, migration `0035`) + `TxnTaxDetail.TxnTaxCodeRef` (QBO computes), replacing the dropped `TotalTax` override. Phase 1 was a research gate (resolved: CA sandbox `9341457252668239`, manual tax not AST). Code commits `3bb47b4`→`4ae5708`→`19e0c5f`→`fac9d26`→`9507372`; in-cycle eval fix `963ee2e` (province-collision → ambiguous, not mislinked). `/eval` **PASS-with-warnings** ([eval](closed/0074-quickbooks-tax-alignment/eval-2026-06-10-1259.md)): static clean (1095 serial), smoke 1/1, Codex 1 High (fixed) / 1 Medium (parked **0074-a** — stale tax code on taxed→untaxed re-push) / 1 Low (intentional). **Sandbox-only — not on prod; needs the prod `TaxPrefs`/AST check + a live CA smoke.** The bidirectional QBO effort is now functionally complete (0070/0071/0073/0074). Active Plan → `_None_`.
- **2026-06-10** — Scaffolded `0074-quickbooks-tax-alignment` and set as active **Plan** (switched from `_None_`). The QBO **tax-alignment** slice — fixes the confirmed 0073 finding (pushed Estimates omit tax: the `TxnTaxDetail.TotalTax` override is dropped by QBO's automated sales tax → Estimate total = pre-tax subtotal). Approach: pull QBO `TaxCode`/`TaxRate` → map province ↔ tax code → set a real `TaxCodeRef` on the Estimate (drop the override) → reconcile so total matches the quote. **Phase 1 is a research/decision GATE** — owner resolves the **US-sandbox-vs-Canadian-prod blocker** (the Canadian tax-code shape can't be built against the US California sandbox) + the AST/tax-code-shape questions before building; Phases 2–6 are provisional. Wrote `intent.md` (blocker + 5 design Qs) + `plan.md` (6 phases). No code yet. Evidence: [0073 eval addendum](closed/0073-quote-estimate-push/eval-2026-06-10-0911.md).
- **2026-06-10** — **Built `0073-quote-estimate-push` end-to-end + closed** (auto-close on PASS-with-warnings). **Slice 3 — the final build slice**: quote → QBO Estimate push. 5 phases on `main`: `0f4a1df` (P1 — `quotes.quickbooks_estimate_id` + partial unique index, migration `0034`, sandbox) → `788ea03` (P2 — `client.ts` `createEstimate`/`updateEstimate`/`fetchEstimateById`) → `d2cc9c7` (P3 — `quote-push.ts`: `checkQuotePushReadiness` preflight + `mapQuoteToEstimate` + push core) → `9a589fb` (P4 — `pushQuoteToQuickbooks` Server Action + gate-matrix row + `loadQuoteEstimatePushData` + admin-gated quote-page button + `quickbooksEstimateId` on `loadQuote`) → `25090fd` (P5 — integration suite, QBO mocked). In-cycle eval fix `308d5a8` (Amount = unit×qty for QBO line-consistency). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0073-quote-estimate-push/eval-2026-06-10-0911.md)): static clean (1067 tests run serially — parallel runs flake on the sandbox session-pooler 15-client cap; infra, not code), browser smoke 1/1 (`/quotes/3` button renders), Codex **0 High / 2 Medium** — Amount-consistency fixed in-cycle, **sparse-update line-replacement parked as 0073 follow-up (a)**. **Sandbox-only — not on prod; prod Estimate push additionally gated on prod `service_items` being QBO-linked.** Completes the bidirectional QBO build slices (0070/0071/0073); remaining QBO work = the tax-alignment pull. Active Plan → `_None_`.
- **2026-06-09** — Scaffolded `0073-quote-estimate-push` and set as active **Plan** (switched from `_None_`). **Slice 3 — the final build slice** of the bidirectional QBO effort: push a quote → QBO **Estimate** (app→QBO, on-demand), reusing 0070's `CustomerRef` + 0071's `ItemRef`. Mirrors the dealer-push module (read-before-write SyncToken, guarded create-then-backfill of `quotes.quickbooks_estimate_id`). Centerpiece is a **pre-flight link check** (dealer + every line SKU must be QBO-linked, fail closed). Wrote `intent.md` (5 open Qs: pushable statuses, tax-override-vs-TaxCode, gating admin:access, variable-price lines, fee/travel-vs-lines) + `plan.md` (5 phases: schema `quickbooks_estimate_id` + migration via `db-conventions` → `client.ts` `createEstimate`/`updateEstimate`/`fetchEstimateById` → `quote-push.ts` preflight+map+core → Server Action + quote-page button → tests/smoke/wiki). Sandbox-first; **prod push blocked until prod `service_items` are QBO-linked** (no prod pull yet). Schema touch → `db-conventions` skill.
- **2026-06-09** — **Built `0072-service-items-readonly-list` end-to-end + closed** (auto-close on clean PASS). Read-only "Service items" catalog viewer on `/admin/quickbooks` — fills the view-the-catalog gap 0071's CRUD-removal left. 2 phases on `main`: `5a83d84` (`loadServiceItemsForAdmin` loader + `service-items-list.tsx` read-only table + node-env render test) → `3b45b34` (wired into the page, loaded unconditionally above the QBO change-set). Drive-by `2535934`: a local sandbox "Pull items" had replaced the sandbox catalog with 14 linked QBO sample items, breaking 0071's `item-sync` archive test (`=== 1` → `>= 1`, blanket-archive is shared-state-dependent). Chunk-end `/eval` **clean PASS** ([eval](closed/0072-service-items-readonly-list/eval-2026-06-09-1900.md)): static clean (1047 tests, tsc, 0 new lint), browser smoke 1/1, **Codex 0 findings**. No schema/Server Action. **Prod deploy pending** (`/build 0072 deploy to prod` — code-only). Active Plan → `_None_`.
- **2026-06-09** — Scaffolded `0072-service-items-readonly-list` and set as active **Plan** (switched from `_None_`). Small follow-up to 0071: 0071's removal of the in-app catalog editor left no "view my service items" UI (only the composer picker; `/admin/quickbooks` Items is a change-set preview, not a list). Adds a **read-only "Service items" list** on `/admin/quickbooks` (code · label · price · QB-linked? · archived?), connection-independent. Pure additive read-only UI — no schema/migration/Server Action. Surfaced when the owner asked "is there a UI to see the service items?". 2 phases (`loadServiceItemsForAdmin` + `service-items-list.tsx` → page-wire + smoke). Distinct from the next *slice* (Slice 3 — Quotes → QBO Estimates), which stays unscaffolded.
- **2026-06-09** — **Built `0071-quickbooks-item-pull` end-to-end + closed** (auto-close on PASS-with-warnings). Slice 2 — **QuickBooks is the item master**; `service_items` becomes a read-through mirror + in-app item CRUD removed. 5 phases on `main` (no feature branch): `8192b9a` (P1 — `service_items.quickbooks_id` + partial unique index, migration `0033`, sandbox) → `194bcd0` (P2 — `client.ts` `fetchItems` + `src/lib/quickbooks/item-sync.ts`: map/classify/`applyItemSync` create·overwrite·archive·purge, executor-injection) → `bad1005` (P3 — deleted `services/actions.ts`/`service-schema.ts`/`services-admin.tsx` + `/admin/lookups` editor + 3 gate-matrix rows) → `aa6d0be` (P4 — `/admin/quickbooks` Items change-set + `pullItemsFromQuickbooks` action wrapped in `db.transaction` + gate-matrix row) → `426df00` (P5 — `tests/integration/item-sync.test.ts`, 8 cases incl. snapshot-safety). In-cycle eval hardening `7f86715` (price `toFixed(2)` anti-churn; **zero-syncable destructive-pull guard**; `applyItemSync` requires an explicit executor). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0071-quickbooks-item-pull/eval-2026-06-09-1429.md)): static clean (1045 tests, tsc, 0 new lint), browser smoke 2/2 (Items change-set + Pull button; lookups editor gone), Codex **1 High / 1 Medium / 1 Low** — Medium (tx-safety) + Low (price) fixed in-cycle, **High (partial-read destructive sync) partially fixed in-cycle, residual parked as 0071 follow-up (a)**. **Sandbox-only — migration `0033` + code NOT deployed to prod; `main` not pushed to `origin`.** Active Plan → `_None_` (Slice 3 Estimates is the unblocked next-up, not yet scaffolded).
- **2026-06-09** — **Re-scoped `0071-quickbooks-item-pull` → "QBO is the item master"** (owner directive, same day as scaffold). Inverts the original link-only/never-clobber framing: QBO now owns items, the app's `service_items` is a **read-through mirror**, and **in-app item CRUD is removed**. Rewrote `intent.md` (scope-pivot banner + new open questions: first-pull create-fresh-vs-code-match, empty-catalog guard) + `plan.md` (now **5 phases** — added "remove in-app item CRUD"; apply semantics now create / overwrite-from-QBO / archive-missing / hard-delete-legacy). Owner decisions captured (via AskUserQuestion): old non-QBO SKUs → **hard-delete**; QBO deactivation/deletion → **auto-archive on pull**; items admin → **read-only + Pull on `/admin/quickbooks`**, lookups editor removed. History-safety confirmed via `quote_line_items` snapshots + `set null` FK.
- **2026-06-09** — Scaffolded `0071-quickbooks-item-pull` and set as active **Plan** (switched from `_None_`, where the slot sat after `0070` closed + merged earlier today). **Slice 2 of the bidirectional QuickBooks effort** — one-way **pull** of QBO Items → `service_items` (+ a new `service_items.quickbooks_id`), the `ItemRef` prerequisite for Slice 3 (Quotes → Estimates). Mirrors 0069's dealer-sync diff/apply machinery but for items and pull-only (owner's call — no create-from-app). Owner-curated `label`/`unit_price`/`description` never clobbered (link-only `quickbooks_id` backfill on a `code` match). Wrote `intent.md` (4 open questions: Sku-vs-Name-vs-code match key, Item types Service/NonInventory + skip Category/sub-items, derived-`code` uniqueness on insert, active-only) + `plan.md` (4 phases: schema+migration via `db-conventions` → `fetchItems`+`item-sync.ts` → `/admin/quickbooks` Items change-set + `pullItemsFromQuickbooks` action → tests/smoke/wiki). Sandbox-first. Schema touch → `db-conventions` skill.
- **2026-06-09** — **Built `0070-quickbooks-dealer-push` end-to-end + closed** (auto-close on PASS-with-warnings). Slice 1 of the bidirectional QBO effort — app→QBO dealer push, the reverse of 0069's pull. 4 phases on feature branch `0070-quickbooks-dealer-push`: `3cbcc10` (P1 — `client.ts` `createCustomer`/`updateCustomer`/`fetchCustomerById` + `QboCustomerInput` + `QboDuplicateNameError`; existing `accounting` scope already permits writes) → `763844e` (P2 — `src/lib/quickbooks/dealer-push.ts`: `mapDealerToCustomer` inverse map + `pushDealerToQuickbooks` core, read-before-write SyncToken on update, guarded create-then-backfill; executor-injection for rolled-back integration tests) → `8329203` (P3 — `pushDealerToQuickbooks` Server Action + gate-matrix row + `loadDealer` `quickbooksId` extension + dealer-page QuickBooks section/button + `?qbpush=` flash) → `b31e73c` (P4 — 3 integration tests + `scripts/0070-dealer-push-smoke.ts` fixture). In-cycle eval fix `a7d3cc9` (the `safeparse-required` lint — switched the action to `safeParse(Object.fromEntries(formData))`). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0070-quickbooks-dealer-push/eval-2026-06-09-1211.md)): static clean (1056 tests, tsc, 0 new lint), browser smoke 1/1 (fixture dealer #165 → QuickBooks section + "Push to QuickBooks" button render against the live sandbox connection; not clicked — read-only), Codex **0 High / 3 Medium + 2 Low**. The 3 Mediums (concurrent stale-SyncToken, create-before-guard orphan, sparse-can't-clear) + 1 Low (SyncToken `'0'` guess) + 1 Low (6240 string-code) all **parked as 0070 follow-ups (a)–(e)** above — concurrency/fidelity edge cases or by-design trade-offs out of this sandbox-only slice's scope. **No schema/migration** (reuses `dealers.quickbooks_id`). On feature branch — not merged/deployed; prod push still gated on owner-pending Intuit Production approval. Active Plan → `_None_`.
- **2026-06-09** — Scaffolded `0070-quickbooks-dealer-push` and set as active **Plan** (switched from `_None_`, where the slot sat after `0069` closed 2026-06-08). **Slice 1 of a multi-slice bidirectional QuickBooks effort** the owner requested ("can we make it bidirectional"). Reverses 0069's QBO→app pull: an admin pushes an in-app dealer to QuickBooks via an explicit **"Push to QuickBooks"** button on `/dealerships/[id]` — linked (`quickbooks_id` set) → **update** the QBO Customer with a freshly-read `SyncToken`; unlinked → **create** + **backfill** the returned `Id` (guarded `UPDATE … WHERE id=? AND quickbooks_id IS NULL`, mirrors `applyDealerSync`'s link path). **Owner decisions (via chat):** quote push-back = **Estimate** (Slice 3, not Invoice); trigger = explicit **button** (not auto-on-save); **items one-way pull** (Slice 2, QBO `Item` → `service_items`, not create-from-app); **tax aligned by pulling QBO rates into the app** (later slice, QBO→app). Wrote `intent.md` (4 open questions: SyncToken read-before-write vs stored column, `DisplayName` uniqueness 6240, flat-`address` fidelity vs QBO structured `BillAddr`, email/phone from primary contact) + `plan.md` (4 phases: `client.ts` write helpers + `fetchCustomerById` for SyncToken → `src/lib/quickbooks/dealer-push.ts` inverse `mapDealerToCustomer` + push core → Server Action + dealer-page button + `?qbpush=` flash → tests + fixture-driven smoke). Reuses the unmerged-`0068` plumbing (now live on prod via rev `-00010-759`). Sandbox-only; prod push gated on owner-pending Intuit **Production** redirect URI + API approval.
- **2026-06-08** — **Built `0069-quickbooks-dealer-sync` end-to-end + closed** (auto-close on PASS-with-warnings). Turns the 0068 read-only viewer into a dealer-sync surface. 4 phases, feature+test commits + docs: `79a74ff` (P1 — `dealers.quickbooks_id` nullable + partial unique index `WHERE quickbooks_id IS NOT NULL`, migration `0032` applied to **sandbox**) → `80d502c` (P2 — `src/lib/quickbooks/dealer-sync.ts`: pure `classifyDealerSyncPlan` + DB `computeDealerSyncPlan`/`applyDealerSync`, executor-injection for rolled-back integration tests, guarded link UPDATE + `onConflictDoNothing` on the partial index; 12 unit + 5 integration tests) → `a93f779` (P3 — page pivots to a change-set table, `syncDealersFromQuickbooks` Server Action redirecting `?synced=c.l.s`, gate-matrix row) → `b2e9a7f` (P4 — encode/decode round-trip tests + wiki `data-model.md`). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0069-quickbooks-dealer-sync/eval-2026-06-08-1703.md)): static clean (1035 tests, tsc, 0 new lint on touched files), browser smoke 1/1 (connected change-set table against the live sandbox QB company — 26 customers all "Create", sandbox `dealers` empty), Codex **0 High / 4 Medium / 1 Low**. **Two eval-cycle fixes:** `5ebb6b8` (a stray **NUL byte** in the `nameAddrKey` match separator made git treat `dealer-sync.ts` as binary + hid it from review — replaced with a `JSON.stringify([name,address])` key) and `f96c140` (Low — `decodeSyncSummary` digit-guard so `?synced=1x.2.3` is rejected). The 4 Codex Mediums are all out-of-scope / inherited-from-0060 edge cases → **parked as 0069 follow-ups (a)–(d)** above. **Build decisions:** the sync action lets errors propagate (no catch) so the gate-matrix doesn't read its success-redirect as a deny (mirrors `connectQuickbooks`); mapping helpers **duplicated** (not rewired) from the 0060 script per the intent non-goal. **All on the unmerged `0068-quickbooks-oauth-viewer` branch — 0068 + 0069 merge to `main` together.** Active Plan → `_None_`. ⚠️ `docs/wiki/log.md` (my 0069 entry) + the prior-session GCP docs (`go-live-accounts.md`, `deploy.sh`, `.gitignore`) were left **uncommitted** to avoid entangling unrelated stage-move work; only `data-model.md` shipped with the chunk.
- **2026-06-08** — Scaffolded `0069-quickbooks-dealer-sync` and set as active **Plan** (switched from `_None_`, where the slot sat after 0068 closed earlier today). Follow-up to 0068 that turns the read-only viewer into a write path. Owner-clarified the matching strategy (via chat, after declining the AskUserQuestion round): **sandbox** has no dealers → sync **inserts**; **prod** dealers already exist (0060 QB extract) and just **lack QB IDs** → sync matches by name+address and **backfills the QB ID**. One env-agnostic upsert (match-by-QB-ID → match-by-name+address & backfill → insert); never clobbers local fields. Wrote `intent.md` (3 open questions: Job sub-customers, name+address collisions across two QB customers, inactive customers) + `plan.md` (4 phases: schema `quickbooks_id` + unique partial index via `db-conventions` → shared `dealer-sync.ts` extracting the 0060 map/upsert → Server Action + "Sync dealers" button → tests/smoke). Trigger defaulted to an **explicit button** (not auto-on-connect). Builds on the **unmerged** `0068` branch. **Refined same session** (owner: "not display the customer but display and sync change") — the connected page **pivots from a passive customer list to a sync-diff surface**: `computeDealerSyncPlan` classifies each customer read-only on load (Create / Link → #N / Already linked / Skip) into a change-set table; the button applies via `applyDealerSync`. Phase 2 split into compute-plan (read-only) + apply (writes); Phase 3 renamed to "Sync-diff page + Server Action button".
- **2026-06-08** — **Built `0068-quickbooks-oauth-viewer` end-to-end + closed** (auto-close on PASS). In-app QuickBooks **OAuth read-only viewer** (sandbox) — the in-app OAuth slice 0060 deferred. 4 phases, 4 feature/test commits + docs: `177ebfa` (`quickbooks_connection` singleton + `sealed-box` AES-256-GCM token crypto; migration `0031` applied to **sandbox**) → `a678ca6` (OAuth client + connection/lifecycle + connect/disconnect actions + callback route + `.env.example` + 2 gate-matrix rows) → `aa199eb` (admin viewer page + Admin→QuickBooks nav) → `0338e63` (token-refresh + callback unit tests). Chunk-end `/eval` **PASS** ([eval](closed/0068-quickbooks-oauth-viewer/eval-2026-06-08-1440.md)): 1008 tests / tsc clean / **0 new lint** (the 147 repo-wide lint errors are all pre-existing), browser smoke 2/2 (auth-injected `/admin/quickbooks` disconnected-state + nav item), Codex no findings (final synthesis truncated — known pattern; the captured trace cleared auth-bypass / XSS-via-`?error=` / CSRF-state / singleton-constraint). **Build-time decisions:** renamed crypto helper `secret-box`→`sealed-box` to dodge the repo's `*secret*` .gitignore catch-all; split `connection.ts` (persistence + token lifecycle) out of `client.ts` (pure HTTP); `disconnectQuickbooks` uses `revalidatePath` (not `redirect`) so the gate-matrix doesn't misread its success-redirect as a deny. **All on branch `0068-quickbooks-oauth-viewer` — NOT merged to `main`.** Parked **0068 follow-up (a)** (owner end-to-end OAuth round-trip) + **(b)** (deploy.sh QBO secret-wiring). Active Plan → `_None_`. ⚠️ Separately flagged to the owner: an unrelated working-tree `.gitignore` edit (dropped `*secret*` / `*.secret`) appeared mid-session — not from this chunk, not committed.
- **2026-06-08** — Scaffolded `0068-quickbooks-oauth-viewer` and set as active **Plan** (switched from `_None_`, where the slot had sat since `0067` closed earlier today). The **in-app QuickBooks OAuth slice `0060` explicitly deferred** — 0060 shipped only the one-time script seed (Playground token, no client secret; 137 dealers on prod 2026-06-05). Owner has now loaded the **sandbox** client ID + secret into Secret Manager and chose (via AskUserQuestion) a **read-only viewer** scope: Admin → QuickBooks page, OAuth connect/callback/refresh/disconnect, encrypted singleton token store, live customer-list **display**, **no DB writes**. Wrote `intent.md` (why + non-goals + the OAuth facts settled in 0060) + `plan.md` (4 phases: token store/crypto via `db-conventions` → OAuth flow + config wiring → admin viewer + nav → tests/smoke), cross-linked to 0060; updated 0060's **Parked** entry to point its in-app-OAuth line here. Schema/migration (Phase 1) gates through `db-conventions`.
- **2026-06-08** — **Built `0067-send-test-msa` end-to-end + closed** (auto-close on PASS-with-warnings). Admin Send Test MSA BoldSign-verification tool (sibling of 0064): `sendTestMsa` action + Zod schema + form + `/admin/send-test-msa` page + nav + gate-matrix row + a webhook `metaData.test` guard. 5 commits: `7377653` (P1 action/schema/webhook) → `85ebd48` (P2 form) → `61d4eab` (P3 page/nav) → `1b78eab` (P4 wiki) → `2c9a011` (eval fix). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0067-send-test-msa/eval-2026-06-08-0917.md)): static clean (964/2-skip, tsc, lint 0 in changed files), browser smoke 3/3, **Codex 1 Medium fixed in-cycle** (`APP_ENV` `isSandbox` normalization mismatch — pre-existing in `client.ts`, deduped into `isProductionEnv()`). **Build decision:** gated `admin:access` not `msa:edit` (the latter admits coaches; this sends real prod envelopes). No parked follow-ups. **Code-only — no migration; prod deploy next.** Active Plan → `_None_`.
- **2026-06-08** — Scaffolded `0067-send-test-msa` and set as active **Plan** (switched from `_None_`). Admin BoldSign-verification tool mirroring 0064 (Send Test Email): `/admin/send-test-msa` page (gated `msa:edit`) → thin Server Action over `renderMsaPdf` + `sendSignatureRequest`, surfacing the BoldSign `documentId`; no DB row, no quote bundle. Carries two open questions for build (real prod `isSandbox=false` send; signed-test webhook 404 guard). Also: the prior prod-deploy carry-forward is resolved (0065+0066 live on prod since today) — condensed to the **Prod baseline** line up top.
- **2026-06-08** — **Built `0066-flatten-service-item-units` end-to-end + closed** (auto-close on PASS-with-warnings). Flattened `service_items` to a single `unit_price`: dropped the `service_item_unit` enum + `unit`/`unit_price_min`/`unit_price_max` (migration `0030`, generated via `db:generate` so the drizzle snapshot stays honest, hand-annotated with a leading backfill `UPDATE`), backfilled `record-retrieval` to **$100** (no longer seeds $0), simplified the `/admin/lookups` form + Zod schema + tests, ingested the flat shape into `data-model.md` + `log.md`. Shipped as 2 commits: `6f4057c` (schema + migration + all consumers — one atomic tsc-unit, since `unit` was `.notNull()` so inserts couldn't compile without it until dropped) + `f6e8309` (wiki + chunk docs). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0066-flatten-service-item-units/eval-2026-06-08-0807.md)): tests 940/2-skip, tsc clean, **0 new lint errors** (the 2 in quotes/actions.ts are pre-existing safeparse-required, untouched lines), browser smoke 2/2 (form simplified · `record-retrieval` seeds $100), **Codex 0 findings**. **Deviation from plan Phase 3:** kept `unit_price` **optional** (blank = variable) rather than "always-required" — preserves the NULL-price `travel` row; trivially flippable later. Applied migration to **sandbox**; **prod apply pending** (see Carry-forward up top). Active Plan → `_None_`.
- **2026-06-05** — Scaffolded `0066-flatten-service-item-units` and promoted to active **Plan:** (switched away from `_None_`). Collapses the vestigial `service_items` pricing model — `unit` enum (`flat`/`per-record`/`per-touch`/`per-day`/`range`) + `unit_price_min`/`unit_price_max` — down to flat `unit_price`, the only field the post-0053/0062 line-item composer reads. Backfills the one `range` row (`record-retrieval`, seeds **$0** today) before dropping min/max; simplifies the `/admin/lookups` form; fixes the stale `service-items.ts` header comment + wiki. 4 phases (backfill+migration → schema/query → admin form → tests/wiki/smoke). Open decision: backfill price for `record-retrieval`. Origin: scoping "QuickBooks as SKU source" established the `unit`/range model is dead and exposed the $0-seed bug; flat shape also pre-aligns with a QBO `Item`. Schema change → `db-conventions` skill.
- **2026-06-04** — **Built `0065-dealer-province-tax` end-to-end + closed**; migrations applied to **stage + prod**. 6 phases (`0315ebd` dealer province field → `136e17f` tax_rates table+seed → `1fe86f6` rate admin → `f706075` auto-compute+override → `3db5555` composer/PDF → `a70a61e` wiki). Chunk-end `/eval` **PASS-with-warnings** ([eval](closed/0065-dealer-province-tax/eval-2026-06-04-1631.md)): static green (943 tests; tsc clean; 2 **pre-existing** safeparse-required errors on createQuote/setQuoteInputs), browser smoke 3/3 (tax-rate admin seeded · composer province hint · dealer province select), Codex 0 High / 2 Medium (1 parked → follow-up (a) rate-snapshot TOCTOU; 1 accepted → drafts-locked-as-override matches intent). Migrations `0026`/`0027`/`0028` applied + verified on **sandbox AND prod** (`fkfybeddnfxnjuxkqidp`, 13 seeded rates incl QC 14.975, `dealers.province`/`quotes.tax_override`/`tax_pct(6,3)`). On branch `0065-dealer-province-tax` (not yet merged/deployed — prod DB migrated ahead, safe/additive). **`0064-send-test-email` also moved to `closed/`** (shipped to prod earlier today). Active Plan → `_None_`.
- **2026-06-04** — Scaffolded `0065-dealer-province-tax` and promoted to active **Plan:** (switched away from `0064-send-test-email`, which is **Done + shipped to production** — pending formal move to `closed/`). New chunk: auto-compute a quote's Canadian sales tax from the **dealer's province** via an admin-editable, **seeded** province→rate table, keeping a per-quote manual override. Owner decisions: structured `dealers.province` field · admin-editable seeded `tax_rates` table · auto-compute with override. 6 phases (province field → rate table+seed → rate admin → compute+override → composer/PDF → tests). Schema touches need the `db-conventions` skill.
- **2026-06-04** — **Built `0064-send-test-email` end-to-end + shipped to PRODUCTION.** All 4 phases (`95957ff` action+schema → `5f1f2d0` form → `30dbdce` page+nav → `02311ac` in-cycle Codex hardening: reject multi-recipient + CRLF subject). Chunk-end `/eval` **PASS** ([`eval`](0064-send-test-email/eval-2026-06-04-1412.md)): static green (919 tests), browser smoke 4/4, Codex clean (2 Lows fixed in-cycle). Live dev send confirmed (Resend id `cb007019`). Then: cleaned up dangling Firebase hosting files, **folded the `deploy-prod-db-secret` branch into `main`** (17 commits, pushed), verified prod DB at migration `0025`, and **deployed to production** (us-east4, image `:20260604-190537`, live on `eventpro.salesability.ca`). First deploy mistakenly hit the orphaned Montreal service (deploy.sh default-region footgun — see **Last deploy** above); corrected with `GCP_REGION=us-east4`.
- **2026-06-04** — Scaffolded `0064-send-test-email` and promoted to active **Plan:** (switched away from `0060-quickbooks-integration`, which stays **blocked on the owner minting a QBO production access token** → moved to **Parked:**; un-park when the token arrives). New chunk: a standalone admin **Send Test Email** page — free-compose form (To / Subject / Body) gated by `email:send`, a thin Server Action over the existing `sendEmail()` helper (`src/lib/email/send.ts`). Owner scope decisions: **free compose** · **standalone admin tool** · primary purpose **deliverability test** (surface the Resend message-id / error in the UI). No schema change, no new capability (reuses `email:send`). Built on `intent.md` + `plan.md`; 4 phases (schema+action → compose form → admin page+nav → tests+smoke).
- **2026-06-03** — **Un-deferred `0060-quickbooks-integration`** (`future/` → top-level) and made it the active Plan. Owner prioritized the QBO dealer import and answered Open Decision #1 → **one-time import**. Wrote `plan.md` (one-time seed slice, no schema change — name+address dedup) + `runbook.md` (Intuit OAuth Playground production-token steps) and the importer `scripts/import-from-quickbooks.ts` (QBO Customers → `dealers` + `dealer_contacts(role='staff')`, modeled on `import-from-sheets.ts`; lint-clean, module-loads smoke). Swept cross-refs (`go-live-accounts.md`, this file). **Blocked on owner** to mint a production access token; the in-app OAuth connection + `external_account_links` + living sync remain later slices (still in `intent.md`/`research.md`). Previous Plan was `_None_`.
- **2026-06-02** — **Shipped + closed `0062-quote-line-item-picker`** → [`closed/0062-quote-line-item-picker/`](closed/0062-quote-line-item-picker/plan.md). The quote composer pivoted from the 0035 parametric calculator to a **SKU line-item picker** (owner: "the preloaded is too complicated and buggy"): pick SKUs from the `service_items` catalogue, per-quote editable price (catalogue-seeded, reuses 0052 override), lines persist to a relational `quote_line_items` table. 8 phases built via `/build` on branch `0062-quote-line-item-picker` (`2b735dd`→`ea0bb7e`); reversed the locked 0035 calculator decision; superseded + folded 0053; deleted `computeQuote`; dropped the `quotes.line_items` jsonb (`0025`, after backfill in `0024`); added the `renderLines` correlated-subquery so the PDF paths read the table inline (low test churn). Also shipped a **New Quote** button on the `/quotes` grid (it had no create entry). **Migrations applied to stage Supabase first** (12 quotes → 26 line rows; the `0024` backfill was fixed mid-flight to carry `override_unit_price` + `service_item_id` — stage had **5 real overrides** that would otherwise have been lost), **then deployed** to Cloud Run sandbox (rev `event-manager-pro-00036-jxb`). **Live-smoked end-to-end** on Cloud Run against the migrated DB: `/quotes` lists all 12, a real backfilled quote hydrates its lines through the picker, create-and-save works (after mirroring the dev auth user into the container's stub — a 0063 harness fix). 901 tests, tsc clean. Closed without a formal `/eval` — the live production smoke is the gate. Branch fast-forward-merged to `main` + pushed. Active → `_None_`. Parked follow-up: `0062 follow-up (a)` real-DB integration tests via `db:test:reset`.
- **2026-06-01** — Closed `0063-test-db-harness` → [`closed/0063-test-db-harness/`](closed/0063-test-db-harness/plan.md) (3/3, `b2357f8`). Containerized `postgres:17` rebuilt-from-migration-0 harness, isolated from the shared DB (targets `TEST_DATABASE_URL`, localhost-guarded in `drizzle.test.config.ts` + `reset.sh`). Verified `0000→0024` replay clean + `0024` backfill extraction + clean teardown. Key discovery baked into `bootstrap-auth.sql`: replay-from-zero needs stubs for `auth.users`, `auth.uid()`, and roles `anon`/`authenticated`/`service_role` (the migrations reference Supabase-managed bits they never create). Closed without a formal `/eval` — hands-on verification (replay + guard + teardown) is the gate; tsc clean + 953 tests pass. **Active flipped back to `0062-quote-line-item-picker` Phase 7** (the harness's whole purpose). Same branch throughout.
- **2026-06-01** — Scaffolded `0063-test-db-harness` and promoted to active **Plan:** (switched away from `0062-quote-line-item-picker`, which moved to **Parked:** at 6/8 in progress on the same feature branch). Built 0062 Phases 1–6 end-to-end via `/build` (commits `2b735dd`→`6f2dd8f`, 953 tests green) — the SKU picker is working. **Paused before 0062 Phase 7** (destructive `DROP COLUMN quotes.line_items` + delete the dead calculator): the user asked for "a container-based DB we rebuild from migration 0" to rehearse destructive migrations safely rather than letting them hit the shared DB first. 0063 builds that harness (postgres:17, replay-from-zero, localhost-guarded, `auth.users` bootstrap stub since migrations reference but never create it). After 0063 verifies, resume 0062 Phase 7 against the container. Same branch throughout.
- **2026-06-01** — Scaffolded `0062-quote-line-item-picker` and promoted to active **Plan:** (switched away from `_None_`). Pivots the quote composer from the parametric calculator (`computeQuote()` auto-derive from structured inputs) to a **SKU line-item picker** over the `service_items` catalogue, after the owner called the current page "too complicated and buggy" and asked to "select from a list… build items separately… load the price in on every quote separately." Two owner decisions captured: price = **catalogue-seed-then-editable** (reuses the 0052 override mechanic) · old structured inputs **drop from the UI but keep their columns** (so production/reports/calendar readers stay green — the 0037 Phase 4 deferral). Reverses the locked 0035 "calculator, not a picker" decision (plan.md:92,146) and **supersedes `0053-quote-line-items-table`** — moved that 0/7-never-started chunk to `closed/` and folded its `quote_line_items` migration into this chunk (migrations renumbered to `0024`/`0025` since `0022`/`0023` are taken + applied). 8-phase plan: (1) table + JSONB backfill, (2) pricing module retire-derivation + picked-line totals, (3) read path, (4) write path delete-and-insert, (5) composer UI picker, (6) send/PDF + empty-quote guard, (7) drop JSONB + delete `computeQuote`, (8) tests + smoke + wiki. Building on feature branch `0062-quote-line-item-picker`.
- **2026-05-29** — **Deployed the 0057→0061 backlog to Cloud Run (sandbox)** via `./deploy.sh` (rev `event-manager-pro-00035-r7n`); applied migrations `0022`+`0023` to the shared DB first (`pnpm db:migrate`). The first deploy surfaced a latent **0059** bug: `/reports` 500'd with `column "letters" is ambiguous` (Postgres 42702) — `billingPivotSubquery` aliased a column `letters` that collides with `campaigns.letters`, only firing once `billing_adjustments` existed (0059's browser smoke was deferred + its tests mock the DB, so it shipped untested against a real DB). Fixed by aliasing the pivot columns `adj_*` (`1b1c1d1`), verified `/reports` 200 locally against the real DB, redeployed clean (deployed `/reports` + `/quotes` both 200, 0061 live). Lesson: don't defer browser smoke on DB-backed pages. Kept **sandbox** (real-email production is a separate go-live step).
- **2026-05-29** — Closed `0061-move-msa-action-to-quote` → [`closed/0061-move-msa-action-to-quote/`](closed/0061-move-msa-action-to-quote/plan.md) (5/5). Moved the MSA "Send for signature" bundled-envelope action off the admin-only dealer page onto the admin+coach quote composer; relaxed `sendMsaEnvelope` + the auto-accept path to accept a `draft|sent` quote (4 guards, not the 2 the plan first assumed — the two `lifecycle.ts` auto-accept guards were the silent gap); new `MsaSendForSignatureButton` + `deriveQuoteMsaState` helper; dealer-page trigger + dead `firstDraftQuoteForDealer` deleted. Commits `96c75eb`→`f8386c2`. Chunk-end `/eval` **PASS-with-warnings** ([`eval-2026-05-29-1418.md`](closed/0061-move-msa-action-to-quote/eval-2026-05-29-1418.md)): static green, **browser smoke 5/5 GREEN** (dev server started for the run), Codex 1 Medium **fixed in-cycle** (`099d6a4` — bundle button now blocks on unsaved edits, else a signed envelope could carry stale pricing) + 1 High **parked** (`0061 follow-up (a)` — pre-existing link-after-send race). Active → `_None_`. (Also committed prior-session docs left uncommitted: go-live-accounts runbook + 0060 quickbooks `future/` scaffold.)
- **2026-05-29** — Scaffolded `0061-move-msa-action-to-quote` as the active plan (switched away from `_None_`). Moves the "Create MSA + send for signature" bundled-envelope action off the admin-only dealer page (`dealerships/[id]`) onto the admin+coach quote composer (`/quotes/[id]`) — also fixes a latent capability asymmetry (coaches hold `msa:edit` but couldn't reach the only trigger, which lived behind `admin:access`). Owner decisions via AskUserQuestion: button model = **primary+secondary** (no MSA → "Send for signature" primary, "Send Quote" demoted outline); eligibility = **draft or sent** (relax `sendMsaEnvelope`'s draft-only guard at `actions.ts:257` **and** its `quotes.msa_id` link UPDATE at `:387`); dealer page = **keep read-only status, remove action**. `intent.md` + `plan.md` (5 phases) written. Carried-forward note: 0057/0059 deploy (`pnpm db:migrate` for `0022`+`0023`) still pending.
- **2026-05-29** — Scaffolded `0060-quickbooks-integration` straight into [`future/`](closed/0060-quickbooks-integration/intent.md) (un-deferred to top-level 2026-06-03) from a research conversation (QBO Online API + auth). Captured `intent.md` (why + scope + non-goals + the two open decisions) and `research.md` (OAuth 2.0/OIDC flow, `realmId` gotcha, single-tenant one-connection model, QBO Customer→`dealers` field map, the `external_account_links` schema gap, idempotency via the existing `contact_identifiers` partial-unique, pagination/rate limits, `intuit-oauth`/`node-quickbooks` SDKs). No `plan.md` — blocked on owner decisions (sync cadence · system-of-record). Active plan pointer unchanged (`_None_`); added to **Future:**.
- **2026-05-22** — Closed `0059-reports-inline-edit` → [`closed/0059-reports-inline-edit/`](closed/0059-reports-inline-edit/plan.md) (5/5). Last of the 0057→0059 batch. Admin-only inline billing-figure edits on `/reports`: new `billing_adjustments` overlay table + RLS (`0023`, `a4f4769`), `setBillingAdjustment` action + editable cells + effective totals via a `billingPivotSubquery` LEFT-join coalesce (`706dca4`), tests + wiki + `useCan` pairing (`8b3c0c6`), zod-safeParse lint fix (`71d0b89`). Owner decisions (table model · Records/SMS-Email/Letters/BDC · admin-only) gathered via AskUserQuestion. Chunk-end `/eval` PASS-with-caveats ([`eval-2026-05-22-1524.md`](closed/0059-reports-inline-edit/eval-2026-05-22-1524.md)): Codex affirmed the SQL pivot (no fan-out) but its runtime truncated the final synthesis (same companion artifact as 0057) — all focus areas independently verified; browser smoke deferred (dev server down + `0023` not yet applied). Parked `0059 follow-up (a)` (pre-existing `msa:edit` pairing gap). Active → `_None_` (batch done). **Deploy: apply `0022` + `0023` via `pnpm db:migrate`.**
- **2026-05-22** — Deferred `0058-production-sheet-and-date-range` → [`future/0058-production-sheet-and-date-range/`](future/0058-production-sheet-and-date-range/plan.md) on owner's "park 0058". Phase 1 (1/2/3-month forward date-range filter) **shipped to `main` at `9b15e29`** and stays merged — client-side `filterFn` + CSV-export mirror, sharing only the `rangeWindowEndIso` date math (`filter.ts`), 11 unit tests. Phases 2–4 (the Google Sheet) deferred: need an owner Google-auth decision + Google Cloud setup (Sheets/Drive API + credentials) the agent can't do. Un-defer trigger captured in the plan header + the **Future:** field. Active → `0059` (itself blocked on owner decisions).
- **2026-05-22** — Closed `0057-travel-estimated-label` → [`closed/0057-travel-estimated-label/`](closed/0057-travel-estimated-label/plan.md) (1/1). Built as part of the 0057→0059 batch. Relabeled Travel → "Estimated Travel" in the composer + the `travel` service-catalog row (migration `0022` + seed `0013`; PDF label is catalog-driven, not hardcoded — the plan's `render-quote.ts` anchor was corrected). Feature `2b63b0a`. Chunk-end `/eval` PASS-with-caveats ([`eval-2026-05-22-1146.md`](closed/0057-travel-estimated-label/eval-2026-05-22-1146.md)): browser smoke skipped (dev server down); Codex caught a real **migration-ordering bug** — `drizzle-kit --custom` stamped `0022`'s journal `when` (real clock) earlier than `0021`'s (future-dated), so the gate `created_at < folderMillis` would have silently skipped `0022` on the shared DB — fixed in-cycle `3404fae` (bumped `when` to restore monotonicity). No parked follow-ups. **Deploy step:** apply `0022` via `pnpm db:migrate`. Active → `0058`.
- **2026-05-22** — Closed `0055-quote-msa-one-document` → [`closed/0055-quote-msa-one-document/`](closed/0055-quote-msa-one-document/plan.md) (6/6). Phase 5 was a no-op (owner's resent quote statement matched the in-code constants verbatim). Phase 6 live BoldSign sandbox smoke run on **Cloud Run** (`nnwweb`/`event-manager-pro`, rev `00031`): real round-trip flipped MSA #7→active, auto-accepted quote #3, persisted `gs://event-pro/msa/7/signed.pdf`, template `2026-05-21`. The smoke caught + fixed two prod bugs the mocked tests missed — webhook 307'd by the auth proxy (`097893c`) and field anchors sent in PDF points vs BoldSign's 96-DPI px (`d06082b`); both committed + deployed. Resolved parked `0041 follow-up (e)`. No formal `/eval` (verified live instead). Soft gate still open: owner/lawyer eyeball of the verbatim MSA prose (non-code). Plan slot stays `_None_`; next candidates `0057`/`0058`/`0059`.
The bullets below are a *curated* layer over that log: same dates, but with the eval verdict, why the slot vacated, what got requeued, etc. — the context a commit message wouldn't carry. When the active plan or sub-plan changes, prepend a one-line entry (newest first) noting date, what changed, where it ended up (Done → `closed/`, Paused, Abandoned), and any non-obvious context.

- **2026-05-21** — Closed `0056-booking-inline-add-dealer-coach` → [`closed/0056-booking-inline-add-dealer-coach/`](closed/0056-booking-inline-add-dealer-coach/plan.md). All 5 phases shipped this session (4 commits): focused `CoachAddForm` + pure `coach-add-schema.ts` (deviation from "extract PersonForm" — see plan decision); "+ Add" dealer/coach buttons + dialogs in `booking-form.tsx` (DealerForm `defaultStatus='prospect'` + the new coach form); both pickers auto-select the inline-created record (coach via the new form's `onCreated`; dealer via a now-additive `createDealer` → `{ ok: true, dealerId }` threaded through `DealerForm.onSuccess(created?)`); browser smoke PASS (booking dialog shows both "+ Add"; each opens its dialog) + an a11y fix (distinct `aria-label`s on the two "+ Add" buttons, found via a strict-mode click collision). Static green: tsc clean, 884 unit tests pass, lint clean. Closed **without a formal `/eval`** (per session cadence — static + browser smoke substituted, like 0052). No unresolved follow-ups (the dealer auto-select that was parked mid-chunk got done in-cycle). Plan slot reset to `_None_`; next candidates: `0057` (quick travel-label copy change), `0058`, `0059`, plus paused `0055`. Branch still `0051-dropbox-sign-to-boldsign`; `main` 120 commits ahead of `origin/main`, unpushed.
- **2026-05-21** — Switched active **Plan:** from `0055-quote-msa-one-document` (→ Parked, **paused at 4/6**) to `0056-booking-inline-add-dealer-coach` at the user's direction ("move to 0056"). 0055 is blocked, not abandoned: Phases 1–4 (verbatim MSA, quote+MSA merge, Client-initials field, single-file envelope + sign-time auto-accept of the bundled quote) are built, tested (882 pass), and on `main`; only Phase 5 (owner's resent quote statement) and Phase 6 (deployed BoldSign sandbox smoke — no localhost webhook ingress) remain, both external. 0056 Phase 1 **deviates from the scaffolded "extract PersonForm"**: that component (`people-admin.tsx:336-635`) is ~300 lines, security-sensitive (roles/app-access/dealer-links), and overkill for a booking-time coach add — building a focused coach quick-add form (name + email + phone, forces `role=coach`) that reuses the `createPerson` Server Action (the real single-source) instead. Decision recorded in 0056 plan.
- **2026-05-21** — Closed `0052-quote-coach-price-override` → [`closed/0052-quote-coach-price-override/`](closed/0052-quote-coach-price-override/plan.md) by user direction. The chunk was already **built + committed** (`5aff65f feat(quotes): coach-editable per-line unit-price override on Summary`) — only the close-out ritual had been skipped, so the active pointer + Status sat at the stale pre-build "Phase 1 about to begin" from 2026-05-15. Flipped Phase 5 → Done (143 unit tests pass; formal `/eval` waived per user), Overall Progress → 100%, moved the folder to `closed/`, and repaired the two `0053` supersession links (`../0052-…` → `../closed/0052-…`). Plan slot advanced to `0055-quote-msa-one-document`, whose Phase 1 verbatim-MSA rewrite shipped this session (`826c517`). All already on `main` via today's branch→main fast-forward.
- **2026-05-21** — Scaffolded five chunks from the owner's change-request email and added them to **Parked:** (Active block untouched — still `0052-quote-coach-price-override`). The chunks: `0055-quote-msa-one-document` (merge Quote + lawyer's Agreement into one signable doc with initials + contract-page signature; Phase 4 blocked on the owner resending the quote statement, Agreement text stays verbatim), `0056-booking-inline-add-dealer-coach` (inline "+ Add" in the booking dialog — wires the dealer entry point `0035` Phase 2 deferred + extracts `PersonForm` for the coach side), `0057-travel-estimated-label` (trivial "Estimated Travel" copy change, plan-only), `0058-production-sheet-and-date-range` (shareable Google Sheet link + 1/2/3-month date-range filter; no Sheets API exists today), `0059-reports-inline-edit` (editable billing figures on the report — owner "nice to have," needs a data-model decision first). The owner's sixth request item ("when can we have a working copy online + update through versions") was **explicitly dropped at the owner's direction** — not scaffolded; it's a hosting/release question, not a feature. No feature branch cut; no code written yet — pure backlog scaffold.
- **2026-05-15** — Un-superseded `0052-quote-coach-price-override` (moved from `closed/` back to top-level) and promoted to active **Plan:**. `0053-quote-line-items-table` demoted to **Parked:** (still 0/7, never started). Reversed the 2026-05-15 supersession decision after the user pointed out the actual feature gap: smoke-testing the composer shows only the `taxOverride` row is editable (line 662-668 of `quote-composer.tsx`), the Unit column at line 638 is plain text — exactly the gap 0052 was scoped to close. The bundling into 0053 was *my* call after the user's clarifying question "is there db scheam change in the plan", not user direction, and it pulled forward a 4-6h relational normalization to gate a feature that 0052 ships in ~1-2h on JSONB-additive shape. The schema comment at `src/lib/db/schema/quotes.ts:28-32` is explicit that normalization is "deferred to 7.3 if invoicing needs per-line reporting" — invoicing isn't here, so the urgency was artificial. 0053's plan kept intact in case it picks up later; un-park trigger sharpened from the old "JSONB pressure" framing to "invoicing actually needs per-line `JOIN`/`GROUP BY`". When 0053 eventually runs, the `override_unit_price` column 0052 ships today is one extra column in `0022_quote_line_items_table.sql` — trivial cost. Five phases now starting: (1) `overrideUnitPrice?` on `ComputedLine` + `effectiveUnit()` + `recomputeTotalsWithOverrides()` in `src/lib/quotes/pricing.ts`, (2) `setQuoteInputs` accepts `overrides` map + persists into `lineItems` JSONB, (3) composer UI toggle + per-line editable Unit cell at `quote-composer.tsx:638` mirroring the `taxOverride` input shape, (4) PDF render prefers `effectiveUnit(line)`, (5) tests + smoke + wiki. Branch stays `0051-dropbox-sign-to-boldsign` (continuation of same session).
- **2026-05-15** — Closed `0054-boldsign-msa-signature-fields` → [`docs/chunks/closed/0054-boldsign-msa-signature-fields/`](closed/0054-boldsign-msa-signature-fields/plan.md). All 4 phases shipped same-session on branch `0051-dropbox-sign-to-boldsign`: P1-P3 in commit `3d4deeb` (`feat(msa): thread signature-field anchor from renderer to BoldSign`) — `renderMsaPdf()` now returns `signatureAnchor: { pageNumber, x, y, width, height }` captured at the right-column "For the Client" underline draw site in `src/lib/pdf/render-msa.ts:308-313` (top-left-origin BoldSign coords, 1-indexed page resolved via `doc.getPages().indexOf(sigPage) + 1`); `src/lib/boldsign/client.ts:67-100` builds a `FormField` + `Rectangle` from the anchor with `fieldType=Signature` + `id='ClientSignature'` + `isRequired=true` + `signerType=Signer`; `src/features/msa/actions.ts:325-336`'s `sendSignatureRequest` call now passes `signatureAnchor: msaPdf.signatureAnchor` (note threaded into the call site explains that the anchor's `pageNumber` is relative to the first envelope file, the MSA, not the bundled Quote attachment). 77 unit tests PASS across `render-msa.test.ts` (+2 new), `client.test.ts` (+2 new), `actions.test.ts` (unchanged behavior — mocks `sendSignatureRequest` so the new param is type-checked at call site only). P4 live smoke: user confirmed envelope sent successfully via Cloud Run after `DEPLOY_APP_ENV=sandbox ./deploy.sh` rolled the revision; BoldSign UI showed the Signature field correctly positioned over the right-column underline; user said "it is workin i think we can close 0054". Closed **without running `/eval`** per user direction (hands-on smoke replaced the formal gate). Open Questions all resolved empirically: pdf-lib bottom-left → BoldSign top-left translation via `pageHeight - (y + SIG_BOX_HEIGHT)` is correct (signature field landed where expected); box height of 22pt is visually comfortable; final-page tracking via `doc.getPages().indexOf(sigPage) + 1` (preferred over `doc.getPageCount()` since the renderer might add pages *after* the signature block in a future refactor); multi-signer future stays deferred (single anchor v1, no Salesability counter-signature surface). Branch still `0051-dropbox-sign-to-boldsign`; commits not yet pushed to remote. Plan slot returned to `0053-quote-line-items-table` (re-promoted from Parked back to Active). Sequence forward: 0053 work resumes when user picks it up; the parked **0041 follow-up (c)** "Resend envelope" button is now nice-to-have rather than load-bearing since the BoldSign happy path works end-to-end. The 0051 follow-ups (a) + (b) remain in Parked.
- **2026-05-15** — Promoted `0054-boldsign-msa-signature-fields` to active **Plan:** (un-park trigger fired). `0053-quote-line-items-table` demoted to **Parked:** with 0/7 phases done. Trigger was the exact predicted failure from the 0054 scaffold: after the regional-host fix (`a6ba420` `fix(boldsign): regional API host + EMAIL_DEV_TO signer redirect`) landed and `DEPLOY_APP_ENV=sandbox ./deploy.sh` rolled a Cloud Run revision with `BOLDSIGN_API_BASE_URL=https://api-ca.boldsign.com` + `EMAIL_DEV_TO=david.hogan@networknode.ca` threaded through, the next Create MSA attempt got past the 401 region/auth gate and hit BoldSign's validation layer at 400: `Signers.FormFields: "Form fields cannot be null"` + `UseTextTags.Signers.FormFields: "Form fields cannot be null"`. Now building the 4-phase plan: (1) renderer captures `signatureAnchor` at the right-column underline draw site (`render-msa.ts:308-313`), (2) `client.ts` builds `FormField` + `Rectangle` from the anchor, (3) `sendMsaEnvelope` threads the anchor renderer → BoldSign, (4) tests + live BoldSign smoke against `app-ca.boldsign.com`. Stuck MSA #3 (created during this attempt) deleted via `scripts/flip-msa-status.ts delete 3`. Branch remains `0051-dropbox-sign-to-boldsign` (continuation of the BoldSign migration's discovered-mid-test gap).
- **2026-05-15** — Scaffolded `0054-boldsign-msa-signature-fields` and added to **Parked:** (Active stays on `0053-quote-line-items-table`). Discovered mid-test of the just-shipped 0051 BoldSign migration: `src/lib/boldsign/client.ts:72-75`'s `sendSignatureRequest` builds a `DocumentSigner` without `formFields`, so BoldSign envelopes ship without a defined Signature box — visible via the BoldSign Node SDK quick-start snippet pasted from the user's `app-ca.boldsign.com` console (`formField.fieldType = FormField.FieldTypeEnum.Signature; formField.pageNumber = 1; formField.bounds = bounds; documentSigner.formFields = [formField]`), which our code omits. The structured fix: `renderMsaPdf()` captures a `signatureAnchor: { pageNumber, x, y, width, height }` from the existing right-column underline draw site at `src/lib/pdf/render-msa.ts:308-313` (which already sits at known `(rightColX, y)` coords inside the same dynamic-pagination scope at `:282-284`), `sendSignatureRequest` builds a `FormField` + `Rectangle` from the anchor and attaches to the signer, `sendMsaEnvelope` threads it through. This is the structured form of the renamed-from-0041 follow-up (e) "live BoldSign sandbox smoke" Medium that 0051's chunk-end eval explicitly noted "now also covers signable-field-placement verification". 4 phases: (1) renderer captures anchor, (2) client builds `FormField`, (3) action threads it, (4) tests + live BoldSign smoke against `app-ca.boldsign.com`. Four open questions seeded in `intent.md`: pdf-lib vs BoldSign coordinate-origin (empirical resolution in Phase 4 smoke), box height (lean ~22pt), final-page tracking (lean `doc.getPageCount()`-derived), multi-signer future (single anchor v1, map by signer role if Salesability ever counter-signs). Parked rather than promoted because 0053 is active; un-park trigger is concrete and imminent — next BoldSign send attempt will hit the missing-fields error class right after the user finishes setting `BOLDSIGN_API_BASE_URL=https://api-ca.boldsign.com`.
- **2026-05-15** — Same-session unblock of BoldSign 401 mid-test (no chunk, single-commit fix on whatever branch the user lands on). Issue: `src/lib/boldsign/client.ts:13`'s `BASE_URL = 'https://api.boldsign.com'` was hardcoded to the US region. User's BoldSign account is in the Canada region (`app-ca.boldsign.com` console URL → `account-ca.boldsign.com` OIDC redirect). Empirical probe via `scripts/probe-boldsign.ts` confirmed: `https://api.boldsign.com` → 401 empty-body, `https://api-eu.boldsign.com` → 401 empty-body, `https://api-ca.boldsign.com` → 200 OK with real JSON. Fix: `BASE_URL` constant → `DEFAULT_BASE_URL`; client reads `process.env.BOLDSIGN_API_BASE_URL` with US fallback; comment block rewritten to spell out the three regional hosts (US/EU/CA) and clarify that sandbox-vs-prod is signaled by the per-request `isSandbox` flag, not by a separate host. Pre-existing test at `client.test.ts:85` renamed to "default US host when BOLDSIGN_API_BASE_URL is unset"; new test asserts the env override path. All 16 client tests pass. Resolves 0051 Open Question #5 (sandbox-vs-prod URL strategy) — answer is *regional*, not sandbox-vs-prod. Also same-session: user reported MSA panel stuck on dealer #1 (status=pending, provider_document_id=NULL — Create button hidden, no resend-envelope button); resolved by running `scripts/flip-msa-status.ts delete 1` against the user's remote dev/staging Supabase (0 referencing quotes, 1 audit row purged), restoring the "No MSA on file yet" + Create MSA state for fresh BoldSign testing. Scripts left in `scripts/` as throwaway diagnostic artifacts: `flip-msa-status.ts` (list/unpost/activate/delete subcommands for MSA cleanup) + `probe-boldsign.ts` (raw-fetch probe across US/EU/CA hosts with key-shape printout).
- **2026-05-15** — Scaffolded `0053-quote-line-items-table` and promoted to active **Plan:** (switched away from `0052-quote-coach-price-override`, which moved to `closed/` as **superseded** — zero phases started, no commits). User pivoted after asking "we use jsonb to persist quote detail?" and learning `quotes.lineItems` JSONB carries the line snapshot today (per `src/lib/db/schema/quotes.ts:78` + comment at lines 28–32 calling out the v1 decision + the deferred-to-7.3 normalization note). New chunk bundles the relational normalization with the override feature so `overrideUnitPrice` ships as a real column instead of a JSONB field extension — avoids the double-migration (JSONB → JSONB+override → table). 7-phase plan: (1) schema + backfill migration `0022_quote_line_items_table.sql` with `INSERT … SELECT … jsonb_array_elements WITH ORDINALITY`, (2) read path — `queries.ts` rehydrates `ComputedLine[]` from the table (JSONB stays one phase as safety net), (3) write path — `setQuoteInputs` delete-and-inserts table rows in the same transaction as the `subtotal/tax/total` write, `hashLineItems` re-roots to table rows, (4) composer Summary override toggle + per-line input bound to the new column (the user-visible piece of the now-superseded 0052), (5) `sendQuote` PDF feeds `effectiveUnit(line) = override_unit_price ?? unit_price`, (6) second migration `0023_drop_quotes_line_items_jsonb.sql` retires the JSONB column + sweeps remaining references, (7) integration tests + web-test smoke + `docs/wiki/data-model.md` update. Six open questions seeded in `intent.md`: JSONB-column lifecycle (lean drop same chunk), `(quote_id, code)` unique constraint (lean add), `display_order` stored vs derived (lean stored), `override_unit_price` precision (lean match `numeric(10,2)`), override toggle persistence (lean derive from `exists(override_unit_price)` — no separate flag column), `hashLineItems` digest swap to table rows. Anchors fixed — `dealer_contacts.ts:1-53` for the child-table shape (FK + cascade + composite-unique-index + `actors`/`timestamps` mixin); `drizzle/0021_rename_provider_document_id.sql` for the custom-written migration prose pattern. Closes 0052 in-place with a supersede note at the top of `closed/0052-quote-coach-price-override/plan.md` linking to 0053. Not yet on a feature branch (current branch: `0048-quote-timestamp-naming`).
- **2026-05-15** — Scaffolded `0052-quote-coach-price-override` and promoted to active **Plan:** (switched away from `0051-dropbox-sign-to-boldsign`, which moved to **Parked:** untouched — feature branch already cut, zero phases started). User-driven feature request: on the Summary section of the quote composer, let the coach toggle per-line unit-price overrides; the catalogue-derived `unitPrice` is preserved on the persisted `quotes.lineItems` JSONB snapshot (the "house quote"), and the PDF/email sent to the prospect renders the tuned `overrideUnitPrice` when present. 5-phase plan: (1) extend `ComputedLine` with optional `overrideUnitPrice` + `effectiveUnit()` helper + `recomputeTotalsWithOverrides()` in `src/lib/quotes/pricing.ts`, (2) `setQuoteInputs` action accepts an `overrides: Record<string, number | null>` map + Zod-validates against the recomputed catalogue codes + persists into `lineItems` JSONB without schema migration, (3) composer UI — checkbox above Summary at quote-composer.tsx:609 + per-line editable Unit cell mirroring the taxOverride input shape at :662-668 + catalogue price shown as dim reference below the input, (4) `sendQuote` PDF path prefers `effectiveUnit(line)` over raw `unitPrice`, (5) integration tests + web-test smoke against `/quotes/<draft-id>` + wiki update to `docs/wiki/data-model.md` for the JSONB shape extension. No DB migration (JSONB is additive). Three open questions seeded in `intent.md`: clear-vs-hide on toggle-off (leaning clear), prospect-visible "tuned" footnote (leaning no), tax-fallback math when `taxOverride` is null and overrides present (leaning: recompute against override subtotal). Anchors fixed — every new code surface paired with its closest sibling in the same file (taxOverride input for the per-line input shape, ComputedLine for the type extension, setQuoteInputs for the action contract). User invoked via "make a new check[chunk]" — reading as scaffold-and-promote per the /plan skill default. Not yet on a feature branch (current branch: `0048-quote-timestamp-naming`).
- **2026-05-15** — Closed `0051-dropbox-sign-to-boldsign` → [`docs/chunks/closed/0051-dropbox-sign-to-boldsign/`](closed/0051-dropbox-sign-to-boldsign/plan.md). All 7 phases shipped via `/build` chunk loop on feature branch `0051-dropbox-sign-to-boldsign` (6 code commits + Phase 7 wiki/smoke): P1 `682b1c2` (`pnpm add boldsign@3.1.4` + `.env.example` block rewrite), P2 `287a17d` (`src/lib/boldsign/{client,webhook-verify}.ts` + 28 tests — DocumentApi wrapper with `APP_ENV`-derived basePath + Stripe-style `X-BoldSign-Signature: t=<ts>, s0=<hex>` HMAC-SHA256 over `t + "." + body` with 300s replay tolerance + s0-or-s1 key-rotation match), P3 `b4baee8` (`/api/boldsign/webhook` route + 12 tests — `request.text()` BEFORE `JSON.parse`, dispatch on `Signed` / `Declined` event types), P4 `ba29aeb` (Drizzle `0021_rename_provider_document_id.sql` + hand-crafted `0021_snapshot.json` copying 0020 with the column key renamed — drizzle-kit's rename-vs-drop-and-add prompt can't run non-interactively; 18-file sweep across actions/lifecycle/UI/test refs/scripts/integration test ordering), P5 `25e7562` (`sendMsaEnvelope` cut over to `@/lib/boldsign/client` import + `sendResult.documentId` field swap + audit-payload key `signatureRequestId` → `providerDocumentId` + lifecycle.ts comment updates), P6 `0ad23d4` (deleted `src/lib/dropbox-sign/` + `/api/dropbox-sign/webhook/` + `@dropbox/sign` dep + `DROPBOX_SIGN_*` env vars; relocated `currentMsaTemplateVersion` to provider-agnostic `src/features/msa/template-version.ts` + tests; swept comments + user-visible MSA dialog body copy to BoldSign). Phase 7: wiki sweep (`docs/wiki/architecture.md` gained dedicated E-signature row + Server-Action-vs-route example updated + Send-time MSA gate paragraph + Migration roadmap; `docs/wiki/commercial-spine.md` happy-path diagram + spine table updated; `docs/wiki/data-model.md` column list + section copy + MSA-pending-gate paragraph updated; `docs/wiki/log.md` prepended 2026-05-15 entry); browser smoke confirmed dialog body copy verbatim ("A bundled envelope is sent to the Client via BoldSign. The Client signs once and both documents take effect."). **Decision records D #1–#5** in [`closed/0051-dropbox-sign-to-boldsign/intent.md`](closed/0051-dropbox-sign-to-boldsign/intent.md) Resolved decisions: D #1 column rename to provider-agnostic `provider_document_id`; D #2 official `boldsign` Node SDK; D #3 no in-flight envelope handover needed (Dropbox Sign was never used in production — zero rows, audit-payload-key rename trivially safe); D #4 webhook signature scheme; D #5 `APP_ENV`-derived sandbox-vs-prod URL (no extra env var). Chunk-end `/eval` at [`closed/0051-dropbox-sign-to-boldsign/eval-2026-05-15-1644.md`](closed/0051-dropbox-sign-to-boldsign/eval-2026-05-15-1644.md) verdict **PASS with warnings**: static green (tsc clean / vitest 843 PASS / lint 0 errors + 6 carryforward warnings), browser smoke Phase 1 4/4 + Phase 2 3/3 PASS (MSA-flow-scoped: `/dealerships/12` MSA panel + BoldSign-copy dialog + `/quotes/4` composer all clean), Codex 0 High / 1 Medium / 2 Low. **Codex Medium parked under existing `0041 follow-up (e)`** (renamed "live Dropbox Sign sandbox smoke" → "live BoldSign sandbox smoke" — now also covers signable-field-placement verification; localhost has no public ingress for BoldSign webhook callback + no sandbox creds in `.env.local`, so the round-trip is its own small chunk when ready). **Codex Lows parked as 0051 follow-ups (a) + (b)** above — UTF-8 byte-length check in constantTimeEquals + plan-doc grep-gate hygiene. Plan: slot was concurrently set by the user to `0053-quote-line-items-table` during the eval run; History entry below records the original switch-into for 0051.
- **2026-05-15** — Scaffolded `0051-dropbox-sign-to-boldsign` and promoted to active **Plan:** (switched away from `_None — pick a new plan._`). User-driven provider swap: migrate MSA e-signature flow from Dropbox Sign (`@dropbox/sign` SDK, `/api/dropbox-sign/webhook`, `dropbox_sign_document_id` column) to BoldSign while **keeping the inline-MSA upload model** — MSA prose continues to render in-repo via `src/lib/pdf/render-msa.ts`; no BoldSign template is provisioned or referenced. 7-phase plan: (1) SDK choice + env wiring + cutover preflight on in-flight envelopes, (2) `src/lib/boldsign/` client + webhook-verify modules mirroring the existing Dropbox Sign shape, (3) `/api/boldsign/webhook` route handler with HMAC-verify-before-mutation gate, (4) schema rename `dropbox_sign_document_id` → `provider_document_id` (provider-agnostic to dodge the next swap), (5) cut-over `sendMsaEnvelope` + `markMsaSigned`/`markMsaDeclined`, (6) delete Dropbox Sign code/route/dep/env, (7) wiki sweep + smoke verification. Created on new feature branch `0051-dropbox-sign-to-boldsign` off `0048-quote-timestamp-naming`. Five Open Questions seeded in `intent.md` (SDK-vs-fetch, column-name strategy, in-flight envelope handover, webhook signature scheme, sandbox-vs-prod URL strategy) — Phase 1 resolves the first four. Non-goals: BoldSign templates, embedded signing, multi-signer, 0041 follow-ups (a)-(e) (stay in Parked).
- **2026-05-14** — Closed `0050-grid-pattern-resend` → [`docs/chunks/closed/0050-grid-pattern-resend/`](closed/0050-grid-pattern-resend/plan.md). All 5 phases shipped via `/build` chunk loop on feature branch `0048-quote-timestamp-naming` (12 commits across Phase 1 → Phase 5 + lint fix + Codex fixes + plan-doc maintenance). Phase 1 `fae3713` (audit + wiki rewrite: `docs/wiki/layout.md` View-xor-Edit rule replaced by edit-default doctrine; one-primitive grid consistency statement landed; `docs/wiki/log.md` entry appended), P2 `8226cd2` (`src/components/app/row-identity-cell.tsx` + 6-case test — dotted-underline label + tinted-icon chip + optional sublabel; later extended at P5c with a `{ onClick }` button-shape variant for surfaces whose canonical editor is a dialog), P3 `be35c97` (`src/components/app/row-overflow-menu.tsx` + 7-case test — Catalyst `<Dropdown>` based `…` trigger, re-uses `RowActionKind`/`ROW_ACTION_ICONS`/`ROW_ACTION_LABELS` vocabulary maps, `tone === 'danger'` items render red via `!`-modifier Tailwind classes), P4 `5520046` (`src/components/ui/token-pill.tsx` + 4-case test — monospace truncated chip; pagination footer prose polish `Page X of Y` + `[select] per page`), P5 `3c7581d` + `fae828a` (every grid on `<DataTable>`: `/quotes` raw `<table>` → `quotes-admin.tsx` + `quotes-columns.tsx`; `/production` raw `<table>` → `production-admin.tsx` + `production-columns.tsx` + `production-page-actions.tsx`; `/dealerships/[id]` Quotes panel → `dealer-quotes-panel.tsx`; `/admin/dealers` + `/admin/people` columns swept onto `RowIdentityCell` + `RowOverflowMenu` (people uses button-shape); `/reports` columns sweep — see chunk-end fixes for the drill-through rollback; `src/lib/ui/data-table-filters.ts` `makeNeedleFilter<T>(rowToStrings)` factory consolidates 4 surfaces' search predicates; 5 retired files; 5 new tests). Chunk-end `/eval` at [`closed/0050-grid-pattern-resend/eval-2026-05-14-1059.md`](closed/0050-grid-pattern-resend/eval-2026-05-14-1059.md) verdict **PASS with warnings**: static green (tsc clean / vitest 824/826 PASS / lint 0 errors + 6 carryforward warnings), browser smoke 6/6 PASS (`/quotes`, `/production`, `/dealerships/12`, `/dealerships`, `/admin/people` with overflow popover open, `/reports`), Codex returned 0 High / 2 Medium / 1 Low. **Both Mediums fixed in-cycle at `9db4298`** — (i) `/production` filter closure now captures `todayIso` server-passed-in (was browser-local `todayIsoSafe()`, drifted from cell render + CSV export at midnight / TZ-skewed deploys); (ii) `/reports` dealer drill-through retired (coach-vs-admin capability mismatch on `/dealerships/[id]` + archived-dealer 404 via `loadDealer()` archive filter; `docs/wiki/layout.md` exception-note expanded with the three reasons). **Low #1 parked as 0050 follow-up (a)** above — `<TokenPill>` UTF-16 surrogate-pair truncation; no consumer today, primitive-only hardening. Verification checks pass: `rg "<table\b" src/app src/features` returns exactly `src/features/quotes/quote-composer.tsx:611` (excluded form sub-element); `rg "filterQuotes\b|filterCampaigns\b" src/` returns zero matches. Plan slot reset to `_None — pick a new plan_`; sequence forward remains 0043 follow-up (b)+(c)+(d)+(e), then 0046/0044/0045/0041/0026 follow-ups, then 0049/0048 follow-ups, then 0025 epic leftovers.
- **2026-05-14** — Un-deferred `0050-grid-pattern-resend` and promoted from **Future:** → Active **Plan:**. Folder moved `docs/chunks/future/0050-grid-pattern-resend` → [`docs/chunks/closed/0050-grid-pattern-resend`](closed/0050-grid-pattern-resend/plan.md) (now under `closed/` post-shipping); cross-refs swept (1 internal `../../closed/` → `../closed/`; broken `future/...` link targets in the older 2026-05-13 scaffold-history bullet repointed to `0050-grid-pattern-resend/...`). Scope grew during the un-defer review in two ways: (a) **edit-default row pattern** baked in via a new header note — row click → single editable detail page (no separate View vs Edit modes), `…` overflow menu owns Delete + non-CRUD, role-based read-only rendering replaces the separate "View" surface, edit-permission-to-author scoping acknowledged as future work; rewrite of `docs/wiki/layout.md` View-xor-Edit rule (currently L86–95) added as a Phase 1 deliverable; (b) **one-primitive grid consistency precondition** — codebase audit found three raw `<table>` callsites today (`src/app/(app)/quotes/page.tsx:55`, `src/app/(app)/production/page.tsx:54`, `src/app/(app)/dealerships/[id]/page.tsx:195`) alongside the three TanStack `<DataTable>` consumers (`/admin/people`, `/admin/dealers`, `/reports`); Phase 5 sweep expanded from "three representative grids" to "**every** grid on the new shape," with the three raw-table surfaces converted to `<DataTable>` *and* the new identity-cell + overflow-menu shape in the same pass. Code Anchors table reshaped: bad anchors fixed (`dealership-columns.tsx` doesn't exist → `dealers-columns.tsx`; `quote-columns.tsx` doesn't exist → created in Phase 5). New chunk-end gate: `rg "<table\b" src/app src/features` returns only `quote-composer.tsx:611` (line-items table in editable form — explicitly excluded). User direction across the session: "we need consistency" + "default to edit for CRUD-on-document since users are coaches/admins working on documents" — both folded into the plan headers + checklists.
- **2026-05-14** — Closed `0048-quote-timestamp-naming` → [`docs/chunks/closed/0048-quote-timestamp-naming/`](closed/0048-quote-timestamp-naming/plan.md). All 4 phases shipped via `/build` chunk loop on feature branch `0048-quote-timestamp-naming` (8 commits): P1 `0d317ed` (shared `quoteDisplayName(createdAt)` + `quoteDownloadFilename(createdAt)` helpers in `src/features/quotes/display-name.ts` — `Intl.DateTimeFormat` `en-CA` + `America/Toronto` + `formatToParts` extraction of year/month/day/hour/minute concatenated to `quote-YYYYMMDD-HHmm`; 5-case test sibling covering EDT/EST/DST-boundary/zero-pad/filename), P2 `1a24048` (UI surfaces — composer page-header `/quotes/[id]:180` swapped from `Quote #${quote.id}` to `quoteDisplayName(quote.createdAt)`; MSA-create dialog renamed `firstDraftQuoteIdForDealer` → `firstDraftQuoteForDealer` returning `{ id, createdAt } | null` and threaded `firstDraftQuoteId` + `firstDraftQuoteCreatedAt` props through `msa-panel.tsx` + `dealerships/[id]/page.tsx`; `/quotes/new` left as `"New Quote"` since no `createdAt` pre-save), P3 `b212299` (PDF + email — `QuoteData.quoteNumber: string` → `createdAt: Date` in `render-quote.ts`, `QuoteEmailFields.quoteNumber` → `createdAt` in `templates/quote.tsx` with subject `Your Salesability Quote — quote-<ts>` + body inline reference; both `features/quotes/actions.ts` callers + `features/msa/actions.ts` bundled-quote path swept including the Dropbox Sign customer message + attachment filename `saledayevents-quote-<ts>.pdf` via `quoteDownloadFilename`; preview-path projection at `:676` + send-path projection at `:777` extended with `createdAt`; test fixtures gained `createdAt: 2026-05-12T11:00:00Z` (= EDT 07:00 → `quote-20260512-0700`)), P4 `40d6090` (`render-quote.test.ts` drawText-spy assertion confirms `quote-20260508-0900` is drawn as the left-column subtitle). Chunk-end `/eval` at [`closed/0048-quote-timestamp-naming/eval-2026-05-14-0859.md`](closed/0048-quote-timestamp-naming/eval-2026-05-14-0859.md) verdict **PASS with warnings**: static green (tsc clean / vitest 802/804 PASS / lint 0 errors + 11 carryforward warnings), browser smoke 3/3 PASS (`/quotes/4` composer heading = `quote-20260513-1433 draft`, `/quotes` list rows show no `Quote #N`, `/dealerships/12` Create MSA dialog "First Quote" reads `quote-20260513-1433` linking to `/quotes/4`), Codex returned 0 High / 0 Medium / 2 Low. **Low #2 (MSA dialog prop type allowed `firstDraftQuoteId` non-null + `firstDraftQuoteCreatedAt` null leak to `quote ${id}` fallback) fixed in-cycle at `bc4d423`** — collapsed two parallel nullable props into a single discriminated object `firstDraftQuote: { id: number; createdAt: Date } | null` on `MsaCreateDialog` + `MsaCreateTrigger`; leaky fallback branch eliminated by construction; dealer-detail caller simplified to pass `firstDraftQuote` directly. **Low #1 (DST fall-back collision producing identical display names for two instants an hour apart) parked as 0048 follow-up (a)** above — presentation-only, server persistence is id-keyed, one hour per year. Plan slot reset to `_None — pick a new plan_`. Sequence forward unchanged (0043 follow-up (b)+(c) on deck).
- **2026-05-14** — Merged `0049-migrate-to-catalyst` into `main` (fast-forward, 23 commits, +5,XXX / -5,XXX). `main` advanced `f56609d` → `e29d284`; `[ahead 67]` vs `origin/main`, still unpushed. Branch `0049-migrate-to-catalyst` preserved (not deleted). Post-close tail also included: status-token sweep (`41bf7b8`, drops `--color-status-*` + Send/+Book Event hand-rolled buttons → Catalyst), in-cycle Dialog z-index fix + Preview Close button (`e29d284`), badge-position fix on composer (`4f557e1`), Close-button drop from composer toolbar (`89c0071`), Tailwind docs-scan exclusion (`a539f3e` — `@source not "../../docs"` in globals.css so prose mentions of `[--btn-bg:var(--color-...)]` syntax stop breaking PostCSS).
- **2026-05-13** — Closed `0049-migrate-to-catalyst` → [`docs/chunks/closed/0049-migrate-to-catalyst/`](closed/0049-migrate-to-catalyst/plan.md). All 8 phases shipped via `/build` chunk loop on feature branch `0049-migrate-to-catalyst` (13 commits): P1 `762ac1a` (install 27 Catalyst sources + audit + mapping doc + `@headlessui/react`/`motion`/`clsx` deps + Tabs gap surfaced), P2 `9279617` (reset `globals.css` to canonical create-next-app shape + OKLCH-spaced `brand-50..950` ramp from seed `#1a5fa8` + `--color-status-*` + drop `tw-animate-css` + `brand:` color entry added to Catalyst button/badge maps; 143→64 line collapse, gold gone), P3 four commits — Button `99a9a1e`, Badge `1fdc328`, Field+Label+ErrorMessage `757152d` (`field-compat.tsx` shim for `FieldError` errors-array), Input+Textarea `ef216fb`, P4 four commits — Dialog `73735a5` (9 callsites, `onCancel` props threaded into DealerForm/PersonForm/AdoptForm to replace `<DialogClose>`), Combobox `99bd8a2` (2 callsites, Base UI → Headless UI), Listbox+Checkbox `5e1c9e2` (1+1 callsites swapping Radix Select+Checkbox), Dropdown+Tabs `0c3441e` (2 Radix Dropdown callsites + new custom Tabs primitive + Catalyst `Link` rewired to `next/link`), P5 `5436457` (DataTable wraps Catalyst Table primitives keeping TanStack row model + 478-replacement codemod sweep at `codemod.mjs` across 34 files mapping `text-muted-foreground`→`text-zinc-500`, `text-primary`→`text-brand-700`, `bg-accent`→`bg-brand-600`, etc.), P6+P7 `efddfe8` (delete 15 shadcn primitive files from `src/components/ui/*` retaining only `data-table.tsx` + `toaster.tsx`, remove 8 deps: `@base-ui/react` / 4 Radix primitives / `class-variance-authority` / `shadcn` / `tw-animate-css` — net -223 transitive packages, delete `components.json`, build `catalyst/toggle-group.tsx` on Headless UI `<RadioGroup>`), P8 chunk-end `/eval` at [`closed/0049-migrate-to-catalyst/eval-2026-05-13-2316.md`](closed/0049-migrate-to-catalyst/eval-2026-05-13-2316.md) verdict **PASS with warnings**: static green (tsc clean / vitest 797/799 PASS / lint 0 errors after Catalyst-dir overrides for `prefer-const` + `<img>` warnings / 11 carryforward warnings), production `pnpm build` clean across all 21 routes, browser smoke 9/9 PASS (Phase 1 3/3: `/`→307 `/login`, `/login` with brand-blue Send magic link, `/share/coach/1`; Phase 2 6/6 auth-injected: `/calendar` with Master Schedule + Block Date dialog + + Book Event status-green + brand-blue chrome, `/quotes` + `/quotes/new` composer + `/quotes/4` edit-mode with Save Draft brand-blue + KeyValueStrip + Catalyst Dialog + ToggleGroup, `/admin/people`, `/dealerships`, Block Date Catalyst Dialog open), Codex returned 0 High / 2 Medium / 0 Low. **Medium #1 (aria-invalid vs data-invalid) fixed in-cycle at `f6e9aeb`** — extended Catalyst `input.tsx` + `textarea.tsx` to also style on `aria-[invalid=true]:border-red-500` so RHF callsites' `aria-invalid` triggers the red border without per-callsite changes. Same commit added Catalyst-dir lint relaxations (`prefer-const` + `<img>`) to keep verbatim re-copies of Catalyst sources a no-op + fixed Tailwind v4 source-scan leak in `mapping.md` (literal `var(--color-...)` prose was being compiled as a CSS class). **Medium #2 (custom Tabs missing keyboard/ARIA contract) parked as 0049 follow-up (a)** — Phase 4d's context-based Tabs lacks Arrow/Home/End keyboard nav + `aria-controls`/`aria-labelledby` panel↔tab linkage vs. the Base UI Tabs it replaced; rebuild on Headless UI `<TabGroup>` is structurally distinct enough to merit its own chunk. Net: every primitive in `src/components/catalyst/*` (Button/Badge/Field/Input/Textarea/Dialog/Combobox/Listbox/Checkbox/Dropdown/Tabs/ToggleGroup) drives every callsite; `src/components/ui/*` is 2 files (`toaster.tsx` retained, `data-table.tsx` restyled in-place); shadcn semantic-token layer gone from `globals.css`; gold (`#b88a3a`) gone everywhere via codemod sweep; brand-blue from logo eyedropper drives all action surfaces; design-chroma north-star ([[project_design_chroma_north_star]]) now one OKLCH ramp away from any future re-tune. Plan slot promotes 0048 (`0048-quote-timestamp-naming`) to Active per its un-park trigger ("0049 ships").
- **2026-05-13** — Scaffolded `0050-grid-pattern-resend/plan.md` (deferred, not Active). User pasted two Resend API-keys table screenshots ("we should model our grids after this") asking for a chunk; both screenshots copied into the plan folder as [`resend-row-with-overflow.png`](closed/0050-grid-pattern-resend/resend-row-with-overflow.png) + [`resend-row-action-menu.png`](closed/0050-grid-pattern-resend/resend-row-action-menu.png). 5 phases — (1) post-0049 audit + lead-surface pick, (2) `<RowIdentityCell>` primitive (leading icon + dotted-underline View link), (3) `<RowOverflowMenu>` primitive (`…` trigger + popover with icon+label rows, destructive-red for delete) reusing existing `ROW_ACTION_ICONS`/`ROW_ACTION_LABELS`/`RowActionKind` vocabulary maps from `src/lib/ui/`, (4) `<TokenPill>` + footer-chrome polish, (5) sweep `/quotes` + `/dealerships` + `/admin/people` columns + visual smokes + Codex accessibility pass on the dotted-underline affordance. User explicitly tied the un-defer trigger to "0049 tailwind catalyst migration" — `0049-migrate-to-catalyst` is already scaffolded + active, so the trigger is concrete (catalyst ships → re-audit anchors → un-defer). Filed under `future/` rather than `Parked:` because the trigger is a dependency event rather than a near-term sequencing call (and 0049 will reshape both primitives the plan extends). Plan headers + Code Anchors mark all primitive references as tentative pending the post-0049 audit. Active **Plan:** stays on `0049-migrate-to-catalyst` — this scaffolding doesn't shift focus.
- **2026-05-13** — Fast-forwarded `main` from `8d225b0` → `f56609d` to absorb the 0047 close-out work (5 phases + chunk-end eval + 2 in-cycle fixes; `[ahead 32]` → `[ahead 37]` vs `origin/main`, still unpushed). Triggered by the user-direction switch to 0049, which structurally builds on a post-0047 globals.css baseline; merging 0047 into main first kept `main` ahead of every active branch. Branch `0047-drain-legacy-css-tokens` preserved (not deleted).
- **2026-05-13** — Switched Active **Plan:** from `0048-quote-timestamp-naming` → `0049-migrate-to-catalyst` per user direction ("build 0049 on a feature branch"). 0048 demoted back to **Parked:** without ever starting any phase — zero in-flight state, un-park trigger flipped to "0049 ships". Feature branch `0049-migrate-to-catalyst` cut off `main` after 0047 fast-forwarded in (so `main` = `f56609d`). 0048 + 0049 plan-folder scaffolding (originally drafted on the `0047-drain-legacy-css-tokens` branch the same day) carried over via `git stash` since both folders were untracked when the branch flip happened.
- **2026-05-13** — Scaffolded [`0049-migrate-to-catalyst/plan.md`](0049-migrate-to-catalyst/plan.md) and added to **Parked:** (0048 already active). Driven by same-session pushback after 0047 closed: "where does the gold color come from" → "i thought we drained the globals.css" → user clarified intent was to drain ALL legacy CSS and migrate to Catalyst (Tailwind UI Kit at `/Users/davidwhogan/Downloads/catalyst-ui-kit/typescript/`, license confirmed) rather than just rename the legacy token layer. Plan scopes 8 phases: Catalyst install + dep mapping + gap decisions (DataTable kept TanStack and restyled, ToggleGroup either rebuilt on Headless UI `<RadioGroup>` or kept as the one retained shadcn primitive, Toaster kept since no Catalyst equivalent), starter Tailwind palette (`zinc` neutrals + `blue` brand stand-in — chroma-from-logo deliberately queued as a follow-up chunk to keep velocity), primitive sweep across forms (Button/Badge/Field/Input/Textarea) then overlays (Dialog/Combobox/Listbox/Checkbox/Dropdown), DataTable restyle + 0043 conventions rewire (PageHeader/RowActions/KeyValueStrip/Section internals → Catalyst primitives), drain `globals.css` to Tailwind base + print stylesheet + `@layer base` only, retire orphaned UI deps (`@base-ui/react`, shadcn CLI, three Radix primitives, `cva`, `tailwind-merge` if unused), smoke + Codex eval. Code anchors paired across the old/new boundary — every Catalyst component file at `/Users/davidwhogan/Downloads/catalyst-ui-kit/typescript/<name>.tsx` pairs with the `src/components/ui/<name>.tsx` it replaces. Open questions surfaced in plan body: starter palette brand-color choice (`blue` vs `sky` vs `indigo`), neutrals choice (`zinc` vs keeping warm-stone), ToggleGroup keep-or-build decision, `tw-animate-css` keep-or-drop. Un-park trigger: 0048 ships.
- **2026-05-13** — Scaffolded `0048-quote-timestamp-naming/plan.md` and flipped Active **Plan:** from `_None — pick a new plan_` → `0048-quote-timestamp-naming`. User-requested rename: quote display name `Quote #<id>` → `quote-<timestamp>` and PDF attachment `quote-<id>.pdf` → `saledayevents-quote-<timestamp>.pdf`. Approach: derive from `quotes.createdAt` via a shared `quoteDisplayName()` helper — no schema change, zero migration. 4 phases — (1) helper + format decision (`quote-YYYYMMDD-HHmm`, America/Toronto), (2) UI surfaces (composer page-header + MSA-create dialog; `/quotes/new` left as `"New Quote"` since no `createdAt` yet pre-save), (3) PDF body title + email subject + email body inline reference + attachment filename, (4) integration test + web-test smoke + `grep -rn "Quote #"` cross-check. Code anchors named for `src/features/quotes/status-display.ts:1-30` (helper template), `src/features/quotes/status-display.test.ts:1-40` (test sibling template), `src/app/(app)/quotes/[id]/page.tsx:180` (composer header callsite), `src/features/msa/msa-create-dialog.tsx:121` (MSA dialog reference — needs new `firstDraftQuoteCreatedAt` prop threaded from caller), `src/lib/pdf/render-quote.ts:184` (PDF body title), `src/lib/email/templates/quote.tsx:31,83,111` (email template — `quoteNumber: string` swapped for `createdAt: Date`), `src/features/quotes/actions.ts:982` (attachment filename) + `:967-973` (email field plumbing). Tests-to-update: `src/features/quotes/actions.test.ts:178,332` subject-assertion lines. Surfaces deliberately left alone: `/quotes` list-page columns (no `Quote #N` rendered there today), `/quotes/new` page-header (pre-save), `quotes.id` column itself (DB key, just not user-visible). Branch not yet created — first commit will land on a new `0048-quote-timestamp-naming` branch off `main`.
- **2026-05-13** — Closed `0047-drain-legacy-css-tokens` → [`docs/chunks/closed/0047-drain-legacy-css-tokens/`](closed/0047-drain-legacy-css-tokens/plan.md). All 5 phases shipped via `/build` chunk loop on feature branch `0047-drain-legacy-css-tokens`: P1 `f510218` (audit + 31-file classification table in [`audit.md`](closed/0047-drain-legacy-css-tokens/audit.md) — original ripgrep missed `text-stone-*`/`bg-stone-*`/`border-stone-*` literals by anchoring on `color-stone-`; widened scan caught all 31), P2 three commits `1166391`/`eea89f2`/`8a22e72` (class-swap codemod sweep via [`codemod.mjs`](closed/0047-drain-legacy-css-tokens/codemod.mjs) across `(app)/`, `features/*`, modernized-tree stragglers, coach-shared public surfaces — `login` and `share/coach/[id]` migrated rather than kept-brand, decision rationale in `audit.md`), P3 `62dcbe4` (4 hand-rolled button classes in `people-admin.tsx` → `<Button>` variants incl. `DialogClose className={buttonVariants(...)}`, 3 composerActions buttons in `quote-composer.tsx` → `<Button>` — Send-Quote kept hand-rolled to preserve `bg-status-green`; 8 `font-display` callsites → `font-sans font-bold tracking-tight`; `accent-navy` + `ring-navy/30` follow-ups picked up by codemod extension), P4 `dbbce7e` (collapse `globals.css` to single-layer semantic tokens — deleted `--color-navy*`/`--color-cream`/`--color-stone-*`/`--color-accent-light`/`--font-display` from `@theme inline`, inlined stone-scale hex values directly into `:root`; kept `--color-brand-blue` + `--color-status-*` as load-bearing palette tokens). Chunk-end `/eval` at [`closed/0047-drain-legacy-css-tokens/eval-2026-05-13-2004.md`](closed/0047-drain-legacy-css-tokens/eval-2026-05-13-2004.md) verdict **PASS with warnings**: static green (tsc clean / vitest 790/790 with the pre-existing Supabase pool-flake RLS+MSA-schema integration files excluded — confirmed orthogonal via `git stash` + individual-file passes; lint 0 errors / 10 pre-existing warnings), browser smoke 10/10 routes PASS (`/login`, `/share/coach/1`, `/calendar` Phase 1 redirect, then auth-injected `/calendar`, `/quotes`, `/quotes/new`, `/quotes/4` edit-mode with all 4 action Buttons, `/admin/people` + Add Person dialog, `/dealerships`, `/production`), Codex returned 1 Medium (`divide-stone-100` survived the migration in 5 files — codemod table covered `border-stone-*` but not `divide-stone-*`) + 1 Low (stale "navy-on-white" comment in `people-admin.tsx`) **both fixed in-cycle at `1c6b28c`**, zero parked follow-ups. Net: every `src/features/*` + `src/app/(app)/*` file now reads from semantic shadcn tokens, `globals.css` is single-layer source of truth, design-chroma north-star ([[project_design_chroma_north_star]]) is now one CSS edit instead of a 30-file sweep. Plan slot reset to `_None — pick a new plan_`; next on the runway per Status footer: 0043 follow-up (b)+(c) bundled (row-action/status-pill completion sweeps).
- **2026-05-13** — Scaffolded `0047-drain-legacy-css-tokens/plan.md` and flipped Active **Plan:** from `_None — pick a new plan_` → `0047-drain-legacy-css-tokens`. Surfaced from same-session conversation: while researching shadcn theming and the Catalyst-vs-shadcn approach, the audit revealed `src/features/*` + `src/app/(app)/*` (~29 files) still bind to legacy brand tokens (`text-navy`, `text-stone-*`, `font-display`, `bg-cream`) — the un-migrated half of the May 12 `0042 Phase 1` shadcn init. Only 2 files use *both* layers (`dealerships/[id]/page.tsx`, `quotes/[id]/page.tsx`); the rest of the contradiction is layer-vs-layer, not chaotic. Plan structure: 5 phases — (1) audit + classification table, (2) class-swap codemod sweep across `(app)/*` and `features/*`, (3) component-swap sweep (hand-rolled buttons/badges → `<Button>`/`<Badge>` primitives) + `font-display` retirement, (4) delete the legacy brand block from `globals.css` (lines 30–46), (5) smoke + Codex eval. Code anchors named for `src/components/app/page-header.tsx:27` (canonical surface chrome shape), `src/components/ui/button.tsx:8–35` (cva variant template for hand-rolled button replacements), `src/app/globals.css:30–46` (the block to delete). Public-page surfaces (`src/app/share/coach/[id]/page.tsx`, `src/app/login/page.tsx`) flagged for explicit Phase-1 keep-or-flip decision since their cream/legacy branding may be intentional. Un-park trigger N/A (now Active). Feature branch `0047-drain-legacy-css-tokens` created off `main` at `8d225b0`.
- **2026-05-13** — Merged `0043-portal-shell-conventions` into `main` (fast-forward, 24 commits, 39 files, +1878 / -725). `main` is now 32 commits ahead of `origin/main` — unpushed. Branch `0043-portal-shell-conventions` preserved (not deleted).
- **2026-05-13** — Closed `0043-portal-shell-conventions` → [`docs/chunks/closed/0043-portal-shell-conventions/`](closed/0043-portal-shell-conventions/plan.md). All 7 active phases shipped via `/build` chunk loop on a fresh feature branch (Phase 1 sidebar shell stayed `Skipped` per same-day pivot): P2 `d440fe9` (`<PageHeader>` shell — title + actions slot + sticky `top-16 z-10` parking under the 64px `AppHeader`, post-0042 bold-Inter type scale), P3 `2541149` (PageHeader sweep across 10 `(app)/` routes — `/quotes`, `/quotes/new`, `/quotes/[id]`, `/dealerships`, `/dealerships/[id]`, `/calendar/calendar-view`, `/production`, `/reports`, `/admin/lookups`, `/admin/people`; hand-rolled `<h1>` + button-cluster pairs collapsed into the actions slot), P4 `2ce556b` (detail-page convention — new `<KeyValueStrip>` + `<Section>` primitives; `/dealerships/[id]` STATUS/MSA STATE/CONTACT/PHONE/EMAIL/ACQUIRED VIA strip + MSA + Quotes Sections; `/quotes/[id]` STATUS/DEALER/AUDIENCE/EVENT DAYS/AUDIENCE SOURCE/TOTAL strip — CAMPAIGN/EVENT-START/END dropped since quotes don't carry campaign linkage today, parked for the 0035 P7.2 / 0025 Contract phase), P5 `2d28a39` (`<ListToolbar>` + URL-driven `?q`/`?status` filters on `/dealerships` mirroring `QuotesFilters`'s shape — 250ms debounce, back-nav restores filter state per the "Resend pattern"), P6 `0e74772` (**row-action convention** — new `src/lib/ui/labels.ts` + `src/lib/ui/icons.ts` + `<RowActions>` + `eslint-plugins/no-inline-row-action-label.mjs`; refactored `quotes/row-actions.tsx` to `view`-only, `production/row-actions.tsx` to `edit`-only locking View-xor-Edit, `dealers-columns.tsx` to canonical `view`+`quote`+`activate?`+`edit`+`archive` vocabulary with `useCan`-gated entries; deferred kebab-overflow `<DropdownMenu>` as Phase-6 follow-up since inline-only covers every current consumer ≤4 actions), P7 `c5b2e65` (new `src/components/ui/badge.tsx` with `success`/`warning`/`info` cva variants + `<QuoteStatusBadge>`/`<DealerStatusBadge>`/`<MsaStatusBadge>`/`<CampaignStatusBadge>` wrappers; `<RelativeTime>` via native `Intl.RelativeTimeFormat` for list-view "Sent"/"Created" columns + send-history rows; `STATUS_PILL_CLS`/`MSA_STATUS_PILL_CLS` consumer sites swept; inline `DealerStatusPill` deleted; chose native `Intl.RelativeTimeFormat` + `<time title>` over `date-fns` + `<Tooltip>` shadcn primitive to avoid two new deps), P8 `7c1f830` (wiki `layout.md` shipped — codifies post-0043 page shape + cross-linked from `forms.md`/`index.md`/`log.md`; chunk-end `/eval` ritual delegated to the build skill). Chunk-end `/eval` at [`closed/0043-portal-shell-conventions/eval-2026-05-13-1730.md`](closed/0043-portal-shell-conventions/eval-2026-05-13-1730.md) verdict **PASS with warnings**: static green (tsc clean / test 797 / lint 0 errors after `6966836` ported `people-columns.tsx` onto `<RowActions>` in-cycle — the new lint rule fired on its pre-existing `Edit` literal, fixed by extending the chunk's row-action sweep to people), browser smoke 8/8 routes PASS + URL-state restoration on `?status=prospect` verified, Codex returned 0 High / 3 Medium / 2 Low all classified as sweep-completeness or out-of-scope. **Parked as 0043 follow-ups:** (a) composer-actions lift (Medium — sticky chrome shipped but composer Save/Send still at page bottom), (b) row-action sweep into `services-admin`/`availability-admin`/nested-dealer-Quotes (Medium — same vocabulary win, just outside the lint rule's path scope), (c) status-pill sweep into calendar `event-detail.tsx` + people lifecycle span (Medium — sister enums to the swept ones), (d) retire stale `STATUS_PILL_CLS` map (Low), (e) add `Quote` to the lint rule's `FORBIDDEN` set or derive from the labels map (Low). Plan slot set to `_None — pick a new plan_` (0043 follow-ups now queued at the top of the sequence).

- **2026-05-13** — Reframed `0043-portal-shell-conventions` and promoted from Parked → Active **Plan:**. Same-day user pivot dropped the shadcn Sidebar shell swap (Phase 1) and the feature branch `0043-portal-shell-conventions` (15 commits, tip `f5d3770`) was deleted — both the sidebar work and the page-conventions work it carried (PageHeader, KeyValueStrip+Section, ListToolbar, Badge, `layout.md`) retracted in favor of a fresh build per the updated plan; tip recoverable via reflog through ~2026-06-12. Chunk reframed around **app-wide consistency in operation + look-and-feel** (user prompt: "the spirit of the ticket is to bring app with consistency in operation and look and feel, for instance in the list we have 'view' in one list and 'edit' in another"). Concrete inconsistencies named in the plan: `/quotes` row → `View` only (`quotes/row-actions.tsx:16`), `/production` row → `View + Edit` (`production/row-actions.tsx:37,44`), `/dealers` row → `Activate + Edit + Archive` with no `View` (`dealers-columns.tsx:113-162`). New Phase 6 inserted for **row-action convention** (`<RowActions>` component + shared `src/lib/ui/labels.ts`/`icons.ts` + lint rule against inline row-action label literals); old Phases 6–7 renumbered to 7–8. Structural reference cited: [bcgov/biohubbc-platform `app/src`](https://github.com/bcgov/biohubbc-platform/tree/dev/app/src) — `components/data-grid/`, `components/button/`, `constants/i18n.ts`, `constants/icon.ts` pattern (one folder per pattern, centralized labels/icons). Sticky `<PageHeader>` now parks at `top-16` (below the retained 64px `AppHeader`) rather than `top-0`. Un-park trigger N/A (now Active). Audit trail: this History entry is the canonical record since `docs/` is now tracked (the "gitignored" line in CLAUDE.md was stale — verified 2026-05-13).
- **2026-05-13** — Closed `0046-quote-mutable-after-send` → `docs/chunks/closed/0046-quote-mutable-after-send/`. All 6 phases shipped same-day via `/build` chunk loop (7 commits): P1 `adcdfc9` (relaxed `setQuoteInputs` guard, drops `WHERE status='draft'` in favor of `status NOT IN ('accepted','declined')` + `date_trunc('ms', updatedAt)` optimistic-lock, emits `quote.edited` audit row on priced-output diff via the new `audit_action` enum value in `drizzle/0020_quote_edited_audit.sql`); P2 `86e636e` (`sendQuote` unified for first-send + Re-send — every send re-renders the PDF, overwrites the GCS object, advances `sent_at`, emits a fresh `quote.sent` audit row; concurrent-send semantics preserved per OQ #6); P3 `d07c3cf` (composer `isReadOnly`/`canSend` flipped — only terminal statuses lock; Send Quote → Re-send Quote label flip on `sentAt != null`; new banner copy + ConfirmSendDialog re-send variant with validity-window-reset description); P4 `9fe577c` (`loadQuoteSendHistory` returns `QuoteSendReceipt[]` desc-ordered + multi-row `<ul>` Send-history Section on `/quotes/[id]`; Download PDF link only on the "Latest" row); P5 `9f10969` (MSA-pending in-flight gate on re-send — refuses when the dealer's MSA has `dropbox_sign_document_id != null` while `status='pending'`; composer mirrors with disabled button + amber hint); P6 `4d880b1` (wiki: `commercial-spine.md` lifecycle diagram annotated for editable-when-sent, `data-model.md` audit-enum + multi-row `quote.sent` paragraphs, `log.md` entry codifying the doctrine flip). Chunk-end `/eval` at `4caa049` (report [`closed/0046-quote-mutable-after-send/eval-2026-05-13-1636.md`](closed/0046-quote-mutable-after-send/eval-2026-05-13-1636.md)) verdict **PASS with caveats**: static green (tsc clean / test 784 / lint 0 errors), browser smoke SKIPPED (multi-row Send-history needs a seeded re-sent fixture that wasn't created — flagged as a caveat, not a blocker), Codex returned 2 High + 2 Medium + 2 Low. 4 fixed in-cycle at `4caa049`: **High #1** (concurrent-edit on sent quote misclassified as race-won re-send — added `sentAt`-advanced check + new regression test), **Medium #1** (Send-history showed current denorm for every row — added `sentToEmail`/`sentToFirstName` to the `quote.sent` audit payload + per-row render with fallback), **Medium #2** (TypeScript `pgEnum` order didn't match the live Postgres order — reordered to mirror snapshot), **Low #1** (wiki retry text). Parked as 0046 follow-ups: **High #2** outbox-style degraded-send retry (a), **Low #2** ConfirmSendDialog hydration-drift hypothesis (b). The same eval-rescue commit also added a `Close` button to the composer button row per same-session user feedback ("there is no close button for qoutes") — composer-page exit was only the small `← Quotes` breadcrumb. Plan slot set to `_None — pick a new plan_` (0043 still parked-in-flight on its feature branch).

- **2026-05-13** — Scaffolded `0046-quote-mutable-after-send/plan.md` (now at [`closed/0046-quote-mutable-after-send/plan.md`](closed/0046-quote-mutable-after-send/plan.md)) and flipped Active **Plan:** from `0043-portal-shell-conventions` → `0046-quote-mutable-after-send`. Surfaced from same-session conversation after closing 0043 on a feature branch: user typed "let scaffold a chunck to simply the quiste syste, no need to freeze the qoute after is is sent, support edits and re send." Plan structure: 6 phases — (1) `setQuoteInputs` server guard relaxation + `quote.edited` audit on priced-output change, (2) `sendQuote` re-send branch (sentAt reset, multiple `quote.sent` audit rows, PDF storage-key overwrite), (3) composer always-editable + Send/Re-send label flip + new confirm-dialog copy mentioning validity-window reset, (4) `loadQuoteSendReceipt` → `loadQuoteSendHistory` returning ordered array + multi-row Send-history Section, (5) accepted/declined immutability gates + MSA-bundle re-send gate (first send bundles; re-sends Resend-only; MSA-pending-in-flight blocks re-send), (6) wiki + smoke + `/eval`. Open Questions: `quote.edited` debounce policy (recommend semantic emit only on priced-output change), expiry-reset opt-in via confirm dialog, query-rename strategy, MSA-bundle gate semantics + the MSA-pending-in-flight sanity check, banner copy on sent-quote edit, concurrent re-send race (out-of-scope, tracked by closed/0041 follow-up (a)). Code Anchors named for `actions.ts:270-340` (setQuoteInputs UPDATE), `actions.ts:641-820` (sendQuote), `lifecycle.ts:76-140` (two-layer guard pattern from 0044), `queries.ts:168-185` (loadQuoteSendReceipt → loadQuoteSendHistory), `quote-composer.tsx:142-150, 588-602` (isReadOnly/canSend + Send button), `quote-composer.tsx:679-735` (ConfirmSendDialog re-send variant). Depends on 0043 landing first (post-0043 `<Section variant="card" title="Send history">` shell is the multi-row Send-history anchor for Phase 4). 0043 stays parked-in-flight on its feature branch — note in **Parked (in-flight):** block above. Un-park trigger for 0046: 0043 ships to main, OR user explicitly switches focus.
- **2026-05-13** — Closed `0044-quote-expiry-enforcement` → `docs/chunks/closed/0044-quote-expiry-enforcement/`. All 4 implementation phases shipped same day via `/build` chunk loop (Phase 4 correctly skipped under OQ #1 Option B); 5 commits: P1 `3b60c93` (`acceptQuote` time-based expiry guard inside `markQuoteAccepted` — pre-load `sentAt + quoteValidDays`, return friendly error if expired; layered on top of the existing `status='sent'` atomic-UPDATE guard so idempotent + illegal-source paths still flow through the existing reselect; 3 new test cases for 31-days-ago / 8-days with per-row=7 override / TOCTOU race), P2 `b99d381` (PDF + email validity surface — extended `QuoteData.validUntilDate` + `QuoteEmailFields.validUntilDate`, rendered "Valid until: YYYY-MM-DD" as a sibling to "Issued:" in `render-quote.ts`, email body picks up "Please respond by … to lock in this pricing"; `sendQuote` anchors both row's `sentAt` and rendered strings on the same `new Date()`; `previewQuotePdf` anchors on `quote.sentAt ?? new Date()`; MSA bundle flow also extended; `MAX_LINE_ITEMS` 13 → 12 to accommodate the new vertical-space cost), P3 `4e0d31c` (derived `isExpired` projection — `mapRow()` adds `isExpired = status==='sent' && sentAt + quoteValidDays < now()` + `quoteValidDays`; new shared `src/features/quotes/status-display.ts` module replaces the duplicated `STATUS_PILL_CLS` blocks across 3 pages — `/quotes`, `/quotes/[id]`, `/dealerships/[id]` — `displayStatusKey()` returns `'expired'` when `isExpired` is true; 4 new test cases for the projection + 3 for the helper). Chunk-end `/eval` at `ff2bc06` (report [`closed/0044-quote-expiry-enforcement/eval-2026-05-13-1116.md`](closed/0044-quote-expiry-enforcement/eval-2026-05-13-1116.md)): verdict **PASS with warnings**. Static gate clean (tsc + 770/772 tests + lint 0 errors / 8 pre-existing warnings). Browser smoke Phase 1 5/5 + Phase 2 9/9 PASS (caveats: no expired-quote fixture in dev DB so the amber Expired pill couldn't be visually captured — projection is fully unit-tested; PDF iframe content not OCR-verifiable but "Valid until" line asserted in `render-quote.test.ts`). Codex returned 1 High + 1 Medium + 3 Lows. **High (TOCTOU expiry guard) + 2 Lows fixed in-cycle at `29a39b4`** — added Postgres-time predicate `sent_at + quote_valid_days * interval '1 day' >= now()` to the UPDATE WHERE so the time-check is atomic with the lifecycle flip, reselect path now distinguishes "missed because not sent" from "missed because expired between load and UPDATE", extracted `expiredErrorMessage()` helper, composer banner reads "expired" when `initial.isExpired` (threaded through `InitialQuote.isExpired`), wiki doc `docs/wiki/data-model.md:408` updated MAX_LINE_ITEMS=12 with 0044 P2 cause; new regression test in `actions.test.ts` exercises the SQL-side TOCTOU guard. **M1 + L3 parked as 0044 follow-ups (a) and (b)** above — MSA bundled-quote validity-deadline mismatch (out of scope, closed/0041 territory) + Expired filter pill on `/quotes` (Phase 3 explicitly punted, makes sense alongside Option A promotion). Bookkeeping: 3 feature commits + 1 in-cycle eval-fix commit + 3 plan `docs(plan)` updates + 1 eval-report commit + 1 close-out commit. Cross-refs swept: this CURRENT.md only (the moved folder had no external `../X` siblings; the eval-report's `Plan:` header kept its historical `docs/chunks/0044-…/plan.md` snapshot path per the convention recorded in closed/0035's close-out).
- **2026-05-13** — Switched Active **Plan:** from `0043-portal-shell-conventions` → `0044-quote-expiry-enforcement`. User typed `/build 0044` immediately after 0045 closed and 0043 re-promoted to Active. 0043 demoted back to **Parked:** without ever starting any phase (zero in-flight state to preserve); un-park trigger from 0042 close still satisfied. 0044's original un-park trigger ("0043 ships OR user explicitly switches focus") fulfilled via the second clause. Plan structure (5 phases) per `0044-quote-expiry-enforcement/plan.md` — recommended path = Option B (derived `isExpired` projection, no migration, Phase 4 skipped).
- **2026-05-13** — Closed `0045-form-schema-as-contract` → `docs/chunks/closed/0045-form-schema-as-contract/`. All 9 phases shipped same day via `/build` chunk loop (11 commits): P1 `25cc0f0` (forms.md schema-as-contract doctrine + log.md entry — codifies "one zod schema per form lives in a sibling `*-schema.ts` module, imported by both client `zodResolver` and Server Action `safeParse(Object.fromEntries(formData))`"; B-shape variant carved out for partial-RHF + single-field admin forms), P2 `2e9853d` (extracted `dealerFormSchema` into `src/features/dealers/dealer-schema.ts`; `quoteInputsSchema` stayed at `src/lib/quotes/pricing.ts` — already the right home; wired `safeParse` into `createDealer`/`updateDealer` + `createQuote`/`setQuoteInputs`; new "surfaces per-field errors on safeParse failure" tests in dealers + quotes), P3 `80e082e` (services-admin full A-shape RHF port — factored shared `<ServiceForm mode='create'|'edit'>` component, new `service-schema.ts` covers per-field validation, `normalizeMoney` helper handles wire→DB padding without IEEE-754 rounding, `toLegacyResult` extended to forward `fieldErrors`), P4 `9879c98` (availability-admin full A-shape RHF port — `availability-schema.ts` + 6-case schema test, factored shared `<AvailabilityForm mode='create'|'edit'>`, action `parseAvailabilityInput` rewritten to safeParse), P5 `d287c5f` (lookup-admin B-shape carve-out — `lookup-schema.ts` is single-field, native `<form action={action}>` keeps the existing UI; action `parseLookupLabel` now safeParses), P6 `2ef7f94` (B-shape backfill for booking-form + PersonForm — new `person-schema.ts` + `src/app/(app)/calendar/booking-schema.ts`; `parseCampaignInput` rewritten to safeParse `bookingFormSchema` + cross-field rules; `createPerson`/`updatePerson` safeParse scalar fields while roles/dealerLinks stay on `formData.getAll(...)`), P7 `1d71b3f` (retired `validateContactInputs`/`parseDate`/`parseOptionalInt` from `schedule/validators.ts` along with their test blocks — replaced by zod schemas + the action-layer `validateContactCross` helper; kept `field`, `parseId`, `parseOptionalId`, `EMAIL_RE`, `parseCampaignInput` since they have non-schema consumers), P8 `ef0474c` (new `eslint-plugins/safeparse-required.mjs` rule + `eslint.config.mjs` registration scoped to `src/features/**/actions.ts`; `wrapperNames: ['parseCampaignInput']` config trusts the cross-file booking-form wrapper; opt-out via `// validation: skip` comment with one-line rationale on each of ~20 id-only / payload-less actions). Chunk-end `/eval` at `4f41e5e` (report [`closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md`](closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md)): verdict **PASS with warnings**. Static gate clean (tsc + 757 tests + lint 0 errors). Browser smoke: Phase 1 6/6 PASS + Phase 2 plan-targeted PASS on the chunk's touched surfaces (with /admin/lookups verifying the new ServicesAdmin RHF shape and /calendar Block Date verifying the AvailabilityAdmin RHF dialog; Edit-dealer dialog click skipped on strict-mode collision — A-shape form unchanged per Phase 2). Codex returned 3 Mediums: **M2 (in-chunk regression) fixed in-cycle at `7c535b4`** — `dealerFormSchema` status field rejected empty string after the Phase 2 preprocess was dropped; restored the "status='' as absent" semantics via a `normalizeDealerWire` wire-format pre-pass + new regression test. **M1 + M3 parked as 0045 follow-ups (a) and (b) above** — pre-existing appAccess+empty-email guard scope-creep in `people/actions.ts` (predates 0045, surfaced during the eval focus on cross-field logic) and ESLint rule policy-strength gap (doesn't inspect argument shape or call ordering). Bookkeeping: 11 feature commits + the in-cycle fix + 2 plan `docs(plan): mark Phase N commit` updates + the chunk-end eval-report commit. Side-effect: extended `toLegacyResult` to forward `fieldErrors` through the adapter (Phase 3) so future A-shape forms can route per-field via `setError`. Cross-refs swept: this History block + the **Status:** line + the **Plan:** flip back to 0043 (un-park trigger "0043 + 0044 ship OR user switch" was fulfilled mid-session by the user-switch clause; 0043 returns to Active without needing the original trigger). New auto-close + auto-park doctrine landed mid-session in CLAUDE.md → "Closing a chunk" and `.claude/skills/build/SKILL.md` → "Auto-close on clean eval" — this close-out manually mirrors what the next chunk-end PASS-with-warnings will trigger automatically.
- **2026-05-13** — Switched Active **Plan:** from `0043-portal-shell-conventions` → `0045-form-schema-as-contract`. User typed `/build 0045` immediately after scaffolding, explicitly switching focus before 0043 started. 0043 demoted back to **Parked:** (still standalone, un-park trigger from 0042 close still satisfied — sequencing is now a user-choice queue). 0044 stays Parked. Un-park trigger for 0045 (which named "0043 + 0044 ship" or "user explicitly switches focus") fulfilled via the second clause.
- **2026-05-13** — Scaffolded `0045-form-schema-as-contract/plan.md` and added it to **Parked:** (did *not* flip Active — 0043 is the freshly un-parked Active plan and hasn't started yet; 0044 is the older parked sibling). Surfaced from same-day audit conversation: user asked "are we using zod and shadcn forms across the board?" — audit found three coexisting patterns (full-RHF + zod, useActionState + native, hand-rolled `useTouched()`) but the bigger gap was that **zero Server Actions in the repo currently use zod** (`schedule/validators.ts` is hand-rolled; the existing `safeParse` grep hit in `quotes/actions.ts` is a `JSON.parse`, not a zod parse). `docs/wiki/forms.md` already documents the two acceptable form *shapes* (full RHF + partial RHF), so the doctrine question is settled — this chunk closes the **schema-sharing** loop. Plan structure: 8 phases — (1) extend `forms.md` with the "one zod schema, imported by both client and action" rule + B-shape variant clarification; (2) extract canonical schemas (dealer + quote) into shared `*-schema.ts` modules + wire `safeParse(Object.fromEntries(formData))` into existing actions; (3) `services-admin` A-shape port (currently has no validation at all — biggest bug-catching win); (4) `availability-admin` A-shape port; (5) `lookup-admin` carve-out (single-field B-shape, no RHF, but action still `safeParse`s); (6) B-shape backfill — `booking-form` + PersonForm keep `useActionState` UI per `forms.md`'s explicit carve-out but the action `safeParse`s the same schema; (7) retire superseded `schedule/validators.ts` helpers (this is the 0033 carry-forward parked item — now scaffolded); (8) smoke + `/eval`. Code Anchors: `src/lib/quotes/pricing.ts:quoteInputsSchema` (existing shared-schema precedent), `src/features/dealers/dealer-form.tsx:33` (existing A-shape — anchor for extracted schemas + new A-shape ports), `src/features/people/people-admin.tsx` (existing B-shape anchor), `src/features/schedule/validators.ts` (replaced hand-rolled helpers). Supersedes the parked "0033 follow-up (carry-forward): retire hand-rolled validators" line — Phase 7 covers that work. Un-park trigger: 0043 + 0044 ship, OR user explicitly switches focus.
- **2026-05-13** — Scaffolded `0044-quote-expiry-enforcement/plan.md` and added it to **Parked:** (did *not* flip Active — 0043 is the freshly un-parked Active plan and hasn't started yet). Surfaced from the same-session post-close 0042 conversation: user asked "do the quotes expire in 30 days" after the close-out commit; answer was "schema says yes via `quote_valid_days NOT NULL DEFAULT 30`, but no code reads/enforces/renders it — `acceptQuote` doesn't refuse stale, the PDF doesn't print 'Valid until', the email doesn't either, and `quote_status` has no `expired` value." User asked "can we fix it or should we make a chunk", answered with the three coupled questions (guard semantics, validity rendered to the dealer, enum-vs-derived-vs-timestamp lifecycle representation) and the recommendation to scaffold. Plan structure: 5 phases — (1) `acceptQuote` expiry guard inside `markQuoteAccepted` lifecycle helper (the minimum behavior fix, layered on top of the existing `status='sent'` atomic-UPDATE guard); (2) surface validity on PDF + email (extend `QuoteData` with `validUntilDate`, sibling line below the existing `Issued: ...` line in `render-quote.ts:192`; same for email template's `QuoteEmailFields`); (3) decision-and-implement on OQ #1 lifecycle representation — three options laid out (A enum extension + sweep — most semantically honest, matches MSA's `expired` enum value, largest scope; B derived `isExpired` projection at read time — no migration, no sweep, recommended v1 default; C nullable `expired_at` timestamp — compromise); (4) conditional nightly sweep — only runs if Phase 3 picked (A) or (C), skipped for (B); (5) tests + chunk-end `/eval`. Seven OQs captured: lifecycle representation (recommend B), declined-Quote-blocks-acceptance layering, rejection error message wording (recommend verbose + actionable), "Valid until" vs "Valid for X days" PDF phrasing (recommend absolute), email body wording (recommend "Please respond by"), pairing with 0026 follow-up (c) staff accept/decline UI (recommend keep separate), prod backfill concerns. Code Anchors: `lifecycle.ts:69` for `markQuoteAccepted` (guard goes inside), `actions.ts:825` for `acceptQuote` (no change — new error flows through existing pipe), `render-quote.ts:192` for the "Issued:" line (anchor for the new "Valid until:" sibling), `actions.test.ts:268` for the test block, `drizzle/0019_msa_audit_actions.sql` for the enum-extension precedent (Option A only), `scripts/0041-msa-smoke.ts` for the sweep script shape (Options A/C only). Un-park trigger: 0043 ships, OR user explicitly switches focus.
- **2026-05-13** — Closed `0042-shadcn-ui-sweep` → `docs/chunks/closed/0042-shadcn-ui-sweep/`. All 7 phases shipped via `/build` chunk loop: P1 `fd2ad89` (shadcn init `base-nova` preset + `globals.css` token reconciliation, strategy-A semantic-alias to existing named tokens, DM-Serif-Display + DM-Sans dropped, switched sans to Inter via `next/font/google`), P2 `09ad506` (form stack — pivoted mid-phase from classic `<FormField>`/`useFormField` to shadcn 4.x `<Field>` + `<FieldGroup>` per the shadcn skill's authoritative rules; hand-written `form.tsx` wrapper + `form-bind.ts` setError-router deleted), P3 `2462b0c` (quote-composer port — `<Field>` + `<FieldGroup>` for inputs, `<FieldSet>` + `<FieldLegend>` + `<ToggleGroup>` for retrieval-bracket with the Base-UI-array-shape adapter), P4 `29fd30d` (dealer-form full RHF + zod port, booking-form partial-RHF kept `useActionState` because auto-fill UX doesn't fit RHF), P5 `b4df471` + `3c5c7a7` (primitive sweep — 9 dialog consumers + 2 combobox consumers + 1 tabs consumer ported to shadcn Base UI; `src/components/ui/command.tsx` deleted + deps `cmdk` / `@radix-ui/react-dialog` / `-popover` / `-tabs` removed; chunk-introduced Tabs layout regression on `/reports` caught + fixed by rewriting `data-horizontal:flex-col` to `data-[orientation=horizontal]:flex-col` since shadcn-shipped class only matches `[data-horizontal]` attribute-presence and the wrapper sets `data-orientation="horizontal"` instead), P6 `de4da59` (PersonForm in `people-admin.tsx` ported off `@radix-ui/react-form` — Phase 4's "only dealer-form left" grep audit was stale; `pnpm remove @radix-ui/react-form` clean afterward; `docs/wiki/forms.md` written + cross-linked from `index.md` + `conventions.md` + `log.md`), P7 `3a1a16e` (chunk-end `/eval` rescue — Codex Medium "dialog default-width regression" addressed in-cycle: old in-house `Dialog.Panel` defaulted to `max-w-[560px]` but shadcn ships `sm:max-w-sm` ~384px, fixed by overriding the shadcn default to `sm:max-w-[560px]` so the Book Event / Edit Campaign / Add Person dialogs aren't cramped; also dropped orphan `inputClass` const in `quote-composer.tsx` for lint-clean). Chunk-end `/eval` verdict **PASS with warnings** ([`eval-2026-05-13-0701.md`](closed/0042-shadcn-ui-sweep/eval-2026-05-13-0701.md)): static all green, browser smoke 12/12 PASS (Add Dealer, Book Event, /quotes/new Field+ToggleGroup, /reports Tabs orientation, /admin/people PersonForm post-port), Codex 1 Medium → addressed. Concurrent first Codex pass orphaned mid-run (PID died at ~18min elapsed with state still `running`); swept via eval skill's stall recipe; second pass returned the canonical findings. Cross-refs swept: `docs/wiki/forms.md` (link `docs/chunks/0042-…` → `docs/chunks/closed/0042-…`); inside moved folder, `eval-2026-05-13-0701.md`'s `../../../src/...` paths climbed to `../../../../src/...`; `[plan.md](plan.md)` siblings stayed valid. Active **Plan:** flipped to `0043-portal-shell-conventions` (un-park trigger "0042 closed" fulfilled this turn); Active sub-plan stays None (0043 is standalone, not a 0025 sub-plan). 0025 umbrella stays in **Parked:** — slot will return to it after 0043 ships.
- **2026-05-12** — Switched Active **Plan:** from `0025-quote-to-payment` umbrella → `0042-shadcn-ui-sweep` standalone. Un-park trigger fully cleared (0041 closed at `270f8e8`; 0035 quote-composer RHF carry-forward committed at `f540c46`; 0042 theme/style/font decisions committed at `bfbd80a`). 0025 umbrella displaced from Plan: temporarily — its epic leftovers (7.2 second-half, 7.3, 7.4) stay in **Parked:** until 0042 closes, then 0025 returns to the Plan: slot. 0042 is standalone — Active sub-plan field deliberately set to None (it's not a 0025 sub-plan). User invoked `/build` immediately after the focus switch.
- **2026-05-12** — Closed `0041-msa-send-bundled-envelope` → `docs/chunks/closed/0041-msa-send-bundled-envelope/`. All 6 phases shipped same day via the `/build` chunk loop (14 commits + 1 lint-fix + 1 eval report): P1 `3e1b6bd` (Dropbox Sign SDK + `client.ts` lazy `SignatureRequestApi` singleton + `templates.ts` env-version helper + `.env.example` env-var documentation for `DROPBOX_SIGN_API_KEY` / `DROPBOX_SIGN_CLIENT_ID` / `DROPBOX_SIGN_WEBHOOK_SECRET` / `MSA_TEMPLATE_VERSION`; @hookform/resolvers + react-hook-form package.json + lockfile lines pulled in as tag-along from the uncommitted 0035 RHF refactor — those dep declarations now land in committed history but the 0035 code changes in `quote-composer.tsx` + `pricing.ts` remain uncommitted) + bookkeeping `04ff0e9`; P2 `f07a952` (`renderMsaPdf` mirroring `renderQuotePdf` shape — §-clauses 1/2/3/5/9 hardcoded with variables `clientName` / `terminationNoticeDays` / `governingLaw` / `termStart` / `termEnd` interpolated; multi-page renderer that paginates body + auto-pages-on signature-block overflow) + bookkeeping `98f1719`; P3 `22c6a0f` (capabilities `msa:edit` admin+coach / `msa:read` admin+coach+viewer + 14-case admit-set tests; `msa.created/sent/signed/declined` added to `auditAction` pgEnum via `drizzle/0019_msa_audit_actions.sql` + journal `when` bumped to `1779724800000` + `0019_snapshot.json` patched; `createMsaDraft` with FOR-UPDATE dealer lock + same-tx one-MSA-per-dealer guard + `templateVersion` from env; `sendMsaEnvelope` with pre-load → render-both-PDFs → upload-draft-to-GCS → post-to-Dropbox-Sign → atomic guarded UPDATE on `dropbox_sign_document_id IS NULL` → audit; `sendSignatureRequest` + `getSignedFileBytes` helpers added to client.ts; 14-case action.test.ts including idempotent re-send + cross-dealer + API-failure-no-mutation) + bookkeeping `f9fdbb7`; P4 `81f18b1` (webhook route at `/api/dropbox-sign/webhook` with HMAC-SHA256(event_time+event_type, DROPBOX_SIGN_WEBHOOK_SECRET) verify via constant-time `crypto.timingSafeEqual` in `webhook-verify.ts`, dispatch by `signature_request_all_signed` / `signature_request_declined`, `Hello API Event Received` ack body; `markMsaSigned` + `markMsaDeclined` lifecycle helpers — non-`'use server'` so the 0031 action-gate lint rule doesn't flag them — that emit audit rows directly with `actorUserId: null, actorRole: 'system'`; 11-case webhook route test + 7-case lifecycle test + 7-case webhook-verify test) + bookkeeping `cc73442`; P5 `784a080` (MSA panel block on `/dealerships/[id]/page.tsx` rendered **above** the Quotes section per the umbrella-contract framing in `commercial-spine.md`; `loadMsasByDealer` + `loadActiveOrPendingMsa` + `firstDraftQuoteIdForDealer` server-only queries; `MsaCreateDialog` client component + `MsaCreateTrigger` wrapper for `useState`-driven open; status-pill `MSA_STATUS_PILL_CLS` sibling to quote `STATUS_PILL_CLS`; disabled-state UX surfaces "create a draft Quote first" gating + "No customer contact with primary email" gating; 7-case queries test) + bookkeeping `574f212`; P6 `e20ffa4` (`scripts/0041-msa-smoke.ts insert|cleanup` fixture script — pending MSA + signed MSA both against dealer #1, signed PDF GCS key is dummy + 404s when clicked; audit-row purge included in cleanup; sibling pattern to `scripts/0040-cleanup-quote-1.ts`) + bookkeeping `c95641a`. Chunk-end `/eval` at `408e2c9`: initial run FAIL on lint — `// authz: public` marker was in the top-of-file comment block instead of adjacent to the `export async function POST` declaration, fixed at `b3663fc` (the 0031 action-gate lint rule scans for the marker in the function's leading comment, not anywhere in the file). Post-fix verdict PASS-with-warnings: static gate clean (tsc + 757/759 tests + lint 0 errors / 6 pre-existing warnings), browser smoke Phase 1 5/5 + Phase 2 8/8 PASS (MSA panel + dialog verified end-to-end on `/dealerships/1` no-MSA branch + "create one first" gating link; regression-clean on `/calendar` + `/quotes`), Codex 1 High + 3 Medium + 1 Low — all routed to **Parked:** as 0041 follow-ups (a–e). Eval report `closed/0041-msa-send-bundled-envelope/eval-2026-05-12-2229.md` covers the verbatim findings + explicitly-not-an-issue notes. Live Dropbox Sign integration NOT exercised — no sandbox creds in `.env.local`; the mocked-client + webhook-route + lifecycle tests are sufficient to declare 0041 shipped, but the live envelope-post + webhook-signed round-trip is a separate "credentials wired in sandbox" follow-up (0041 follow-up (e)). Active sub-plan flipped to **None** — sequence forward is queued in **Parked:** with 0041 follow-ups (a + c bundled — concurrent-send race + resend UI), 0042-shadcn-ui-sweep (un-park-eligible once 0035 RHF work commits), 0026 follow-ups (a–d), 0025 epic leftovers (7.3 Invoice / 7.4 Payment), 0037 `audience_source_id` drop follow-up. No external `../X` refs to sweep — only CURRENT.md referenced 0041 externally; the moved folder has no `../` links to siblings.
- **2026-05-12** — Scaffolded `0042-shadcn-ui-sweep/plan.md` and added it to **Parked:** (did *not* flip Active — 0041 is mid-flight, and the working tree has uncommitted RHF + zod refactor on `quote-composer.tsx` that this sweep's Phase 3 would re-touch). Surfaced from same-session conversation: user asked whether to adopt the RHF+Zod+shadcn baseline as a project convention after dropping in a forms-stack recommendation; only the RHF+Zod half is already in flight (in `quote-composer.tsx`, uncommitted), and shadcn isn't installed. Plan structure: 7 phases — (1) `shadcn init` with explicit choices (`new-york` style recommended; theme reconciliation via semantic-alias strategy A — `--primary: var(--color-navy)` etc. — to preserve the existing navy/accent/stone palette and Tailwind v4 `@theme` setup, since this repo has *no* `tailwind.config.*`); (2) Form stack (`<Form>` + helpers; optional `src/lib/actions/form-bind.ts` adapter mapping Server Action `{error, fieldErrors?}` results to RHF `setError`); (3) port `quote-composer.tsx` — primitive swap only since the file is already RHF + zod; (4) port `dealer-form.tsx` + `booking-form.tsx` off Radix Form + `useActionState` onto the shared pattern, keeping Server Actions as submission target (TanStack `useMutation` explicitly out of scope per CLAUDE.md's "Mutations go through Server Actions" rule); (5) primitive sweep — replace in-house `dialog`/`combobox`/`tabs` with shadcn equivalents, **keep** `data-table.tsx` (column-config conventions from 0023) and `toaster.tsx` (audit-log hooks from closed/0030); (6) `docs/wiki/forms.md` + remove `@radix-ui/react-form` from `package.json` once the last consumer is ported; (7) full `pnpm test` + browser smoke + chunk-end `/eval`. Un-park trigger: **0041 ships AND uncommitted 0035 quote-composer work is committed** — otherwise Phase 3 will collide with same-file in-flight changes. Seven open questions captured in plan: style choice, theme strategy (A semantic-alias vs B template-edit-on-add), `components.json` paths (Tailwind v4 special-case), Server-Action ↔ RHF helper (shared vs inline), Radix Form removal scope verification, composer-port timing, in-house primitive keep-list. Code Anchors: `quote-composer.tsx:131` (current RHF use, anchor for `<Form>` wrapper); `src/components/ui/dialog.tsx` (in-house primitive shape, anchor for shadcn-installed primitives' file structure); `src/lib/actions/legacy-result.ts:1` (`toLegacyResult` shape, anchor for `form-bind.ts`); `docs/wiki/conventions.md` (sibling wiki page).
- **2026-05-12** — Scaffolded `0041-msa-send-bundled-envelope/plan.md` and set as Active sub-plan under the 0025 umbrella. Surfaced from same-session conversation: user asked "how do we send a MSA" after the 0040 close; answer was "we don't — schema exists from closed/0037 P2, nothing else does." Plan implements 0025 epic Phase 7.2 (Contract). Six phases: (1) Dropbox Sign client `src/lib/dropbox-sign/` (anchored on `gcs.ts` lazy-singleton pattern); (2) `renderMsaPdf` (anchored on `renderQuotePdf` shape); (3) `createMsaDraft` + `sendMsaEnvelope` Server Actions (anchored on `createQuote` + `sendQuote` pair) + add `msa.created/sent/signed/declined` to `auditAction` enum (via `ALTER TYPE … ADD VALUE` precedent in `0016_flat_typhoid_mary.sql`); (4) **route handler** (the rare legitimate one per CLAUDE.md — external caller, Dropbox Sign webhook) at `/api/dropbox-sign/webhook` with HMAC signature verify before any mutation, dispatching into a non-`'use server'` `markMsaSigned`/`markMsaDeclined` helper in `src/features/msa/lifecycle.ts` (sibling of `src/features/quotes/lifecycle.ts`); (5) MSA panel on `/dealerships/[id]` (sibling of 0040's send-receipt panel on `/quotes/[id]`) + `MsaCreateDialog` (single-click create-draft-and-send flow); (6) tests + smoke (mocked Dropbox Sign client; fixture-driven UI smoke; **no live envelope-send** until sandbox creds wired — separate follow-up). v1 scope-locks: **MSA-sign and Quote-send are independent transitions** (an MSA-active gate on `acceptQuote` is in scope but split into a follow-up chunk per 0026 follow-up (b)); one MSA per dealer; no re-sends; no auto-renewal — when `expiresAt` arrives a banner surfaces, renewal is its own chunk. Eight open questions inherited from closed/0037: termination-notice days (recommend 30), renewal flow (out of scope v1), envelope shape (template-side vs upload-inline), `templateVersion` strategy (recommend env-var bump), MSA-gate-on-acceptQuote (recommend yes, separate chunk), cancellation-fee math (lives in 7.3 or 7.4), quotes-per-MSA cap (recommend none v1), webhook security (HMAC-SHA256 with `DROPBOX_SIGN_WEBHOOK_SECRET`). Phase 1 triages which OQs need answers before Phase 3 kicks off. Inherits the new `/build` cadence shipped during 0040 (fast `tsc + test` gate per phase, single full `/eval` at chunk-end).
- **2026-05-12** — Closed `0040-quote-send-receipts` → `docs/chunks/closed/0040-quote-send-receipts/`. All 5 phases shipped same day via the **first invocation of the updated `/build` skill cadence** (fast gate `tsc + test` per phase, single full `/eval` at chunk-end — replaces the prior per-phase Codex+web-test). Commits: P1 `ba16de8` (schema add `sent_to_email` + `sent_to_first_name` on `quotes`, migration `drizzle/0018_acoustic_cannonball.sql`; Codex flagged a journal `when` non-monotonicity Low, fixed in-chunk) + bookkeeping `71f37f1`; P2 `3d0aed1` (`sendQuote` UPDATE writes the denorm in the same atomic `draft → sent` flip; `loadQuote` projection extended) + `eb75034`; P3 `023a7cc` (`loadQuoteSendReceipt` audit-row reader + `signedQuotePdfUrl` Server Action; first production caller of `gcs.ts signedUrl`) + `9296da4`; P4 `52a0ddd` (send-receipt panel between header and composer on `/quotes/[id]`, server-renders the GCS signed URL directly since `assertCan('quote:edit')` already gated the page — server-action `signedQuotePdfUrl` reserved for future client-side refresh paths; side-fix added `pdfStorageKey` to the `Quote` type/projection that wasn't carried before) + `50d9171`; P5 `a7ec213` (test coverage — extended `sendQuote` happy-path to assert `patch.sentToEmail`/`sentToFirstName`, extended `loadQuotes` round-trip baseRow + a "lifecycle timestamps" assertion to cover the new denorm + `pdfStorageKey`, added a 2-case `loadQuoteSendReceipt` suite). Chunk-end `/eval` at `ed0bef0` — static + browser smoke clean; Codex SKIPPED (subagent returned no output; treated as PASS with caveat per the eval skill contract). Browser smoke confirmed the panel renders on `/quotes/1` with the "(recipient unknown)" fallback (the existing sent quote predated this chunk's denorm) + a working GCS V4 signed URL link. Quote #1 + 2 audit rows + GCS object subsequently deleted via `scripts/0040-cleanup-quote-1.ts --apply` (user-authorized via `AskUserQuestion`; classifier blocked direct psql + the dry-run, user ran the script via `!`-prefix shell themselves) — `/quotes` index now `All (0)`. Side-effect during smoke: first `pnpm db:migrate` run reported success but the runtime DB didn't have the column (`/quotes/1` 500'd until a second `db:migrate` invocation landed it) — flagged in the eval report for future investigation; no reproducible test today. Active sub-plan flipped to **None** — sequence forward is 0026 follow-ups (a–d) → 0025 7.2/7.3/7.4. The `/build` skill itself was updated mid-chunk (between Phase 2 and 3) — old per-phase `/eval` cadence kept emitting Codex Low ceremony on tiny mechanical phases (e.g. the Phase 1 journal-`when` finding cost more than the fix), so it now runs `tsc + test` per phase and saves Codex + web-test + lint for the chunk-end. Skill change at `.claude/skills/build/SKILL.md` (untracked — `.claude/` gitignored).
- **2026-05-12** — Scaffolded `0040-quote-send-receipts/plan.md` and set as Active sub-plan under the 0025 umbrella. Switched away from `None` (slot had been idle since 0035 closed earlier the same day). Surfaced from same-session conversation: user asked "is the data available about the send we could surface to the quote?" while reviewing the RHF+zod refactor of `quote-composer.tsx` — answered with the inventory (sentAt + pdfStorageKey + acceptToken already on the row; audit_log has actor + Resend `emailId`; missing piece is recipient-email denorm since `resolveQuoteRecipient` runs against the dealer's *current* primary contact and would lie after a contact rotation). User said "no make a chunk to do it properly." Plan structure: 5 phases — (1) schema add `sent_to_email` + `sent_to_first_name` (anchor on `pdfStorageKey` shape + the `0011_slim_wonder_man.sql` ADD-COLUMN-to-quotes migration); (2) wire `sendQuote` to denormalize in the same atomic UPDATE as `sentAt`/`pdfStorageKey`, extend `loadQuote` projection (anchor on `actions.ts:683-704` + `queries.ts:50-52`); (3) audit-row reader (`loadQuoteSendReceipt`, first reader of `auditLog` so anchored on `loadQuote`'s query shape) + new `signedQuotePdfUrl` Server Action (first caller of `gcs.ts:94 signedUrl`; sibling of `previewQuotePdf`); (4) send-receipt panel rendered between header and composer on `/quotes/[id]/page.tsx` (anchor on the existing header+status-pill block lines 47-67); (5) integration tests + smoke. Denorm is **set-once on the `draft → sent` flip** — re-sends not a v1 thing; if 0026 follow-up (a) degraded-send retry lands later, that chunk owns the re-send semantics. Audit `payload` schema intentionally **not** extended (avoids a parallel source of truth). Touches the **0026 follow-ups (c) staff accept/decline UI affordance** queue lightly — the panel scaffolds the surface where Accept/Decline buttons could later attach, but (c) is not in scope for 0040.
- **2026-05-12** — Closed `0035-quote-composer` → `docs/chunks/closed/0035-quote-composer/`. Phase 4 (PDF preview pane + Send wiring on `/quotes/[id]`) shipped via `/build` chunk loop: `feat(quotes): pdf preview + send wiring on composer (0035 Phase 4)` at `fea4d82` + bookkeeping `docs(plan): mark 0035 Phase 4 commit + eval report` at `6ebb235`. New `previewQuotePdf` Server Action (renders the PDF from the persisted snapshot, returns `data:application/pdf;base64,…` for an `<iframe src=…>`; no GCS, no email, no lifecycle change) + UI surface in `quote-composer.tsx` (Preview/Send buttons, two Radix dialogs, dirty-state Send gate, recipient resolved server-side via `resolveQuoteRecipient` and threaded as a `Recipient` prop). MSA-gate per the 2026-05-11 commercial-spine alignment **deferred** to 0025 P7.2 — both 7.2 and the in-app MSA-creation surface are unscaffolded, so route (a) (active-MSA → `sendQuote`) is the only path today; Send-without-MSA support comes back when 7.2 lands. Codex pass-1 surfaced 1 High + 1 Medium, both self-resolved in-pass (orchestrator didn't dispatch `codex:rescue` — fixes were mechanical UI tweaks): **High** = confirm-dialog totals came from live `computed` while `sendQuote` emits the persisted snapshot, fixed with an `isDirty` memo (Send disabled when dirty + dialog reads `initial.lineItems.length` / `initial.total`); **Medium** = Preview button sat inside `<fieldset disabled={isReadOnly}>` and cascaded-disabled on sent quotes, fixed by pulling the buttons row outside the fieldset. Browser smoke clean (Phase 1 3/3, Phase 2 7/7, plus a post-fix re-verify that Preview opens on a sent quote). Eval report at `closed/0035-quote-composer/eval-2026-05-12-1650.md`. **Two follow-up commits** (`7366bbd` then `87022c6`) shipped after the close-the-chunk commit landed, on a different bug surfaced during the live send: stranded quote #1 hit the "Quote cannot be sent from status 'draft'" fallthrough because the atomic UPDATE's `eq(quotes.updatedAt, draft.updatedAt)` compared a microsecond-precision Postgres `timestamptz` against a millisecond-precision JS Date roundtrip — only matters when the row's `updated_at` was written by non-Drizzle SQL (e.g. `now()` in a one-off `UPDATE` to un-strand a previous degraded send). Fix: `date_trunc('milliseconds', updated_at) = $1::timestamptz` with explicit `.toISOString()` binding because drizzle's `sql\`\`` template doesn't propagate column-type metadata to bound JS Dates (postgres-js throws `ERR_INVALID_ARG_TYPE`). The original separate-issue trap that *caused* the strand: `GCS_CREDENTIALS_JSON` in `.env.local` was a **file path** but `gcs.ts:39-46` only handles inline JSON; `JSON.parse(path)` throws → `putObject` returns `{ error: 'GCS_CREDENTIALS_JSON is not valid JSON.' }` → `sendQuote` returns the "Quote sent but PDF upload failed; admin repair required" degraded path after the UPDATE already flipped status to `sent`. User-side fix: rename env to `GOOGLE_APPLICATION_CREDENTIALS=/path` (ADC fallback at `gcs.ts:37-49` picks it up, no code change); restart `pnpm dev` to clear the module-level cached `Storage` client (`gcs.ts:22`). Live verification: real send to `shannon@salesability.ca` dev-redirected to `david.hogan@networknode.ca` with subject prefix `[DEV→…]`, Resend `emailId 28a24ca4-2ec8-440e-8978-a758ff00567e`, PDF at `quotes/1/1.pdf` in `event-pro` bucket, `quote.sent` audit emitted. Cross-refs swept: `CURRENT.md` (this entry); `closed/0026-quote-pdf/plan.md` (`../../0035-…` → `../0035-…`); `closed/0037-commercial-spine-msa/eval-2026-05-11-1637.md` (absolute path got `closed/` injected); `closed/0038-rename-audience-sources/plan.md` (same); `wiki/log.md` (same). Internal `../X` refs inside moved 0035/plan.md climbed: `../0025-…` → `../../0025-…`, `../closed/0026-…` → `../0026-…`, `../closed/0037-…` → `../0037-…`, `../../wiki/…` → `../../../wiki/…`. Eval reports inside the moved folder kept their historical `docs/chunks/0035-quote-composer/...` Plan headers (snapshot convention). Active sub-plan is now **None** — sequence forward is 0026 follow-ups (a–d) → 0025 7.2/7.3/7.4. Two new feedback memories worth saving: (i) Drizzle `sql\`\`` template doesn't propagate column-type metadata to bound JS Dates → pass `.toISOString()` + explicit `::timestamptz` cast; (ii) `<fieldset disabled>` cascades through descendant `<button>`s — pull always-enabled buttons outside the fieldset.
- **2026-05-12** — Closed `0039-quote-visibility` → `docs/chunks/closed/0039-quote-visibility/`. All 5 phases shipped same day via `/build` chunk loop: P1 query layer (`d04afeb`), P2 `/quotes` index + filter pills + nav entry (`4a772d2`; Codex pass-1 3 Medium + 2 Low self-resolved — stale-state `pushParams` closure, `searchParams` array shape, nav-tab capability gap leaking `Quotes` to non-coach/non-admin staff, unvalidated URL status, actions-column a11y; orchestrator caught Codex's first attempt at a rules-of-hooks violation `useCan` inside `.filter()` and replaced with `useContext(CapabilityContext)` + bound `can` predicate), P3 `/quotes/[id]` edit-mode + composer `initial` prop + redirect fix (`56006fb`; Codex 2 Medium self-resolved — archived-dealer `—` fallback fixed by threading `dealerName` through `initial`, read-only catalog drift fixed by surfacing persisted `lineItems`/`subtotal`/`tax`/`total` from `loadQuote`; pass-2 1 Low deferred per "trust internal writes"), P4 `/dealerships/[id]` detail + dealer-name link + `parsePositiveIntPathSegment` helper (`b484de4`; Codex 2 Medium self-resolved — archived-dealer dead link routed to plain text, lax `Number.isInteger` validation tightened to `/^\d+$/` regex on both dynamic routes; pass-2 Medium re: trailing-`\n` was a verified false positive — `/^\d+$/.test('1\n')` returns `false` in JS), P5 closeout (`5eded2d` + `036e4e7`). UI polish on top: `a2e71ae` (table fills card width with `w-full` + Dealer `<th>` absorbing slack with `w-full`; nav reorder placing Reports right of Quotes). Also cleared pre-existing tsc fallout from 0037 P4 in `scripts/import-from-sheets.ts` (five dead lines referencing dropped commercial columns) up-front so the Phase 1 static gate cleared. 0035 P4 promoted Parked → Active per the queued sequence — it adds the email-a-quote UI surface (PDF preview pane + Send button on `/quotes/new`), gated on GCS+Resend creds being wired in `.env.local`. **No new Server Action, no migration in 0039** — every save path reuses the existing `setQuoteInputs` / `setQuoteTax` / `setQuoteDealer` setters on draft rows; the lifecycle guard (`actions.ts:281-289`) keeps non-draft quotes immutable. Cross-refs swept: CURRENT.md (3 lines including this entry); internal `../0025-...` ref inside moved folder climbed to `../../`.
- **2026-05-12** — Scaffolded `0039-quote-visibility/plan.md` and set as Active sub-plan under the 0025 umbrella. Surfaced in same-session conversation immediately after 0037 closed: user asked "where in the UI is the start of the prospect-to-invoice flow" → answered with `/dealerships` + `+ Add Dealer` + per-row `Quote` action → user noticed composer saves dump to `/production` (a leftover placeholder from 0035 P3 ship `0a17124`, `quote-composer.tsx:96`) → confirmed there's no `/quotes` index, no `/dealerships/[id]` detail, no quote-history surface today (only `pnpm db:studio` against the `quotes` table can show them). User said "yes" to a small chunk to close the gap, then immediately followed with "we need to add the edit route too" — scope expanded same-session to 5 phases. Final shape: query layer (`loadQuotes` / `loadQuote` / `loadQuotesByDealer`, anchor on `loadCampaigns`) → `/quotes` index page (anchor on `production/page.tsx`) → `/quotes/[id]` edit-mode (composer accepts initial-values prop; save handler branches between `createQuote` and the existing draft-only `setQuoteInputs` setter — no new Server Action, no migration, lifecycle-guard already present at `actions.ts:281-289`) → `/dealerships/[id]` detail + dealer-name link → tests + smoke. Composer `/production` redirect fix folded into Phase 3 (post-create now lands on `/quotes/<id>` so subsequent saves are UPDATEs). 0035 P4 kicked from Active to Parked (was briefly Active immediately after 0037 close).
- **2026-05-12** — Closed `0037-commercial-spine-msa` → `docs/chunks/closed/0037-commercial-spine-msa/`. All 5 phases shipped (Phases 1–3 on 2026-05-11; Phases 4 + 5 today via `/build` chunk loop). Phase 4 shipped at `b089d47` ("refactor(db): drop legacy commercial cols from campaigns (0037 Phase 4)") with **scope narrowed mid-flight**: the original plan dropped six columns; pre-drop audit found `audience_source_id` has real readers in the booking-form Data Source dropdown + calendar event-detail popover + production view + two CSV exports + the `Campaign` type / `loadCampaigns` join — none for "commercial purposes" but dropping the column would have stripped Data Source attribution from the direct-booking path on `/calendar`. User-confirmed via `AskUserQuestion` (Option A: drop 5, defer 6th; no backfill needed) — five strictly-commercial cols (`fee`/`travel`/`deposit_pct`/`tax_pct`/`quote_valid_days`) dropped via `drizzle/0017_tranquil_living_mummy.sql`; `audience_source_id` deferred to a follow-up chunk that rewires the booking-form/composer flow first. Drizzle journal `when` for 0017 hand-bumped (generator-default fell before 0016 → monotonic bump to 1779552000000, same pattern as 0026 P2 / 0038 P1). Phase 5 (`docs(wiki): post-drop sweep` at `035d5e6` + bookkeeping `8143c1c`) ran the wiki sweep doc-only: `data-model.md` campaigns row + walkthrough rewritten to "operational delivery"; `commercial-spine.md` Phase 4 description records the narrowing; new `log.md` 2026-05-12 entry "Commercial spine landed"; 0025 plan annotated; CURRENT.md updated; CRT cross-refs swept. Codex P5 pass-1 surfaced 2 Medium + 2 Low (log.md replace-vs-insert headerless-bullets bug; CURRENT.md stale Status line; "no edits needed" claim false; commercial-spine.md stale future-tense cross-link) — all resolved in-pass per `feedback_docs_eval_single_pass.md` (single-pass policy on doc-only diffs; no pass-2). Eval reports at `closed/0037-commercial-spine-msa/eval-2026-05-12-1230.md` (Phase 4) and `eval-2026-05-12-1238.md` (Phase 5). External cross-refs swept: `0025/plan.md` (2 refs), `0035/plan.md` (1 ref), `closed/0026-quote-pdf/plan.md` (1 ref climbed `../../` → `../`), `wiki/commercial-spine.md` (preamble link + Cross-links section past-tense). Browser smoke (Phase 4 eval): public + gated PASS; "Book Event" dialog still renders Data Source field reading the retained `campaigns.audience_source_id`. Active sub-plan flipped to **0035-quote-composer Phase 4** (PDF preview + Send wiring polish; low-urgency).
- **2026-05-12** — Closed `0026-quote-pdf` → `docs/chunks/closed/0026-quote-pdf/`. Phase 5 (tests + smoke + wiki close-out) shipped via `/build` chunk loop: `docs(quotes): wiki + plan close-out (0026 Phase 5)` at `7842063` + bookkeeping `docs(plan): mark 0026 Phase 5 commit + eval report` at `ae17a33`. All 5 phases now Done — chunk fully shipped. Codex pass-1 surfaced 1 High + 4 Medium + 2 Low factual drifts in the new wiki prose (incorrect "inside `acceptQuote`'s transaction" claim; `MAX_LINE_ITEMS=25` should be 13; degraded-state audit-payload overstatement; predicate-blindness parking pointer mis-pointed at 0033 vs 0018; commercial-spine.md still described the dropped public-accept-link surface in three places; architecture.md:102 stale `sendQuote` stub claim; `audience_source_id` "v1 sets at composer-time" overstated; `toRendererLines` bad symbol pointer) — all corrected directly in same eval pass without a pass-2 verification round per new feedback memory `feedback_docs_eval_single_pass.md` (doc fact-check loops don't converge cheaply; single-pass is the policy). Eval report at `closed/0026-quote-pdf/eval-2026-05-12-1143.md`. Browser smoke clean (Phase 1 + Phase 2 + quote-composer page render verified at `/quotes/new?dealerId=1`); 673 vitest tests pass. Four 0026 follow-ups deferred to chunk-sized future work and captured under "0026 follow-ups" in Parked: (a) degraded-send retry path + `emailed_at` schema, (b) split `quote:send` from `quote:edit`, (c) staff accept/decline UI affordance, (d) live-send dev smoke once GCS+Resend creds wired in a sandboxed env. External cross-refs swept: `0025/plan.md` sub-plan link, `0035/plan.md` sibling link, `0037/plan.md` (5 inline path mentions), `wiki/architecture.md` (2 links), `wiki/log.md` (1 link). Internal `../X` refs in moved 0026/plan.md climbed to `../../X`. Eval reports inside the moved folder kept their historical `docs/chunks/0026-quote-pdf/...` Plan headers (snapshot convention, same as 0036/0004 closes). Active sub-plan flipped to **0037-commercial-spine-msa Phase 4** — destructive column drops on `campaigns`, needs confirm-before-run.
- **2026-05-12** — `0035-quote-composer` Phase 5 (wiki bookkeeping pass) shipped via `/build` chunk loop: `docs(wiki): document service_items + dealers.status; quote-composer architecture (0035 Phase 5)` at `3f9edbe` + bookkeeping `docs(plan): mark 0035 Phase 5 commit + eval pass-1/2/3 reports` at `84a6760`. Three Codex review passes self-resolved 3 High + 4 Medium + 2 Low factual inaccuracies (composer-setter overclaim, /dealerships coach-gating misattribution, money-parse `MONEY_RE` cross-attribution, "Add new prospect" entry-point claimed shipped when deferred, MSA gate framed as current when planned, stale `blocked_dates` in audit-columns paragraph, quote-row precision overgeneralized as `numeric(12,2)`, log-entry MSA-gate framing inconsistent with corrected architecture.md). All review passes kept strictly read-only / findings-only per the new feedback memory `feedback_codex_review_invocations.md` — verdict is the orchestrator's call, not the reviewer's. Eval report at `0035-quote-composer/eval-2026-05-12-0841.md`. Plan now 80% (4/5 Done); Phase 4 stays Blocked on 0026 P3+P4 (which is itself blocked on GCS creds carry-forward). Phase 4 won't move until either (a) 0026 P3 lands (needing `GCS_PROJECT_ID` / `GCS_BUCKET` / `GCS_CREDENTIALS_JSON` in `.env.local`), or (b) the sequence is re-ordered. Sequence remains: **(pause)** → 0026 P3 → 0026 P4–P5 → 0037 P4 cleanup → 0037 P5 sweep → 0035 P4 → 0026 close.
- **2026-05-11** — `0035-quote-composer` Phases 1+2+3 all shipped in one `/build` chunk-loop invocation (six commits): P1 service-catalog data model + admin UI at `bfaeff7` (Codex pass-1 4 Mediums self-resolved → pass-2 clean — un-archive recovery, money decimal-precision, overflow caps, gate-matrix coverage), bookkeeping `4453b11`; P2 prospect status + acquired_via + filter pills at `dcc80bc` (Codex pass-1 2 Mediums + 1 Low self-resolved → pass-2 "Ship" — TOCTOU on `updateDealer` archive-race closed via guarded UPDATE inside transaction with patch-style omit-when-absent for `status`/`acquiredVia`, Edit-on-archived UX bug hidden), bookkeeping `9de58f3`; P3 pricing module + composer page at `0a17124` (Codex pass-1 1 High + 4 Mediums → pass-2 2 Mediums residual → pass-3 "Ship" — setter race silently-OK bug, arbitrary JSON keys persisted, record-retrieval range-fail-open, tax-cap inconsistency, dealer archive race; pass-3 cleared the residual range-fail-closed + tax-rounding canonicalization), bookkeeping `567415c`. Tests went 542 → 655 (+113). 11 Codex findings + 1 new-medium self-resolved across 3 phases. Audit-action enum gained `dealer.activated` via `ALTER TYPE ... ADD VALUE` (migration `0016`). Paused at Phase 4 per plan body's "Blocked on 0026/4" instruction; Phase 5 partial (wiki updates + inline-prospect smoke pending). New `MAX_DOLLARS` constant exported from `pricing.ts` to canonicalize tax cap across all three persistence paths. Combobox primitive's lack of inline-add support deferred the composer's "Add new prospect" affordance — DealerForm already accepts `defaultStatus='prospect'` for the eventual wire-up.
- **2026-05-11** — `0026-quote-pdf` Phase 2 shipped via `/build` chunk loop (`6c65c80`: `quotes` table + bare-bones Server Actions + RLS + follow-up migration `0011` adding `sent_at`/`accepted_at`/`declined_at` lifecycle timestamps; bookkeeping at `55dd03c`). Codex pass-1 surfaced 1 High (TOCTOU on `markQuoteAccepted`/`markQuoteDeclined` SELECT-then-UPDATE) + 4 Medium (same TOCTOU on `sendQuote`, missing `archivedAt IS NULL` check on `createQuote`, missing row-local lifecycle timestamps, `quotes` missing from RLS test list) + 3 Lows; direct self-resolve made transitions atomic via `UPDATE … WHERE id + status = from RETURNING id` (cancelCampaign pattern) with re-select on miss for not-found/idempotent/illegal-status; pass-2 Codex verified all High/Medium closed, 1 residual Low (test-mock predicate-blindness — repo-wide parked follow-up, same disclaimer on `people/actions.test.ts`). 19 new vitest cases, 523/523 green. Migration journal `when` bumps stay monotonic. Active sub-plan flipped to `0035-quote-composer` P1 because 0026 P3 is blocked on: (a) the `service_items` catalog 0035 P1 owns (needed for `lineItems` computation in the quote PDF), and (b) the Phase 1 GCS-creds carry-forward (`GCS_PROJECT_ID`/`GCS_BUCKET`/`GCS_CREDENTIALS_JSON` still empty in `.env.local`). Umbrella stays 0025.
- **2026-05-11** — `0037-commercial-spine-msa` Phases 1–3 shipped same session via the chunk-level `/build` loop: Phase 1 decision doc + reconciliation at `3b9b18e` (already in place before today's session); Phase 2 `master_service_agreements` schema + migration `0008_bright_lockjaw.sql` + RLS in `0009_msa_rls.sql` + reachability test + wiki MSA section at `da05c54` (`/build` loop cleared eval P1 verdict — Codex High RLS-missing + Medium zero-rows-assertion + 4 Lows — via in-skill self-resolve + pass-2 Codex verify); Phase 3 bookkeeping close-out at `46db02b` confirming all four Phase-3 checklist items were already executed during Phase 1's cross-plan reconciliation (Codex spot-checked the four citations to `0026/plan.md:69-71`, `:72`, `:108`, and `0035/plan.md:126`). Phases 4–5 paused 2026-05-11: P4 (drop commercial cols from `campaigns`) gated on 0026 P2 + 0035 P3 + destructive enough to need confirm-before-run; P5 (wiki sweep) is post-P4. Active plan flipped to `0025-quote-to-payment` umbrella with `0026-quote-pdf` resumed as Active sub-plan at Phase 2 (`quotes` data model + Server Actions), the next link in the chain. The MSA + commercial-spine prerequisites that justified parking 0026 on 2026-05-08 are now in place; 0026 P2 can ship cleanly with the locked-in column shape.
- **2026-05-11** — Closed `0038-rename-audience-sources` → `docs/chunks/closed/0038-rename-audience-sources/`. All 4 phases shipped same day: Phases 1+2 atomic at `1b64d12` (schema rename `sales_lead_sources` → `audience_sources` + 20-file call-site sweep via `sed`), Phases 3+4 at `31f0ca2` (wiki + cross-plan reconciliation in `data-model.md`, `log.md`, 0035, 0037, CURRENT.md), bookkeeping at `63cd952`. Zero data migration (`ALTER TABLE … RENAME` preserved all 4 seeded rows + 13 campaign FK values). Notable deviations: (1) `pnpm db:generate` TTY-blocked in harness → used `--custom` + hand-written RENAME SQL + `sed`-patched snapshot + bumped journal `when`; (2) Codex re-review (post-Phase 2) stalled in `verifying` for 12 min → cancelled, SKIPPED per eval rules — Phase 1 Codex review (in `eval-2026-05-11-1404.md`) still covers the diff's risk surface; (3) plan assumed UI heading was "Sales Lead Sources" but it was already "Data Sources" (neutral) — left unchanged, flagged for a separate UX chunk if "Audience Sources" should be the canonical UI vocabulary. Carry-forward: `ALTER POLICY … RENAME TO` for `sales_lead_sources_*` policies in `drizzle/0003_enable_rls.sql:188-198` (cosmetic — bound by OID, not name). External refs were limited to `CURRENT.md`; internal absolute-path self-refs in the moved folder rewrote `docs/chunks/0038-…` → `docs/chunks/closed/0038-…`. Active plan flipped to `0037-commercial-spine-msa` per the queued sequence — its Phase 3 was the original driver for 0038 (now lands cleanly under `audienceSourceId`).
- **2026-05-11** — Closed `0036-capabilities-only` → `docs/chunks/closed/0036-capabilities-only/`. Same bookkeeping drift class as 0004's 5.7 cell: all 4 phases shipped 2026-05-08/09 (last code commit `6253773` "retire require-role.ts; declare capabilities-only-at-gates" + bookkeeping commit `69f00f0` "mark Phase 4 commit + close chunk"); plan tracker correctly showed 4/4 Done; the folder simply never got moved and CURRENT.md kept claiming Active. End state per `wiki/log.md` 2026-05-09 entry: capabilities-only at gates declared the durable convention; 4 new capabilities (`app:access`/`admin:access`/`reports:view`/`availability:edit`); `roleListClient` factory + `require-role.ts` deleted; 480/481 vitest. No external refs to sweep (only CURRENT.md referenced 0036). Active plan flipped to `0038-rename-audience-sources` per the queued sequence.
- **2026-05-11** — Closed `0004-port-migration` umbrella → `docs/chunks/closed/0004-port-migration/`. Phases 1–4 + sub-phases 5.1–5.6 had long since shipped; 5.7 (Booking summary & reports) shipped 2026-05-07 via the closed `0014-summary-reports/` but the umbrella tracker still showed `Pending` — corrected to `Done` (`155df20`). 5.8 (Full-calendar share link) dropped without shipping per user: per-coach share at `/share/coach/[id]` covers the real use case. Sub-plan `0010-calendar-share-full/` also moved to `closed/` with an abandoned header; body stays as a starting point if "shareable read-only link" returns per roadmap Phase 2. Cross-refs swept in `docs/chunks/0025-quote-to-payment/plan.md`, `docs/strategy/roadmap.md`, `docs/wiki/log.md` (today's funnel entry's path updated), plus internal path-climb (`../X` → `../../X` for non-closed siblings) inside 0004's plan + open-questions. Active sub-plan stays at 0036 — no focus switch.
- **2026-05-11** — Scaffolded `0038-rename-audience-sources/plan.md` and added to Parked **ahead of 0037 Phase 3**. Surfaced from a data-model review of 0037 (same day): the `sales_lead_sources` lookup carried three overloaded meanings — (a) audience source for a dealer's marketing campaign (the only meaning actually in use; seeded values `Dealer Database / PBS / Third Party List / Previous Buyers`), (b) reserved-for-future per-`(campaign × contact)` target table per `docs/wiki/data-model.md` OQ #6 (never built), (c) acquisition source for dealerships (already split off onto `dealers.acquiredVia` per the same-day funnel review). 0037 Phase 3 would propagate the misleading `salesLeadSourceId` name onto the new `quotes` table; renaming first means `quotes.audienceSourceId` lands cleanly. Four phases (~half-day): schema rename + migration (RENAME TO, zero data migration); sweep call sites in `src/features/schedule/{actions,lookup-admin}.tsx` + tests + any reports; wiki + cross-plan reconciliation (kills `data-model.md` OQ #6 reserved-for claim, updates 0037 P3 checklist to `audienceSourceId`); tests + smoke. Recommended sequence updated to `0036 → 0038 → 0037 P1+P2 → 0026 P2 → 0035 → 0037 P4`. Active sub-plan stays at 0036 — no focus switch. Same review also locked in 30 days for 0037 OQ #1 (MSA §2.ii termination-notice gap).
- **2026-05-11** — Funnel review pass. Three changes: (1) introduced `docs/chunks/future/` as a third bucket parallel to `closed/` for chunks that are scaffolded but explicitly waiting on a trigger event (e.g. "v2", "when web intake matters"). Distinct from CURRENT.md **Parked** which means "queued for soon." Convention captured in CLAUDE.md `### Deferring a chunk`. (2) Moved `0016-book-your-event-intake` → `future/0016-book-your-event-intake` after the user clarified v1 uses manual in-app intake (existing booking modal + 0035 P2 inline-create dealer flow); web intake is v2. Added deferral header to the plan; swept cross-refs in `docs/chunks/0004-port-migration/open-questions.md`, `docs/strategy/vision.md`, `docs/wiki/security.md`. New **Future:** field in CURRENT.md captures the un-defer trigger. (3) Same review collapsed 0037 OQ #7 to "no lead entity in v1; `salesLeadSourceId` set at composer time, never propagated" — surfaced that `sales_lead_sources` was being overloaded between audience-source (consumer list) and acquisition-source (how the dealership found us); split the second concept off into a new `dealers.acquiredVia` text column added to 0035 Phase 2. Active sub-plan stays at 0036 — no focus switch.
- **2026-05-11** — Scaffolded `0037-commercial-spine-msa/plan.md` and added to Parked **ahead of 0026/0035** as a prerequisite to 0026 Phase 2 unparking. Surfaced from a sales-funnel discussion that worked upstream from the existing 0026/0035 plans: user asked "shouldn't a quote move to an order which then creates a campaign?" Schema inspection confirmed `campaigns` was carrying double-duty commercial+operational columns (`fee`, `travel`, `depositPct`, `taxPct`, `quoteValidDays`, `salesLeadSourceId`) on what should be a pure delivery record. User then supplied the full Salesability MSA text, which resolved the question definitively — §1.iii: *"Each Quote issued by Salesability and accepted by the Client shall constitute a separate distinct and independent agreement and contractual obligation of the Parties hereto."* So accepted Quote IS the contract — no separate `orders` table. Locked: (1) `master_service_agreements` table introduced — per-Client, 12-month term per §2.i, with `signedAt`, `expiresAt`, `signedPdfStorageKey`, `dropboxSignDocumentId`, status enum, `templateVersion`, termination cols (§2.ii); (2) commercial columns move off `campaigns` onto `quotes`; (3) FK direction flips to `campaigns.acceptedQuoteId` so the operational delivery (Event in MSA noun) hangs off the accepted Quote; (4) first-deal e-sig **bundles MSA + first Quote in one Dropbox Sign envelope** (user 2026-05-11 — single envelope with two documents, not merged PDF); (5) NS HST 15% confirmed as seller-side default per §9 governing law (Dartmouth NS — partially resolves 0026's tax OQ). Five phases scaffolded: decision doc + cross-plan reconciliation (edits 0025/0026/0035 plan docs); MSA schema + migration; quotes schema patch into 0026 Phase 2 sketch; drop commercial cols from `campaigns` (gated on 0026 P2 + 0035 P3); tests + wiki sweep. Eight open questions captured (termination-notice `XX days`, quote validity default, MSA renewal flow, cancellation-fee math, envelope shape, `templateVersion` strategy, lead → quote attribution propagation, quotes-per-MSA-term). Two related project memories saved: `project_coach_owned_business` (multi-tenant-by-coach: leads/campaigns/quotes coach-owned, dealers/contacts shared registry — drives upstream funnel design) and `project_msa_structure` (the MSA clauses driving the schema). Active sub-plan stays at 0036 — no focus switch.
- **2026-05-08** — Promoted `0036-capabilities-only` from Parked → Active per user request ("can we build 0036 now"). Paused `0026-quote-pdf` (Phase 7.1) mid-flight; Phase 1 already shipped at `3c4b5b9`. 0036 is a standalone plan (no umbrella). 0026 returns to Active sub-plan slot under 0025-quote-to-payment after 0036 Phase 4 ships. Rationale matches the original parking note: ahead of 0035 so the composer's `quote:edit` inherits the uniform convention.
- **2026-05-08** — Scaffolded `0036-capabilities-only/plan.md` and added to Parked **ahead of 0035** as a half-day authz cleanup. Surfaced from the 0035 Q3 discussion (composer authorization gate): inventory showed 22 capability sites + 3 `roleListClient` stragglers (availability blocks) + 6 page-level `requireRole` / `requireStaffAccess` calls + 1 reports/export route handler, so the codebase is *almost* unified. The chunk collapses every gate at the app layer onto `assertCan` / `capabilityClient`; adds 4 capabilities (`app:access`, `admin:access`, `reports:view`, `availability:edit`); deletes `roleListClient` from `action-client.ts`; 0034 pairing CI then covers every gated action with no role-list opt-outs. Edge middleware (`ADMIN_PATHS`) keeps its JWT-claim path-match as defense Layer 1 — explicitly an optimization, not the durable answer. Four phases: matrix rows + tests, page+layout migration, action migration + roleListClient delete, docs + smoke. Sequencing: parallelizable with 0035 Phases 1+2 (catalog + dealer status — pure data-model work, no authz overlap); 0035 Phase 3 ideally lands after 0036 so `quote:edit` inherits the uniform convention without re-establishing it. Original active sub-plan stays at 0026; 0036 + 0035 stay parked until the user picks which to promote next.
- **2026-05-08** — Scaffolded `0035-quote-composer/plan.md` and added to Parked (kept 0026-quote-pdf as Active sub-plan; its Phase 1 shipped earlier today at `3c4b5b9`). Surfaced when the user asked for "a way for coach to create a quote and send it" plus "a quote build system" — turned into a real chunk after the user supplied concrete v1 pricing (Base Event $6,900 incl. 500 records, BDC $2.25/call, Letter $2.50, Digital $0.59, Additional Day $995, Record Retrieval $100–$400, Travel pass-through, plus a per-contact "Additional Contacts" item for audience > 500). Decisions locked in same conversation: (1) DB-backed `service_items` table — admin-editable prices, surfaces under `/admin/lookups`; (2) `status: 'prospect'|'active'` flag on `dealers` (no separate prospects table; `archivedAt` from the existing `archivable` mixin stays the source of truth for archived state, independent of `status`); (3) multi-channel touches per record allowed (no cap enforcement). Five phases scaffolded: catalog data model + admin UI; dealer status + inline-create; pricing module + composer page at `/quotes/new`; PDF preview + send wiring (depends on 0026 Phases 3–4); tests + smoke. Sequencing: Phases 1+2 are independent of 0026 and can run in parallel with 0026 Phase 2; Phase 3 needs 0026 Phase 2 (`quotes` table) first; Phase 4 needs 0026 Phases 3+4. Six open questions captured (capability key, Additional Contact price TBD, travel sub-fields, campaign-detail entry point, quote↔campaign nullability, custom one-off lines v2). Two resolved same-turn: #1 (status='archived' vs archivedAt) → `archivedAt` is the source of truth (`status` enum becomes `'prospect'|'active'` only); #2 (audience-overage UX) → composer pivoted from line-item-picker to **structured-input calculator** (audience + days + per-channel counts as inputs; line items are computed read-only output) so the input snapshot becomes the quote→invoice contract — drift class of bug eliminated. Phase 3 of 0035 fully rewritten to the calculator shape; 0026 Phase 2's `quotes` schema updated to add the `inputs` jsonb column the invoice recomputes against.
- **2026-05-08** — `0033-next-safe-action` → 4/5 phases shipped (commit `0d2fe95`); Phase 4 (retire validators / write Zod schemas) deferred as carry-forward to revisit alongside 0016 booking-intake. Active plan switched back to `0025-quote-to-payment` with `0026-quote-pdf` as active sub-plan (resuming Phase 7.1 mid-flight from where it was paused 2026-05-08 AM). All four authz tooling/migration chunks now in place: 0031 lint catches gate-missing, 0032 test catches wrong-admit-set, 0034 script catches Can/assertCan-asymmetric, 0033 middleware makes auth-required the *default* invocation pattern. Migration touched: bootstrap `src/lib/actions/action-client.ts` (4 tiers — `baseClient`, `authedClient`, `capabilityClient(cap)`, `roleListClient(roles)` — built on `next-safe-action@8` + `zod@4`); 22 Server Actions across `people/`, `schedule/`, `email/`; 8 form callers wrapped with `toLegacyResult` adapter; production export Route Handler kept on imperative `assertCan` per Open Question 6. Test mocks updated for `getUser` + `loadCurrentMembership` + Next-redirect-shaped errors so `isNavigationError` recognises them. 0034 pairing script extended with `capabilityClient('X')` regex. 450/450 vitest still green (was 441 pre-0033 — added 9 action-client tier tests). The 0031 lint rule's coverage now scopes to legacy `export async function` shapes (the safe-action middleware enforces gate-presence by construction); documented in auth.md. Carry-forward Phase 4 (write 12+ Zod schemas, delete `validators.ts`, retire `toLegacyResult`) — revisit when 0016 forces public-input validation.
- **2026-05-08** — `0034-can-assertcan-pairing` → Done (commit `47df3f6`); active plan switched to `0033-next-safe-action` (last in the authz-tooling sequence; afterwards 0025/0026 resume). Three phases shipped same-session: extraction script (`scripts/check-capability-pairing.mjs` — Node ESM, no deps), set diff → exit non-zero on asymmetric gates, wire + document. **Real UI gaps surfaced**: the script's first run flagged 5 server-only capabilities, three of which were genuine `<Can>` omissions (the page-level `requireRole('admin')` was carrying the load). Wrapped `+ Add Person`, `+ Add Dealer`, and the orphan `Adopt` button with `<Can capability=...>`. Two legitimately server-only — `production:export` (Route Handler) and `coach-availability:edit-own` (row-relative server check) — opted out via per-line `// expected: server-only`. Sabotage smoke confirmed both directions of asymmetry are caught (UI-only + server-only). 14/14 capabilities now paired or opted out. 441/441 vitest still green; tsc + lint + new check all clean. **Three layers of structural enforcement now active**: 0031 lint catches gate-missing, 0032 test catches wrong-admit-set, 0034 script catches asymmetric. The bulk safe-action migration (0033) can proceed under all three safety nets.
- **2026-05-08** — `0032-action-matrix-test` → Done (commit `3648eec`); active plan switched to `0034-can-assertcan-pairing`. Three phases shipped same-session: matrix data file (24 rows), test harness (24 × 7 = 168 outcome assertions + drift detection), wire-in + close. Codex review caught two Highs (4-role matrix admit-set blind spot — fixed by extending `RoleKey` to all seven roles `unauth/admin/staff/coach/viewer/dealer/orphan`; drift-detection regex missed const-async / default-async exports — fixed with five export-shape patterns) and Medium 2 (file-discovery brittleness — extended to `*/actions/**/*.ts` shapes + comment-tolerant `'use server'` detection); all three fixed in-eval. Medium 1 (deny-not-tied-to-gate) parked as known caveat — no current Server Action issues post-gate redirects, but if one ever does, a wrong admit could be masked by a downstream `redirect:/` that the harness can't distinguish from a gate denial. AST-level gate-call telemetry is the carry-forward. 441/441 vitest (was 272 pre-0032; +169 new). Sabotage smoke confirmed: flipping `cancelCampaign` to admin+staff makes `staff → redirect:/` row fail, as designed.
- **2026-05-08** — `0031-action-gate-lint` → Done (commits `55203ca` + `ce40ece`); active plan switched to `0032-action-matrix-test`. Three phases shipped in the same session: custom ESLint rule + AST traversal (`eslint-plugins/action-gate.mjs`); wired into `eslint.config.mjs` against `src/features/**/actions.ts` + `src/app/**/route.ts`; sweep + auth.md/log.md updates. Codex review caught three Highs on the v1 rule (gate-name-spoof via member access; default + specifier exports invisible; gate calls inside nested closures count) — all three fixed in-eval before commit, plus three more residual evasion vectors (member-property spoofing, dead-code-after-return, swallowed-gate-via-try/catch) addressed by the same tightening (gate must be a direct `Identifier` callee resolved to an `@/lib/auth/*` import; walk skips into branching constructs and stops at top-level Return/Throw). 33 rule tests + 272/272 full vitest pass; `pnpm lint` clean against the existing surface (only 4 pre-existing warnings). Folder stays at top level for now; will move to `closed/` after the rest of the 0032/0033/0034 sequence ships and a single sweep moves all four.
- **2026-05-08** — Re-ordered the authz risk-reduction sequence after an ordering critique. Original switch promoted `0033-next-safe-action` to Active; the critique pointed out the bulk-migration of ~20 gated actions is the highest-risk hour of authz work this quarter and shouldn't run without CI safety nets already in place. Scaffolded three new tooling chunks — `0031-action-gate-lint` (catches no-gate-at-all), `0032-action-matrix-test` (catches wrong-admit-set), `0034-can-assertcan-pairing` (catches asymmetric gates) — and switched Active to `0031` as the first in the new sequence. New order: 0031 → 0032 → 0034 → 0033 → resume 0025/0026. 0033 reverts to Parked; 0025/0026 stay parked. Skipped serial 0033 in the tooling triplet because it was already taken by next-safe-action — explained inline in 0034's plan. The three tooling chunks are each ~half-day; sequencing them ahead means CI rejects gate omissions / admit-set drift / Can-assertCan asymmetry *during* the bulk migration, mitigating the failure modes the migration is most likely to introduce.
- **2026-05-08** — Active plan switched: `0025-quote-to-payment` (with active sub-plan `0026-quote-pdf` mid-Phase-7.1) → Parked; `0033-next-safe-action` promoted to Active as a standalone plan (no umbrella). User committed to the safe-action migration as a risk-reduction investment ahead of the `0016` public-intake work; doing it now means 0016 inherits the convention rather than seeding it. 0025/0026 resume after 0033 ships. Same-day promotion (the plan was scaffolded earlier in this turn — see entry below). **Superseded same-day** — see top entry: 0033 demoted back to Parked, 0031 promoted to Active.
- **2026-05-08** — Scaffolded `0033-next-safe-action/plan.md` and added to Parked. Surfaced from a "how do we make authz mistakes harder to ship" research pass that synthesised the modern Next 16 / React 19 server-action authz literature (next.js auth guide, makerkit security tutorial, next-safe-action + zsa comparison). Shape decision: stay on imperative `assertCan` today; migrate when 0016 lands OR when the team grows past one developer; user picked **migrate now** as a risk-reduction investment. Five phases: action-client bootstrap → dealer CRUD pilot → bulk migrate ~17 actions → retire hand-rolled validators → wiki + smoke. Capability strings unchanged (forward-compatible with 0029); only the *invocation pattern* shifts (line-1 imperative → `.use()` chain). Serial 0033 picked explicitly to leave 0031 + 0032 free for the parked-but-not-yet-scaffolded action-gate ESLint rule, action-matrix negative tests, and Can/assertCan pairing CI check (the three other risk-reduction levers floated in the same conversation). Active sub-plan stays at 0026 — no focus switch.
- **2026-05-08** — Tightened coach surface (`4f07995`) — five Server Actions still admitting coach via `requireRole(['admin','staff','coach'])` (createDealer/updateDealer/createCampaign/updateCampaign/email-send helper) migrated to admin-only `assertCan` per role-purpose intent. Added 4 capabilities (`campaign:create`, `campaign:edit`, `campaign:cancel`, `email:send`); reused existing `dealer:create`/`dealer:edit`. UI affordances tightened: `+ Book Event` + event-detail's Email Client / Email Coach / Cancel Campaign / Edit all wrapped in `<Can>` with the matching capability. End-state coach surface: Calendar (read-only views + own availability blocks via Block Date) + Reports. 239/239 vitest. Wiki updated (auth.md role-purpose rewrite + matrix table + per-action gate row updates; new wiki/log entry). 17 capabilities in v1.
- **2026-05-08** — `0029-capability-layer` → Done as a same-day Parked → ship side-task (active sub-plan stayed at 0026); chunk moved to `closed/0029-capability-layer/`. Five phases bundled into a single commit. Phase 1 added the pure PDP `src/lib/auth/capabilities.ts` (13-cap string-literal union, admin-shortcut on `app_metadata.role || roles.includes('admin')`, resource-relative branch for `coach-availability:edit-own`) + the server PEP `assert-can.ts` (mirrors `requireRole`'s redirect-on-fail control flow); 50 new tests (43 pure + 7 mock-driven). Phase 2 migrated 12 Server Action call sites: 4 person CRUD → `person:create`/`:edit`/`:archive`/`:adopt-orphan`, `archiveDealer` → `dealer:archive`, 6 lookup admin actions → `lookup:edit`, `production/export` Route Handler → `production:export`. Multi-role staff sends (`createDealer`/`updateDealer`/`createCampaign`/`updateCampaign`), `cancelCampaign`, and the auth flow stayed on `requireRole` per Open Question 5 (no semantic gain). `availability-authz.ts` refactored to delegate the row-ownership predicate to `can(profile, 'coach-availability:edit-own', facet)` — soft-error `{error}` return contract preserved (Open Question 6). Tests updated: `people/actions.test.ts` mocked `assertCan` instead of `requireRole`; `availability-authz.test.ts` admin-no-DB-hit assertion dropped (admin path now goes through can()-with-membership-load, cached). Phase 3 added `CapabilityProvider` + `<Can>` / `useCan` in `src/components/auth/`; provider mounted in `(app)/layout.tsx` fed from the existing cached `loadCurrentMembership` (no extra DB hit). Component test for `<Can>` deferred — vitest is `environment: 'node'` and `@testing-library/react` not installed; predicate test coverage in `capabilities.test.ts`/`assert-can.test.ts` is comprehensive (carry-forward: install `@testing-library/react` + `happy-dom` if a future component-rendering question lands). Phase 4 wrapped admin row-actions: dealer Edit + ✕ in `dealers-columns.tsx`, person Edit + Archive in `people-columns.tsx`, lookup Add form + Rename + ✕ in `lookup-admin.tsx`. `app-nav.tsx` deliberately stayed on `isAdmin` boolean (server-derived) rather than migrating to `<Can capability="…:view">` — nav is essentially-static and a hydration round-trip for the admin-dropdown reveal would be churn for no UX gain (documented in `auth.md` § Capability matrix). Phase 5 updated `auth.md` (3 layers → 4; new "Capability matrix" subsection; per-action gate matrix rewritten), `security.md` (layer 3 renamed + intent-vs-enforcement caveat inlined; availability special-case updated to mention `can()` delegation), `wiki/log.md` entry. 227/227 vitest (was 172 pre-0029), tsc + lint clean throughout. Browser-smoked all three row-action surfaces as admin: `/dealerships` → Edit + Remove buttons render per row; `/admin/people` → Edit + Archive render; `/admin/lookups` → Add form + Rename + Archive render. Coach-only smoke deferred (same fixture-not-wired carry-forward as 0028); verified by code-trace through `can()`. Capability count: 13 in v1; future expansion path documented (`campaign:cancel`, `email:send` if those gain coach/staff carve-outs; `coach-availability:edit-any` already in the union for symmetry though only admin holds it today).
- **2026-05-08** — `0028-role-route-scoping` → Done as a same-day Parked → ship side-task (active sub-plan stayed at 0026 throughout); chunk moved to `closed/0028-role-route-scoping/`. Three phases bundled into a single small commit (43 lines, 6 files): edge `ADMIN_PATHS` extended to `['/admin', '/production', '/dealerships']` with two new test cases pinning the admin-only assertion + a leftover non-admin assertion now covering `/reports`; page-level `requireRole('admin')` added to `production/page.tsx` + `dealerships/page.tsx`; `production/export/route.ts` tightened from `requireStaffAccess()` to `requireRole('admin')` (matches the page gate, closes the Route-Handler bypass that 0018's late Critical established the pattern for); `app-nav.tsx` Tab type extended with optional `requiresAdmin` flag, applied to Production List + Dealers, with a one-line `OPERATIONAL_TABS.filter` keeping the dropdown shape clean. Plan adapted on-the-fly: original Phase 3 instructions ("add `admin: true` to tabs, rely on existing filter") were stale post-2026-05-08-AM dropdown reshape (`8910a2b`) — replaced with the new `requiresAdmin` flag pattern; Production + Dealers stay top-level for admins (operational-shape, daily-use) rather than getting buried in the settings-shaped Admin dropdown. Reports left out of scope — already gated `['admin', 'coach']` and the plan's "coach = Calendar only" body claim contradicted that gate; updated `auth.md` to reflect actual coach surface (Calendar + Reports), closed the now-stale gap paragraph, kept Reports' multi-role gate alone. Smoke green: tsc/lint clean, 172/172 vitest (was 171; new middleware test cases brought it up by one), browser-verified `/production` + `/dealerships` render for admin and Admin-dropdown trigger persists across both. Coach-only smoke deferred (no coach-only test fixture wired today — verified via code-trace: middleware redirects non-admin off `ADMIN_PATHS` at line 59, page gate redirects on role mismatch as second layer, nav filter hides tabs as third layer). Closes 0027 Phase 2 Codex Medium implicitly (staff-visible `✕` on `/dealerships` is moot once non-admins can't reach the page).
- **2026-05-07** — `0030-nav-bar-improvements` → Done same-day side-task; chunk moved to `closed/0030-nav-bar-improvements/`. Four phases shipped + Phase 4 (responsive collapse) skipped per the plan's working assumption (no overflow at 1280px after the user-menu reclaimed bar width). Phase 1 contrast bump (`9718a4d`), Phase 2 Radix DropdownMenu user-menu (`ca8ba48`) — added `@radix-ui/react-dropdown-menu ^2.1.16`; collapsed the inline email + standalone Sign out behind a single avatar trigger; form-in-menu-item submission hardened to `onSelect` + `e.preventDefault()` + `requestSubmit()` (canonical Radix pattern) after Codex caught the unmount-vs-submit race. Phase 3 admin/operational tab separator (`edff423`) — driven off a per-iteration `tab.admin && !prev.admin` predicate so future non-contiguous admin tabs work too (closed Codex Concern in-eval). Phase 5 smoke + closure (`6589356`) — three reference screenshots archived in the chunk folder; verified menu open/close cycle via the browse tool's `click-name` / `press-name` subcommands (the workaround for the Radix Trigger MCP a11y-snapshot ref gap surfaced in Phase 2). 171/171 vitest, tsc + lint clean throughout. Active sub-plan slot returns to `0026-quote-pdf`. Carry-forward (tooling, not product): browse-tool keyboard-flow exercise needs richer Radix-Trigger handling for end-to-end ArrowDown/Escape sequences. Phase 4 (responsive collapse) revisitable when 0028 / 0029 push admin tab count past ~3 OR a 7th operational tab lands.
- **2026-05-07** — Scaffolded `0030-nav-bar-improvements/plan.md` and added to Parked. Surfaced when the user shared a screenshot of the staff-app top nav and asked "what do you think" — review flagged four issues (inactive-tab contrast at AA boundary, admin tabs visually identical to operational tabs, email + standalone Sign out button eating ~30% of bar width, no responsive plan). Plan captures all four as five phases: contrast bump (one-line), Radix DropdownMenu user-menu replacing the inline pill (largest chunk; introduces `@radix-ui/react-dropdown-menu` if Open Question 1 resolves that way over reusing Popover), `admin: true`-driven separator between operational and admin tabs (forward-compatible with 0028/0029 admin surface growth), responsive-overflow spike (likely parks until a 7th tab lands), smoke. Active sub-plan stays at 0026 — no focus switch.
- **2026-05-07** — `0014-summary-reports` → Done end-to-end same day; chunk moved to `closed/0014-summary-reports/`. Four phases shipped in sequence: Phase 1 aggregation queries (`3f052aa`), Phase 2 Reports UI with tabbed dealer/coach/month/full-production views (`ebfa63f`), Phase 3 per-tab CSV export + print (`3f4643f`), Phase 4 route-handler test coverage (`155df20`). Active sub-plan slot returns to 0026-quote-pdf; umbrella flips back to 0025-quote-to-payment. The chunk also closed the parked Codex Medium from 0013 Phase 5.6 by extracting `csvCell` / `buildCsv` / `csvResponse` to `src/lib/csv.ts` with a hardened formula-prefix mitigation (`/^\s*[=+\-@]/` — covers leading whitespace including NBSP/BOM bypasses) and retrofitting `production/export` onto the shared helper. Each phase's eval caught Codex findings that were fixed in-eval rather than parked: Phase 2 dropped a dead `coachRowKey` helper, consolidated a duplicated month-formatter into `src/lib/dates.ts`, and (per user request) lifted DataTable sort/page state to `ReportsTabs` so tab switches preserve sort; Phase 3 hardened the CSV regex against whitespace-prefix bypasses and fixed a print-pagination trap by toggling `<DataTable>` to `getFilteredRowModel().rows` on `beforeprint`; Phase 4 added gate-before-loader ordering invariant + rejection-path test to the new route handler. 171/171 vitest, tsc + lint clean throughout. Carry-forwards (none blocking): aggregation tests upgraded to real DB fixture (paired with parked 0023 follow-up); `::int` cast overflow ceiling on `count()`/`sum()` if production totals exceed 2^31; `csvResponse` filename sanitization if helper ever takes user-controlled values.
- **2026-05-07** — Active sub-plan switched: 0026-quote-pdf (Phase 7.1 of 0025) → Parked; 0014-summary-reports (Phase 5.7 of 0004) promoted to Active. User redirected focus mid-session to ship the parked summary-reports work; umbrella also flipped back from `0025-quote-to-payment` to `0004-port-migration` to match the sub-plan's parent. 0014 Phase 1 (aggregation queries) shipped at `3f052aa` immediately before this switch — eval PASS with caveats (1 Codex Medium re test predicate-blindness, 2 Lows on `::int` cast ceiling and nullable `groupKey`; all carry-forward into Phases 2/4). Three remaining phases queued (UI / print + CSV / verification).
- **2026-05-07** — Scaffolded `0029-capability-layer/plan.md` and added to Parked. Surfaced from a "think hard about role access down to action buttons" research pass — synthesised the 2026 PDP/PEP literature (Aserto, Cerbos, Permit.io, AWS) and CASL/OpenFGA patterns into a project-fit shape: stay on RBAC at the floor (right scale for 3 roles), introduce a thin in-process PDP (`capabilities.ts`) so the role↔permission matrix lives in *one* file, and pair it with `assertCan` (server) + `<Can>`/`useCan` (client) as the two PEPs. The user's question was the prompt; the plan is the deferred follow-up to 0028's route-level tightening. Five phases: (1) `can()` + `assertCan` + tests, (2) selective Server Action migration (~10 sites where capability tightens intent), (3) `CapabilityProvider` + `<Can>` wrapper, (4) first UI adoption on row-actions, (5) wiki update + smoke. Open questions called out: capability naming (`subject:verb`), hide vs disable default for `<Can>` (hide), whether `availability-authz.ts` absorbs into `coach-availability:edit-own` (yes). Active sub-plan stays at 0026 — no focus switch.
- **2026-05-07** — `0027-lists-to-dealers` → Done; chunk moved to `closed/0027-lists-to-dealers/`. Five-phase chunk shipped over a single afternoon: Phase 1 chrome simplify (commit `539746f`) — dropped umbrella h1 + subtitle + redundant ListCard wrapper, renamed page header to "Dealers". Phase 2 DataTable + route move (commit `62de77d`) — TanStack DataTable mirroring `/admin/people` shape, plus mid-build route move `/lists` → `/dealerships` (user picked `/dealerships` over the plan's prior working assumption); 4 `revalidatePath` call sites + middleware test updated. Phase 3 DealerForm rewrite (commit `eadff5f`) — replaced bespoke `Field` helper with PersonForm-shape inline labels; deleted dead `list-actions.tsx`; archive-confirm copy fixed in-eval after Codex caught a misleading "contact links stay readable" claim. Phase 4 Radix Form adoption (commit `0564cb2`) — wrapped DealerForm + PersonForm standard text fields in `<Form.Root>` + `<Form.Field>` + `<Form.Message match="typeMismatch">` for email; user picked option (b) post-eval, added a 25-line `useTouched` hook inline in each form for blur-driven required messages (custom controls — Roles checkboxes + Combobox/Select dealer picker — coexist inside Form.Root without wrapping since they have no native `valueMissing` semantics). Phase 5 verified by cross-walk against the four prior phase evals (no separate eval). 150/150 vitest, tsc + lint clean throughout. Codex caught one Medium per phase that was either fixed in-eval (Phase 3 archive copy, Phase 4 blur-driven messages) or deferred to `0028-role-route-scoping` Phase 2 (Phase 2's staff-visible-but-admin-only `✕` button — pre-existing behavior, covered when 0028 page-gates `/dealerships` to admin). Folder cross-references swept: 0028 plan's Open Question 3 now points to `closed/0027-...`. Active sub-plan stays at 0026.
- **2026-05-07** — Scaffolded `0028-role-route-scoping/plan.md` and added to Parked. Surfaced from a discussion of role purpose: the user clarified "production is an admin concern, the coach goes to the store on the sales day." Captured the conceptual matrix in `docs/wiki/auth.md` § "What each role is for" (admin = ops/back-office; coach = field/event-day Calendar-only; dealer = customer-side, no staff access) and flagged the gap between intent and current state. Plan tightens three layers: edge `ADMIN_PATHS` middleware (`src/lib/supabase/middleware.ts:5`), page-level `requireRole('admin')` on `/production` + `/dealerships`, and nav `admin: true` flag on those two tabs (`src/components/app/app-nav.tsx`). Three phases. Has a soft dependency on 0027 Phase 2 (route move `/lists` → `/dealerships`) — plan adapts to whichever folder exists at start time. Open question called out: friendlier redirect target on rejection (today `requireRole` redirects to `/`).
- **2026-05-07** — Scaffolded `0027-lists-to-dealers/plan.md` and added to Parked. Surfaced when the user looked at the `/lists` screenshot and called out that "Manage Lists" is now really just the dealers (companies) — the page collapsed to one section after 0020 retired Sales Coaches, but kept the umbrella h1, the link-to-People-page subtitle, and the redundant `🏢 Dealerships 26` ListCard header. Plan migrates the page to mirror `/admin/people`: rename to "Dealers", drop the dead chrome, rebuild on the TanStack DataTable + Radix Dialog pattern that 0021 + 0024 established. Five phases: rename + chrome simplify, DataTable foundation, DealerForm rewrite to PersonForm shape, optional Radix Form adoption (carry-forward from 0024 Phase 4 — only run here if PersonForm migrates in the same pass), tests + smoke. Open questions: route move `/lists` → `/dealers` (working assumption: keep `/lists`); `loadDealers` co-location move from `schedule/` → `dealers/` (working assumption: re-export only, defer the move). Active sub-plan stays at 0026 (Phase 7.1 Quote PDF) — no focus switch without explicit user signal.
- **2026-05-07** — Promoted Phase 7 (Q→C→I→P loop) out of `0004-port-migration` into its own epic at `0025-quote-to-payment/`, and scaffolded sub-plan 7.1 at `0026-quote-pdf/`. Active umbrella switched from `0004` to `0025`; `0004` stays alive as a separate tracker for the remaining 5.7 + 5.8 parked work. Same edit dropped Phase 6 (domain cutover) and Phase 8 (legacy-secret rotation) from `0004` — both now tracked outside the port-migration umbrella. 7.1's frame: PDF templates for branded business documents stored in Google Cloud Storage, filled at runtime via `pdf-lib` (reconsidering the `@react-pdf/renderer` choice from `architecture.md`). Phase 1 of 0026 lays the GCS adapter + `pdf-lib` template loader as foundation for 7.2 (MSA) and 7.3 (invoice PDF) to reuse. Open questions: GCS vs. Supabase Storage for blobs; AcroForm-fill vs. coordinate-overlay; jsonb vs. normalized line items; cascade vs. manual gate from accept → contract send.
- **2026-05-07** — `0024-personform-radix-migration` → Done; sub-plan moved to `closed/0024-personform-radix-migration/`. Five-phase chunk shipped over an evening + the next morning: Phase 1 swapped the `src/components/ui/dialog.tsx` wrapper from `@headlessui/react` to `@radix-ui/react-dialog` (commit `891d569`) — Codex caught a real focus-restore High inline (no `Dialog.Trigger`, so Radix's auto-restore had no target); fixed via a `FocusContext` ref captured on open + restored in `Panel.onCloseAutoFocus`. Phase 2 replaced the three native role checkboxes with `<Checkbox.Root>` (commit `529be61`) — clean PASS, 10 Codex non-issues confirmed; the explicit hidden `<input name="roles">` rows that 0023 Phase 3 added became redundant since Radix Checkbox emits its own. Phase 3 introduced `src/components/ui/combobox.tsx` (cmdk + Radix Popover) for the dealer picker + Radix Select for the dealer-contact role (commit `3f07ddc`) — Codex caught a Medium inline: `Command.Item value={o.label}` would have made keyboard Arrow+Enter ambiguous on duplicate dealer names; fixed with `value={o.value}` (dealer ID) + `keywords={[o.label]}` for filter matching. Phase 4 deferred per the plan's decision tree (keep `useActionState` + toast; revisit when the `0016` booking-intake form lands so both forms migrate to Radix Form in one pass). Phase 5 ripped out `@headlessui/react` (commit `cb5da66`) — `grep -rn "@headlessui" src/` returns empty; both dialog call sites smoke-confirmed post-removal. Three in-eval Codex catches landed across the chunk; 149/149 vitest, tsc + lint clean throughout. The largest UX win is the typeable dealer-picker Combobox replacing the native `<select>` for the 50+-dealer list.
- **2026-05-06** — Picked `0024-personform-radix-migration` off Parked → in flight as Active sub-plan, immediately after 0023 closed. Five-phase chunk: dialog wrapper swap (Headless UI → Radix), Roles fieldset → Radix Checkbox, Dealers section → cmdk Combobox + Radix Select, optional Radix Form (deferred), tests + smoke. Surfaced after the user noticed the codebase was already on `@headlessui/react` and asked whether PersonForm could migrate to Radix. Going with the plan's working assumptions on all open questions: rip out HUI in Phase 5, Checkbox not RadioGroup, defer Radix Form. Largest UX win is the typeable dealer-picker Combobox (50+ dealers).
- **2026-05-06** — `0023-people-dealer-role` → Done; sub-plan moved to `closed/0023-people-dealer-role/`. Six-phase chunk shipped over a single afternoon: Phase 1 enum + parser whitelist (`drizzle/0005_team_member_role_add_dealer.sql` + `0006_is_staff_member_excludes_dealer.sql`; commit `d2a1344`) — Codex caught a real authz hole during eval (dealer aliasing to staff); fixed in-eval with `STAFF_APP_ROLES` whitelist. Phase 3 picked up before Phase 2 (commit `cdf554b`) to avoid the backfill-then-stale-UI race; the form-precondition discovery was that `4a4afbd refactor(forms)` had silently broken role-submission for hours. Phase 2 backfill (commit `c8c586e`) — 23 dealer-side contacts, zero truly-orphan. Phase 4 auto-assign (commit `ea3f522`) — Codex caught a re-grant-after-archive bug, fixed in-eval with `onConflictDoUpdate` un-archive. Phase 5 app-level invariant (commit `3056603`) — chose app-level over DB trigger for simpler rollout + cleaner carve-outs. Phase 6 verification (commit `3bfc37a`) — backfilled contacts render correctly in /admin/people; edit-dialog open hit a strict-mode collision (23+ Edit buttons), verified via snapshot + code trace instead. Six dated eval reports captured. 149/149 vitest, tsc + lint clean. Carry-forwards listed under Parked: dealer-role grant audit emission, production data triage, hybrid `dealer+admin/coach` confirm, schedule-actions vitest fixture, real form-submission test.
- **2026-05-06** — Picked `0023-people-dealer-role` off Parked → in flight as Active sub-plan, immediately after 0019 closed. Six-phase chunk: enum + parser whitelist for `dealer`, backfill of existing roleless dealer-side contacts, PersonForm Dealer checkbox + section gate + require ≥1 role guard, `createDealer`/`updateDealer` auto-assigning the role, DB-or-app-level "every contact has a role" invariant, integration tests + smoke. Surfaced when the user noticed dealer staff exist as roleless contacts and asked to gate the Dealers section on a new role. Migration numbering needs bumping (plan assumes `0004_*.sql` but `0004_worthless_titanium_man.sql` already shipped under 0019 — actual filename will be `0005_*`).
- **2026-05-06** — `0019-security-architecture` → Done; sub-plan moved to `closed/0019-security-architecture/`. Eight-phase chunk shipped over a single day: Phase 1 RLS baseline (`drizzle/0003_enable_rls.sql` — 11 tables, 22 policies, helper `is_staff_member()`; commit `ae3dbe3`), Phase 2 `requireRole(role|role[])` per-action audit retiring `requireUserId`/`requireAdmin` async wrapper (commit `04ad552`), Phase 3 coach availability row-level ownership check with TOCTOU-hardened WHERE (commit `278c429`), Phase 4 `audit_log` + `recordAudit()` best-effort helper wired into 6 emit points (commit `7a187cd`), Phase 7 email-send hardening: SITE_URL-only origin + booked-only status gate + inverted dev-redirect with case-normalised APP_ENV (commit `e7c497a`), Phase 8 wiki updates: rewrote `auth.md` RBAC section + new `Defence in depth` section + per-action gate matrix; updated `conventions.md` Drizzle/`supabase-js` section with the BYPASSRLS posture; new `docs/wiki/security.md` is the five-layer defence-in-depth map + threat models + grep cheat-sheet (commit `854dc8a`). Phases 5 (boundary-discipline `secrets-boundary.test.ts`) and 6 (MFA enablement) left parked with documented rationale. Each phase paired Codex adversarial review + in-eval inline fixes (Codex highs/mediums fixed before commit on every phase). Six dated eval reports captured in the chunk folder. 144/144 vitest, tsc + lint clean. Manual security walk-through verified RLS enforcement (3 tables × forged authenticated JWT → 0 rows) + Server Action gates (3 admin-required actions → redirect for non-admin via `require-role.test.ts` 8-case suite). Carry-forwards listed under Parked.
- **2026-05-06** — Scaffolded `0024-personform-radix-migration/plan.md` and added to Parked. Surfaced after the user noticed the codebase is already on `@headlessui/react` (not raw HTML) and asked whether the PersonForm could migrate to Radix. Plan frames it as a Headless-UI → Radix swap with `cmdk` Combobox for the dealer picker as the largest UX win. Five phases (dialog wrapper swap, Roles fieldset, Dealers section, optional Radix Form for inline validation, smoke verification). Strong sequencing preference: 0023 ships first so the Dealer checkbox lands before the Radix migration absorbs all three role checkboxes in one pass. Open questions: HUI rip-out vs. coexistence; Checkbox vs. RadioGroup (tied to 0023's mutual-exclusion question); Radix Form adopt vs. defer; future form-state library choice.
- **2026-05-06** — Scaffolded `0023-people-dealer-role/plan.md` and added to Parked (kept 0019-security-architecture as Active sub-plan; its Phase 1 shipped earlier today, remaining 5 phases in flight). Surfaced when the user noticed dealer staff exist as roleless contacts and asked to gate the Dealers section in the Person edit dialog on a new `dealer` role. Six phases: enum + parser whitelist, backfill of existing roleless dealer contacts, PersonForm Dealer checkbox + section gate + require-role guard, `createDealer`/`updateDealer` auto-assigning the role, DB-or-app-level "every contact has a role" invariant, integration tests + smoke. Open questions (mutual exclusion with admin/coach; whether `customer` is a sibling addition; trigger vs. app-level for the invariant; App-access semantics for dealer-only) called out in the plan.
- **2026-05-06** — Picked `0019-security-architecture` off Parked → in flight as Active sub-plan. Unblocked since `0018-user-system` shipped 2026-05-05 (role taxonomy + `requireAdmin` foundations). Phase 4 of the plan (boundary-discipline checks / secrets-in-bundle smoke test) was parked earlier today as low-marginal-value vs. cost; remaining 6 phases active.
- **2026-05-06** — `0022-saledayevents-logo-refresh` → Done; sub-plan moved to `closed/0022-saledayevents-logo-refresh/`. Three-phase chunk: dropped the new mark (`TILL SDE logo colour RGB-100.jpg`, 246×155) into `public/saledayevents-logo.jpg`, renamed off the old `saleday-logo.jpg`, swapped the three call sites (app-header, login, share-coach), and `git rm`'d the old asset. Browser smoke confirmed the new mark renders on `/login`, `/calendar`, and `/share/coach/1`. Pre-commit eval (eval-2026-05-05-1908.md) PASS — tests 100/100, tsc + lint clean (4 pre-existing warnings), Codex 0 findings. Bundled into commit `91fd228` as a single rename + swap. Active sub-plan slot vacated; the staff-app form-migration to React 19 primitives that ran in parallel landed as `4a4afbd refactor(forms)` outside any plan slot.
- **2026-05-05** — `0021-people-polish` → Done; sub-plan moved to `closed/0021-people-polish/`. Despite the mid-day pre-emption by 0022, the user picked up 0021 anyway and ran all five phases end-to-end. End state: `/admin/people` adopts TanStack Table (sortable headers + pagination + faceted filters) as the admin-table foundation; `firstName`/`lastName` surfaced on `AdminPersonRow` (no more `displayName.split` heuristic); page subtitle + "Team & contacts" subhead dropped; the `App access` checkbox dropped in favour of *implicit* sign-in (Admin/Coach role implies app access — convention now: "everyone who needs APP gets it by default, except dealer staff"); the previously-planned `app` chip reversed in the same review pass; `updatePerson` now lifts any active ban on Save when granting/maintaining roles (closes the in-app restore loop after Adam Godin got accidentally banned during the session); pre-submit confirm guards the role-removal-bans-auth-user transition; per-row archive confirm composed from facets (`drop coach role, end relationship with X (customer), and ban sign-in for …`); search box + Coach/Admin/Customer-side pills land on the table. Codex 1 Low (search-input aria-label) fixed in-eval. Two carry-forward items deferred for the future Production+Lookups port (keyboard-accessible sortable headers; Last-sign-in column visibility on filtered rows). Commits: `d0c6f04` → `cc480d5` → `aab1755` → `646e16a` → `0b0bb4b` → `6b4e96a`.
- **2026-05-05** — Scaffolded `0022-saledayevents-logo-refresh/plan.md` and picked it up as the active sub-plan. Pre-empts `0021-people-polish` (scaffolded earlier the same day, phases not yet started) — moved to Parked. Surfaced when the user dropped a fresh `TILL SDE logo colour RGB-100.jpg` (246×155, same dims as existing `public/saleday-logo.jpg`) and asked to rename the public asset off `saleday-logo.jpg` to match the new wordmark. Three call sites identified: `src/components/app/app-header.tsx:18`, `src/app/login/page.tsx:25`, `src/app/share/coach/[id]/page.tsx:44`. favicon + `<title>` metadata explicitly out of scope.
- **2026-05-05** — Scaffolded `closed/0021-people-polish/plan.md` and picked it up as the active sub-plan. Five phases: TanStack Table foundation on `/admin/people` (sortable headers + pagination + global/faceted filters), then the 0020 carry-forward UX polish (drop "Team & contacts" subhead, drop the four-sentence page subtitle, `app` chip, raw firstName/lastName from the query, per-row archive-confirm preview, confirm-on-disable-app-access, auto-uncheck role boxes when App access flips off, `Last sign-in` column visibility tied to population). User reversed my "stay native" recommendation by calling out muscle memory for grid behaviors and the desire to apply the same pattern to Production + Lookups in future polish chunks. User also called out the page subtitle as documentation-in-chrome that doesn't earn its place on every visit; folded into Phase 2 alongside the card subhead drop.
- **2026-05-05** — `0020-people-unification` → Done; sub-plan moved to `closed/0020-people-unification/`. End-to-end: `/admin/people` is the single entry point for managing humans (Add/Edit/Archive Person + adopt orphan auth users); `/admin/users` redirects there; Sales Coaches section gone from `/lists`; legacy Server Actions deleted (`createUser`, `linkUserToContact`, `setUserRoles`, `deactivateUser`, `createCoach`, `updateCoach`, `archiveCoach`); auth.users abstracted as a *facet* of a person via the App access toggle. Codex flagged blocker pairs each phase (TOCTOU + roles=[] coercion in Phase 2; lifecycle helper + partial-success contract in Phase 3; UUID validation + atomic per-orphan tx in Phase 4); all six fixed pre-commit. Final eval PASS with caveats (Phase 5 was docs-only; Codex + smoke skipped). Carry-forward Phase 3 UI follow-ups remain (auto-uncheck role boxes on App-access flip, confirm dialog on disable, raw firstName/lastName from query). Commits: `82e3564` → `1053257` → `33bfc51` → `3dc6ec0` → `a5c0ab0`.
- **2026-05-05** — Scaffolded `0020-people-unification/plan.md` and picked it up as the active sub-plan. Folds `/admin/users` + Manage Lists Sales Coaches into a single `/admin/people` flow; the data model already supports the unification (one `contacts` table, role facets) but the UI fragments the human across three pages. Surfaced while walking through the Tilley Shaye provisioning UX with the user — "yuck, the supabase user should be abstracted away."
- **2026-05-05** — `0018-user-system` → Done; sub-plan moved to `closed/0018-user-system/`. End-to-end: in-app `/admin/users` provisioning with three-tab Add User dialog (Create new / Pick existing / No link); `linkUserToContact` Server Action with TOCTOU-safe conditional UPDATE; auto-link trigger on `auth.users` insert; coach auto-filter on `/calendar` for signed-in coaches; role-aware `/auth/callback` routing PLUS a durable `requireStaffAccess()` gate in `(app)/layout.tsx` and `/production/export` that re-runs the same decision tree on every request. Two follow-up commits (`b4e3b6a`, `9231bd8`) closed Codex Critical + High + a new Critical (Route Handler bypass on `/production/export`); final eval PASS with warnings (81/81 tests, tsc + lint clean). 0017-user-admin moved to `closed/` as superseded. Two known follow-ups remain (Medium: `createUser` partial-success; Low: test-mock predicate-blindness) — not blockers.
- **2026-05-05** — Scaffolded `0019-security-architecture/plan.md` (RLS defence-in-depth + sensitive-op audit log + per-action role audit + email-send hardening + MFA enablement). Parked behind 0018 since it depends on the role taxonomy + `requireRole` helper that 0018 establishes. Surfaced after a discussion of the Drizzle-direct-vs-PostgREST trade-off — the staff app's drift from the original "Drizzle owns SQL, supabase-js owns RLS-bound reads" intent (`0002-nextjs-scaffold/decision.md:10`) is now an explicit re-alignment plan.
- **2026-05-05** — Scaffolded `0018-user-system/plan.md` and picked it up as the active sub-plan. Subsumes the parked `0017-user-admin` (which was unstarted) and extends to RBAC enforcement, contact-user linkage, coach auto-filter, role-aware login routing. Surfaced while provisioning Tilley Shaye and discussing the legacy `Users!A:E` parity gap. 0017 stays in Parked as `subsumed`; will be moved to `closed/` with a supersession note when 0018's Phase 6 ships.
- **2026-05-04** — `0015-calendar-grid-clamp` → Done; sub-plan moved to `closed/0015-calendar-grid-clamp/`. Eval-smoke PASS with warnings on 2026-05-04: 44/44 tests, tsc clean, lint clean (4 pre-existing warnings); browser smoke green across both phases plus cross-month visual smoke run on dev DB (throwaway fixture inserted via `scripts/calendar-clamp-smoke.ts`, both leading and trailing clamped ribbons rendered correctly across April/May/June 2026, cleanup verified). Codex Low (test gap for prior-month-strip-only and next-month-strip-only overlaps) closed in the same commit. Throwaway smoke script deleted with ship. Active sub-plan slot vacated.
- **2026-05-03** — Picked `0015-calendar-grid-clamp` off Parked → in flight. Code + unit tests landed in working tree; visual smoke pending (needs a cross-month-boundary fixture in Supabase, deferred until user confirms it's OK to insert + remove).
- **2026-05-03** — Scaffolded `0017-user-admin/plan.md` for the in-app User Admin page (replaces dashboard-only provisioning). Parked. Surfaced while manually provisioning Shannon — clear UX win for adding the rest of the team.
- **2026-05-03** — Scaffolded `0016-book-your-event-intake/plan.md` for the public-intake + marketing-site cutover work. Parked outside the port-migration umbrella (new entry-point work, not a 5.x phase).
- **2026-05-03** — Deployed HEAD `5825971` to Cloud Run revision `event-manager-pro-00003-drf` (100% traffic). URL: https://event-manager-pro-248904507231.northamerica-northeast1.run.app. Bundle includes Phase 5.6 export+print, SaleDay logo + favicon (RGBA-fixed), Drizzle HMR cache, docs/ tracking change.
- **2026-05-03** — Phase 5.6 (Production export + print) → Done in commit `2a42c93`; pushed to `origin/main`; sub-plan moved to `closed/0013-production-export/`. Eval-smoke PASS with warnings (1 medium CSV-injection + 3 low — captured in eval-2026-05-03-1024.md, parked for follow-up). Active sub-plan slot vacated; queued options remain 5.7 / 5.8 / slot-pack clamp.
- **2026-05-03** — Phase 5.6 (Production export + print) picked from Parked → in flight at `0013-production-export/plan.md`.
- **2026-05-01** — Phase 5.5 (Email send, Resend) → Done in commit `f963da7`; sub-plan moved to `closed/0011-email-send/`. Removed from Parked. Active sub-plan slot remains vacated; queued options now 5.6 / 5.7 / 5.8 / slot-pack clamp.
- **2026-05-01** — Phases 5.3 (Lookup admin) and 5.4 (Availability admin) → Done in commit `023ee01`; both sub-plans moved to `closed/`. Removed from Parked. Active sub-plan slot remains vacated; queued options updated.
- **2026-05-01** — Phase 5.2 (Campaign CRUD) → Done; sub-plan moved to `closed/0008-campaign-crud/`. Active sub-plan slot vacated; queued options listed under Parked.
