# Active chunk

**Plan:** [`docs/chunks/0102-pr-based-deploy-workflow/plan.md`](0102-pr-based-deploy-workflow/plan.md)
**Intent:** [`docs/chunks/0102-pr-based-deploy-workflow/intent.md`](0102-pr-based-deploy-workflow/intent.md)

**Started:** 2026-07-08
**Status:** Scaffolded — phases not yet started

Move from main-trunk (commit to `main`, backport to `dev` for stage) to the conventional **dev-trunk / promote-to-prod** flow: feature branch → **PR into `dev`** (merge → stage build) → verify → **PR `dev`→`main`** (merge → prod build). The two Cloud Build triggers already map correctly (`dev`→stage, `main`→prod) and a PR-merge *is* a push — so this is **guardrails + process, not a trigger rebuild**: branch protection on `main` (+ `dev`), a required PR-time test gate (blocks a red *merge*, not just the deploy), a promotion runbook with the **prod-migration gate** (CI never runs migrations — the sharpest risk) + a hotfix path, and a one-time reconcile of `origin/main` (+28) / `origin/dev` to a known baseline. **No app code / migration / secret.** 5 phases; heavily **owner-driven** (GitHub branch protection + GCP trigger settings live in consoles). Non-goal: changing the trigger→branch mapping itself.

**Deployed:** prod = `event-manager-pro-00048-wdp` (2026-07-08 — through 0099 / 0100 / 0101 + calendar UI fixes; migration `0048` applied to the prod DB + trigger `_MSA_TEMPLATE_VERSION=2026-07-07`). stage = latest `dev` push.

---

**Parked:** _(recent follow-up queue. The older long-tail backlog — QBO / MSA / security follow-ups from 0026–0085 — is archived in [`shipped-log.md`](shipped-log.md); pull an item back up here when a chunk revisits that area.)_
- **0100-a** (follow-up, Codex Medium) — the `acceptQuote` MSA/waiver gate (`isAcceptMsaSatisfied`) runs **outside** `markQuoteAccepted`'s atomic `sent → accepted` UPDATE, so a concurrent `setMsaWaived(false)` can race an in-flight accept: the gate reads waived=true and passes, the un-waive commits, then the accept still lands (the atomic UPDATE guards only status + expiry, not the MSA/waiver). **Pre-existing shape of the 0082 accept gate** — the MSA check was always non-atomic w.r.t. the accept (a concurrent MSA termination raced it identically); 0100 only adds the waiver as another racer, and it needs two admins on the same event within a sub-ms window. Fix shape: fold the MSA/waiver predicate into `markQuoteAccepted`'s atomic UPDATE (touches the 0082 gate broadly). `Un-park trigger:` an accept-gate atomicity/hardening pass. See [`closed/0100-…/eval-2026-07-07-1841.md`](closed/0100-msa-optional-per-event/eval-2026-07-07-1841.md).
- **0099-a** (follow-up, Codex Medium) — a primary contact with a **blank `contacts.last_name`** (the Atlantic import allows single-token names → `lastName: ''`) makes `sendMsaEnvelope` fall back to a **first-name-only** BoldSign `signer.name`, so the adopted-signature *default* isn't a full name. **Mitigated by design** — the signer-filled `ClientPrintedName` TextBox is the actual legal safeguard, and BoldSign lets the signer adopt a full-name signature regardless of the pre-fill — so this is a nicety, not a hole. Not mechanically fixable (can't invent a surname). `Un-park trigger:` an MSA is sent to a blank-last-name primary contact and the owner wants the pre-fill to prompt for a full name. See [`closed/0099-…/eval-2026-07-07-1531.md`](closed/0099-msa-signature-legal/eval-2026-07-07-1531.md).
- **0097 — go-live the feed** ✅ **DONE 2026-07-07.** Steps 1+2 done by Claude (secret `production-feed-token` v1 created in `eventpro-498313`, compute SA granted `secretAccessor`, mount uncommented in `cloudbuild.deploy.yaml` `b4f7f01`) + deployed via the keyless main→prod trigger (rev `event-manager-pro-00046-mrm`). **Live-verified:** no-token → 401 (was 500), valid-token → 200 CSV (header + rows, PII-clean). Remaining owner-side: create + share the Google Sheet — now via the **IMPORTRANGE indirection** (private source Sheet holds the `=IMPORTDATA(…token…)`; implementers pull the shared Sheet via `=IMPORTRANGE(<key>, "Sheet1!A:J")` so the token stays hidden) + format cols A:B as Date. Live 200-CSV confirmed; authed `/production` panel eyeball still owner-side.
- **0097-a** (follow-up, Codex Medium) — the feed's `todayIso()` uses the **host process timezone**, so a campaign ending "today" (Atlantic) can drop from the feed after 00:00 UTC but before local midnight. Pre-existing **app-wide** pattern (`production/filter.ts`, CSV export, calendar all do this — `/production` and the feed agree), so fixing only the feed would desync it. `Un-park trigger:` an app-wide business-timezone pass. See [`closed/0097-…/eval-2026-07-06-2038.md`](closed/0097-production-sheet-feed/eval-2026-07-06-2038.md).
- **0096-a — authed `/production` render eyeball** (owner-verify) — **✅ deployed to prod 2026-07-06** (rev `event-manager-pro-00044-qfj`, us-east4, with 0094). The pre-deploy browser smoke couldn't reach the authed table (sandbox Supabase paused), so the render is unconfirmed. Owner: load `/production` on prod and confirm a **Date** column (no **Status**), a search box + **Show cancelled** checkbox (no time-window dropdown), and that clicking the **Date** header re-orders rows. `Un-park trigger:` owner walks the prod `/production` list. See [`closed/0096-…/eval-2026-07-06-1600.md`](closed/0096-production-date-column/eval-2026-07-06-1600.md).
- **0094 — SME click-through verify** (owner-verify) — **✅ deployed to prod 2026-07-06** (rev `event-manager-pro-00044-qfj`, us-east4, with 0096). **Backfill ran read-only vs prod → 0 campaigns with an accepted quote** (no-op; nothing to `--write`, so existing prod campaigns keep their legacy-imported metrics and there's no blank-metrics regression). Remaining owner step: SME click-through — Book Event dialog shows **no** volume fields, and once a real quote is accepted the event-detail/production numbers derive from it. `Un-park trigger:` owner walks a booked-event → quote-accept flow on prod.
- **0094-b** (follow-up, Codex Medium — adversarial) — a pathological quote (≥5 `base-event` lines at qty 1M) derives `qty_records` beyond Postgres int4; the campaign UPDATE then fails *after* the quote already flipped accepted. Adversarial-only (the composer never generates this) + loud-failing. `Un-park trigger:` a quote-input hardening pass. Fix shape: cap/validate derived values or bound line count. See [`closed/0094-…/eval-2026-07-06-1503.md`](closed/0094-decouple-booking-metrics/eval-2026-07-06-1503.md).
- **0094-c** (follow-up, root of the eval's cross-dealer High) — `setQuoteDealer` swaps a draft quote's `dealerId` without reconciling its `campaignId`, so a quote can point at another dealer's event. 0094 guards the *write* (dealer-scoped UPDATE) so it can't corrupt, but the stale link remains. `Un-park trigger:` a quote-composer dealer/event reconciliation pass. Fix shape: clear or re-validate `campaignId` on dealer-swap. See [`closed/0094-…/decision.md`](closed/0094-decouple-booking-metrics/decision.md) D2.
- **0095-a** (owner follow-up) — the `Salesability` GitHub org owns the repo but Shannon (`EventPro2026`) is only a **member**; `dwhogan` is the sole owner. Promote her to **Owner** (github.com/orgs/Salesability/people → EventPro2026 → Change role → Owner) to complete the "business owns it" governance. Optional cleanups: delete the stale `github-eventpro` connection (installation `52644501`, dwhogan-personal, unused) in `eventpro-498313`; add a Cloud Build **included/ignored file filter** to `deploy-prod-on-main` if docs-only prod deploys get noisy. `Un-park trigger:` owner does the promotion. See [`closed/0095-keyless-ci-deploy/plan.md`](closed/0095-keyless-ci-deploy/plan.md).
- **0092-a** (owner-verify + live risk) — [`closed/0092-boldsign-sender-onbehalfof`](closed/0092-boldsign-sender-onbehalfof/plan.md) Phase 4. Prod now sends MSAs **from Shannon** (her own Live key v4, rev `-00038-w2z`). **Verify:** send a test MSA → envelope reads *from Shannon* AND a real sign → webhook `download` returns 200 → MSA flips `active`. ⚠️ The in-flight **Summerside Hyundai** MSA is still `pending` under David's *old* v3 key — if the dealer signs it now (prod is on Shannon's v4), her key may **403** on the webhook download → that one MSA won't auto-activate/archive. `Un-park trigger:` owner runs the test MSA (and voids + re-sends Summerside under Shannon's key, or accepts a manual archive for it). See plan Phase 4 + [go-live-accounts.md](../wiki/go-live-accounts.md) §3.
- **0093-a** (owner-verify) — [`closed/0093-calendar-quote-msa-status`](closed/0093-calendar-quote-msa-status/plan.md). SME click-through of the event-detail commercial surface (quote/MSA badges + the needs-attention ribbon marker + create-quote / send-MSA CTAs) — the imperative-DOM canvas ribbons aren't exercisable by the read-only browser smoke. `Un-park trigger:` owner walks a booked-event → quote → MSA surface on prod.
- **0091-a** (follow-up, no plan yet) — archive the stale shared-rooftop `dealer_contacts` links 0091's D6 "keep" deliberately left behind (e.g. Andres Monterrosa kept on Mercedes-Benz though he's Acura's GM; Kirt Macdonald on 2 Steele rooftops; Neal Noseworthy on Fairley & Stevens). Archive the **link**, not the shared `contacts` row. `Un-park trigger:` owner wants a dealer-contact freshness pass. See [`closed/0091-…/decision.md`](closed/0091-atlantic-dealer-contact-refresh/decision.md) D6/D8.
- **0086-a** (follow-up) — duplicate **dealer** records (Parkway Hyundai, Sydney Mazda — two rows each); no DB unique on `dealers(name, address)`. Surfaced by 0091 (one Parkway has no `quickbooks_id`; the dup Sydney Customers tripped a QBO 6240). `Un-park trigger:` dealer-dedup pass.
- **0089-a** (follow-up, pre-existing) — `createQuote` / `setQuoteInputs` in `src/features/quotes/actions.ts` don't run a zod `safeParse` (the `safeparse-required` lint rule flags both). Surfaced by 0089's eval (those functions predate the chunk, untouched). `Un-park trigger:` quote-input validation hardening pass. See [`closed/0089-…/eval-2026-06-23-1717.md`](closed/0089-dealer-contact-roles/eval-2026-06-23-1717.md).
- **0088-a** (follow-up, Codex Low) — the dashboard's 30-day `dealer_activities` window read (`gte(occurred_at, since)`) has no leading-`occurred_at` index (only `(created_by_id, occurred_at)` exists), so it can degrade to a scan as activity volume grows. `Un-park trigger:` activity volume grows / a dealer-pipeline perf pass (needs a migration). See [`closed/0088-…/eval-2026-07-06-1222.md`](closed/0088-dealer-pipeline-dashboard/eval-2026-07-06-1222.md).

**Future:**
- **[`future/0079-quote-document-library/`](future/0079-quote-document-library/plan.md)** — the reusable "from the system" document library (admin-managed library + send-dialog picker), additive on top of 0078's attachment spine. ✅ **0078 shipped 2026-06-12** (the spine is built — a nullable `document_id` FK on `quote_attachments` is 0079's additive migration). **Un-defer trigger (remaining):** the owner makes the two product calls — banking-info delivery (attachment vs short-lived signed link) + library scope (global vs per-client).
- **[`future/0058-production-sheet-and-date-range/`](future/0058-production-sheet-and-date-range/plan.md)** — the shareable Google Sheet (Phases 2–4). Phase 1 (1/2/3-month date-range filter) already shipped to `main` (`9b15e29`); only the Sheet work is deferred. **Un-defer trigger:** owner is ready to set up Google Cloud (enable Sheets/Drive API + provision credentials) and has chosen the auth approach (service account vs user OAuth).
- [`future/0016-book-your-event-intake/plan.md`](future/0016-book-your-event-intake/plan.md) — public-form + Squarespace-cutover work, deferred to v2 on 2026-05-11. v1 funnel uses manual in-app intake (existing `+ Book Event` modal + 0035 P2 inline-create dealer). Un-defer trigger: prospect volume justifies replacing manual-entry-from-Shannon, OR marketing-site CTA is ready to point at the app. See `docs/chunks/future/` and CLAUDE.md `### Deferring a chunk`.

---

_**History moved out (2026-07-08).** Per-chunk narratives, the full parked backlog (older long-tail follow-ups), the deploy baseline, and the pointer-transition log now live in [`shipped-log.md`](shipped-log.md). Pointer transitions are also in `git log -- docs/chunks/CURRENT.md`; per-chunk detail in `docs/chunks/closed/NNNN-slug/`. CURRENT.md is now just the active pointer + the recent parked/future queue._
