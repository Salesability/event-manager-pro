# Eval: 0035-quote-composer (Phase 1, pass 2)

**Plan:** docs/chunks/0035-quote-composer/plan.md
**Branch:** main
**Date:** 2026-05-11

---

## Verdict: PASS

All four pass-1 Mediums verified closed by Codex pass-2 with no new findings. Static checks green, browser smoke green (verified pass-1), Codex clean.

**Static checks:** tests 568/568 PASS (+1 skipped) · tsc PASS · lint PASS (4 pre-existing warnings carry over from unrelated files)
**Browser smoke:** carried from pass-1 (Phase 1 3/3 PASS · Phase 2 11/11 PASS — UI did not change between passes)
**Codex review:** 0 Critical, 0 High, 0 Medium, 0 Low — all pass-1 Mediums verified closed

---

## Static Checks

All passed. Test count went 542 → 568 (5 new service-item action cases + 21 new action-gate-matrix cases for the three new service actions × 7 roles).

---

## Browser Smoke

UI unchanged between passes — Phase 2 fixes were action-layer only (validation regex, un-archive path, action-gate-matrix wiring). The pass-1 smoke (`eval-2026-05-11-1943.md`) covers the user-visible surface for this phase.

---

## Codex Adversarial Review (pass 2)

### Critical / High / Medium / Low
None.

### Fixes verified clean

- **Medium #1 (archived codes blocked recreation):** `createServiceItem` un-archives by `code` + `archivedAt IS NOT NULL`, sets `{ ...fields, archivedAt: null }`, returns before insert when restored. Falls through to insert when no archived row exists. Concurrent races resolve as either one restore/insert winner or the friendly duplicate-code error — no row corruption. Keying on `code` only is appropriate because the catalog is admin-scoped (`lookup:edit`).
- **Medium #2 (money parsing IEEE-754):** `MONEY_RE` correctly rejects `01.50` (leading zero), `5.` (empty fraction), `.50` (no whole), negatives, and over-precision like `2.675`. Accepts `0`, `0.50`, and normal values. Normalization is string-only — never goes through `Number`.
- **Medium #3 (overflow caps):** `MONEY_RE`'s 8-whole-digit cap matches `numeric(10,2)`'s max of $99,999,999.99. `MAX_PG_INTEGER = 2,147,483,647` is correct for Postgres `integer`. Negatives still rejected.
- **Medium #4 (action-gate-matrix):** Three new rows use `ADMIN_ONLY` — same pattern as the existing lookup-admin actions. Harness iterates all 7 roles.

### Deferred Lows (still reasonable)

- **Low #1 — substring-based duplicate detection:** still deferred. Follows the existing sibling-lookup precedent (`schedule/actions.ts:408`).
- **Low #2 — DB CHECK for range/non-range invariant:** still deferred. Server Action canonicalizes wrong-side fields; a CHECK would require an extra migration with no immediate threat model.

---

## Cross-Reference

None.

---

## Action Required

None. Ready to commit.

---

## Coverage Caveats

- Browser smoke carried from pass-1 — UI did not change between passes, so re-running would be a no-op.
- Two pass-1 Lows remain deferred with documented rationale (repo-pattern consistency + no immediate threat).
