# Eval: 0035-quote-composer Phase 5

**Plan:** `docs/chunks/0035-quote-composer/plan.md`
**Branch:** main
**Date:** 2026-05-12 08:41

---

## Verdict: PASS

Phase 5 wiki bookkeeping pass. No code changes — `service_items` + `dealers.status`/`acquired_via` documented in `data-model.md`, new "Quote composer" subsection in `architecture.md`, log entry recorded, plan tracker flipped to Done. Three Codex review passes self-resolved 3 High + 4 Medium + 1 Low factual inaccuracies in the wiki text without invoking the write-capable rescue loop (surgical text edits from each Codex pass's citations).

**Static checks:** 3/3 PASS — pnpm test 655 passed | 1 skipped; tsc clean; lint 4 pre-existing warnings, 0 errors.
**Browser smoke:** 6/6 PASS — Phase 1 public (`/`, `/login`) + Phase 2 gated plan-specific (`/admin/lookups`, `/dealerships`, `/quotes/new`). Screenshot: `/tmp/web-test-0035-p5-composer.png`.
**Codex review:** 3 review passes, all residuals cleared.

---

## Static Checks

```
$ pnpm test
Test Files  28 passed (28)
     Tests  655 passed | 1 skipped (656)
  Duration  3.30s

$ pnpm tsc --noEmit
(clean, no output)

$ pnpm lint
4 warnings, 0 errors — all pre-existing in calendar-view.tsx (1) and production-filters.tsx (3), not introduced by this diff.
```

---

## Browser Smoke

### Phase 1 — Public surface

| Check | Result |
|-------|--------|
| `goto /login` → "Continue with Google" + email textbox + magic-link button | PASS |
| `goto /` → redirects to `/login?next=%2F` (status 200) | PASS |

### Phase 2 — Auth-injected gated surface (plan-specific)

`inject-supabase david.hogan@networknode.ca`: success, cookies set.

| Check | Result |
|-------|--------|
| `/admin/lookups` heading "Lookup Admin" + Services section heading + 8 v1 service items (Base Event, Additional Contact, BDC Call, Letter/Postage, Digital, Additional Day, Record Retrieval, Travel) | PASS |
| `/dealerships` heading + filter pills `Active (26) / Prospect (0) / Archived (0)` + per-row Quote → `/quotes/new?dealerId=<id>` links | PASS |
| `/quotes/new` heading "New Quote" + dealer Combobox + inputs panel (audience / event days / BDC / letters / digital / record-retrieval bracket / travel + notes fields) + computed line-items table + Subtotal / Tax override / Total + Save Draft button | PASS |

Screenshot: `/tmp/web-test-0035-p5-composer.png`.

---

## Codex Adversarial Review

Three read-only review passes (write-capable rescue loop not used — the author applied surgical text edits directly from each pass's citations).

### Pass-1 findings

**High:**
1. `architecture.md` overstated all three composer setters as recompute-and-persist-lineItems; only `setQuoteInputs` does that. `setQuoteTax` writes `tax`/`total` only; `setQuoteDealer` writes `dealerId` only.
2. `architecture.md` said coaches add prospects from `/dealerships`, but `/dealerships` is admin-gated and `dealer:create` is admin-only per `capabilities.ts:85`.
3. `architecture.md` attributed string-only `MONEY_RE` + `numeric(10,2)` money parsing to composer Server Actions, but that posture belongs to `src/features/services/actions.ts`. Quote tax parsing is `TAX_RE` + `Number()`.

**Medium:**
4. `data-model.md` said the composer's "Add new prospect" entry-point pre-sets `status='prospect'`, but the affordance is explicitly deferred per the plan body.
5. `architecture.md` described the Send-time MSA gate as the current posture; it's the planned 0035 Phase 4 + 0025 Phase 7.2 posture (current `sendQuote` is a guarded `draft → sent` stub).
6. `data-model.md` audit-columns paragraph kept stale `blocked_dates`; current schema uses `availability_blocks` (and `availability_blocks` carries `actors`, so it doesn't belong in the skip-`actors` list anyway).

### Pass-1 fixes applied

1. (H1) `architecture.md` bullet 3 split into three sub-bullets distinguishing the three setters.
2. (H2) `architecture.md` "Entry points" paragraph reworded — admin-only `/dealerships → Add` is the dealer-creation path; coaches pick up prospects with `(prospect)` suffix in the picker.
3. (H3) Money-parse claim moved out of composer-actions bullet and attributed to `src/features/services/actions.ts`.
4. (M1) `data-model.md` `dealers` lifecycle-columns bullet reworded — affordance deferred, DealerForm accepts `defaultStatus='prospect'` for future inline-create.
5. (M2) `architecture.md` paragraph re-headed "Send-time MSA gate (planned — 0035 Phase 4 + 0025 Phase 7.2)"; current `sendQuote` stub posture documented.
6. (M3) Dropped `blocked_dates` from skip-`actors` list; now `(campaign_styles, audience_sources, service_items)`.

### Pass-2 findings

**High:**
- `architecture.md` overgeneralized `numeric(12,2)` across all quote money columns. Reality at `src/lib/db/schema/quotes.ts`: `fee` / `travel` are `numeric(10,2)`; only `subtotal` / `tax` / `total` are `numeric(12,2)`.

**Low:**
- "TODOs flagged inline in `actions.ts`" lumped the MSA gate in with PDF/email TODOs; the MSA gate is not flagged in code — it's a Phase 4 add.

### Pass-2 fixes applied

- (H) Rewrote the precision claim to call out the mixed precision explicitly (`fee`/`travel` 10,2 vs `subtotal`/`tax`/`total` 12,2).
- (L) Separated the TODO list — PDF/GCS/email TODOs are flagged inline in `actions.ts`, MSA gate is a Phase 4 add (not yet flagged).

### Pass-3 findings

**Low:**
- `log.md` entry's summary still framed the MSA gate as if Send currently performs it ("Send fires the gate and routes through 7.2's bundled envelope when no active MSA exists"). Inconsistent with the now-corrected architecture.md framing.

### Pass-3 fixes applied

- (L) Log entry rephrased — "**planned** Send-time MSA gate (0035 Phase 4 + 0025 Phase 7.2 — `sendQuote` today is a guarded `draft → sent` stub with no MSA lookup)".

### Pass-1 explicitly-fine callouts (still valid)

- `service_items` schema columns, enum, `numeric(10,2)`, archivable-only mixin, 8-row seed, RLS posture verified at `src/lib/db/schema/service-items.ts`, `drizzle/0012_round_skrulls.sql`, `drizzle/0013_seed_service_items.sql`, `drizzle/0014_service_items_rls.sql`, `tests/integration/rls.test.ts:27`.
- Dealer `status` / `acquired_via`, `dealers_status_idx`, archive-orthogonal filter math, patch-style `updateDealer`, guarded `convertProspectToActive` verified at `src/lib/db/schema/dealers.ts`, `src/features/schedule/actions.ts:188`/`:371`.
- Pricing constants (`MAX_AUDIENCE` etc.), `validateQuoteInputs`, `roundCents`, fail-closed range bounds verified at `src/lib/quotes/pricing.ts:64`/`:81`/`:185`.
- `setQuoteDealer` `FOR UPDATE` on candidate dealer row verified at `src/features/quotes/actions.ts:392`.
- Phase 5 tracker bookkeeping — 80% (4/5) coherent: P1/P2/P3/P5 Done, P4 stays Blocked.

---

## Cross-Reference

The 3 Highs + 3 Mediums from pass-1 all clustered in `architecture.md`'s "Quote composer" subsection (newly authored in this diff). Pattern: when ingesting recently-shipped code into wiki narrative, easy to overgeneralize ("all three setters do X") or to forward-project plan-stated posture as if it's the current implementation. The pass-1/2/3 lineage tightens the bound between wiki claim and code reality.

---

## Action Required

### Must Fix (blockers)

None.

### Should Fix

None.

### Consider

None.

---

## Coverage Caveats

- Web-test ran plan-specific gated routes only (not the full default Phase 2 table) — appropriate for a docs-only diff where the underlying UI surface is unchanged from earlier 0035 P1/P2/P3 eval reports.
- Web-test was deferred to after the Codex review (a single-pass parallel-dispatch wouldn't have caught the pass-2 + pass-3 residuals); the sequence works for a docs diff but isn't the eval skill's prescribed parallelization. For code-touching diffs the parallel dispatch should hold.
- Codex review was kept strictly read-only across all three passes (findings-only, no verdict, no write) per the feedback recorded at `~/.claude/projects/-Users-davidwhogan-v-event-manager-pro/memory/feedback_codex_review_invocations.md`. Verdict was the orchestrator's call.

---

## Next steps

Per Build skill chunk-loop:

1. Commit Phase 5 with `docs(plan)`-shaped message (this report referenced in plan.md Phase 5 row alongside the SHA).
2. Step 7 picks the next non-`Done` phase. Phase 4 is `Blocked on 0026 P3+P4`. Per Build skill major-concern criterion #1 (open dependency the phase depends on), pause and surface — Phase 4 cannot ship until 0026 P3 ships, and 0026 P3 itself is blocked on the GCS credentials carry-forward (`GCS_PROJECT_ID` / `GCS_BUCKET` / `GCS_CREDENTIALS_JSON` still empty in `.env.local` per CURRENT.md). Skill ends after the commit.
