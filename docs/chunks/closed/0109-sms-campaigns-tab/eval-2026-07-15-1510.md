# Eval: 0109-sms-campaigns-tab (chunk-end)

**Plan:** docs/chunks/0109-sms-campaigns-tab/plan.md
**Branch:** 0108-appointment-booking (chunk diff `b373504..HEAD`, tip `8bf0a4c`)
**Date:** 2026-07-15

---

## Verdict: PASS with warnings

Static and browser green; Codex found no Highs and exactly one Medium — the new index gave history-only campaigns a visible door onto a page that hid their send ledger — fixed in-cycle at `8729c91` and re-verified in the browser. A test-infrastructure change rode along (`8bf0a4c`): the integration suite's 5s default timeout flaked suite-wide as sandbox-pooler wire latency drifted during the day; the vitest ceiling is now 15s (inert for ms-fast unit tests), after which the full serial suite ran 22/22 files, 96/96 tests green.

**Static checks:** tests PASS (unit 1377/1377 incl. 3 new list-component tests; integration 96/96 serial after the timeout fix — see warning below) · tsc PASS · lint PASS-for-chunk (0 findings in 0109 files; repo noise unchanged)
**Browser smoke:** PASS — 5/5 checks
**Codex review:** 1 finding — 0 High, 1 Medium (fixed in-cycle), 0 Low

---

## Browser Smoke

| Check | Result |
|-------|--------|
| Nav shows the **SMS** tab between Quotes and Messages (capability-gated visible) | PASS |
| `/sms` renders "SMS" heading + rows for real gate-active sandbox campaigns (Century Mazda, Bathurst Toyota) with dealer/date/state/badges | PASS |
| Row click lands on `/calendar/92/sms` — "Campaign SMS — Century Mazda" | PASS |
| Post-fix: seeded history-only (completed) campaign lists with the `history` badge + "1 send, last …" | PASS |
| Post-fix: its `/calendar/<id>/sms` page shows the read-only **Send log** section + Conversations, no composer | PASS (fixture cleaned) |

Screenshot: `/tmp/web-test-0109-sms-tab.png`.

---

## Codex Adversarial Review (summary; full text in the job log)

**Medium (fixed in-cycle at `8729c91`)** — send-history-only campaigns are listed in `/sms` (`hasSends` qualification) but `/calendar/[id]/sms`'s non-gate-active branch rendered only the explanatory paragraph + ConversationsPanel, hiding `loadSmsSendLog` — a completed campaign with one launch showed "SMS is only available for booked campaigns" and no delivery log. Fix: the lapsed branch now loads the send log and renders a read-only Send log section (SmsPanel untouched per the chunk's non-goal).

**Explicitly not an issue (Codex):** `/sms` not in PUBLIC_PATHS and no ADMIN_PATHS conflict (page gates `assertCan('sms:send')`); nav `isActive` can't over-highlight `/sms`; the unread subselect matches the inbox derivation (null handling incl. `-infinity` coalesce); all EXISTS/subselect terms ride existing `campaign_id` indexes; `lastSendAt` parsing safe for Date or string; dealer names render as React text (no XSS vector); cancelled/draft-with-history inclusion is the intended rule.

---

## Action Required

### Must Fix (blockers)
None.

### Parked
None — the single finding was fixed in-cycle.

---

## Coverage Caveats

- **Integration-timeout warning (environmental):** during this eval the shared sandbox pooler's latency drifted enough that round-trip-heavy tests exceeded vitest's 5s default — failures moved between unrelated suites run-to-run (pure `Test timed out` errors; every test passed in isolation; `EXPLAIN` showed 0.055ms execution). Root-caused to wire-latency × round-trip-count, not any chunk. Fixed at the config level (`testTimeout: 15_000`, matching the existing `{ timeout: 30_000 }` precedent in sms-conversations); full serial suite green afterwards. If sandbox latency degrades further, that's a Supabase-instance conversation, not a test one.
- Codex ran read-only per the eval contract; the fix went through the write-capable rescue with orchestrator verification (tsc, suites, browser fixture walk).
- Scale posture of the index query (6 scalar subselects × qualifying campaigns) is fine at tens of campaigns; it inherits the 0107-a inbox-scale follow-up's un-park trigger if campaign volume ever makes it visible.
