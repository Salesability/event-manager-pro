# Eval: 0068-quickbooks-oauth-viewer

**Plan:** docs/chunks/0068-quickbooks-oauth-viewer/plan.md
**Branch:** 0068-quickbooks-oauth-viewer
**Date:** 2026-06-08

---

## Verdict: PASS

In-app QuickBooks OAuth read-only viewer is implementation-complete; static + browser smoke green, Codex surfaced no issues (its final synthesis truncated, but the captured investigation affirmatively cleared the main risk areas).

**Static checks:** test PASS (1008 passed / 2 skipped) · tsc PASS · lint PASS for the chunk (0 new errors/warnings in 0068's files; the 147 repo-wide lint errors are all pre-existing in untouched files, e.g. the known `quotes/actions.ts` `safeparse-required` pair)
**Browser smoke:** PASS — 2/2 checks verified (auth-injected admin)
**Codex review:** no findings surfaced (thorough investigation; final synthesis truncated — known `codex:codex-rescue` flush pattern)

---

## Static Checks

- **`pnpm test`** — 63 files, **1008 passed / 2 skipped**. Includes the new `sealed-box.test.ts` (7), `client.test.ts` (12), `connection.test.ts` (10), `callback/route.test.ts` (7), and the two new action-gate-matrix rows.
- **`tsc --noEmit`** — clean.
- **`pnpm lint`** — repo-wide reports 246 problems (147 errors / 99 warnings), **all pre-existing**. Lint scoped to 0068's 15 changed files exits **0** — the chunk introduces no new lint errors or warnings.

---

## Browser Smoke

Auth-injected admin (`inject-supabase david.hogan@networknode.ca`).

| Check | Result |
|-------|--------|
| `goto /admin/quickbooks` → 200 (admin gate passes), heading "QuickBooks" + "Connect to QuickBooks" button (disconnected state) | PASS |
| No console errors on the page | PASS |
| Admin dropdown nav contains a "QuickBooks" item (with Contacts / Lookups / Send Test Email / Send Test MSA) | PASS |

Screenshot: `/tmp/web-test-quickbooks.png`.

The disconnected state is what the auth-injected admin sees (no `quickbooks_connection` row exists in the sandbox DB). The connected-state customer table + the OAuth round-trip itself are the owner-manual verification (needs sandbox `QBO_CLIENT_ID`/`QBO_CLIENT_SECRET` + `QBO_TOKEN_ENC_KEY` in `.env.local`; can't be auto-driven — leaves Intuit's domain).

---

## Codex Adversarial Review

Job `b90c7j7f4` ran a **thorough** read-only investigation (read every chunk file plus `auth.md`, `require-admin.ts`, `assert-can.ts`, `capabilities.ts`, `proxy.ts` (the Next-16 middleware entrypoint), `auth/callback/route.ts`, and the `0031` migration/snapshot), but **exited before flushing its final severity-classified synthesis** — the known `codex:codex-rescue` truncation pattern. Per project guidance, reconstructed from the job log rather than retried.

Captured interim observations (all neutral — no issue raised):
- "the callback checks admin **before** token exchange/save, so the main bypass path [is closed]"
- "the UI reflection concern looks neutral … callback error text is passed through React text [→ escaped, no XSS]"
- "the middleware confirms `/auth/quickbooks/callback` is intentionally public" (matches the existing `/auth/callback` pattern)
- "the migration matches the schema's singleton constraint: `singleton` is NOT NULL DEFAULT true [+ UNIQUE + CHECK]"
- a context correction (the callback is under `/auth`, not a broad `/admin` path) — not a finding

**No High / Medium / Low findings appeared in the captured output.**

---

## Cross-Reference

None — no Codex finding to cross-reference against a static/browser anomaly (there were no static/browser failures and no flushed Codex findings).

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high/critical)
None surfaced.

### Consider (Codex medium/low)
None surfaced. (Codex synthesis truncated — see Coverage Caveats.)

---

## Coverage Caveats

- **Codex final synthesis truncated** — the formal findings list was never flushed to the job log. Mitigation: the captured investigation trace shows Codex affirmatively probed and cleared the headline risk areas (CSRF/state, token-at-rest crypto, auth bypass on the public callback, XSS via `?error=`, singleton-upsert correctness). Not retried, per project guidance.
- **OAuth round-trip not exercised end-to-end** — only the disconnected-state UI + unit-mocked flow are verified. The real connect → read customers → disconnect against the sandbox company is **owner-pending** (needs sandbox creds + enc key in `.env.local`; registered redirect URI `http://localhost:3000/auth/quickbooks/callback` already added on the Intuit Development tab).
- **deploy.sh secret-wiring deferred** — `QBO_CLIENT_ID`/`QBO_CLIENT_SECRET`/`QBO_ENV`/`QBO_TOKEN_ENC_KEY` are documented in `.env.example` but not yet wired into `deploy.sh` (not needed for local sandbox validation; deferred to stage-deploy time, also to avoid entangling with an unrelated pre-existing uncommitted `deploy.sh` edit).
- **Branch not merged** — all work is on `0068-quickbooks-oauth-viewer`, not `main`.
- **Unrelated `.gitignore` change in the working tree** — `*secret*`/`*.secret` catch-alls were removed during the session (not by this chunk's work, not committed here); flagged separately to the owner.
