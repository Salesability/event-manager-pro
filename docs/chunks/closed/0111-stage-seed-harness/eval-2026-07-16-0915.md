# Eval: 0111-stage-seed-harness

**Plan:** docs/chunks/0111-stage-seed-harness/plan.md
**Branch:** 0108-appointment-booking
**Date:** 2026-07-16

---

## Verdict: PASS with warnings

All statics green, plan-specific browser smoke 3/3, all four Codex High/Medium regressions fixed in-cycle at `85c0afc` and confirmed clean by a re-review pass; two Codex Mediums parked as documented v1 posture.

**Static checks:** tsc PASS · unit 1403/1403 PASS (94 files, incl. 8 guard regression tests) · integration 103/103 PASS (serial, 24 files) · lint: 0 findings in chunk files (150 pre-existing errors, all in stale `.claude/worktrees/` copies + legacy scripts)
**Browser smoke:** PASS — 3/3 plan-specific checks (gated surface, auth-injected)
**Codex review:** pass 1 — 4 High / 3 Medium / 2 Low; pass 2 (post-fix re-review) — **No findings observed**

---

## Static Checks

All passed. Notes:
- Integration suite run with `--no-file-parallelism` per the documented sandbox-pooler discipline (the parallel run trips the shared 15-conn `EMAXCONNSESSION` flake; not a code defect).
- Lint failures are entirely pre-existing (stale worktree copies, `docs/chunks/closed/0049` palette script, legacy `scripts/` and `sms-opt-outs.ts`/`quotes/actions.ts` warnings) — none in files this chunk touched.

---

## Browser Smoke

Plan-specific checks (Phase 5), driven via `.claude/tools/browse` with `inject-supabase`, seeded demo campaign id 3921:

| Check | Result |
|-------|--------|
| `/calendar/3921/sms` — funnel 5 sent / 4 delivered / 1 response / 4 no response / 1 stops; pre-send 6 imported / 4 eligible / 1 opted out / 1 stale; both exclusion rows (Morgan Grant stale-CASL, Pat Quinn STOP); prior-send "same person as before" identity badges; fabricated send log (1 sent / 4 delivered) | PASS |
| `/messages` — Alex Morgan thread: sentiment dot (positive), hot prospect + new reply + awaiting-your-reply, Demo Motors attribution, outbound+inbound transcript | PASS |
| `/sms` — Demo Motors row: SMS active, 1 new reply, 1 hot, 6 recipients · 1 send · 1 conversation; Century Mazda reads 0 recipients / no sends (post-sweep pre-fixture state) | PASS |

Screenshots: `/tmp/web-test-demo-sms.png`, `/tmp/web-test-demo-messages.png`, `/tmp/web-test-demo-smstab.png`.

---

## Codex Adversarial Review

### Pass 1 (pre-fix)

**High**
- `scripts/seeds/guard.ts:29/35` — Guard bypass via percent-encoding. The prod-ref hard stop is raw case-sensitive substring matching and the sandbox allowlist matches any substring. Concrete slip: `postgresql://postgres.fkfy%62eddnfxnjuxkqidp:pw@…?application_name=qppenapeguwevcheqwpz` — the postgres driver decodes `%62`→`b`, reconstructing the real prod username.
- `scripts/seeds/guard.ts:29/47` — Case + opt-in evasion. `postgres.FKFYBEDDNFXNJUXKQIDP` bypasses the case-sensitive prod-ref check; with `SEED_DEMO_ALLOW_UNKNOWN_TARGET=1` the guard admits it as "unknown."
- `tests/integration/seed-demo.test.ts:39/45` — Test bypasses the CLI guard entirely; if `DATABASE_URL` points at prod when vitest runs, seeds execute against prod.
- `scripts/0111-century-mazda-sweep.ts:47` — Sweep claims scope to send `674` but deletes ALL `sms_sends`/messages on campaign 92; a legitimate later send would be deleted on a re-run.

**Medium**
- `scripts/0111-century-mazda-sweep.ts:81` — Opt-out delete global by phone only, not tied to source/evidence.
- `tests/integration/seed-demo.test.ts:135/163` — Exact non-marker table-count equality races concurrent sandbox writers (flake, not correctness).
- `scripts/seeds/10-demo-dealer.ts:81` / `30-sms-history.ts:137` — No transaction/advisory lock around clean+seed; concurrent `pnpm seed:demo` runs could interleave (hypothesis, unconfirmed).

**Low**
- `10-demo-dealer.ts:85/91` — Clean sweeps the broad `demo-%` prefix rather than exact module-owned ids.
- `seed-demo.test.ts:123` — `afterAll` always cleans marker rows; running the suite during demo prep wipes staged demo state.

**Not issues (Codex false-positive notes):** the Twilio-webhook `PUBLIC_PATHS` addition does not over-expose (exact-or-`/`-prefix matching); the opt-out `onConflictDoNothing` is safe; no SQL injection (all Drizzle operators); the fabricated funnel reconciles with `loadSmsCampaignFunnel`'s read model.

### In-cycle fixes — `85c0afc`

1. Guard canonicalization: prod-ref refusal now runs on both raw-lowercased and percent-decoded-lowercased URL forms, ahead of (and immune to) the unknown-target opt-in; sandbox allowlist matches the decoded-lowercased form; undecodable URLs refused. +2 regression tests (percent-encoded prod ref with smuggled sandbox ref; uppercase prod ref with opt-in).
2. `seed-demo.test.ts` classifies the target (`classifySeedTarget(dbUrl, false)`) before constructing any client; non-sandbox targets skip.
3. Sweep scoped to campaign 92 AND send id 674 (select + both deletes).
4. Sweep opt-out delete additionally requires `source = 'manual'`.

### Pass 2 (post-fix re-review)

> No findings observed. — False-positive notes: double-encoded `%2562` does not produce a prod credential in this path because both `classifySeedTarget` and `postgres-js` decode once; malformed percent sequences are refused at `scripts/seeds/guard.ts:30`; uppercase prod refs are covered by raw lowercasing at `scripts/seeds/guard.ts:28`; the integration test now avoids constructing `postgres()` unless the guard passes at `tests/integration/seed-demo.test.ts:41`; the sweep is scoped to campaign 92/send 674 and manual opt-out source at `scripts/0111-century-mazda-sweep.ts:52` and `:84`.

---

## Cross-Reference

None — no Codex finding mapped to a static-check failure or browser anomaly.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high/critical)
None outstanding — all four fixed in-cycle at `85c0afc`, confirmed by the re-review pass.

### Consider (Codex medium — parked)
1. **0111-a** — `seed-demo.test.ts`'s non-marker count-equality assertion can flake against a concurrent sandbox writer (Codex Medium). Accepted v1 posture on the shared DB (suite runs serially; concurrent writers are rare). Parked as follow-up.
2. **0111-b** — no advisory lock around the harness's clean+seed sequence; two concurrent `pnpm seed:demo` runs could interleave into unique violations (Codex Medium, hypothesis). Single-operator demo tool by design. Parked as follow-up.

Codex Lows accepted as designed: the broad `demo-%` clean prefix is the documented marker-ownership contract (`markers.ts`); the `afterAll` clean is documented in the test with the one-command restore.

---

## Coverage Caveats

- Browser smoke ran the plan's targeted checks, not the repo-wide default table (unchanged surfaces; the SMS surface itself was fully exercised).
- The Century Mazda sweep was executed once by the user and verified (0 recipients / 0 sends / 0 threads / 0 fixture opt-outs on campaign 92); the committed script is for the audit trail, not re-running.
- `pnpm lint` not clean repo-wide — pre-existing noise documented above; 0 new findings.
