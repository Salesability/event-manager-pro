# Eval: 0106-sms-conversation-console

**Plan:** docs/chunks/0106-sms-conversation-console/plan.md
**Branch:** 0103-sms-service (chunk diff `60ede6a..HEAD`; the branch also carries the already-shipped 0103/0105 work)
**Date:** 2026-07-14

---

## Verdict: PASS with warnings

All static checks and the browser smoke pass; Codex pass 1 surfaced 1 High + 2 Medium + 1 Low — the Mediums and the Low were fixed in-cycle at `d72c1cc` and verified cleared by a second Codex pass (no new findings); the High is a pre-existing accepted concurrency posture shared with the shipped 0103 launch loop and is parked as follow-up **0106-a**.

**Static checks:** tsc PASS · unit 1358/1358 PASS (86 files) · integration 87/87 PASS (19 files, run serially per the pooler doctrine) · lint 0 findings in chunk-touched files (repo-wide count unchanged: pre-existing stale-worktree/scripts noise)
**Browser smoke:** PASS — unauthed redirect + full gated console render verified
**Codex review:** pass 1: 1 High / 2 Medium / 1 Low → 3 fixed in-cycle (`d72c1cc`), 1 parked (0106-a); pass 2 (fix re-review): no findings, all three fixes verified cleared

---

## Static Checks

All passed.

- `npx tsc --noEmit` — clean.
- Unit suite (`vitest run --exclude tests/integration`): 86 files, 1358 passed, 2 skipped.
- Integration suite (`vitest run tests/integration/ --no-file-parallelism`): 19 files, 87 passed — includes the new `tests/integration/sms-conversations.test.ts` (5 tests: capture + sid-replay idempotency, most-recent-send attribution + reassign candidates, mid-thread STOP evidence + opt-out, no-thread-for-unknown-numbers, and the full reply round-trip: dev-redirect proven, `aiDrafted` provenance persisted, status callback flips the thread ledger, STOP halts before dispatch).
- `pnpm lint` — 150 errors / 104 warnings repo-wide, all pre-existing (stale `.claude/worktrees` copy, `scratchpad/`, old scripts, 0103-era `sms-opt-outs.ts` unused-var warning). Zero findings in files this chunk touched (the one new warning, an unused destructure in `conversations/queries.ts`, was fixed during the eval).

---

## Browser Smoke

Plan-specific check (per web-test Step 4) against the extended `scripts/sms-service-smoke.ts` fixture (campaign 2008, inserted + cleaned up around the run). Dev server already running on :3000.

| Check | Result |
|-------|--------|
| Unauthed `goto /calendar/2008/sms` → `/login?next=%2Fcalendar%2F2008%2Fsms` with Google + email + magic-link | PASS |
| `inject-supabase` (explicit email) | PASS |
| Authed page: `Campaign SMS — [0103-smoke] SMS Panel Dealer` heading + Recipients / Pre-send review / Compose / Send log sections | PASS |
| **Conversations** section renders with two thread rows | PASS |
| Halted thread (+19997770004): `new reply` + `opted out (STOP)` badges, inbound bubbles (`not interested`, `STOP`), permanent-halt explanation, **no** reply textarea | PASS |
| Active thread (+19997770001): `new reply` badge, inbound `Interested! What time do you open Saturday?`, outbound with `delivered` badge, outbound with `sent` + `AI draft` badges, inbound `YES please, 10am Saturday` | PASS |
| Reply affordance: textarea (`Type a reply, or draft one with AI…`) + `Draft AI reply` + `Send reply` buttons + `Mark read` | PASS |

Screenshot: `/tmp/web-test-sms-console.png`.

---

## Codex Adversarial Review

### Pass 1 (full chunk diff, verbatim)

**High — src/lib/sms/conversations.ts:167** — STOP can race between the opt-out check and the Twilio dispatch. `sendThreadReply` checks `sms_opt_outs`, then inserts the outbound row, then calls `sendSms`. A STOP webhook writes the opt-out with no per-phone lock or atomic "claim then recheck" serializing STOP insertion against reply dispatch. Trigger: start a staff reply; have the customer's STOP webhook commit after the check reads no opt-out but before the Twilio call. Falsifies "STOP always wins" under concurrency.

**Medium — src/features/sms/actions.ts:492** — `reassignThread` accepts any existing campaign, not just campaigns that texted the number; a user with `sms:send` could bypass the select control and move the transcript, corrupting future attribution.

**Medium — src/features/sms/conversations/queries.ts:56** — conversation reads and AI transcript reads are unbounded; a real recipient can spam inbound SMS (each authenticated webhook inserts a row) creating a customer-triggerable page/AI cost path and unbounded hostile influence over prompt size.

**Low — src/lib/ai/draft-sms-reply.ts:69** — customer SMS bodies placed into the model prompt without an explicit untrusted-data boundary; prompt-injection content could produce a misleading staff-facing draft (mitigated by human approval).

**Not issues (false-positive reduction):** webhook signature verification precedes all DB access; non-prod redirect doctrine intact; same-MessageSid replay idempotent (partial unique + `onConflictDoNothing`, no unread re-bump); status callbacks monotonic on both ledgers; the AI draft path cannot send (returns text only; sending goes through `replyToThread` → `sendThreadReply`).

### In-cycle fixes (`d72c1cc`)

- **Medium (reassign):** `reassignThread` now requires the target campaign to have launch-send history (`sms_messages` ⋈ `sms_sends`) for the thread's phone — the same predicate as the UI's candidate list.
- **Medium (unbounded reads):** `loadCampaignConversations` bounds the message read to the 500 most recent per campaign (desc + limit, re-sorted ascending); `loadThreadDraftContext` bounds the AI transcript to the last 30 messages with bodies truncated to 500 chars.
- **Low (prompt boundary):** `buildDraftPrompt` gained an explicit untrusted-customer-input hard rule + unit-test assertion.

### Pass 2 (fix re-review, verbatim summary)

High / Medium / Low: no findings observed at any tier. All three prior findings verified **cleared** — the reassign guard matches the UI candidate predicate exactly (legitimate candidates unaffected), the bounds are off-by-one-correct with oldest-first ordering and per-thread grouping preserved, and the untrusted-input rule + test assertion are in place.

### Parked (not fixed in-cycle)

**High → 0106-a follow-up.** The STOP-vs-dispatch race is the same accepted v1 posture as the shipped 0103 launch loop (its per-recipient recheck "immediately before its Twilio call" narrows the same window without closing it — closing it requires holding a lock across an external Twilio call or an outbox/worker). Intent explicitly rides replies on the 0103 doctrine; fixing only the reply path would leave the launch loop inconsistent. Window is sub-second and requires a STOP landing in the exact instant of a staff send; Twilio Advanced Opt-Out additionally suppresses delivery to STOPped numbers at the service level. **Parked as 0106-a**, same un-park trigger and fix shape as 0103-a (SMS reliability pass: outbox/worker or per-phone advisory lock spanning dispatch).

---

## Cross-Reference

No Codex finding overlapped a static failure or browser anomaly. The re-review independently confirmed the fix commit the static suite had already passed.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high/critical)
None remaining in-cycle — the High is explicitly parked as the **0106-a** follow-up (see above; joins 0103-a's reliability pass).

### Consider (Codex medium/low)
None remaining — both Mediums and the Low were fixed in-cycle at `d72c1cc` and verified by pass 2.

---

## Coverage Caveats

- Browser smoke ran the plan-specific check, not the default repo-wide gated table (per web-test Step 4); `/calendar`, `/production`, etc. were not re-walked (no changes there).
- `Draft AI reply` and `Send reply` were not clicked (read-only discipline: Send is a real mutation; Draft would call the live Anthropic API — the module is fully unit-tested with the SDK mocked, and the send path is integration-tested with Twilio mocked).
- No console/network log assertion (the browse tool in this repo exposes no log commands).
- Integration tests run against the shared sandbox DB; the full-suite parallel run trips the known EMAXCONNSESSION pooler flake, so integration was run serially (documented posture).
- `ANTHROPIC_API_KEY` is not yet provisioned in any deploy environment — the Draft button degrades to a friendly error until the owner adds the secret (stage first; see `.env.example`).
