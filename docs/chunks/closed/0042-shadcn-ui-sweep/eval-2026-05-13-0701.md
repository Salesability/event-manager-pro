# Eval: 0042-shadcn-ui-sweep

**Plan:** [docs/chunks/0042-shadcn-ui-sweep/plan.md](plan.md)
**Branch:** main
**Date:** 2026-05-13
**Chunk diff:** `f540c46..HEAD` — 36 files, +4,415 / −1,002, the 0042 shadcn/ui sweep (Phases 1–6 + this chunk-end fix)

---

## Verdict: **PASS with warnings**

The chunk is shippable. Static checks all green. Browser smoke confirms every plan-named flow (Add Dealer, Book Event, /quotes/new, /reports tabs, Add Person). Codex's one Medium finding (dialog default-width regression — old in-house default was 560px, shadcn ships `sm:max-w-sm` ~384px) is **already fixed** in this eval cycle. Lint dropped from 7 → 6 warnings (the chunk-introduced one is gone; the remaining 6 are pre-existing patterns unrelated to 0042).

**Static checks:** `pnpm tsc --noEmit` PASS · `pnpm test` 757 PASS / 2 SKIPPED / 759 total · `pnpm lint` 0 errors / 6 warnings (all pre-existing).
**Browser smoke:** PASS — Phase 1 4/4, Phase 2 8/8 (auth injected as `david.hogan@networknode.ca`).
**Codex review:** 1 Medium finding, addressed in this cycle. Concurrent first-pass review orphaned mid-run (PID died, swept).

---

## Static Checks

All passed.

- `npx tsc --noEmit` — exit 0, clean.
- `pnpm test` — Test Files 36 passed, Tests 757 passed / 2 skipped (759 total), 2.80s.
- `pnpm lint` — exit 0, 0 errors, 6 warnings, all pre-existing:
  - `src/app/(app)/calendar/calendar-view.tsx:135` — `getCoachColor` `react-hooks/exhaustive-deps` (predates 0042).
  - `src/app/(app)/production/production-filters.tsx:30,33,36` — 3× `no-unused-expressions` (pre-existing filter-pattern).
  - `src/app/(app)/quotes/quotes-filters.tsx:53,56` — 2× same pattern (pre-existing).
- The single chunk-introduced lint warning (`inputClass` unused in `quote-composer.tsx` after the Phase 3 port) was **removed in this eval cycle** at `3a1a16e`.

---

## Browser Smoke

Dev server up (`curl http://localhost:3000/` → 307 redirect). Auth injected as `david.hogan@networknode.ca`.

### Phase 1 — public surface (4/4 PASS)

| Check | Result |
|-------|--------|
| `goto /` redirects to `/login?next=%2F` (status 200) | PASS |
| `/login` renders Continue with Google + email textbox + "Send magic link" button | PASS |
| `goto /calendar` (gated) redirects to `/login?next=%2Fcalendar` | PASS |
| `goto /share/coach/1` renders Schedule heading + month-nav `‹` / `›` buttons (advances on click) | PASS |

Screenshot: `/tmp/web-test-share.png`.

### Phase 2 — auth-injected gated surface (8/8 PASS)

`inject-supabase david.hogan@networknode.ca` → success. Cookies set: `sb-qppenapeguwevcheqwpz-auth-token.{0,1}`.

| Check | Result |
|-------|--------|
| `/dealerships` — heading + count pills + `+ Add Dealer` button | PASS |
| Click `+ Add Dealer` → dialog "Add Dealer" with 8 expected fields: Dealership name / Contact first / Contact last / Email / Phone / Address / Status / How did this dealer find us? | PASS (all fields present) |
| `/calendar` — Master Schedule heading + stats row + `Block Date` + `+ Book Event` | PASS |
| Click `+ Book Event` → dialog "Book Event" with full booking-form field list (Start Date, Duration, End Date, Dealership, Contact, Phone, Email, Event Format, Data Source, Qty Records, SMS/Email, Letters, BDC, Sales Coach, Notes) | PASS (all fields present) |
| `/quotes/new?dealerId=1` — left-pane Field primitives (Dealer combobox + 5 spinbuttons + retrieval-bracket ToggleGroup + Travel + 2 textareas), right-pane Line items table updates (Base Event $6,900 → Subtotal → Tax override → Total) | PASS (left-pane renders via shadcn `<Field>` + `<FieldGroup>` + `<ToggleGroup>`; right-pane computed table works) |
| `/reports` — Tabs row above table (orientation-selector fix held), tab switching (`By Dealer` → `By Coach`) updates table content | PASS |
| `/production` — Production List heading + search + filter combobox + Show-cancelled checkbox + table | PASS |
| `/admin/people` — People heading + Add Person dialog renders with First name / Last name / Email / Phone + Admin/Coach/Dealer checkboxes (PersonForm post-Phase-6 port) | PASS |

Screenshots saved:
- `/tmp/web-test-share.png` — share page
- `/tmp/web-test-add-dealer.png` — Add Dealer dialog
- `/tmp/web-test-book-event.png` — Book Event dialog (pre-width-fix, ~384px)
- `/tmp/web-test-book-event-560px.png` — Book Event dialog (post-width-fix, ~560px)
- `/tmp/web-test-quote-new.png` — Quote composer
- `/tmp/web-test-reports.png` — Reports Tabs (orientation fix held)
- `/tmp/web-test-add-person.png` — Add Person dialog (PersonForm port)

---

## Codex Adversarial Review

Two concurrent Codex review jobs were dispatched against `/tmp/eval-0042-diff.patch`:

- `task-mp3xiqxh-3zr19f` (first launch) — **ORPHANED** mid-run: PID 83319 died at ~18min elapsed with state.json still `running`. Swept via `eval` skill's stall recipe (`status: failed`, `phase: orphan`). No findings recovered.
- `task-mp3xpe0t-05psge` (follow-up rescue Agent's own launch) — **COMPLETED** in ~3min. Returned 1 Medium finding verbatim:

> **Medium:** [src/components/ui/dialog.tsx](../../../../src/components/ui/dialog.tsx) (line 56 pre-fix) now defaults every dialog to `sm:max-w-sm`, but dense form dialogs like calendar create/edit still pass no wider className ([calendar-view.tsx](../../../../src/app/(app)/calendar/calendar-view.tsx) line 595) while [booking-form.tsx](../../../../src/app/(app)/calendar/booking-form.tsx) line 146 still uses fixed 3-column and 4-column grids. This shrinks core booking modals from the previous ~560px panel to ~384px, making the create/edit booking form cramped and overflow-prone on desktop.

No High, no Low. No findings on wire-format / FormData contract preservation, dialog open-handler semantics, combobox value-mapping, accessibility/focus-restore, Server Action targets, or the Tabs `data-orientation` selector. Codex did not flag the removal of the `<Form.Message match="typeMismatch">` PersonForm hint (Phase 6 dropped it; browser-native validation suffices, and the email field is non-required so the path is rarely exercised).

**Action taken this cycle:** restored the previous 560px default by changing `dialog.tsx`'s `sm:max-w-sm` to `sm:max-w-[560px]`. All call sites that previously passed an explicit override (`sm:max-w-4xl` for quote preview, `sm:max-w-lg` for MSA, `sm:max-w-[780px]` for availability) continue to override correctly. Commit `3a1a16e`.

---

## Cross-Reference

The Codex Medium overlaps with a Phase 5 implementation gap (the in-house Dialog.Panel had `max-w-[560px]` as its non-overridden default; the shadcn port to `<DialogContent>` adopted shadcn's narrower `sm:max-w-sm` default without restoring the project's previous baseline). The fix is the single line change in `dialog.tsx`. No other static-check or browser-smoke finding maps to this Medium.

---

## Action Required

### Must Fix (blockers)

None.

### Should Fix (Codex High)

None.

### Consider (Codex Medium/Low) — addressed in this cycle

1. ~~Dialog default-width regression~~ — addressed at `3a1a16e` (Medium). `sm:max-w-sm` → `sm:max-w-[560px]` in `src/components/ui/dialog.tsx`.
2. ~~Chunk-introduced lint warning `inputClass` unused in `quote-composer.tsx`~~ — addressed at `3a1a16e`. Pre-Phase-3 const left behind when raw inputs were swapped for shadcn `<Input>`; removed.

---

## Coverage Caveats

- **Concurrent Codex passes.** The first-launched review (`task-mp3xiqxh-3zr19f`) orphaned mid-run — the worker process died without writing a completion record. The follow-up rescue Agent's own Codex pass (`task-mp3xpe0t-05psge`) completed cleanly with 1 Medium finding on the same diff; treating that as the canonical review output. If the orphaned pass would have surfaced additional findings, they're not in this report; a future re-eval would catch them.
- **Visual regression coverage was reactive, not pre-emptive.** The dialog-width Medium illustrates the trade-off: the per-phase fast gate (`tsc + test`) does not catch CSS-default-value regressions, and per-phase web-test smoke was deferred to the chunk-end ritual per the post-0040 cadence. The chunk-end smoke did surface the live-rendered dialogs, but only via the screenshot — without Codex's visual reasoning the regression could have shipped. Two adversarial passes (static + Codex) caught it before the chunk closed.
- **`@admin/people` route check.** The plan body referenced "people-admin" but the actual route is `/admin/people` (not `/people`); confirmed mid-smoke after a 404 on the bare path. Smoke now covers the correct route.
- **Read-only discipline preserved.** No form was submitted (no `Add Person` / `Add Dealer` / `Book Event` save), no destructive action invoked. All three form dialogs verified by rendering + field-list inspection + close.
