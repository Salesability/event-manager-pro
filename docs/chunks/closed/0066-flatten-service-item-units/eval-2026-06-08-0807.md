# Eval: 0066-flatten-service-item-units

**Plan:** docs/chunks/0066-flatten-service-item-units/plan.md
**Branch:** main
**Date:** 2026-06-08

---

## Verdict: PASS with warnings

Chunk 0066 flattens `service_items` to a single `unit_price` — clean across all three evaluators for the chunk's own diff. The only "warning" is the repo's pre-existing, stable lint debt (the chunk introduced zero new lint findings). Codex found nothing; both plan-specific browser smokes passed, including the headline fix (record-retrieval seeds $100, no longer $0).

**Static checks:** tests **940 passed / 2 skipped** · tsc **clean (0 errors)** · lint **0 new errors from 0066** (repo carries pre-existing debt — see below)
**Browser smoke:** **PASS — 2/2 plan-specific checks** (auth-injected gated surface)
**Codex review:** **0 findings** (read-only adversarial pass; clean)

Diff scope: `dbc744c..HEAD` (the two 0066 commits `6f4057c` + `f6e8309`), deliberately excluding 3 unrelated unpushed prior commits (0060 QBO + dealers fix) that `HEAD~5..HEAD` would have swept in.

---

## Static Checks

- **`pnpm test`** — PASS. 58 files, 940 passed / 2 skipped (942 total). No fixtures left referencing the dropped `unit`/`unit_price_min`/`unit_price_max` fields (repo-wide grep clean).
- **`pnpm tsc --noEmit`** — PASS. Exit 0, no `error TS`.
- **`pnpm lint`** — repo-wide FAIL (246 problems / 147 errors), but **scoped to 0066's 8 changed source files → only 2 errors, both pre-existing**:
  - `src/features/quotes/actions.ts:253` `createQuote` — `safeparse-required` (lines I never touched; my edit was the `loadActiveCatalog` select ~line 75)
  - `src/features/quotes/actions.ts:500` `setQuoteInputs` — `safeparse-required` (ditto)
  These predate the chunk and belong to the existing repo lint debt that the build fast-gate intentionally skips (same family as the parked `0045 follow-up (b)` safeparse-rule hardening). **No lint error introduced by 0066.**

---

## Browser Smoke

Auth-injected as `david.hogan@networknode.ca` (sandbox ref `qppenapeguwevcheqwpz`, the migrated DB). Dev server was already running (left up). Plan-specific checks (preferred over the default table):

| Check | Result |
|-------|--------|
| `/admin/lookups` — Services form shows Code / Label / Sort / Unit price / Description; **no** Unit dropdown, **no** Min/Max | **PASS** — form fields: `Code`, `Label`, `Sort`, `Unit $ (blank = variable)`, `Description (optional)`, `Add Service`. No combobox/select, no min/max. |
| `/quotes/new` — add "Record Retrieval and Preparation" → line seeds **non-zero** (~$100) | **PASS** — line seeded **Unit price $100.00**, Line total $100.00, Subtotal/Total $100.00 (was $0.00 pre-0066). |

Screenshots: `/tmp/web-test-0066-lookups.png`, `/tmp/web-test-0066-quote-seed.png`.

---

## Codex Adversarial Review

> **High** — None.
> **Medium** — None.
> **Low** — None.
>
> No findings in this read-only adversarial pass.
>
> **Explicitly NOT an issue:**
> - Migration ordering: `0030_…sql` backfills first, then drops columns/type (`UPDATE … WHERE code='record-retrieval' AND unit_price IS NULL`, then `DROP COLUMN unit`/min/max, then `DROP TYPE`).
> - Travel-row guard: the update is scoped to `record-retrieval`, so the also-NULL `travel` row is untouched.
> - Journal monotonicity: `0030` `when` 1780919258320 > `0029` 1780676928308.
> - Snapshot/schema consistency: `service-items.ts` defines only `code/label/unitPrice/description/sortOrder/archivedAt`; `0030_snapshot.json` matches; no `service_item_unit` remains — a future `db:generate` won't re-emit the drops.
> - Removed-column readers absent in live `src/`; `loadServiceItems`/`loadActiveCatalog` now select only the surviving columns.
> - Optional-price path safe: blank `unitPrice` → null; the composer seeds null as 0 and a typed nonzero price becomes an override — variable rows don't lose entered prices.
> - UI removal complete: only the flat `Unit $ (blank = variable)` field remains; no unit/min/max controls.

(Codex job `a2acfa890a9266045`, 22k tokens, 141s — completed cleanly, no synthesis truncation.)

---

## Cross-Reference

No Codex finding to cross-reference (clean pass). The static-lint pre-existing `safeparse-required` on `createQuote`/`setQuoteInputs` is unrelated to this chunk's diff.

---

## Action Required

### Must Fix (blockers)
_None._

### Should Fix (Codex high/critical)
_None._

### Consider (Codex medium/low)
_None from Codex._ One minor data observation (not a code issue, not parked): the seeded `record-retrieval` row's **`description`** still reads "Catalog menu: $100 / $200 / $300 / $400 (coach picks at quote time)" — stale now that the range is gone. Out of 0066's scope (the chunk touched the pricing columns, not the description prose); editable any time via `/admin/lookups`. Surfaced in the composer line description.

---

## Coverage Caveats

- Lint runs repo-wide and FAILs on pre-existing debt; the verdict scopes lint to the chunk's changed files (0 new errors). This matches the build fast-gate's documented lint-skip rationale and prior PASS-with-warnings evals.
- Prod migration apply (`0030`) is a **pending deploy-time step** — recorded in plan Phase 4 + CURRENT.md, NOT run by this chunk.
- The two unrelated unpushed prior commits (0060/dealers) were deliberately excluded from the Codex diff scope; not evaluated here (not part of 0066).
