# Eval: 0020-people-unification (Phase 5)

**Plan:** docs/chunks/0020-people-unification/plan.md
**Branch:** main
**Date:** 2026-05-05

---

## Verdict: **PASS with caveats**

Phase 5 is a docs-only phase (auth.md / lifecycle.md / log.md). Static checks all clean. Codex review and browser smoke skipped at user request — for a wiki-rewrite phase the marginal value is low (no new code to attack; no UI surface change to regress).

**Static checks:** tests PASS (100/100), tsc PASS, lint PASS (0 errors / 4 pre-existing warnings).
**Browser smoke:** SKIPPED — user opted out (no UI changes in this phase; surface unchanged from Phase 4 eval `eval-2026-05-05-1600.md`).
**Codex review:** SKIPPED — user opted out (would have fact-checked the wiki claims against the code; deferred to a future audit pass).

---

## Static Checks

```
pnpm test       → 100/100 (9 files; unchanged from Phase 4)
pnpm tsc        → no output (clean)
pnpm lint       → 0 errors, 4 warnings (pre-existing)
```

All passed.

---

## Browser Smoke

SKIPPED — user opted out. Phase 4 eval `eval-2026-05-05-1600.md` covered the post-retirement surface (`/admin/users` redirect, `/lists` Dealerships-only, `/admin/people` rendering, `/calendar` regression). Phase 5 made no UI or Server Action changes.

---

## Codex Adversarial Review

SKIPPED — user opted out. The intended pass would have fact-checked the new wiki claims against actual code (does `/admin/people` exist? do the Add Person checkboxes match the description? are the legacy actions actually deleted?). Static-checks already exercise compilation + tests; doc drift is the only residual risk and it's low-stakes.

---

## What Phase 5 actually changed

- **`docs/wiki/auth.md`** — provisioning section rewritten to describe `/admin/people` and the Add Person dialog instead of `/admin/users`. Two-surfaces section updated to point at `createPerson`/`updatePerson` and the new `{ ok: true, warning }` partial-success contract.
- **`docs/wiki/lifecycle.md`** — two stale references updated (`/admin/users` → `/admin/people`; `users-admin.tsx` → `people-admin.tsx`; re-activation example pointing at `syncTeamMemberRoles` + `updatePerson` instead of the deleted `applyRoleSet`).
- **`docs/wiki/log.md`** — appended a dated entry summarising the 0020 ship: retired Server Actions, new code surface, partial-success / lifecycle / orphan-recovery details, commit chain (`82e3564` / `1053257` / `33bfc51` / `3dc6ec0`).
- **`docs/chunks/0020-people-unification/plan.md`** — Phase 5 status flipped to Done; checklist items ticked; Overall Progress flipped to 100%.

The chunk is ready to close (move folder to `closed/`, vacate `CURRENT.md` sub-plan slot, sweep cross-references).

---

## Action Required

### Must Fix (blockers)
*None.*

### Should Fix
*None.*

### Consider (carry-forward)
1. **Phase 3 UI follow-ups** — auto-uncheck role boxes when App access flips off; confirm dialog when disabling App access; expose raw `firstName`/`lastName` on `AdminPersonRow`. All flagged in Phase 3 eval; not blockers; small chunk's worth of polish.
2. **Test mock predicate-blindness** — same parked follow-up across 0018 + 0020.
3. **Stale `/admin/users` revalidation** — `revalidatePeopleViews()` still revalidates the redirected path. Cosmetic.
4. **`sendCoachShareLinkEmail` orphaned** — action exists, no UI caller. Decide: add to People row, or delete.

---

## Coverage Caveats

- **Codex skipped** for this phase — wiki fact-check deferred. Expected to be done at the next /eval cycle that touches `auth.md` or `lifecycle.md`.
- **Browser smoke skipped** for this phase — Phase 4 eval covered the post-retirement surface; Phase 5 added no UI.
