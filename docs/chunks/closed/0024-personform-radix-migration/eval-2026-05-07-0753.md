# Eval: 0024-personform-radix-migration (Phase 5 — chunk close)

**Plan:** docs/chunks/0024-personform-radix-migration/plan.md
**Branch:** main
**Date:** 2026-05-07

---

## Verdict: PASS with caveats

Phase 5 (Headless UI rip-out + smoke verification) is shippable. Static checks green post-removal, browser smoke confirmed both dialog call sites still work. Codex skipped — the diff is `pnpm remove @headlessui/react` + plan close-out edits, no code logic to adversarialize.

**Static checks:** tests 149/149 PASS · tsc PASS · lint PASS (4 pre-existing warnings, unchanged)
**Browser smoke:** Add Person dialog + Block Date dialog both render correctly post-removal
**Codex review:** SKIPPED — dependency-removal + plan-update phase has no code-logic surface

---

## Static Checks

```
pnpm test    → 15 files, 149 tests passed
pnpm tsc --noEmit → clean
pnpm lint    → 0 errors, 4 pre-existing warnings (unchanged)
```

`pnpm remove @headlessui/react` succeeded. `grep -rn "@headlessui" src/` returns no results — every consumer of the previous Headless UI primitives is now on Radix.

---

## Browser Smoke

Inject-supabase: success.

| Check | Result |
|-------|--------|
| `goto /admin/people` → People heading + `+ Add Person` button | PASS |
| Click `+ Add Person` → Radix Dialog opens with title, description, all form fields (First/Last name, Email, Phone, three Radix Checkbox role controls, Cancel + Add Person buttons) | PASS |
| Click `Cancel` → dialog closes | PASS |
| `goto /calendar` → Master Schedule + `Block Date` button | PASS |
| Click `Block Date` → Radix Dialog opens with "Block Out Dates" title + Reason field + Close ✕ | PASS |

Both dialogs are now Radix-only — no Headless UI fallback path remains. The wrapper API at `src/components/ui/dialog.tsx` is unchanged from the consumers' perspective.

---

## Codex Adversarial Review

Skipped intentionally. Phase 5's diff is `pnpm remove @headlessui/react` + plan close-out edits — there's no code-logic surface for an adversarial review to bite on. The substantive code changes were in Phases 1 (Dialog wrapper), 2 (Checkbox), and 3 (Combobox + Select), each of which had its own Codex pass with findings landed inline.

---

## Cross-Reference

No findings to cross-reference.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix
None.

### Consider
1. **Bundle-size measurement deferred** (Phase 5 checklist item). Worth running `pnpm build` and comparing route-bundle deltas if a build-comparison harness exists. Headless UI swap is roughly even; cmdk + Radix Select + Radix Popover added ~5–10 KB on the people-admin route.
2. **Radix Form adoption** (Phase 4 deferred). Revisit when the booking-intake form (parked at `0016`) lands — landing both forms' validation surfaces in one Radix Form pass is cleaner than retrofitting a single form first.

---

## Coverage Caveats

- **Codex skipped intentionally.** Dependency removal + plan-update phase. The verdict-table mapping says "PASS with caveat — Codex review skipped" rather than full clean PASS, hence the pause for approval per the build skill's policy.
- **Bundle-size baseline not captured.** Worth running before/after on a future polish chunk; not load-bearing for shipping the migration.
- **Other dialog call sites verified by code-trace.** Of the six consumers (`people-admin`, `orphan-auth-users`, `calendar-view`, `booking-form`, `lists/list-actions`, `production/row-actions`), this phase's smoke clicked two (`+ Add Person` and `Block Date`). The other four use the same `Dialog.{Root, Backdrop, Panel, Title, Description, Close}` API — type-checked + the wrapper preserves the shape, so visual regressions would have surfaced as TypeScript errors first.
