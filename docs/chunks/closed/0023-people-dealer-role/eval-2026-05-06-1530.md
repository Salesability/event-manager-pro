# Eval: 0023-people-dealer-role (Phase 2)

**Plan:** docs/chunks/0023-people-dealer-role/plan.md
**Branch:** main
**Date:** 2026-05-06

---

## Verdict: PASS with caveats

Phase 2 (backfill) is shippable. Static checks green (no app-code change), backfill applied to dev DB cleanly with idempotent verification, no truly-orphan contacts remained. Browser smoke and Codex skipped — Phase 2 is a one-shot data script, not user-visible code.

**Static checks:** tests 147/147 PASS · tsc PASS · lint PASS (4 pre-existing warnings, unchanged)
**Backfill apply:** 23 candidates → 23 rows inserted; re-run dry-run shows 0 candidates (idempotent)
**Browser smoke:** skipped — no UI surface
**Codex review:** skipped — backfill script's SQL is straightforward; idempotency proven empirically

---

## Static Checks

```
pnpm test    → 15 files, 147 tests passed
pnpm tsc --noEmit → clean (script TypeScript also clean — `pnpm dlx tsx` ran the file successfully)
pnpm lint    → 0 errors, 4 pre-existing warnings (unchanged)
```

The `is_staff_member()` regression guard from Phase 1's eval (`/auth/callback?dealer-only → 'Portal not yet available'`) still passes — confirms the 23 new dealer rows don't promote anyone to staff-app access.

---

## Backfill Apply

### Dry-run

```
Backfill candidates (dealer-link, no team_member_roles): 23
      6  Veronica Kennedy  (Century Honda)
      7  Tammy   (Century Hyundai Saint John)
      8  Kevin Moore  (Fairley and Stephens)
      9  Jordan Newcombe  (Ramsay Honda)
     10  Nick Lovegrove  (Myers GM Kanata)
     11  Wendy Grant  (Terra Nova Motors St. John's)
     12  Craig Marsh  (Terra Nova Motors GFW)
     13  Jeremy Dube  (Ron McGillary GM)
     14  Zack   (Century Mazda)
     15  Amanda Arbeau  (Trevors Nissan)
     16  Greg Armstrong  (Myers VW)
     17  Ryan Haynes  (Capital Ford)
     18  Chris Owens  (Capital Hyundai)
     19  Kevin Heyerhoff  (Hunt Club VW)
     20  Neal Noseworthy  (Fair Isle Ford)
     21  Mark Rogers  (Yarmouth Nissan)
     22  Browns VW  (Browns VW)
     23  Rebeccah White  (FairIsle Ford Montaque)
     24  Nick Heer  (D Alex MacDonald)
     25  Josh Gill  (Summerside Hyundai)
     26  Marco Russell  (Moncton Honda)
     27  Wallace Morey  (Steele Hyundai GFW)
     28  Sarah Cole  (Hickmans GM (St. John's))

No truly-orphan contacts (everyone has a role or a dealer link).
```

### Apply

```
Applied: 23 row(s) inserted.
```

### Post-apply verification

```
SELECT role, count(*) FROM team_member_roles WHERE archived_at IS NULL GROUP BY role;
  role  | count
--------+-------
 admin  |     3
 coach  |     5
 dealer |    23
```

Re-run dry-run after apply: `Backfill candidates: 0`. Idempotent — the partial unique index `team_member_roles_contact_id_role_unique` AND the candidate-filter (`NOT EXISTS team_member_roles row`) both prevent duplicate inserts.

**No truly-orphan contacts were found.** The Phase 5 invariant (every contact has a role) has nothing to triage on the dev DB — every row either has a `team_member_roles` row (us-side) or a `dealer_contacts` row that this backfill paired with `team_member_roles(role='dealer')`.

---

## Browser Smoke

Skipped — Phase 2 added no UI surface. Phase 3 already smoked the form's edit-mode default for `dealer` (visible via the `Dealer` checkbox + Dealers section gate) using the post-Phase-2 backfilled data this phase produced.

---

## Codex Adversarial Review

Skipped. The backfill script is a one-shot SELECT + INSERT-in-transaction with three knobs (dry-run/apply, NOT-EXISTS filter, single-tx commit). Idempotency is empirically verified by the post-apply dry-run. The SQL uses parameterized Drizzle's `sql\`...\`` template with no user-controlled values; no injection surface. Codex review on this kind of code typically finds style nits more than security bugs. Skipping.

If Phase 5's invariant lands as a DB trigger, Codex review of THAT migration is warranted — it touches every future `contacts` insert/update.

---

## Cross-Reference

The `is_staff_member()` filter from Phase 1's `0006_*.sql` migration is now load-bearing — without it, the 23 new dealer rows would have promoted those contacts to staff-app access. Tested via the existing `tests/integration/rls.test.ts` and the Phase 1 callback regression test.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix
None.

### Consider
1. **Production apply.** When this chunk ships to prod, run the backfill there too. Same dry-run-first discipline. Worth re-checking the truly-orphan list on prod data — dev's seed set has none, but prod could have legacy rows worth triaging before Phase 5's invariant lands.
2. **Phase 5 invariant decision.** With zero truly-orphan contacts, Phase 5's "trigger vs app-level" choice is purely about future-proofing. Trigger is the safer route now that the backfill is clean.

---

## Coverage Caveats

- **Production data not yet evaluated.** Dev DB has 23 candidates and zero orphans; prod data is unknown. Worth running `--dry-run` against prod (read-only) before committing to ship the apply.
- **No automated test of the script.** The script's SQL is reviewed-once + empirically-verified-twice (dry-run + apply + re-run dry-run). Adding a unit test would require a Postgres test fixture; deferred.
