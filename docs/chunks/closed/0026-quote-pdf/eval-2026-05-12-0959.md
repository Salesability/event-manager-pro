# Eval: 0026-quote-pdf Phase 3 — pass 2 (after rescue 2 of 2 + orchestrator follow-up)

**Plan:** docs/chunks/0026-quote-pdf/plan.md
**Branch:** main
**Date:** 2026-05-12 09:59

---

## Verdict: PASS with caveat (Codex re-review SKIPPED — retry budget exhausted; orchestrator caught the residual blocker via browser smoke)

After the second Codex rescue pass landed, browser smoke surfaced a Next.js RSC bundler constraint that neither tsc nor vitest catches: rescue-2 added `export const MAX_ADDRESS_LINES = 4;` to `actions.ts`, but `'use server'` files may only export async functions. The orchestrator applied a one-line direct fix (extract the constant to a new non-`'use server'` module `src/features/quotes/constants.ts`; both `actions.ts` and `actions.test.ts` re-import). Codex rescue retry budget (2 of 2) is exhausted; further adversarial review is informational only and the orchestrator skips it on this final pass per the build skill's loop-termination rule.

**Static checks:** test 666/667 PASS (1 skipped opt-in), tsc PASS, lint PASS (4 pre-existing warnings, no new)
**Browser smoke:** Phase 1 5/5 PASS, Phase 2 5/5 PASS, plus `/quotes/new` re-verified post-fix (Build Error cleared, composer renders 200)
**Codex review:** SKIPPED on this pass (retry budget exhausted) — earlier passes addressed 3 High + 5 Medium + 2 Low findings across the rescue loop

---

## Rescue-loop summary (across both Codex passes)

| Pass | Codex findings | Disposition |
|------|----------------|-------------|
| Initial (eval-2026-05-12-0912.md) | 2 High / 4 Medium / 2 Low | Rescue 1 |
| Re-review after rescue 1 | 1 High / 1 Medium / 1 Low | Rescue 2 |
| Re-review after rescue 2 | SKIPPED on this pass (retry budget exhausted; the rescue-2 fix shape was the architectural correction of the pass-1 design — transition-first, no upload-pre-write, no `ifGenerationMatch` in send path. Orchestrator caught the one residual Next-RSC constraint via browser smoke.) | — |

**Net code changes from rescue:**
- `sendQuote` reordered to render → atomic guarded UPDATE (with `updatedAt` predicate) → upload-after-success; degraded-state error on post-transition upload failure.
- `putObject` accepts `ifGenerationMatch` precondition; the API surface remains for future callers, `sendQuote` no longer uses it.
- `validatePersistedLines` fail-closed validator for the persisted `lineItems` jsonb shape (Medium 1).
- `MAX_LINE_ITEMS` lowered to 13 with y-position-aware test (Medium 2).
- `truncateToWidth` + `sanitizeWinAnsi` helpers for `clientName` / `clientAddress[*]` (Medium 3).
- `MAX_ADDRESS_LINES = 4` cap on the rendered Bill To address (pass-2 Medium).
- New `src/features/quotes/constants.ts` non-`'use server'` home for module-level constants (orchestrator residual fix for Next-RSC bundler constraint).
- 2 new vitest cases for the concurrent-edit race + GCS precondition-loser idempotent path; 1 new test for multi-line address truncation.

**Deferred** (no fix this phase):
- Medium 4 (`quote:send` separate from `quote:edit`) — plan body explicitly says `quote:edit`; carry-forward Phase 5 follow-up note added.
- Low 1 (`eventName='Sales Event'` placeholder) — already TODO'd inline.
- Pass-2 Low (concurrent-edit test predicate-blindness) — repo-wide test-mock-predicate-blindness limitation already parked under 0033 follow-up. Phase 5 follow-up note added.

---

## Static Checks

```
Test Files  28 passed (28)
     Tests  666 passed | 1 skipped (667)
```

`pnpm tsc --noEmit` — clean.

`pnpm lint` — 0 errors, 4 warnings (all pre-existing).

---

## Browser Smoke

Phase 1 (public) + Phase 2 (auth-injected gated): same checks as pass-1 (eval-2026-05-12-0912.md). All PASS.

Additional spot check on this pass:
- `goto /quotes/new` post-fix: status 200; composer renders "New Quote" heading + "Quote header" / "Inputs" / "Line items (computed)" sections. Build Error overlay cleared.

Pre-fix snapshot (the residual blocker the orchestrator caught) was status 500 with `Only async functions are allowed to be exported in a "use server" file.` against `actions.ts:442` — the direct extraction of `MAX_ADDRESS_LINES` to `constants.ts` resolved it.

---

## Cross-Reference

The orchestrator's residual fix is a class of bug that tsc + vitest cannot catch — `'use server'` constraint is enforced only by the Next.js bundler. Worth a separate parked follow-up: extend the action-gate ESLint plugin (0031) to also flag non-async `export const` / `export function` declarations in `'use server'` files. Out of scope here.

---

## Action Required

### Must Fix (blockers)

None remaining.

### Should Fix

None remaining (rescue loop closed High + actionable Mediums; deferred items documented).

### Consider

- Add an ESLint check in the action-gate plugin for `'use server'` files exporting non-async declarations. Carry-forward.

---

## Coverage Caveats

- Codex re-review skipped on this pass (retry budget exhausted at 2/2 rescue passes). Earlier passes covered the diff thoroughly; the orchestrator inspected the post-rescue-2 code shape directly before declaring the pass.
- The pass-2 Low (concurrent-edit test predicate-blindness) requires a repo-wide predicate-aware DB mock; tracked in the 0033 follow-up parked in CURRENT.md.
- Phase 4 will need a `sendQuote` retry path that handles "sent row with missing GCS object" (post-transition upload failure degraded state) — captured as a Phase 5 follow-up note in `plan.md`.
