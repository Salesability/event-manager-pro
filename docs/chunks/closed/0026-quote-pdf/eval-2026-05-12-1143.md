# Eval: 0026-quote-pdf — Phase 5 close-out

**Plan:** docs/chunks/0026-quote-pdf/plan.md
**Branch:** main
**Date:** 2026-05-12

---

## Verdict: PASS (with single-pass Codex; pass-2 deliberately skipped on doc-only diff)

Phase 5 is wiki + plan close-out only — no code touched. Static checks + browser smoke green; Codex pass-1 surfaced 1 High + 4 Medium + 2 Low factual drifts in the new wiki prose, all corrected directly against the source files. Codex pass-2 was deliberately skipped per new feedback memory `feedback_docs_eval_single_pass.md` — doc fact-check loops don't converge cheaply, and a thorough reviewer can always find one more thing in 80+ lines of new prose.

**Static checks:** PASS — 673 tests pass + 1 skipped; tsc clean; lint 0 errors (4 pre-existing warnings outside the 0026 surface).
**Browser smoke:** PASS — Phase 1 4/4, Phase 2 5/5 + composer page render confirmed.
**Codex review:** PASS-1 surfaced 7 factual drifts (1 H + 4 M + 2 L), all closed inline; pass-2 skipped (single-pass-on-prose policy).

---

## Static Checks

- `pnpm test`: 28 files, 673 passed, 1 skipped — clean.
- `pnpm tsc --noEmit`: clean (silent exit).
- `pnpm lint`: 0 errors, 4 warnings (pre-existing in `calendar-view.tsx` + `production-filters.tsx`; unrelated to 0026).

---

## Browser Smoke

### Phase 1 — Public surface

| Check | Result |
|-------|--------|
| `goto /` redirects to `/login?next=%2F` | PASS |
| `/login` renders Continue with Google + email + magic-link | PASS |
| `/calendar` (gated) redirects to `/login?next=%2Fcalendar` | PASS |
| `/share/coach/1` renders calendar grid + nav arrows | PASS |
| Month-nav `›` click on `/share/coach/1` (page intact post-click) | PASS — page didn't error, structure intact (accessibility tree summarizes month text, so visual verify done via screenshot) |
| `/auth/auth-error` 200 | PASS |

Screenshot: `/tmp/web-test-share.png`.

### Phase 2 — Auth-injected gated surface

`inject-supabase` as `david.hogan@networknode.ca`: success.

| Check | Result |
|-------|--------|
| `/calendar` heading + Block/+Book buttons + nav + coach filters | PASS |
| `/production` heading + filters (`All campaigns` / `Upcoming` / `Past`) + cancelled checkbox + table | PASS |
| `/dealerships` heading + status pills (Active/Prospect/Archived) + per-row `Quote` / `Edit` / `Remove` actions | PASS — Quote entry-point present per row (link `/quotes/new?dealerId={n}`) |
| `/quotes/new?dealerId=1` composer renders | PASS — Header / Inputs / Line items (computed) / Save Draft surfaces all present; computed `Base Event` line + Subtotal/Tax/Total visible |
| `/admin/lookups` Event Styles + Data Sources sections | PASS |

Screenshots: `/tmp/web-test-calendar.png`, `/tmp/web-test-quote-composer.png`.

**Out-of-surface for this phase:** no staff-side `acceptQuote` / `declineQuote` UI affordance to drive (deferred to a future chunk per Phase 4 reshape note); no `Send Quote` button on the composer (Send is the post-save action, not on the draft page). Both consistent with the plan's "future chunk" framing.

---

## Codex Adversarial Review

### Pass 1 — findings (all corrected)

**High**
- `docs/wiki/data-model.md` + `docs/wiki/log.md` — claim "inside `acceptQuote`'s transaction." `acceptQuote` is *not* wrapped in `db.transaction()` — `markQuoteAccepted` (standalone UPDATE), quote audit, dealer UPDATE, and dealer audit run sequentially. → **Fixed:** rewrote the data-model paragraph to describe the steps as sequential post-transition with idempotent-on-retry note; log entry got a matching parenthetical.

**Medium**
- `MAX_LINE_ITEMS = 25` claimed; actual is `13` in `src/lib/pdf/render-quote.ts:30`. → **Fixed in data-model.md + log.md.**
- Claim "audit payload carries both `pdfStorageKey` and the Resend `emailId`" on degraded send — actually the audit row is *only* written after a fully-successful send; degraded-state returns happen *before* the audit emit. → **Fixed:** rewrote the data-model paragraph to clarify the audit-row-on-success-only semantics and that degraded retries land on the idempotent `status='sent'` early-return path.
- Plan strikethrough said predicate-blindness follow-up "Already parked under 0033" — it's actually parked under 0018 follow-ups (`docs/chunks/CURRENT.md:13`). → **Fixed in plan.md.**
- `docs/wiki/commercial-spine.md` still described the dropped public-accept-link surface in three places: the bullet at the top, the happy-path ASCII diagram ("Client clicks accept"), and the expired-quote less-happy-path ("App-layer guard on the public accept route"). → **Fixed:** all three updated to point at the staff `acceptQuote` Server Action with a back-reference to the 0026 Phase 4 reshape rationale.
- `docs/wiki/architecture.md:102` still said `sendQuote` was a stub with PDF/GCS/email as TODOs. → **Fixed:** rewrote to describe the shipped flow (render → guarded UPDATE → GCS upload → React Email → `sendEmail` → audit), clarified the MSA-gate check itself remains the Phase 4 add.

**Low**
- `audience_source_id` "v1 sets at composer-time" overstated — `createQuote` writes only `dealerId` + `inputs` + computed totals; the column stays null. → **Fixed:** walked back to "Not yet populated by the v1 composer."
- `toRendererLines` symbol pointer doesn't exist in the codebase — the parse/validate boundary is `validatePersistedLines` in `src/features/quotes/actions.ts:456`. → **Fixed in same edit that addressed MAX_LINE_ITEMS.**

### Pass 2 — skipped

Per the new feedback memory `feedback_docs_eval_single_pass.md`: doc-only diffs don't get a verification round. Codex pass-1 surfaced precise file:line findings; corrections were tight (drop the wrong claim, replace with the verified text from the source file); residual drift, if any, will be picked up incidentally when something else changes near it.

---

## Cross-Reference

No Codex findings overlapped with static-check failures or browser anomalies — static + browser both clean. The 7 Codex findings were all wiki-prose-vs-code drift, which lint/tsc/test won't catch on a doc-only diff. This is exactly the value Codex earns on a wiki close-out.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high/critical)
None — High closed inline.

### Consider (Codex medium/low)
None — all closed inline.

---

## Coverage Caveats

- **Codex pass-2 deliberately skipped** per single-pass-on-prose policy. If a future change near the new `### quotes` walkthrough surfaces residual drift the corrections introduced, it'll be picked up on that change's `/eval`.
- **End-to-end live send dev smoke not run** — needs real GCS bucket + Resend creds; deferred to a follow-up chunk per CURRENT.md "0026 follow-ups (d)."
- **No staff accept/decline UI surface** to drive in the browser — the action surface exists (`acceptQuote` / `declineQuote` Server Actions); the button affordance is a future chunk per the Phase 4 reshape note and CURRENT.md "0026 follow-ups (c)."
- Month-nav text-advance verified via post-click page integrity (no errors, structure intact) — the accessibility tree summarizes the month-text element; visual verification was via screenshot.
