# Eval: 0081-button-consolidation

**Plan:** docs/chunks/0081-button-consolidation/plan.md
**Branch:** main
**Date:** 2026-06-15

---

## Verdict: PASS with warnings

A pure UI refactor (buttons → shared Catalyst `Button`; green→brand-blue primary; new `compact`+`destructive` variants). Static all green, browser smoke all PASS, the one Codex Medium is a verified false positive.

**Static checks:** tests **1136/1136 PASS** (serial) · tsc **clean** · lint **0 new** (touched files: 4 warnings at BOTH base `ec9c064` and HEAD — all pre-existing; repo-wide 147 errors is the known stale-worktree + pre-existing `safeparse` noise in untouched files)
**Browser smoke:** **PASS** — 7/7 routes verified, both server-page submit buttons render at runtime, 0 console errors
**Codex review:** 0 High / **1 Medium (false positive — verified)** / 0 Low

---

## Static Checks

- `pnpm test --no-file-parallelism`: **1136 passed | 2 skipped (1138)**, 77 files. (Full-parallel `pnpm test` shows 4 failures — all `PostgresError (EMAXCONNSESSION) max clients reached` in `item-sync`/`quote-push` integration tests: the known/parked 0073 pooler-cap flake, unrelated to this chunk; passes serially.)
- `npx tsc --noEmit`: **clean.**
- `npx eslint <touched files>`: HEAD **4 problems (0 errors, 4 warnings)** == base `ec9c064` **4 problems (0 errors, 4 warnings)**. **0 new.** (The 4 are pre-existing: calendar-view `getCoachColor` useCallback, availability-admin RHF `watch` incompatible-library, dealers-admin `no-unused-expressions` ×2.)

---

## Browser Smoke

Auth injected as `david.hogan@networknode.ca` (sandbox `qppenapeguwevcheqwpz`). Read-only — no submit/email/delete clicked.

| Route | Result |
|-------|--------|
| `/calendar` — `+ Book Event` (brand-blue), `Block Date` (outline), `‹`/`›` nav, coach pills | PASS |
| `/admin/lookups` — `Add` (brand) ×2, `Rename` (outline), `Archive ✕` (soft-red destructive), `Refresh rates` | PASS |
| `/admin/quickbooks` — `Pull items` (brand, not green) + `Disconnect` (outline) | PASS |
| `/dealerships` — `+ Add Dealer` (outline), filter pills, row-action menus | PASS |
| `/quotes` — `New Quote`, filter pills, pagination | PASS |
| `/quotes/11` (server component) — `Push to QuickBooks` submit + `Preview PDF`/`Save Quote`/`Re-send` render at runtime | PASS |
| `/dealerships/1` (server component) — `Push to QuickBooks` submit + `Save` render at runtime | PASS |

Visual confirmation (screenshots): brand-blue solid primaries; soft-red destructive `✕` matching the inspiration image; semantic green Badge ("managed by QuickBooks") correctly left green.
Screenshots: `/tmp/web-test-0081-calendar.png`, `/tmp/web-test-0081-lookups.png`, `/tmp/web-test-0081-quote-detail.png`.

---

## Codex Adversarial Review

**High:** None.
**Low:** None.

**Medium (1) — RSC/client boundary for `<Button>` in the two migrated server components.** Codex hypothesized that `button.tsx` (no `'use client'`, renders Headless UI `Button`) used directly in server components (`quotes/[id]/page.tsx`, `dealerships/[id]/page.tsx`) "can fail Next/RSC compilation or runtime rendering," recommending `pnpm build`/`next dev` confirmation.

**Verdict: FALSE POSITIVE — verified two ways:**
1. The exact pattern already ships in production: `quotes/page.tsx` (server) renders `<Button href color="brand">`, and `quickbooks-admin.tsx` (server) renders the non-`href` `<Button type="submit">` branch inside a server-action form.
2. Live browser smoke this run: `/quotes/11` and `/dealerships/1` (both server components) rendered HTTP 200 with their `Push to QuickBooks` submit buttons present and **0 console errors**. (Standard RSC: a server component may render a client component; `forwardRef` + `TouchTarget` are server-safe, interactivity is delegated to the already-client-marked Headless `Button`.)

Codex also explicitly cleared: no submit-semantics loss, no `type` regressions, no conflicting variant props, sizing composition complete, no `<a download>` mis-conversion, no dead button constants, the removed `next/link` import valid.

---

## Cross-Reference

No Codex finding maps to a static-check failure or browser anomaly. The single Medium contradicts the live browser evidence (both server pages render clean).

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high/critical)
None.

### Consider (Codex medium/low)
None actionable — the lone Medium is a verified false positive (0 parked).

---

## Coverage Caveats

- Tests run serially to avoid the parked 0073 pooler-cap flake (the only failure mode under full parallelism; unrelated to this chunk).
- Read-only discipline: no submit/email/cancel/delete exercised on the gated surface (per web-test contract).
- Row-action overflow menus, table pagination, filter pills, the booking-form field-label text links, and the quote-composer inline `✕`/`Remove` were intentionally left un-migrated (documented exceptions in plan.md / decision.md).
