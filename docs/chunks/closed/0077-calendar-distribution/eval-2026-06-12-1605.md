# Eval: 0077-calendar-distribution

**Plan:** docs/chunks/0077-calendar-distribution/plan.md
**Branch:** main (committed directly; chunk = 14 commits `dc733e0`…`3aa1346` on top of phase-0/1 `3d805de`/`9295c07`)
**Date:** 2026-06-12

---

## Verdict: PASS with warnings

Booked campaigns project one-way into Google Calendar (keyless DWD, customer-safe event, best-effort status-driven sync). Static + browser pass; all Codex findings (1 High + 2 Medium + 1 Low, plus 1 High introduced by a fix) were **fixed in-cycle** — none parked. Caveats are coverage-only.

**Static checks:** test PASS (1101 non-integration green + every DB integration file green in isolation; tsc clean; 0 new lint on touched files). **Browser smoke:** PASS — `/calendar` renders clean (Master Schedule + 6 campaigns + filters, no console/network errors); event-detail dialog not browser-exercisable (caveat). **Codex review:** 0 unresolved (4 found + 1 fix-introduced → all 5 fixed in-cycle; final pass clean).

---

## Static Checks

- **`pnpm test`** — PASS. Non-integration suite (`--exclude tests/integration`): **1101 passed / 2 skipped (67 files)**. DB integration files each pass standalone: `calendar-sync` 9/9 (incl. 3 new branch tests), `dealer-push` 3/3, `quote-push` 3/3.
  - The **full-parallel** run flakes with `(EMAXCONNSESSION) max clients reached in session mode — pool_size: 15` on whichever DB files lose the connection race (hit `calendar-sync` AND the unrelated `dealer-push`). This is the **parked 0073 follow-up infra flake** — the running dev server holds sandbox session-pooler connections and the (now 5) DB integration files exceed the 15-client cap together. Not a code regression: every file is green in isolation.
- **`pnpm tsc --noEmit`** — PASS (clean).
- **`pnpm lint`** — PASS for this chunk. The repo carries a stable pre-existing baseline (147 errors incl. `quotes/actions.ts` safeparse — not this chunk). `eslint` scoped to all 10 touched files: **0 errors, 0 warnings**.

---

## Browser Smoke

**Phase 1 — public surface**

| Check | Result |
|-------|--------|
| `goto /` → `/login?next=%2F` (200) | PASS |
| `goto /calendar` (gated) → `/login?next=%2Fcalendar` | PASS |
| `goto /login` (200) | PASS |

**Phase 2 — auth-injected gated surface** (`inject-supabase david.hogan@networknode.ca`: success)

| Check | Result |
|-------|--------|
| `/calendar` "Master Schedule" + stats + Block Date / + Book Event + coach pills | PASS |
| `/calendar` renders 6 booked campaigns as ribbons (June 2026) | PASS (screenshot) |
| console (ERROR) after load | PASS — none |
| network (4xx/5xx) after load | PASS — none |
| event-detail **Calendar** badge + **Re-sync** button | SKIPPED — see caveat |

Screenshot: `/tmp/web-test-calendar-0077.png`.

---

## Codex Adversarial Review

Three passes (initial + fix re-review + final confirmation). **Net: 0 unresolved findings.**

**Initial review** — 1 High / 2 Medium / 1 Low, all in this chunk's new code:
- **High** — orphaned Google event if the guarded backfill UPDATE throws (event sent before durable link stored; outer catch didn't clean up). **Fixed** `ba4d3cb`: the create→backfill is wrapped so a DB exception (or lost race) `deleteEvent`s the created event.
- **Medium** — stale `gcal_event_id` (event deleted out-of-band) → `patchEvent` 404 → marked failed forever; re-sync re-patched the dead id. **Fixed** `ba4d3cb`: `GoogleCalendarError` now carries `status`; a 404/410 on patch clears the link and recreates → `resyncCampaign` is a real recovery path. Non-404 errors re-throw (no duplicate).
- **Medium** — `reconcileCampaignCalendar` could throw (DB read outside the try), violating its "never throws" contract. **Fixed** `ba4d3cb`: the read is now inside the try.
- **Low** — `resyncCampaign` reported a missing campaign as "not configured". **Fixed** `ba4d3cb`: distinct `'missing'` outcome → "Campaign not found."
- Correctly classified as **NOT issues**: UTC date math + EXCLUSIVE end, customer-safe description (no ops-field leak), Google-failure-never-fails-mutation, delete idempotency, coach-colour 1..11 range, NULL-safe coach-email join.

**Fix re-review** — confirmed 3 of 4 resolved; flagged **1 new High**: the stale-link clear was unguarded → a concurrent reconcile could null a *newer valid* link + duplicate. **Fixed** `3aa1346`: the clear is now a CAS (`WHERE id = ? AND gcal_event_id = <observed stale id>`); a lost race (0 rows) skips recreate and returns `synced`.

**Final pass** — High **RESOLVED**, no new control-flow bug (needsCreate state correct, post-clear backfill `IS NULL` still matches, no double-delete, non-404 still re-throws). Nothing actionable.

Coverage added for the fixes: 3 new integration cases (stale-link 404 self-heal → recreate; non-404 patch error → failed, no duplicate; missing campaign → `missing`).

---

## Cross-Reference

No Codex finding overlapped a static or browser failure (all findings were logic/robustness in new code, not type/test breaks). The EMAXCONNSESSION test flake is infra (the parked 0073 follow-up), not connected to any Codex finding.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high)
None — all fixed in-cycle (`ba4d3cb`, `3aa1346`).

### Consider (Codex medium/low — parked)
None — all Codex findings fixed in-cycle; nothing parked from this chunk.

---

## Coverage Caveats

- **Event-detail dialog not browser-exercisable.** The **Calendar** sync badge + **Re-sync** button live in the event-detail dialog, opened by clicking a campaign ribbon. Ribbons are absolutely-positioned overlay `<div>`s built in `drawRibbons` (not a11y-tree elements with roles), so the browse tool can't click one. The badge/button logic is covered by tsc + the rendered-state types + the integration suite. Verified the page itself renders clean.
- **Live Google round-trip not run here.** `GOOGLE_CALENDAR_*` env vars + ADC aren't configured in this dev environment, so `scripts/0077-calendar-smoke.ts` reports "Not configured" (structural pass only). The committed keyless client was validated end-to-end by the Phase-0 ad-hoc smoke; the productionised client awaits the owner setting the 3 env vars (go-live §4a). The reconcile orchestration is fully covered by the mocked integration suite.
- **Test full-parallel flake** (parked 0073 follow-up): run DB integration files isolated, or stop the dev server, or use `--no-file-parallelism` with the dev server down. Every test passes in isolation.
