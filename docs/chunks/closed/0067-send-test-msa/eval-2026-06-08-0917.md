# Eval: 0067-send-test-msa

**Plan:** docs/chunks/0067-send-test-msa/plan.md
**Branch:** main
**Date:** 2026-06-08

---

## Verdict: PASS with warnings

The admin **Send Test MSA** BoldSign-verification tool is clean across all three evaluators. Codex's single Medium (a pre-existing `APP_ENV` normalization mismatch that could make this very tool's prod smoke lie) was **fixed in-cycle** at `2c9a011`. The only residual is a documented best-effort caveat (whether BoldSign echoes `metaData` in the webhook payload).

**Static checks:** tests **964 passed / 2 skipped** · tsc **clean** · lint **0 problems** in the chunk's changed files
**Browser smoke:** **PASS — 3/3** (unauth redirect · gated page fields · nav dropdown)
**Codex review:** **1 Medium (fixed in-cycle), 0 High, 0 Low**

Diff scope: `origin/main..HEAD` = the 5 chunk commits (`7377653`, `85ebd48`, `61d4eab`, `1b78eab`, + the eval fix `2c9a011`).

---

## Static Checks

- **`pnpm test`** — PASS. 59 files, 964 passed / 2 skipped. New coverage: `sendTestMsa` action (7 cases), `testMsaFormSchema` (8 cases), webhook `metaData.test` guard, and the `isSandbox` normalization regression.
- **`pnpm tsc --noEmit`** — PASS (exit 0).
- **`pnpm lint`** — the chunk's 7 changed source files lint **clean (0 problems)** via scoped `eslint`. (Repo-wide pre-existing debt unchanged and unrelated.)

---

## Browser Smoke

Dev server (sandbox DB) auth-injected as `david.hogan@networknode.ca`. Plan-specific checks:

| Check | Result |
|-------|--------|
| unauth `goto /admin/send-test-msa` → `/login?next=%2Fadmin%2Fsend-test-msa` | **PASS** |
| auth'd `/admin/send-test-msa` — heading "Send Test MSA" + `Recipient` / `Signer name` / `Message (optional)` + `Send` | **PASS** (did not submit — read-only) |
| Admin nav dropdown lists "Send Test MSA" | **PASS** |

Screenshot: `/tmp/web-test-0067-send-test-msa.png`.

---

## Codex Adversarial Review

> **High** — none.
>
> **Medium** — `APP_ENV` normalization mismatch (`src/lib/boldsign/client.ts:153`): `decideSignerRedirect()` trims/lowercases `APP_ENV`, but `isSandbox` used the raw value — so `APP_ENV='Production'` / `' production '` would send to the real recipient yet mark the envelope `isSandbox=true`, a false "prod works" signal for the smoke. **→ FIXED IN-CYCLE at `2c9a011`**: deduped into a single `isProductionEnv()` helper used by both the redirect decision and the `isSandbox` flag; added a regression test (`client.test.ts` — `' Production '` → `isSandbox=false`). Pre-existing (predates 0067; `client.ts` untouched by the feature) and doesn't bite our prod (`deploy.sh` sets `APP_ENV=production` exactly), but it directly undermines this tool's purpose, so fixed.
>
> **Low** — none.
>
> **Explicitly NOT an issue** (Codex-verified): coach can't reach the action (`capabilityClient('admin:access')`, `admin:access` denies non-admins); page/nav/middleware all admin-gated; gate-matrix row is `ADMIN_ONLY`; webhook `metaData.test` short-circuit runs only AFTER HMAC verification; real MSAs stamp `{msaId,quoteId}` (never `test:'true'`) so they're never masked; multi-recipient/CRLF rejected at the schema; `sendTestMsa` writes nothing to DB/GCS.

(Codex job `aff8db0df86a7e501`, 22k tokens, 142s — clean completion, no synthesis truncation.)

---

## Cross-Reference

The Codex Medium was a code finding (not a static/browser failure); it's been resolved + regression-tested, so no open cross-reference remains.

---

## Action Required

### Must Fix (blockers)
_None._

### Should Fix (Codex high/critical)
_None._

### Consider (Codex medium/low)
- ~~`APP_ENV` normalization mismatch~~ — **fixed in-cycle at `2c9a011`** (not parked).

---

## Coverage Caveats

- **Best-effort webhook guard.** The `metaData.test` short-circuit only fires if BoldSign actually echoes `metaData` in the webhook payload. Codex confirmed `SendForSign.metaData` exists in the SDK but couldn't prove webhook echo from in-repo docs; the route documents the fallback (a signed test envelope without echoed metadata falls back to the existing 404 — benign, BoldSign gives up after its retry window). Acceptable; verify against a real signed test envelope if/when one is signed.
- **Live prod send is a manual one-off** (plan Phase 4, last item) — the read-only browser smoke does not submit. The real prod envelope is sent by an admin from the deployed tool after deploy.
- Lint runs repo-wide and FAILs on pre-existing debt; verdict scopes lint to the chunk's changed files (0 problems).
