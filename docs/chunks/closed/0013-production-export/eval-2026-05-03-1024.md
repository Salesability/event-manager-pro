# Eval Smoke: 0013-production-export (Phase 5.6)

**Plan:** `docs/chunks/0013-production-export/plan.md`
**Branch:** main (uncommitted working tree)
**Date:** 2026-05-03

---

## Verdict: PASS with warnings

Phase 5.6 (Production export + print) is functionally complete and ready to commit. Static checks all green, browser smoke green on both phases, Codex turned up no blockers — only a medium-severity CSV-formula-injection note plus three low-severity polish items.

**Static checks:** tests PASS (35/35) · tsc PASS · lint PASS (4 pre-existing warnings, none from this chunk)
**Browser smoke:** PASS — Phase 1 (public, 6/6) + Phase 2 (gated, 6/6 + 2 dialogs + print PDF)
**Codex review:** 4 findings (0 critical · 0 high · 1 medium · 3 low) — none block

---

## Static Checks

- `pnpm test` → 35/35 passing
- `pnpm tsc --noEmit` → exit 0, no errors
- `pnpm lint` → 0 errors, 4 warnings — all pre-existing:
  - `calendar-view.tsx:121` `react-hooks/exhaustive-deps` (untouched by Phase 5.6)
  - `production-filters.tsx:30, 33, 36` `@typescript-eslint/no-unused-expressions` on the existing `pushParams` ternaries (untouched by Phase 5.6 — same code as before)

---

## Browser Smoke

### Phase 1 — Public surface

| Check | Result |
|-------|--------|
| `goto /` | 307 → `/login?next=%2F` ✅ |
| `goto /login` | 200, "Continue with Google" + email textbox + "Send magic link" ✅ |
| `goto /calendar` (gated) | 307 → `/login?next=%2Fcalendar` ✅ |
| `goto /production/export?status=upcoming` (gated, NEW) | 307 → `/login?status=upcoming&next=%2Fproduction%2Fexport` ✅ |
| `goto /share/coach/1` | 200, "Schedule" heading, calendar grid + ribbons (Century Mazda, Capital Ford, Capital Hyundai, Fair Isle Ford) ✅ |
| Month nav `›` on share page | "May 2026" → "June 2026" ✅ |
| `goto /auth/auth-error` | 200 ✅ |
| Console / network across all of the above | clean — no errors, no unexpected 4xx/5xx ✅ |

Screenshot: `/tmp/eval-smoke-share.png` (saved at 10:23 local).

### Phase 2 — Auth-injected gated surface

`inject-supabase david.hogan@networknode.ca` → success, cookies set: `sb-qppenapeguwevcheqwpz-auth-token.{0,1}`.

| Check | Result |
|-------|--------|
| `goto /calendar` | 200, "Master Schedule" + stats (9 booked / 42 total / 4 coaches / 26 dealers) + Block Date / + Book Event / coach filter ✅ |
| `goto /production` | 200, "Production List" + filters + search + 26 rows w/ View/Edit + **`⬇ Export CSV` link + `🖨 Print` button (Phase 5.6 NEW)** ✅ |
| `goto /production/export?status=upcoming` (auth) | Playwright errored with `Download is starting` — proves the route returned 200 + `Content-Disposition: attachment` ✅ |
| `goto /lists` | 200, "Manage Lists" + "🏢 Dealerships 26" + "🎯 Sales Coaches 5" + Email link ✅ |
| `goto /admin/lookups` | 200, "Lookup Admin" + Event Styles + Data Sources + Add/Rename ✅ |
| Click `View` on a Production row | `dialog "Campaign Detail"` w/ `Email Client` / `Email Coach` / `Cancel Campaign` (clicked via JS — ref-resolution had multiple "View" matches) ✅ |
| Click `Block Date` on `/calendar` | `dialog "Block Out Dates"` w/ Start Date* / End Date / Statutory Holiday / Company Closure / Coach Unavailable / Add Block ✅ |
| **Print stylesheet (Phase 5.6 NEW)** — PDF rendered with print media | `/tmp/eval-smoke-production-print.pdf` (4 pp, 194 KB): "Production List" heading retained · **no app header** · **no filter row** · **no View/Edit action column** · table flows cleanly with `Upcoming` badges intact ✅ |
| Console / network across all of the above | clean ✅ |

Screenshots: `/tmp/eval-smoke-event-detail.png`, `/tmp/eval-smoke-block-date.png`, `/tmp/eval-smoke-production-print.pdf`.

**Print PDF caveat:** the table is still slightly wider than the page can fit even after `print:text-[10px]` + `print:min-w-0` — right-most columns ("LETTE…") clip. Printable, but a future iteration could tighten column widths or switch to landscape.

---

## Codex Adversarial Review

```
**Critical**

None.

**High**

None.

**Medium**

CSV formula injection is not mitigated: `src/app/(app)/production/export/route.ts:33-47` and `:66-67`. Quoting handles CSV syntax, but Excel/Sheets can still treat cells starting with `=`, `+`, `-`, or `@` as formulas; dealer/contact/notes are user-influenced fields, so this is a real low-friction risk.
Suggested fix: Prefix formula-looking cell values with `'` or `\t` inside `csvCell`, before quote escaping.

**Low**

Export materializes the full campaign table before filtering: `src/app/(app)/production/export/route.ts:28` calls `filterCampaigns(await loadCampaigns(), ...)`; `src/features/schedule/queries.ts:329-361` has no `where`/`limit`. Matches the page's pattern, but the export builds `rows`, `lines`, and the full CSV string in memory.
Suggested fix: Push status/date/search filters into Drizzle and stream CSV rows if data can grow materially.

Toasts and open dialogs can leak into print output: `src/app/(app)/layout.tsx:16` renders `<Toaster />`; `src/components/ui/dialog.tsx:45,65` uses `fixed inset-0 z-40/z-50`. The new `print:hidden` covers header/filters/row actions, but global portals are not hidden when visible at print time.
Suggested fix: Add print-hide styling to the toaster/dialog portal/backdrop/panel classes, or add a global `@media print` rule for known overlay containers.

Export link can lag the visible search input: `src/app/(app)/production/production-filters.tsx:44-58` debounces `setTimeout(() => pushParams({ q: value }), 250)` while `exportHref` uses current `params.toString()`. If a user types then immediately clicks Export CSV, the href reflects the previous query.
Suggested fix: Build `exportHref` from local `q` plus current `status`/`cancelled`, or flush the pending debounce before export.

**Not An Issue**

- Auth bypass: `src/proxy.ts:7-10` matches `/production/export`; `src/lib/supabase/middleware.ts:5,32-36` redirects non-public unauthenticated paths.
- Header injection: filename comes from `todayIso()` (`filter.ts:9-11`), numeric only.
- CSV newlines: `route.ts:54` joins with `\r\n` and `csvCell` quotes/doubles quotes — embedded newlines survive.
- Filter divergence: page (`page.tsx:31`) and route (`route.ts:28`) both call shared `filterCampaigns` (`filter.ts:14-32`).
- Route group matching: `(app)` is not in the URL — `/production/export` is matched by middleware.
- Status date comparisons: ISO `YYYY-MM-DD` string comparisons in `filter.ts:18-19` and `route.ts:77-78` are chronological for valid date-only strings.
```

---

## Cross-Reference

None — no Codex finding maps to a static-check failure or browser anomaly. The medium CSV-injection note is theoretical, not observed in the smoke.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex medium)
1. **CSV formula injection** in `src/app/(app)/production/export/route.ts:66-67` — prefix cells beginning with `=` / `+` / `-` / `@` with a leading `'` (or `\t`) inside `csvCell` before the quote-and-escape step. Dealer name, contact, and notes are user-influenced; a malicious or accidental `=HYPERLINK(…)` in a dealer name would execute when the CSV opens in Excel.

### Consider (Codex low / smoke note)
1. Print-mode leak for `Toaster` / `Dialog` portals — add `@media print { .sonner-toast, [data-sonner-toaster], [role="dialog"] { display: none !important; } }` to `globals.css` so a stray open toast or dialog doesn't print.
2. Debounce vs export link race — build `exportHref` from the local `q` state plus URL `status`/`cancelled` rather than `params.toString()`, so a freshly-typed search includes in the export.
3. PDF column overflow — table still clips right-most columns even with `print:text-[10px]`. Either tighten column widths under `@media print` or add `@page { size: A4 landscape; }`.
4. `loadCampaigns()` has no LIMIT — fine at 42 campaigns, worth revisiting if the dataset grows past a few thousand.

---

## Coverage Caveats

- Email-send buttons (`Email Client` / `Email Coach` / `Email link`) verified as *rendered*, not *clicked* — would otherwise trigger real Resend sends.
- Destructive flows (`Cancel Campaign`, `✕` deletes, `Add Block` submission) not exercised — read-only smoke.
- Authenticated CSV download not byte-verified — Playwright treats `Content-Disposition: attachment` as a download and aborts the navigation; the abort itself is the success signal. A `curl` with extracted cookies could verify byte content if needed.
- Print stylesheet verified via Playwright's `pdf` (which uses print media), not via an actual `window.print()` flow. The PDF rendering is a faithful proxy for what the user would see in the print preview.
