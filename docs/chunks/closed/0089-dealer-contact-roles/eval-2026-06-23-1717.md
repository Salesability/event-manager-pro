# Eval: 0089-dealer-contact-roles

**Plan:** docs/chunks/0089-dealer-contact-roles/plan.md
**Branch:** main
**Date:** 2026-06-23

---

## Verdict: PASS with warnings

The `role` enum → `is_primary` rationalization is shippable. Static clean (tsc, 0 new lint, all tests green serially), browser smoke confirms the new People-admin model renders + the dealers pages are unaffected, and Codex's 2 Medium + 1 Low (all real in-chunk regressions) were fixed in-cycle at `c5b3b25`.

**Static checks:** tsc PASS (clean) · tests PASS (1164 unit + 59 integration serial; parallel hits the parked `EMAXCONNSESSION` pooler-cap flake) · lint PASS (0 new on chunk files; 2 pre-existing `safeparse-required` errors in `quotes/actions.ts` on `createQuote`/`setQuoteInputs` — untouched by 0089)
**Browser smoke:** PASS — People-admin new model verified; `/dealerships` + dealer-detail unaffected; 1 coverage caveat (link-row Primary checkbox not clickable — strict-mode collision)
**Codex review:** 0 High / 2 Medium / 1 Low — **all fixed in-cycle (`c5b3b25`)**

---

## Static Checks

- **tsc --noEmit:** clean.
- **tests:** `pnpm exec vitest run src/` → **1164 passed / 2 skipped**. `pnpm exec vitest run tests/integration/ --no-file-parallelism` → **14 files / 59 passed**. The full-parallel run shows `EMAXCONNSESSION (max clients reached, pool_size 15)` on the DB-gated integration files — the **documented parked pooler-cap flake** (the sandbox session pooler caps at 15 connections; integration files self-load `.env.local` and open a client each). Verified green serially.
- **lint:** 0 new on the chunk's files. The 2 `safeparse-required` errors flagged in `src/features/quotes/actions.ts` (`createQuote:240`, `setQuoteInputs:474`) are **pre-existing** — 0089's only change to that file is a 2-line comment; those functions were never touched (`git diff 8e1de71..HEAD` confirms).

---

## Browser Smoke

`inject-supabase`: success (david.hogan@networknode.ca).

| Check | Result |
|-------|--------|
| `/` → `/login?next=%2F`; `/login` renders Google + email + magic-link | PASS |
| `/admin/people` renders; **"Primary contact"** filter pill present (renamed from "Customer-side") | PASS |
| Dealer badges show `<dealer> · <title>` + `· primary` marker (e.g. "O'Regan's Nissan Halifax · Sales Manager · primary") | PASS |
| Click "Primary contact" filter → narrows to primary-only rows (48 shown, non-primaries hidden) | PASS |
| `/dealerships` commitment queue renders ("Prospect (188)", search, columns) | PASS |
| dealer detail (`/dealerships/1`) renders Details (Contact fields) + Pipeline + QuickBooks + MSA panels | PASS |
| console: no ERROR lines across visited pages | PASS |

Screenshots: `/tmp/web-test-people-primary-filter.png`, `/tmp/web-test-dealer-detail.png`.

Coverage caveat: the Add-Person → link-row **Primary** checkbox couldn't be exercised — the catalyst role/primary checkboxes have no distinct accessible name, so `getByRole('checkbox')` hits a strict-mode collision. The read side (badge + filter) is fully verified; the Primary checkbox is the same `Checkbox` primitive used (tsc-verified) elsewhere.

---

## Codex Adversarial Review

0 High. 2 Medium + 1 Low — all confirmed real and **fixed in-cycle** (`c5b3b25`):

1. **[Medium → fixed]** `scripts/test-db/seed-dev.sql:31` still `INSERT INTO dealer_contacts (..., role)` — raw SQL, so tsc missed it; would break `pnpm db:test:reset`/`seed` after migration `0044`. → changed to `is_primary`.
2. **[Medium → fixed]** Dropping `(dealer_id, contact_id, role)` unique left "one row per (dealer, contact)" unenforced — `.onConflictDoNothing()` in the import scripts lost its conflict target; concurrent `syncDealerLinks` could double-insert. → added partial-unique `dealer_contacts_dealer_contact_active_unique (dealer_id, contact_id) WHERE archived_at IS NULL` (schema + migration `0045`; verified 0 duplicate pairs on sandbox before adding).
3. **[Low → fixed]** Stale "customer recipient / first 'customer' contact" comment in `quotes/actions.ts` send-flow. → updated to the `is_primary` model.

Codex explicitly confirmed as **NOT issues**: recipient `desc(is_primary), asc(id)` ordering + emailless-primary skip + fail-closed; `0043` partial-unique name/predicate; backfill `DISTINCT ON` priority order; demote-before-promote ordering in `syncDealerLinks`; `updateDealer` no-duplicate-primary; the People `dealerId:<0|1>` wire encode/decode; `0044` drop ordering.

---

## Cross-Reference

Codex Medium #2 (missing unique) cross-checks with the `.onConflictDoNothing()` idempotency in the import scripts (`import-from-sheets`, `import-from-quickbooks`) — higher confidence; the in-cycle fix restores the conflict target those rely on.

---

## Action Required

### Must Fix (blockers)
None — all blockers cleared in-cycle.

### Should Fix (Codex high/critical)
None.

### Consider (Codex medium/low)
All three Codex findings fixed in-cycle at `c5b3b25` — none parked.

### Parked (pre-existing, out of scope)
- `createQuote` / `setQuoteInputs` in `src/features/quotes/actions.ts` lack a zod `safeParse` (the `safeparse-required` lint rule). Pre-existing; 0089 never touched those functions. Candidate for a future quote-validation hardening pass.

---

## Coverage Caveats

- Integration tests confirmed green **serially**; the parallel run hits the parked `EMAXCONNSESSION` sandbox-pooler-cap flake (not a regression).
- Add-Person link-row Primary checkbox not browser-clickable (unnamed-checkbox strict-mode collision) — read side verified.
- `seed-dev.sql` fix validated by inspection + schema match (the docker test-DB seed isn't exercised by the sandbox integration suite; `pnpm db:test:reset` needs docker).
- **Prod migrations not yet applied** — `0043`→`0044`→`0045` are sandbox-only. Apply to the prod DB (in order) before deploying the code. Precondition for `0045`: 0 duplicate non-archived `(dealer_id, contact_id)` pairs on prod (verify before applying).
