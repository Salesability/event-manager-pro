# Eval: 0113-sms-one-broadcast-guard

**Plan:** docs/chunks/0113-sms-one-broadcast-guard/plan.md
**Branch:** 0108-appointment-booking (chunk diff `0e855d1..HEAD`: commits `5edf46a`, `168d953`, `1152b97`, + in-cycle fix `0737128`)
**Date:** 2026-07-16

---

## Verdict: PASS with warnings

The one-broadcast gate ships clean: static checks green, targeted browser smoke 5/5, Codex found **no High or Medium** issues — its two Lows were both fixed in-cycle at `0737128` (with a new integration test covering the import gate).

**Static checks:** tsc PASS (clean) · unit tests PASS (95 files, 1409 passed / 2 skipped) · integration tests PASS (serial, 24 files, 106/106 — grew to 107 with the in-cycle import-gate test; sms-service 9/9 re-verified post-fix) · lint PASS for all chunk-touched files (0 findings; repo-wide 150 errors are pre-existing — stale `.claude/worktrees/` copy + parked 0089-a safeparse + old scripts, none in this chunk)
**Browser smoke:** PASS — 5/5 targeted checks (plan Phase 3 smoke items, run 12:03–12:06 as the Phase 3 checklist step via `/web-test` + `scripts/0113-broadcast-guard-smoke.ts insert|cleanup`)
**Codex review:** 2 findings (both Low, both fixed in-cycle at `0737128`) + 5 explicit not-an-issue clearances

---

## Static Checks

All passed. Notes:
- `pnpm test` run split per the repo's pooler posture: unit suite parallel (1409 pass), `tests/integration/` serial (`--no-file-parallelism`, 106 pass at eval time; a first parallel full-suite run tripped the documented EMAXCONNSESSION sandbox-pooler flake — 53 XX000 FATAL failures, all integration, all pass serially).
- `pnpm lint` repo-wide count (150 errors / 102 warnings) is pre-existing noise: the stale `.claude/worktrees/` copy, parked 0089-a (`createQuote`/`setQuoteInputs` safeparse), scratchpad stub, old atlantic scripts. Chunk-touched files lint clean (`npx eslint` on all six → 0 problems).

---

## Browser Smoke

Targeted plan-specific checks (auth-injected via `inject-supabase`, fixtures from `scripts/0113-broadcast-guard-smoke.ts`):

| Check | Result |
|-------|--------|
| `/calendar/<fresh>/sms`: "Compose" section with textarea + `Launch send` button | PASS |
| `/calendar/<broadcast>/sms`: notice "This campaign's broadcast has gone out — see the send log below. One broadcast per campaign." | PASS |
| `/calendar/<broadcast>/sms`: no `Launch send` button anywhere | PASS |
| `/calendar/<broadcast>/sms`: `Import CSV` disabled (greyed) | PASS |
| Bonus: send log shows the fixture broadcast (1 delivered); funnel strip 1 sent / 1 delivered | PASS |

Screenshots: `/tmp/web-test-0113-fresh.png`, `/tmp/web-test-0113-broadcast.png`. Fixtures cleaned after the run.

---

## Codex Adversarial Review

Verbatim final output:

> **High**
>
> None found.
>
> **Medium**
>
> None found.
>
> **Low**
>
> **Client disables CSV import after broadcast, but the Server Action still permits it**
> src/features/sms/sms-panel.tsx:177, src/features/sms/actions.ts:113
> Why it breaks: the UI disables `Import CSV` when `alreadySent` is true and labels the recipient list as locked, but `importSmsRecipients` still takes the advisory lock and wholesale replaces `sms_recipients` without checking `campaignHasDispatchedSend`. A stale tab, scripted Server Action call, or client-side bypass can mutate the recipient list after a broadcast. This does not allow a second broadcast because `launchSmsSend` still refuses, and the send ledger snapshots remain intact, but it can make the post-broadcast page show a new pre-send review/list that was not the sent audience.
> Regression status: NEW 0113 UI/server consistency gap, not the accepted 0103-a 60s dispatch window.
>
> **Smoke cleanup is not fully marker-scoped**
> scripts/0113-broadcast-guard-smoke.ts:127, :134
> Why it breaks: cleanup finds fixture campaigns by `public_id`, but deletes `sms_messages` globally by `phone LIKE '+1999560%'` instead of deriving fixture `sendIds` and deleting only messages attached to those sends. An unrelated sandbox/prod row with that phone prefix would be deleted. FK order is otherwise correct for the rows this script inserts.
> Regression status: NEW fixture-script safety issue.
>
> **Not An Issue**
>
> The launch gate is inside the locked transaction (`lockCampaignSmsTx` then `campaignHasDispatchedSend(campaignId, tx)` before the 60-second guard; the helper accepts the transaction executor).
> The `campaignHasDispatchedSend` SQL shape is correct for the stated rule (join on `send_id`, filter `campaign_id`, `provider_sid IS NOT NULL`; the raw fragment interpolates a Drizzle column only, not user input).
> The commit-to-dispatch gap is pre-existing accepted posture, not a new 0113 regression (relaunch in the no-SID gap remains covered by the existing 60-second guard, matching the accepted 0103-a window).
> The UI/server stale-composer case is harmless for launch (a stale composer can submit; the Server Action rechecks inside the transaction and refuses before creating a new send).
> The new test mocks do not weaken the existing webhook route tests (the mocked session/assert/cache path is not used by the webhook `POST`; the mocked Twilio client only matters for outbound `sendSms`).

---

## Cross-Reference

No Codex finding overlaps a static-check failure or browser anomaly. Both Lows were code-inspection findings invisible to the smoke (the UI correctly disables the button; the cleanup path only misbehaves against non-fixture data).

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high/critical)
None.

### Consider (Codex medium/low)
1. ~~Import gate server-side~~ — **fixed in-cycle at `0737128`**: `importSmsRecipients` now refuses inside its locked transaction when `campaignHasDispatchedSend` is true, with a new integration test (`refuses a recipient re-import once the campaign has broadcast`) asserting the refusal and that the sent audience survives untouched. Wiki `sms.md` Flow §3 updated.
2. ~~Smoke cleanup phone-prefix sweep~~ — **fixed in-cycle at `0737128`**: cleanup now derives fixture `sendIds` from the marker-tagged campaigns and deletes `sms_messages` by `send_id`, never globally by phone prefix.

---

## Coverage Caveats

- The browser smoke ran the plan's targeted checks only (both sides of the composer swap), not the repo-wide default web-test table — appropriate for a two-surface UI change riding an already-smoked page.
- The smoke ran ~15 minutes before the in-cycle fix commit; the fix touches the import action's server path and the fixture script only — no rendered surface changed, so the smoke was not re-run.
- Full-parallel `pnpm test` remains flaky against the sandbox pooler (documented posture); the serial integration run is the authoritative result.
