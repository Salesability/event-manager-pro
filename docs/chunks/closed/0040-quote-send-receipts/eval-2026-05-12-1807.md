# Eval: 0040-quote-send-receipts — Phase 1

**Plan:** docs/chunks/0040-quote-send-receipts/plan.md
**Branch:** main
**Date:** 2026-05-12

---

## Verdict: PASS

Phase 1 (schema add `sent_to_email` + `sent_to_first_name` on `quotes`) ships clean. One Codex Low (non-monotonic journal `when`) caught + mechanically corrected in the same chunk.

**Static checks:** test PASS (687 passed, 1 skipped) / tsc PASS (clean) / lint PASS (0 errors, 6 pre-existing warnings unrelated to Phase 1)
**Browser smoke:** PASS — `/quotes` list + `/quotes/1` detail + `/calendar` render cleanly after the schema add
**Codex review:** 1 Low (resolved)

---

## Static Checks

- `pnpm test` — `Test Files 29 passed (29) | Tests 687 passed | 1 skipped (688)`
- `npx tsc --noEmit` — clean (no output)
- `pnpm lint` — `0 errors, 6 warnings` (all 6 pre-existing in `calendar-view.tsx` / `production-filters.tsx` / `quotes-filters.tsx` — not touched by Phase 1)

All passed.

---

## Browser Smoke

Pre-existing browse-tool session was already auth-injected; Phase 1 public-surface checks (cookie-clean redirects) skipped, gated regression checks driven directly.

| Check | Result |
|-------|--------|
| `/quotes` — heading "Quotes", status pills, search box, table with `View` action | PASS |
| `/quotes/1` (existing sent quote) — heading "Quote #1", Quote header + Inputs + Line items table render | PASS |
| `/calendar` — 200 | PASS |

Screenshot: `/tmp/web-test-quotes-1-p1.png` (composer-rendering quote-detail page, post-schema-add).

**Caveats:** Phase 1 public-surface table not re-driven (browse-tool session was auth-injected from a prior run; cookie eviction would have wiped the same context Phase 2 needed). The schema-only Phase 1 footprint doesn't touch any rendered surface, so default-regression coverage is sufficient.

---

## Codex Adversarial Review

**Low**

- Journal timestamp is not monotonic: `drizzle/meta/_journal.json:124-135` / `/tmp/eval-diff-0040-p1.patch:5-15`
  Why it matters: the plan explicitly calls for the quotes ADD COLUMN migration to follow the sibling `0011_slim_wonder_man.sql` pattern with a "monotonic journal `when` bump" (`docs/chunks/0040-quote-send-receipts/plan.md:21-22`). The new `0018_acoustic_cannonball` entry is appended after `0017_tranquil_living_mummy` but uses `1778623360497`, which is earlier than `0017`'s `1779552000000`. The migration filename and `idx` are monotonic, so this is metadata/order hygiene rather than bad SQL, but it violates the stated Drizzle migration convention and can confuse future migration chronology/debugging.
  Fix: update the `when` value for `0018_acoustic_cannonball` to a timestamp greater than `1779552000000`, ideally by regenerating the migration metadata or applying the same monotonic bump convention used by the prior entries.

**Resolution (in-chunk):** edited `drizzle/meta/_journal.json` entry 18 `when` → `1779638400000` (one day past `0017`'s `1779552000000`). Migration already applied to DB before the edit; `__drizzle_migrations` table tracks idx + tag (which were always monotonic), so the metadata-file `when` adjustment is cosmetic from the DB's POV.

---

## Cross-Reference

No Codex finding overlaps with a static-check failure or browser anomaly.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high/critical)
None.

### Consider (Codex medium/low)
1. ~~Journal `when` monotonicity for `0018_acoustic_cannonball`~~ — resolved in-chunk.

---

## Coverage Caveats

- Phase 1 public-surface table skipped (session reuse); not a regression risk for schema-only work.
- Phase 4/5 of this plan will exercise the real UI surface — keep an eye on `/quotes/[id]` smoke when that phase lands.
