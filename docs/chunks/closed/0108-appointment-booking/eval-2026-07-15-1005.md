# Eval: 0108-appointment-booking (pass 2 — final)

**Plan:** docs/chunks/0108-appointment-booking/plan.md
**Branch:** 0108-appointment-booking (chunk diff `a6ada38..HEAD`, tip `822c8ab`)
**Date:** 2026-07-15
**Prior pass:** [eval-2026-07-15-0926.md](eval-2026-07-15-0926.md)

---

## Verdict: RESIDUE CLEARED after 2 rescue passes — retry budget exhausted, paused for human close

Static and browser green throughout. Pass 1's four findings (1 High, 3 Medium) were fixed at `2a74f55`; the pass-2 re-review confirmed all four closed but surfaced two NEW Mediums (side-effects of the fixes), fixed at `822c8ab`. Both pass-2 fixes were verified by orchestrator diff review (they implement exactly the reviewer-prescribed change), tsc, and the integration suite including a new re-import assertion — but the chunk-end retry budget (2 Codex rescue passes) is now spent, so no third independent Codex pass ran. Per the build skill this is a pause, not an auto-close: the human either blesses the close on this evidence or asks for a third review pass.

**Static checks:** tests PASS (unit 1374/1374; integration 94/94 serial ×3 runs; bookings suite 5/5 incl. new assertions) · tsc PASS · lint PASS-for-chunk (0 findings in 0108 files; 150 pre-existing repo-noise errors)
**Browser smoke:** PASS — 8/8 checks pass 1; post-fix render re-verified (public page 200, greeting + 16-slot grid)
**Codex review:** pass 1 → 1 High + 3 Medium (all fixed `2a74f55`); pass 2 → 2 Medium (both fixed `822c8ab`); pass 3 not run (budget)

---

## Fix history

### Pass 1 findings → fixed at `2a74f55`
1. **(High) Re-import bypassed one-per-recipient** — `bookSlot`'s locked recheck now also matches a live appointment on `(campaign_id, phone)`; race-safe under the existing `booking_` advisory lock, no schema change. Integration test added (delete + reinsert same phone → `already-booked`).
2. **(Medium) Booking enable-able on non-booked campaigns** — `saveCampaignBookingSettings` now requires `campaign.status === 'booked'` (mirrors the SMS launch gate).
3. **(Medium) Cancelled campaigns still bookable** — `bookSlot` returns `event-ended` on cancelled; `loadBookingContext` reports `eventEnded: true` (public page shows the closed state). Integration test added.
4. **(Medium) Capacity-lowering race** — `bookSlot` re-reads `slot_capacity` inside the locked transaction; `saveCampaignBookingSettings` additionally takes the `booking_` lock (order `sms_launch_` → `booking_`; `bookSlot` takes only `booking_` — no cycle).

### Pass 2 findings → fixed at `822c8ab`
5. **(Medium) Booked state invisible after re-import** — `loadBookingContext.existingAppointment` now uses the same `recipient_id OR (campaign_id, phone)` predicate as `bookSlot`, so a re-imported customer sees their booking instead of the form. New integration assertion covers it.
6. **(Medium) Window-shrink race** — the in-transaction settings re-read now includes `day_start_minute`/`day_end_minute` and re-runs `isSlotInGrid` against the fresh window; `invalid-slot` if the slot fell outside.

---

## Verification of pass-2 fixes (in lieu of a third Codex pass)

- Diff review: both changes are literal implementations of the reviewer's prescribed fixes (same predicate reuse; fresh-window revalidation under the same lock the settings writer now holds).
- `npx tsc --noEmit` clean; unit 1374/1374; `tests/integration/bookings.test.ts` 5/5 serially, including the new re-import `existingAppointment` assertion.
- Post-fix browser render check: `/book/<fixture-token>` 200, greeting + grid (fixture seeded/cleaned via `scripts/0108-booking-smoke.ts`).

---

## Coverage Caveats

- No third independent Codex pass (retry budget). The two pass-2 fixes are small and test-covered, but formally unreviewed by a second model.
- Parallel `pnpm test` intermittently trips the shared-sandbox pooler cap (documented flake); serial integration runs are the authoritative gate and passed every time.
- Lint's 150 errors are pre-existing repo noise (stale `.claude/worktrees/` copy + legacy scripts); zero in 0108-touched files.
- Migrations `0054`/`0055` are applied to **sandbox only**; prod is still at `0048` — the whole `0103…0108` line ships together (prod migration before merge to `main`, per CURRENT.md).

## If closing

Auto-close steps per CLAUDE.md apply unchanged (move to `closed/`, sweep refs, shipped-log entry, CURRENT.md pointer → next chunk is parked `0109-sms-campaigns-tab`). No parked follow-ups from this eval — everything found was fixed in-cycle (`2a74f55`, `822c8ab`).
