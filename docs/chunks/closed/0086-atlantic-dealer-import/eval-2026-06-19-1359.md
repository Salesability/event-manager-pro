# Eval: 0086-atlantic-dealer-import

**Plan:** docs/chunks/0086-atlantic-dealer-import/plan.md
**Branch:** main (5 commits `4e5781e`..`c2b1186`)
**Date:** 2026-06-19

---

## Verdict: PASS with warnings

Phases 1–5 (decision gate + schema + push wiring + import script + sandbox run) are complete and verified on sandbox; the chunk is **code-complete**. Phase 6 (prod migration + prod import) is the owner-gated operational step and was **not** run. Static + browser smoke clean; Codex surfaced no blockers (2 by-design/out-of-scope Low observations).

**Static checks:** tsc PASS · tests **1182 pass / 2 skip** (serial) PASS · lint **0 new** (the ~253 raw problems are the pre-existing `.claude/worktrees/` stale-copy noise — none in 0086 files)
**Browser smoke:** PASS — `/dealerships` (Prospect (274) filter + imported rows) + `/dealerships/2172` detail (province "New Brunswick", status Prospect, Push-to-QuickBooks, 0 console errors)
**Codex review:** synthesis truncated (process died pre-flush — known pattern); reconstructed from the investigation log → 0 High / 0 Medium / 2 Low (both by-design / out-of-scope)

---

## Static Checks

- `npx tsc --noEmit` → clean (exit 0).
- `npx vitest run --no-file-parallelism` → 80 files, **1182 passed / 2 skipped**. (Parallel runs hit the parked `EMAXCONNSESSION` pooler-cap flake; serial is green — documented in CURRENT.md.)
  - New: `src/features/dealers/atlantic-import.test.ts` (13), `src/lib/quickbooks/dealer-push.test.ts` (+4 phone-preference).
- `pnpm lint` → no problems in any 0086-touched file. Raw total (253) is the pre-existing stale-worktree artifact (see `project_lint_stale_worktree_noise`).

---

## Browser Smoke

Auth-injected (sandbox ref `qppenapeguwevcheqwpz`).

| Check | Result |
|-------|--------|
| `/dealerships` renders 200, no console errors | PASS |
| "Active (80)" + **"Prospect (274)"** filter chips present | PASS |
| Imported prospect rows render (Acura of Moncton, Atlantic Acura, Acadia Toyota — name + GM contact + email + city + `prospect`) | PASS |
| `/dealerships/2172` (imported prospect detail, uses the changed `loadDealer`) renders: heading, province **New Brunswick**, status **Prospect**, QuickBooks/MSA/Quotes sections, 0 console errors | PASS |

Screenshots: `/tmp/web-test-dealerships.png`, `/tmp/web-test-dealerships-prospects.png`, `/tmp/web-test-0086-detail.png`.

Caveat: `dealers.phone`/`manufacturer`/`notes` are stored (DB-verified) + consumed by the QBO push, but not surfaced in the dealer list/detail UI (no roster UI in scope per the intent non-goals). The list's phone column shows the primary *contact* phone (null for imports → "—").

---

## Codex Adversarial Review

**SKIPPED-as-synthesis — reconstructed from the job log** (`task-mql89sve-k7eheq.log`). The codex-rescue subagent forwarded to the runtime and the runtime process exited at the investigation stage before flushing its final severity-classified block (the `feedback_codex_synthesis_truncation` pattern). Reconstructed from the captured assistant-reasoning messages:

**Confirmed NON-issues (false-positive reduction):**
- The contact + email-identifier insert is wrapped in a real transaction → the 0085 orphan-row guarantee holds (a contact can't exist without its email identifier, so a re-run dedups by email instead of duplicating).
- The inlined dedup uses **parameterized** Drizzle `sql`` fragments — no SQL-injection surface.
- `provinceOrNull`'s null-fallback won't fire for the current artifact (no empty/invalid province codes in the 281 rows).

**Low / informational (by-design, out of scope — no in-cycle fix):**
1. **No DB unique constraint on the dealer idempotency key** — name+address dedup is application-level (lookup), not a DB constraint, so concurrent runs aren't constraint-guarded. This is an explicit intent non-goal ("no DB uniqueness on name+address") and matches `src/features/dealers/dedup.ts` (0085). The import is a **single-operator, sequential, `max:1`** one-time run → no concurrency → not a blocker. **Parked as 0086-a.**
2. **Archived-contact reuse** — archiving a person doesn't archive its identifiers, so the import's active-identifier email dedup could link a new dealer to a contact whose `contacts` row is archived (identifier still active). Matches existing 0085 `findExistingContactByIdentifier` semantics; harmless for a prospect import (re-associates an existing person). **Parked as 0086-b.**

---

## Cross-Reference

None — no Codex finding maps to a static-check failure or browser anomaly. The idempotency property Codex probed (no DB constraint, lookup-protected) was independently confirmed green by the live sandbox re-run (0 inserts on the 2nd pass).

---

## Action Required

### Must Fix (blockers)
- None.

### Should Fix (Codex high/critical)
- None.

### Consider (Codex low — parked)
1. **0086-a** — add a DB partial-unique index on dealers `lower(name)+lower(coalesce(address,''))` if the dealer-create path ever becomes concurrent. Out of scope (intent non-goal; single-operator import). Un-park trigger: dealer creation becomes multi-writer / web-intake.
2. **0086-b** — decide whether contact dedup should exclude archived contacts (currently reuses any active identifier). Matches 0085 app-wide behavior; revisit app-wide, not per-import.

---

## Coverage Caveats

- **Phase 6 (prod) NOT run** — this eval gates the code only. The prod migration (`0041`) + prod import + the read-only `scripts/atlantic-overlap-probe.mjs` run are owner-gated (writes ~274 dealers/~451 contacts to prod). The city-only-address dedup caveat (D6) + prod-overlap handling (D7) need owner confirmation at that gate.
- Codex final synthesis truncated (reconstructed from log) — no severity block was emitted by the model; severities above are the orchestrator's classification of its captured reasoning.
- The 4k-line `scripts/data/atlantic-dealers.json` + auto-generated `drizzle/meta/*` were excluded from the Codex diff (data, not logic).
