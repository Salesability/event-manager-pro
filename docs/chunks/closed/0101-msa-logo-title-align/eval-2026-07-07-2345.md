# Eval: 0101-msa-logo-title-align

**Plan:** docs/chunks/0101-msa-logo-title-align/plan.md
**Branch:** main
**Date:** 2026-07-07
**Chunk diff:** `8465a25..HEAD` — `efae4e5` (P1 MSA logo) · `7709c56` (P2 quote logo) · `cd91ee7` (docs)

---

## Verdict: PASS with caveats

Pure-layout re-anchor of the MSA + Quote header logo (center it on the title's cap band instead of top-anchoring the taller logo box). Static all green, Codex found zero issues, and the "nothing below the masthead moves" invariant is independently proven three ways (byte-identical text layer, byte-identical BoldSign anchors, Codex arithmetic). The only caveats are non-regressions: no browser surface to smoke (server-side PDF renderer) and DB-integration tests excluded (env-gated sandbox pooler).

**Static checks:** tsc **CLEAN** · lint **CLEAN** (0 errors/warnings on both changed source files) · unit tests **1269 pass / 2 skip** (integration excluded — env-gated sandbox pooler, no DB path in this chunk)
**Browser smoke:** **SKIPPED — not applicable** (no UI route; both renderers are server-side `pdf-lib`). Substituted with a rendered-sample + `pdftotext` byte-identity proof (below).
**Codex review:** **0 findings** (0 High / 0 Medium / 0 Low) + 7 affirmatively-verified not-issues.

---

## Static Checks

- **tsc --noEmit:** clean.
- **eslint** (`render-msa.ts`, `render-quote.ts`): clean — 0 errors, 0 warnings.
- **vitest** (unit, `tests/integration/**` excluded): 76 files, **1269 passed / 2 skipped**. The two renderer test files (`render-msa.test.ts`, `render-quote.test.ts`) stay green with no edits — neither asserts the header-logo geometry (they hook the signature/initials anchor + text-field boxes, all unchanged).
- Integration excluded per project convention (sandbox pooler `EMAXCONNSESSION`/paused flake, environmental) — this chunk touches no DB path, so unit coverage is complete for it.

---

## Browser Smoke

**SKIPPED — not applicable.** This chunk changes only two server-side PDF renderers (`src/lib/pdf/render-msa.ts`, `render-quote.ts`); there is no rendered web surface for `web-test` to drive. The plan flags this as a "visual smoke (manual)" chunk.

Substituted verification (stronger than a route smoke for a layout-only move):
- **`pdftotext` byte-identity, all pages, before vs after** — MSA (4 pages) and Quote both `diff`-empty → text content, order, and pagination are unchanged; the move is provably visual-only.
- **Rendered samples eyeballed** (before/after side-by-side): the logo mark now centers on the title cap band instead of hanging below it, address block sits cleanly below, page-1 body unshifted. Artifacts: `scratchpad/msa-0101-sample.pdf`, `scratchpad/masthead-compare.png`, `scratchpad/quote-masthead-compare.png`.
- **MSA BoldSign anchors byte-identical** — `signatureAnchor` `{page4,x321,y174,w241,h22}`, `printedNameAnchor`, `titleAnchor` all match exactly before/after, so signed-field placement is unaffected.

---

## Codex Adversarial Review

0 findings across all severities. Verbatim not-issue confirmations:

**High:** None. **Medium:** None. **Low:** None.

**Not An Issue (verified):**
- **MSA anchor invariance** (`render-msa.ts:293`): `y=742`, `logoH=50`, new `logoBandBottom=692`, new `yRight=678`; pre-change `yRight = y - logoH - 14 = 678` — identical. Later right-column decrements unchanged → `yRight` still ends `634`; body still starts at `Math.min(yLeft,yRight)-22`.
- **MSA BoldSign anchors** (`render-msa.ts:465`, `600-619`): `underlineY` is downstream of the unchanged body flow; body start remains `612`, so `signatureAnchor`/`printedNameAnchor`/`titleAnchor` stay on the same coordinates.
- **Quote anchor invariance** (`render-quote.ts:196`): `yTop=742`, new `logoBandBottom=692`, new `yRight=678`; pre-change `yRight = yTop - logoH - 14 = 678` — identical. Merge point `Math.min(yLeft,yRight)-18` unchanged.
- **Variable ordering**: moving `titleSize`/`titleAscent`/`titleBaseline` above the logo draw introduces no redeclaration; single declaration set per renderer, still feeding `titleMidY`, the title `drawText`, and `yLeft` only.
- **No clipping**: MSA logo top `759.10 < 792`; Quote logo top `758.38 < 792`.
- **No horizontal collision**: logo `x=482.645`; MSA title right edge `404.728` (77.9pt gap); quote title right edge `134.144` (348.5pt gap).
- **Renderer consistency**: both compute title metrics first, keep `logoH=50`, compute `logoBandBottom` from the original top coordinate, draw at `titleMidY - logoH/2`, derive `yRight` from `logoBandBottom - 14`. Only intentional divergence is the existing title size/text (MSA 22pt "MASTER SERVICES AGREEMENT" vs quote 24pt "QUOTE").

---

## Cross-Reference

Codex's independently-computed values (`yRight=678`, body=612, logo tops <792, anchors unmoved) exactly match the orchestrator's own pre-eval checks (byte-identical `pdftotext` diff + byte-identical dumped `signatureAnchor`/`printedNameAnchor`/`titleAnchor`). Highest-confidence agreement on the one risk that mattered — the anchor-invariance claim.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high/critical)
None.

### Consider (Codex medium/low)
None.

---

## Coverage Caveats

- **Browser smoke N/A** — no web surface for a `pdf-lib` renderer; covered by the sample-PDF + `pdftotext` byte-identity proof instead. Not a regression.
- **Integration tests excluded** — env-gated sandbox pooler; this chunk has no DB path, so no coverage lost.
- **Owner visual eyeball** — the masthead alignment is a subjective/visual polish; the rendered samples (`scratchpad/msa-0101-sample.pdf`, `scratchpad/quote-*.pdf`) are for the owner to confirm the balance reads right on a real signed MSA. Owner-verify item, not a blocker.
- **Deploy** — no `MSA_TEMPLATE_VERSION` bump, no migration, no new secret. Ships on the next routine `main`→prod deploy (keyless CI); reaches customers on the next MSA/quote rendered after that deploy.
