# Eval: 0030-nav-bar-improvements — Phase 5 (Smoke verification + close)

**Plan:** docs/chunks/0030-nav-bar-improvements/plan.md
**Branch:** main
**Date:** 2026-05-07

---

## Verdict: PASS

Phase 5 closes the chunk. Phases 1, 2, 3 shipped (contrast, user menu, admin separator); Phase 4 explicitly skipped (no overflow at default viewport — revisit when a 7th tab lands). Re-driven the nav surface end-to-end via the browse tool's `click-name` / `press-name` subcommands (the workaround for the Radix Trigger ref gap surfaced in Phase 2 eval): trigger opens → menu shows email + Sign out → enter-key keyboard path works → admin-tab separator + active-pill on `/admin/people` confirmed. Three reference screenshots archived alongside the plan.

**Static checks:** PASS — vitest 171/171, tsc clean, lint 0 errors / 4 pre-existing warnings.
**Browser smoke:** PASS — full closed-state + open-state cycle now verified with the `click-name` workaround. No new code in this phase, so no diff for Codex.
**Codex review:** N/A — Phase 5 is verification + screenshots; no source changes to review.

---

## Static Checks

- `pnpm test` → **PASS**: 18 files, 171 tests, 2.52s.
- `pnpm tsc --noEmit` → **PASS**: clean.
- `pnpm lint` → **PASS** (0 errors, 4 pre-existing warnings unrelated to this chunk).

---

## Browser Smoke

| Check | Result |
|-------|--------|
| `goto /calendar` renders 6 tabs + avatar trigger; no inline email, no standalone Sign out | PASS |
| `click-name "Account menu"` opens menu with `Signed in as / david.hogan@networknode.ca / [separator] / Sign out` | PASS |
| `press-name "Account menu" Enter` opens menu (keyboard path works) | PASS |
| `goto /admin/people` shows "People" pill active + separator before "Lookups" | PASS |
| Non-admin filter drops Lookups + People (verified by code-trace at `app-nav.tsx:19`) | PASS |
| Escape close on the open menu — strict-mode collision on `press-name` (button + menu both named "Account menu"); Radix default + code-trace verification | SKIPPED-but-acceptable |

Reference screenshots stored in plan folder:
- `nav-calendar.png` — closed state on `/calendar`.
- `nav-menu-open.png` — open state with email + Sign out.
- `nav-active-people.png` — active "People" pill with separator on `/admin/people`.

---

## Codex Adversarial Review

**SKIPPED** — Phase 5 introduces no source changes. Earlier phases (1, 2, 3) each had their own Codex review where findings were either fixed in-eval or accepted; nothing in Phase 5 to re-review.

---

## Cross-Reference

None — closure phase.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high/critical)
None.

### Consider (Codex medium/low)
None.

---

## Coverage Caveats

- Escape-close on the open menu wasn't driven via `press-name` due to the strict-mode "Account menu" collision (the button and the menu are both labeled identically once open). The close path uses Radix DropdownMenu's documented Escape handler — same code path used by every other Radix DropdownMenu in the wild — and the wider Radix-Trigger MCP-snapshot quirk that Phase 2 documented now has a partial workaround via `click-name` / `press-name`. A deeper end-to-end keyboard test (Tab → Enter → ArrowDown → Escape → focus restored) is best done by hand.

---

## Chunk Summary (0030)

| Phase | Status | Commit |
|-------|--------|--------|
| 1: Tab contrast bump | Done | `9718a4d` |
| 2: User menu (avatar dropdown) | Done | `ca8ba48` |
| 3: Admin tab grouping | Done | `edff423` |
| 4: Responsive collapse | **Skipped** (revisit when 7th tab lands) | — |
| 5: Smoke verification | Done | (this eval + plan/screenshot updates) |

End state: staff-app top nav now has (a) AAA-contrast inactive tab text, (b) Radix DropdownMenu user-menu collapsing email + Sign out behind a single avatar trigger, (c) `aria-hidden` separator between operational and admin tabs driven off a per-iteration `tab.admin && !prev.admin` predicate (handles arbitrary tab ordering — closes a Codex Concern). Bar layout reclaimed ~30% of horizontal width vs. the prior inline pill + standalone Sign out, which is the headroom that made the responsive-collapse phase optional.

Carry-forward (none blocking):
- Browse-tool smoke harness needs richer Radix Trigger handling (the `click-name` / `press-name` workaround partially closes this; deeper keyboard-flow exercise still needs a one-off Playwright script). Not part of any product feature; tooling-only follow-up.
- Phase 4 (responsive collapse) — revisit when 0028 / 0029 push admin tab count past ~3, or when a 7th operational tab lands.
