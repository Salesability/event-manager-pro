# Eval: 0059-reports-inline-edit

**Plan:** docs/chunks/0059-reports-inline-edit/plan.md
**Branch:** main
**Date:** 2026-05-22

---

## Verdict: PASS with caveats

Admin-only inline billing-figure edits on `/reports` with an additive `billing_adjustments` overlay. Static all green; Codex affirmed the highest-risk item (SQL pivot) before its runtime truncated the final synthesis; browser smoke deferred (dev server down + migration pending). Shippable.

**Static checks:** tests 916 PASS / 2 skipped · tsc clean · lint 245 (identical to session baseline — 0 new) · capability-pairing 20/21 (only the pre-existing `msa:edit` gap remains)
**Browser smoke:** SKIPPED — dev server unreachable; also requires migration `0023` applied to the shared DB (`/reports` queries `billing_adjustments`)
**Codex review:** partial — runtime exited before flushing the severity-classified synthesis; captured findings affirming, no flagged issues; all focus areas independently verified

---

## Static Checks

- `pnpm test` → **916 passed**, 2 skipped (51 files). Includes 11 new `setBillingAdjustment` cases + the `action-gate-matrix` admin-only row. PASS.
- `pnpm tsc --noEmit` → clean, exit 0. PASS.
- `pnpm lint` → 245 problems — **identical to the session baseline**; the +2 introduced mid-chunk (a `safeparse-required` error + an unused-import warning) were fixed in `71d0b89` (zod `safeParse` in the action; dropped unused `Campaign` import). 0 net new. PASS (warnings = repo's stable pre-existing set).
- `pnpm check:capability-pairing` → 20/21. `reports:edit-billing` is paired (`useCan` in `<ReportsTabs>` ↔ `capabilityClient` in the action). The 1 remaining failure, `msa:edit` (`msa/actions.ts:120,189`), is **pre-existing** (confirmed against baseline) and out of 0059's scope — parked.

---

## Browser Smoke

SKIPPED — `curl localhost:3000` → 000 (dev server not running). Beyond that, `/reports` now reads `billing_adjustments`, which doesn't exist in the shared DB until migration `0023` is applied — so a meaningful smoke needs the deploy step first. Per eval rules, dev-server-unreachable → PASS with caveat. The editable-cell behavior (save/clear/effective display) is unit-covered (`actions.test.ts`).

---

## Codex Adversarial Review

Partial. The codex-companion process exited before writing its final severity-classified findings to its log (same runtime truncation seen on 0057's eval — a companion artifact, not a code signal). Captured intermediate findings before truncation:

- **SQL pivot is structurally sound — no fan-out.** "It groups by `campaign_id` before the report joins, and the report joins back on campaign id, so the aggregate loaders don't fan out." This was the highest-risk item (a LEFT JOIN multiplying rows would inflate sums/counts); the `UNIQUE(campaign_id, field)` constraint + `GROUP BY campaign_id` in `billingPivotSubquery` guarantee one row per campaign. ✓
- Codex was mid-pass on the action parser + the RLS/`is_staff_member` policy boundary when it exited — no issue surfaced in the captured output.

Independent verification of every prompted focus area (since the synthesis was lost):
- **Auth admin-only:** `reports:edit-billing` is a pure-admin capability (`capabilities.ts` deny-unless-admin bucket); the action is `capabilityClient('reports:edit-billing')`; `action-gate-matrix.test.ts` asserts `ADMIN_ONLY` across unauth/admin/staff/coach/viewer/dealer/orphan. A coach calling the action directly is redirected. ✓
- **Validation:** zod `billingAdjustmentSchema.safeParse(Object.fromEntries(formData))` (campaignId positive int, field enum, value string) + integer/0..1e9 range check; empty = clear. 11 unit cases. ✓
- **Migration:** journal `when` bumped to `1780156800000` (> 0022) so the `created_at < folderMillis` gate applies it; CHECK on field + value≥0; RLS mirrors `service_items_rls`. ✓
- **Original recoverability:** clearing DELETEs the adjustment row; campaign columns are never written. Tested. ✓
- **Concurrency:** per-cell upsert is last-write-wins — acceptable for an admin-only, low-contention numeric override (documented). ✓

---

## Cross-Reference

No static/browser failure to cross-reference. Codex's pivot affirmation aligns with the unique-constraint design intent.

---

## Action Required

### Must Fix (blockers)
_None._

### Should Fix (Codex high/critical)
_None captured._

### Consider (Codex medium/low)
_None captured._ (Synthesis truncated by the runtime; re-running was not pursued because the truncation is a known companion artifact, captured findings were affirming, and Codex findings never block the verdict.)

---

## Coverage Caveats

- **Browser smoke skipped** — dev server down; `/reports` also needs migration `0023` applied to the shared DB before it can render (it now queries `billing_adjustments`).
- **Migration not applied** — `0023` committed but must be run via `pnpm db:migrate` at deploy (same deferred-deploy posture as 0057's `0022`).
- **Codex synthesis truncated** — the companion runtime exited before flushing its final findings list; this eval relies on the captured partial findings + independent verification.
- **Effective-total SQL** (`coalesce(override, campaign.value)` in the aggregates) is verified by reading + the unique-constraint guarantee, not by a live-DB integration test (no DB in the unit harness).
- **Pre-existing, parked:** `pnpm check:capability-pairing` flags `msa:edit` (server-gated, no UI affordance) — predates 0059.
