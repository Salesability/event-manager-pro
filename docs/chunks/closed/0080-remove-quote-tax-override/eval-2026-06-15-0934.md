# Eval: 0080-remove-quote-tax-override

**Plan:** docs/chunks/0080-remove-quote-tax-override/plan.md
**Branch:** main (committed directly; chunk diff = `687fb37..HEAD`)
**Date:** 2026-06-15

---

## Verdict: PASS with warnings

The per-quote manual tax override is removed: composer Tax field display-only, the override write/compute path stripped from `pricing.ts` + `actions.ts`, `setQuoteTax` retired, the `tax_override` column kept-but-unused (Option A). Static + browser clean; Codex found 1 Medium + 1 Low — **both fixed in-cycle (`2c893f9`)**, 0 parked.

**Static checks:** tests 1136 PASS / 2 skipped (serial — full-parallel hits the parked 0073 pooler-cap flake) · tsc clean · lint **0 new for 0080** (the 247-problem total is the unchanged pre-existing stale `.claude/worktrees/` artifact; `safeparse-required` count 2 = pre-existing `createQuote`/`setQuoteInputs`).
**Browser smoke:** PASS — the composer Tax field renders with **no Override link**; auto-pill state a coverage caveat (no sandbox quote has a province-set dealer).
**Codex review:** 0 High / 1 Medium / 1 Low — both fixed in-cycle.

---

## Static Checks

- `pnpm test` (serial, `--no-file-parallelism`) → **1136 passed | 2 skipped** (77 files). Full-parallel run flaked 2 integration tests on `EMAXCONNSESSION` (sandbox session-pooler 15-client cap + running dev server) — the parked 0073 follow-up, not a 0080 regression.
- `npx tsc --noEmit` → **clean**.
- `pnpm lint` → 247 problems total = unchanged stale-worktree noise (`.claude/worktrees/epic-keller-0e9cb6/`); `safeparse-required` = 2 (pre-existing). **0 new from 0080** (mostly deletions). No 0080-touched file newly flagged.

---

## Browser Smoke

Auth-injected (`david.hogan@…`, sandbox `qppenapeguwevcheqwpz`). `/quotes/11`:

| Check | Result |
|-------|--------|
| Tax field renders | PASS |
| **No "Override" affordance** in the Tax field | PASS |
| No override `<input>` present | PASS |
| No console errors; read-only (no save/send) | PASS |

Screenshot: `/tmp/tax-0080-after.png`. **Coverage caveat:** quote 11's dealer has no province → the field shows the no-province hint (so the `auto · X%` pill state wasn't browser-exercised — **no sandbox quote has a province-set dealer**; verified instead by code + the `pricing` unit tests). Override removal is the same auto-branch edit, so its absence is covered.

---

## Codex Adversarial Review

0 High / 1 Medium / 1 Low. Both fixed in-cycle (`2c893f9`).

### Fixed in-cycle
- **Medium — historical `tax_override` not cleared on re-save.** `setQuoteInputs` / `setQuoteDealer` recomputed `tax`/`total` to auto but omitted `taxOverride` from the UPDATE, so a pre-0080 overridden quote kept its stale marker → the QB-push guard would keep rejecting it after a clean re-save. **Fixed:** both UPDATEs now set `taxOverride: null`, clearing the marker on any re-save (and draining the retained column toward all-null over time, easing the eventual drop).
- **Low — stale docs.** `architecture.md` (the `computePickedTotals(lines, taxOverride?)` signature + the `setQuoteTax` bullet) and an `actions.ts` header comment still referenced the deleted API. **Fixed:** updated both to the post-0080 shape.

### Explicitly NOT issues (Codex-confirmed)
- No dangling references to `parseTaxOverride` / `TAX_RE` / `setQuoteTax`; tsc clean.
- `computePickedTotals` live callers (composer preview + server persist) both updated to `{ ratePct }` — in lockstep.
- `createQuote` no longer writes `taxOverride`; new rows persist NULL.
- No render path (PDF / `sendQuote` / MSA) recomputes tax from `tax_override` — sent quotes keep their stored `tax`/`total` snapshot.
- The QB-push guard reading `taxOverride` is intentional for historical rows.
- Stale `tax: '0'` in tests is ignored at runtime (harmless).

---

## Cross-Reference

The Medium overlaps the chunk's own intent (re-saving a historical quote should land on the auto rate) — the in-cycle fix makes the override genuinely drain rather than linger as a push-blocking ghost. No static/browser overlap.

---

## Action Required

### Must Fix (blockers)
_None._

### Should Fix (Codex high/critical)
_None._

### Consider (Codex medium/low)
_Both addressed in-cycle (`2c893f9`)._

---

## Coverage Caveats

- **Auto-pill Tax state not browser-exercised** — no sandbox quote has a province-set dealer (so all render the no-province hint + $0 tax). Covered by code + `pricing` unit tests instead.
- **Lint signal** polluted by the pre-existing stale `.claude/worktrees/` worktree (not 0080).
- **No migration** — the `tax_override` column is retained (Option A); a future cleanup chunk drops it. The **prod data-impact count** (how many prod quotes carry a historical override) is still deferred (prod `gcloud` token expired) — informational only, doesn't gate this chunk.
- Sandbox-only verification; no prod change beyond a future code deploy (no schema migration this chunk).
