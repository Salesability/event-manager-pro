# Eval: 0098-production-feed-dealer-contact

**Plan:** docs/chunks/0098-production-feed-dealer-contact/plan.md
**Branch:** main
**Date:** 2026-07-07

---

## Verdict: PASS with warnings (+ browser caveat)

Adds the dealer **primary contact** (name/phone/email, the `is_primary` 0089 contact) + a **Notes** column (`campaigns.notes`) to the public production feed, deliberately un-redacting both while keeping the campaign's OWN booking contact + audience source redacted. Static clean; Codex found **0 High / 0 Medium / 1 Low** (a stale header comment — **fixed in-cycle `41f03fb`**) and affirmatively verified redaction, token-gating, dup-dealer handling, null handling, and CSV escaping. Live-feed 200-CSV smoke is a post-deploy owner-verify (sandbox DB paused).

**Static checks:** tests PASS (1220 unit / 2 skip; integration excluded — paused sandbox pooler, env-only) · tsc PASS (clean) · lint PASS (0 new lint on all 5 changed files)
**Browser smoke:** SKIPPED — backend CSV feed, no UI surface; local dev smoke blocked by the paused sandbox DB. Covered by the post-deploy live-prod feed smoke.
**Codex review:** 0 High / 0 Medium / 1 Low (fixed in-cycle) + 5 confirmed not-issues

---

## Static Checks

- **tsc `--noEmit`:** clean.
- **eslint (5 chunk files):** clean — 0 new lint.
- **Unit tests:** 1220 pass / 2 skip (co-located `src/**`). New/changed: `production-feed.test.ts` 7/7, `route.test.ts` 4/4.
- **Integration:** excluded — the `tests/integration/*` suite fails on `ENOTFOUND … postgres.qppenapeguwevcheqwpz` (paused sandbox). Code-only chunk, no DB schema/query-shape change beyond an additive read → orthogonal.

---

## Browser Smoke

SKIPPED. The change is a backend, token-gated CSV feed with no rendered UI. A local dev-server smoke would 500 at `loadCampaigns` (sandbox Supabase paused) — same environmental gap as 0096/0097. The meaningful check is the **live prod feed** 200-CSV after deploy: `GET /api/production-feed?token=<valid>` returns the header with `Contact,Contact Phone,Contact Email,Notes` and a dealer's primary contact populated. Owner-verify (done by Claude post-deploy — see the deploy note).

---

## Codex Adversarial Review

**High:** None.
**Medium:** None.

**Low (fixed in-cycle `41f03fb`)**

- `production-feed.ts:22` — the `FEED_HEADERS` doc comment still said "never add a PII/notes column here," contradicting the 0098 contract that appends `Contact`/`Contact Phone`/`Contact Email`/`Notes`. Risk: a future edit follows the stale warning. **Fixed:** comment rewritten to "dealer primary contact + Notes are surfaced deliberately (0098); do NOT add the campaign's OWN booking contact or audience source."

**Confirmed NOT issues (Codex-verified)**

- **Redaction holds** — the mapper (`production-feed.ts:60`) emits only dates, dealer/location/format/coach/volumes, `contact?.name/phone/email` (the DEALER primary contact), and `c.notes`. It never reads `c.contact`, `c.phone`, `c.email`, or the audience source.
- **Token-gated** — `loadDealerPrimaryContacts` is reached only after the token check rejects (`route.ts:47-48` gate → `:55` DB read). No unauthenticated DB work.
- **Duplicate dealer IDs safe** — the route may pass dup `dealerId`s (multiple campaigns per dealer), but `loadDealerPrimaryContacts` returns a `Map` keyed by dealerId and the mapper does `contacts.get(c.dealerId)`.
- **Null handling** — no dealer IDs → empty Map; missing identifier → `null`; mapper blanks via `?? ''`. No crash.
- **CSV escaping** — every new cell (incl. free-text `notes`) passes through `csvCell`, which prefixes formula-leading values (`= + - @`) and quote-escapes. Formula-injection safe in the consuming Sheet.

---

## Cross-Reference

No Codex finding overlaps a static failure. The sole Low was a doc/comment contradiction, now fixed. Integration failures are purely the paused-DB environment.

---

## Action Required

### Must Fix (blockers)
_None._

### Should Fix (Codex high/critical)
_None._

### Consider (Codex medium/low)
1. Stale `FEED_HEADERS` comment (Low) — **already fixed in-cycle `41f03fb`.**

---

## Coverage Caveats

- **Live 200-CSV NOT smoked pre-deploy** — sandbox Supabase paused. Verified post-deploy against the live prod feed (real DB): header carries the 4 new columns and a dealer's primary contact + notes populate. Same environmental gap as 0096/0097.
- **`notes` is now externally visible** — a deliberate, owner-requested un-redaction. Its confidentiality is mitigated Sheet-side by the IMPORTRANGE indirection (the feed token stays in a private source Sheet), not by the feed itself.
