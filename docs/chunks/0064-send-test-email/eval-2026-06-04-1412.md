# Eval: 0064-send-test-email

**Plan:** docs/chunks/0064-send-test-email/plan.md
**Branch:** deploy-prod-db-secret (0064 commits `95957ff..HEAD`; 3 unrelated deploy commits precede them)
**Date:** 2026-06-04

---

## Verdict: PASS

Admin "Send Test Email" tool is code-complete and verified: static green, browser smoke 4/4 GREEN (incl. unauth redirect + admin render + nav entry), Codex clean (no High/Medium); the 2 Codex Lows were **fixed in-cycle** at `02311ac`. The only open item is the **manual deliverability send** (a user/ops action, out of the automated gate).

**Static checks:** tests PASS (919 passed / 2 skipped), tsc PASS (clean), lint PASS for the chunk (0064 files lint-clean; full-repo `pnpm lint` red is a pre-existing baseline — see Coverage Caveats).
**Browser smoke:** PASS — 4/4 checks (unauth redirect, admin render, field/button presence, nav dropdown entry). Read-only respected (no send submitted).
**Codex review:** clean — 0 High, 0 Medium, 2 Low (both fixed in-cycle).

---

## Static Checks

- `pnpm test` → **919 passed | 2 skipped (54 files)**. Includes the new `sendTestEmail` action tests (`actions.test.ts`) and `testEmailFormSchema` tests (`test-email-schema.test.ts`).
- `npx tsc --noEmit` → **clean (exit 0)**.
- `pnpm lint` → repo-wide red (147 errors / 100 warnings), but **scoped lint on the 8 touched 0064 files is clean (exit 0)**. The repo-wide failures are the pre-existing `react-hooks/incompatible-library` baseline (RHF `watch()` + React Compiler in `services-admin.tsx`, `schedule/*`) — unchanged by this chunk.

---

## Browser Smoke

Dev server started for the run (Next 16.2.4 / Turbopack), auth injected as `david.hogan@networknode.ca` (sandbox DB `qppenapeguwevcheqwpz`).

| Check | Result |
|-------|--------|
| Unauth `goto /admin/send-test-email` → redirect to `/login?next=%2Fadmin%2Fsend-test-email` | PASS |
| Admin `goto /admin/send-test-email` → 200, `<h1>` "Send Test Email" + deliverability description | PASS |
| Compose form renders `To` (email, placeholder `you@example.com`) / `Subject` / `Body` (textarea) / `Send` button | PASS |
| Admin nav dropdown lists `Contacts` / `Lookups` / `Send Test Email` | PASS |
| Read-only discipline — form NOT submitted, `Send` NOT clicked | PASS |

Screenshot: `/tmp/web-test-send-test-email.png` (clean render — heading, description, three fields, "Plain text only." helper, brand-blue Send button).

---

## Codex Adversarial Review

0 High, 0 Medium. Two Lows, both **fixed in-cycle at `02311ac`**:

1. **Subject allowed CRLF / header-shaped values** (`test-email-schema.ts`). Admin could submit `Hi\r\nBcc: …`. Mitigated by admin-only gate + Resend's JSON API (not raw SMTP header concatenation), but hardened anyway: subject now rejected via `.refine(v => !/[\r\n]/.test(v))`.
2. **Recipient regex permitted comma-lists** (e.g. `a@x.com,b@y.com`) — violated the chunk's explicit "No bulk / multi-recipient send" non-goal at the validation layer. Hardened: char classes now exclude `,` and `;` (`/^[^\s@,;]+@[^\s@,;]+\.[^\s@,;]+$/`), enforcing a single mailbox.

Codex explicitly confirmed as **not issues**: non-admin action access gated (`capabilityClient('email:send')` → `assertCan`, admin-only); non-admin page access gated (page `assertCan` + `/admin` middleware prefix); the non-prod send-redirect gate preserved (reuses `sendEmail()`); CRLF-in-`to` already rejected by the original regex.

---

## Cross-Reference

No Codex finding overlaps a static or browser failure. The recipient/subject Lows were schema-only and are covered by new schema tests (`rejects a comma-separated recipient list`, `rejects a subject containing a line break`).

---

## Action Required

### Must Fix (blockers)
- None.

### Should Fix (Codex high/critical)
- None.

### Consider (Codex medium/low)
- None outstanding — both Lows fixed in-cycle at `02311ac`.

---

## Coverage Caveats

- **Manual deliverability send (out of gate, NOT run).** The chunk's headline purpose — confirm a real email reaches an inbox via the production path (right from-address/secret, message-id surfaced) — is a runtime/ops action a person performs. Code path is verified (action → `sendEmail()` → Resend, id surfaced) but no live send was triggered (web-test read-only discipline + sending a real email is outward-facing).
- **Repo-wide lint baseline is red** (pre-existing `react-hooks/incompatible-library` in `services-admin.tsx` / `schedule/*`). 0064 introduces zero new lint problems; scoped lint on the chunk's files is clean.
- **Branch context.** 0064 landed on `deploy-prod-db-secret` atop unrelated deploy work; Codex/synthesis scoped to the 0064-only diff (`f03a21d..HEAD`).
