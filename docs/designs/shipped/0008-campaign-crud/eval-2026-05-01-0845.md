# Eval Smoke — 2026-05-01

**Plan:** [`plan.md`](plan.md) (Phase 5.2 of the port migration)
**Branch:** `main` (commits `e729d9e`..`3f5b655`, 6 commits)
**Skill:** `/eval-smoke campaign-crud`

## Final verdict (post-fix)

**PASS with warnings + visual smoke pending.** Static checks all green; public-surface browser smoke clean; three Codex high findings either fixed or deferred to a future RBAC chunk; manual auth-gated smoke (Phase 6 of `plan.md`) is still owed.

| | |
|---|---|
| **Static checks** | tests **PASS** (35/35, +2 new bounds tests) · tsc **PASS** · lint **0 errors** |
| **Browser smoke (public surface)** | **PASS** (7/7 — `/`, `/login`, `/calendar` redirect, `/share/coach/1`, month nav, `/auth/auth-error`, console clean) |
| **Browser smoke (auth-gated)** | **PENDING** — no test-user fixture; manual smoke owed per Phase 6 of `plan.md` |
| **Codex review** | 3 high findings — 1 fixed, 2 deferred to RBAC chunk; 5 medium — 4 fixed, 1 deferred |

## Initial verdict (pre-fix)

**FAIL** on first run:
- 1 in-chunk lint error (`production-filters.tsx:22` set-state-in-effect, from commit `3f5b655`)
- 5 pre-existing lint errors in `.claude/tools/browse/` (unrelated to chunk)
- 3 high-severity Codex findings on auth and public-cache leakage

## Fixes applied this turn

| Finding | Source | Resolution |
|---|---|---|
| `production-filters.tsx:22` set-state-in-effect | Lint (in chunk) | Replaced `useEffect` + setState with React 19 "compare-prev-prop" pattern; status / showCancelled now derived directly from params (sync-URL inputs need no local state) |
| `.claude/tools/browse/` lint errors | Lint (out of chunk) | Added `.claude/tools/**` to `globalIgnores` in `eslint.config.mjs` — the browse tool ships its own TS build and isn't part of the Next.js app surface |
| Cancelled campaigns leak into `/share/coach/[id]` | Codex high #3 | Added `c.status !== 'cancelled'` filter in `src/app/share/coach/[id]/page.tsx` |
| `/share/coach/[id]` cache stale after mutation | Codex medium #5 | Added `revalidatePath('/share/coach/[id]', 'page')` to `revalidateCampaignViews()` |
| Cancel not a guarded transition | Codex medium #2 | `cancelCampaign` now requires `status IN ('draft','booked')`; returns "cannot be cancelled in its current state" otherwise |
| Numeric volume fields unbounded | Codex medium #4 | `parseCampaignInput` rejects negatives and values > `2_147_483_647` (Postgres signed-int ceiling); 2 new tests in `validators.test.ts` |

Commits: `c62bd52` (single squash of all 6 fixes).

## Phase 6 visual smoke — completed 2026-05-01

Drivable via the new `.claude/tools/browse inject-supabase` subcommand (admin `generateLink` → `verifyOtp` → `@supabase/ssr` `setSession` cookies → Playwright `addCookies`). All 7 steps from `plan.md:102-109` exercised end-to-end against the dev DB, all PASS:

1. **Book Event** — filled Capital Ford / 2026-05-22, dealer auto-fill populated Ryan Haynes / phone / email; counts went 9→10 / 42→43; ribbon rendered in the right slot.
2. **Day-click pre-fill** — clicked empty May 19 cell; modal opened with `2026-05-19` in Start Date.
3. **Ribbon → Detail → Edit → Save** — re-opened detail showed updated notes (`"…— edited"`).
4. **Cancel + Show-cancelled** — `confirm()` patched to true; ribbon disappeared from `/calendar`; reappeared on `/production?cancelled=1` after toggling the filter.
5. **Production View** — same Campaign Detail dialog rendered.
6. **Production Edit** — Edit Campaign dialog opened with values pre-filled.
7. **`endDate < startDate` validation** — UI computes endDate from `startDate + duration`, so the invalid state is unreachable through normal interaction; verified server-side guard by JS-tampering the hidden `endDate` input. Toast `"End date must be on or after start date."`, modal stayed open.

One smoke-test campaign (`Capital Ford 2026-05-22`, status `cancelled`) left in the dev DB as artifact. RBAC concerns (any-staff mutation by id, Codex high #1/#2) explicitly deferred — exercisable from this auth-gated surface but treated as design under "any signed-in user is staff" until the RBAC chunk lands.

## Deliberately deferred

- **Codex high #1, #2 — row-level auth on `updateCampaign` and `cancelCampaign`.** `docs/wiki/auth.md:96` explicitly notes that RBAC beyond "logged in vs not" doesn't exist yet and lands with a future user-table chunk. In the current "any signed-in user is staff" model these aren't bugs — they're the design. Filed for the RBAC chunk, not this one.
- **Codex medium #1 — calendar slot-pack clamp for events spilling outside the 42-cell grid.** Substantive change to `calendar-view.tsx` rendering logic. Scaffolded as its own chunk: [`docs/designs/shipped/0015-calendar-grid-clamp/plan.md`](../0015-calendar-grid-clamp/plan.md).
- **Codex medium #3 — verify `coachId` resolves to an active coach.** Small but adds a query; folds naturally into the RBAC chunk where coach roles get formalized.
- **4 lint warnings** in `production-filters.tsx` (`no-unused-expressions` ternaries-as-statements in `pushParams`). Stylistic, not blockers, pre-existing — not addressed.

## Coverage caveats

- **Auth-gated CRUD flows were not exercised.** The browse tool's `inject-auth` / `inject-token` exist but no test-user / session-injection fixture is wired up in this repo yet. Phase 6 of `plan.md` (lines 102–109) is the manual smoke owed: `+ Book Event` → save → ribbon, day-click → modal pre-filled, ribbon-click → detail → Edit → save, Cancel + Show-cancelled, Production row View / Edit, validation toast on `endDate < startDate`. Until a fixture lands, this report's "PASS" applies only to the public surface.
- The Codex high findings (#1, #2 — any-user mutation by id) live exactly in this auth-gated surface. Until RBAC is in, they can be exercised by a forged `FormData(id=...)` from any authenticated session — flag for the RBAC chunk's threat model.
- Browser screenshot of `/share/coach/1` was captured at `/tmp/eval-smoke-share.png` (June 2026 view, post-month-nav-click; ephemeral path — not committed).

## Codex "Not Issues" (verified clean)

- SQL injection — campaign queries use Drizzle `eq()` predicates only.
- Server Action auth entry — `createCampaign` / `updateCampaign` / `cancelCampaign` all call `requireUserId()` first.
- Mutation route handlers — only `src/app/auth/callback/route.ts` exists; no campaign mutation snuck into `src/app/**/route.ts`.
- Date validation — missing/malformed dates and `endDate < startDate` rejected.
- Soft-deleted dealers — excluded from `dealerId` validation.
- Auto-fill spoofing — server stores `contact`/`phone`/`email` as free-text; no hidden contact-id trust.
- Audit spoofing — `createdById` / `updatedById` come from session, not form.
- Production cancelled filter is server-side, not client-only.
