# Eval: 0022-saledayevents-logo-refresh

**Plan:** `docs/designs/shipped/0022-saledayevents-logo-refresh/plan.md`
**Branch:** main
**Date:** 2026-05-05

---

## Verdict: PASS

Logo asset swapped + renamed cleanly across all three call sites. No stale references in shipping code, no broken `next/image` sizing, no caching/middleware issues, Codex found nothing.

**Static checks:** tests 100/100 PASS, tsc clean, lint clean (4 pre-existing warnings, 0 errors).
**Browser smoke:** Phase 1 7/7 PASS, Phase 2 3/3 PASS, 0 skipped.
**Codex review:** 0 findings (critical/high/medium/low all empty).

---

## Static Checks

- `pnpm test` → 100/100 passing across 9 test files (475ms).
- `pnpm tsc --noEmit` → clean.
- `pnpm lint` → 0 errors, 4 pre-existing warnings (unchanged from baseline; in `calendar-view.tsx` and `production-filters.tsx`, untouched by this chunk).

---

## Browser Smoke

### Phase 1 — Public surface

| Check | Result |
|-------|--------|
| `goto /login` 200 + Continue with Google + email + magic-link rendered | PASS |
| `/login` `<img alt="SaleDay Events — Automotive Marketing">` present | PASS |
| `/login` HTML references `/_next/image?url=%2Fsaledayevents-logo.jpg` only (no `saleday-logo` hits) | PASS |
| `goto /share/coach/1` 200, navy header + "Personal schedule for Shannon Tilley" | PASS |
| `/share/coach/1` `<img>` with new logo alt; HTML references new asset only | PASS |
| `GET /saledayevents-logo.jpg` → 200 | PASS |
| `GET /saleday-logo.jpg` → 404 (old asset removed cleanly) | PASS |

Screenshots: `/tmp/web-test-login.png`, `/tmp/web-test-share.png`.

### Phase 2 — Auth-injected gated surface

`inject-supabase`: success (david.hogan@networknode.ca, sb-qppenapeguwevcheqwpz-auth-token cookies set).

| Check | Result |
|-------|--------|
| `goto /calendar` 200 | PASS |
| App header logo `<img alt="SaleDay Events — Automotive Marketing">` rendered | PASS |
| Master Schedule heading + Block Date / + Book Event buttons + nav (Calendar / Production List / Manage Lists / Lookups / People) | PASS |

Screenshot: `/tmp/web-test-calendar.png`.

---

## Codex Adversarial Review

**Critical / High / Medium / Low:** none.

**Checks That Passed:**
- Stale references: passed for shipping code. Wide grep found the new path only at `app-header.tsx:18`, `login/page.tsx:25`, and the coach share page. Old `saleday-logo` hits remain only in design notes (`docs/designs/CURRENT.md:5`, `plan.md:13`), not runtime code.
- Asset migration: passed. `public/saleday-logo.jpg` is gone; `public/saledayevents-logo.jpg` exists at 246×155.
- `next/image` sizing: passed. All three call sites still declare `width={246}` / `height={155}` and render with `h-10 w-auto` or `h-16 w-auto`, preserving aspect ratio.
- Caching/config: passed. `next.config.ts:4` has no `images`, `headers`, CDN, or cache override. `proxy.ts:10` explicitly excludes static image extensions from middleware.
- Accessibility: passed. The alt text remains semantically accurate for the stated wordmark/tagline at all three call sites.
- Out-of-scope drift: passed. Metadata remains unchanged in `layout.tsx:17`; favicon/OG/manifest files not touched or newly referenced.
- Stack-specific risks: no SQL, Supabase auth, Server Action, conflict handling, calendar packing, or route-handler boundary changes in this chunk.

**False positives considered:**
- The old filename in design docs is not a runtime stale reference. It's historical/planning context — though `plan.md:56` says "not referenced anywhere in the working tree," which is imprecise because the docs themselves mention it. Cosmetic, not a blocker.
- The metadata still says "SaleDay Events" in `layout.tsx:18`; matches the plan's explicit out-of-scope boundary, not a migration bug.

Codex overall verdict: ship.

---

## Cross-Reference

None — Codex returned zero findings, no overlap with static or browser anomalies (there were none).

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high/critical)
None.

### Consider (Codex medium/low)
None.

---

## Coverage Caveats

- DB query for "active coach id" denied — fell back to the conventional `/share/coach/1` from the default smoke recipe; coach 1 (Shannon Tilley) loaded successfully, so the share-page check landed on real data.
- Codex's note that the `plan.md` "no references in working tree" wording is imprecise (since docs reference the old name) is documentation-only; not worth a follow-up edit.
