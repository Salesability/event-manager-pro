# Eval Smoke: lookup-admin

**Plan:** `docs/designs/shipped/0012-lookup-admin/plan.md`
**Branch:** `main`
**Date:** 2026-05-01

---

## Verdict: PASS with caveats

Static checks passed and the public browser smoke passed; Codex adversarial review was skipped because the `codex:codex-rescue` subagent named by the skill is not available in this environment.

**Static checks:** `npm test` PASS, `npx tsc --noEmit` PASS, `npm run lint` PASS with 4 warnings
**Browser smoke:** PASS with caveats — 6/6 route/UI checks verified; console/network log inspection is not exposed by this browse CLI
**Codex review:** SKIPPED

---

## Static Checks

All required checks passed.

- `npm test`: PASS — 2 test files, 35 tests passed
- `npx tsc --noEmit`: PASS
- `npm run lint`: PASS with existing warnings:
  - `src/app/(app)/calendar/calendar-view.tsx:118` — `react-hooks/exhaustive-deps`
  - `src/app/(app)/production/production-filters.tsx:30` — `@typescript-eslint/no-unused-expressions`
  - `src/app/(app)/production/production-filters.tsx:33` — `@typescript-eslint/no-unused-expressions`
  - `src/app/(app)/production/production-filters.tsx:36` — `@typescript-eslint/no-unused-expressions`

---

## Browser Smoke

| Check | Result | Evidence |
|---|---:|---|
| `goto /` | PASS | HTTP `307`, `location: /login?next=%2F`; browser follows to login `200`. |
| `goto /login` | PASS | Snapshot shows `Continue with Google`, email textbox, and `Send magic link`. |
| `goto /calendar` | PASS | HTTP `307`, `location: /login?next=%2Fcalendar`. |
| `goto /admin/lookups` | PASS | HTTP `307`, `location: /login?next=%2Fadmin%2Flookups`. |
| `goto /share/coach/1` | PASS | HTTP/browser `200`; snapshot shows calendar grid and event labels for Shannon Tilley. |
| month-nav `›` on share page | PASS | Header advanced from `May 2026` to `June 2026`. |
| `goto /auth/auth-error` | PASS | Browser `200`; snapshot shows invalid/expired link message and retry link. |

Share-page screenshot: `/tmp/eval-smoke-share.png`

Console/network caveat: this browse CLI exposes `goto`, `snapshot`, `click`, `fill`, `screenshot`, `status`, and `stop`; it does not expose console or network inspection commands. HTTP spot checks showed the expected `200`/`307` statuses above.

---

## Codex Adversarial Review

SKIPPED — the skill requests `codex:codex-rescue`, but that subagent type is not available in this Codex environment.

---

## Cross-Reference

No Codex findings were available to cross-reference. No static-check failure overlapped with browser smoke.

---

## Action Required

### Must Fix (blockers)

None.

### Should Fix (Codex high/critical)

None; Codex review was skipped.

### Consider (Codex medium/low)

1. Clean up the existing lint warnings in `calendar-view.tsx` and `production-filters.tsx`.
2. Add an authenticated smoke fixture so Phase 5 CRUD flows can be exercised by this eval.

---

## Coverage Caveats

- Auth-gated routes were not exercised beyond redirect checks. The lookup admin CRUD flow, booking modal, event detail, and lists CRUD all require a signed-in session.
- Console and network log inspection were not available from the local browse CLI; HTTP status checks were used for route-level coverage.
- Codex adversarial review was skipped because `codex:codex-rescue` is unavailable here.
