# Eval: 0018-user-system (re-eval after fix commits)

**Plan:** docs/designs/0018-user-system/plan.md
**Branch:** main
**Date:** 2026-05-05 (second pass, ~13:41)

Prior report: [`eval-2026-05-05-1309.md`](eval-2026-05-05-1309.md). Two fix commits landed since: `b4e3b6a` (durable layout gate + TOCTOU on contact-link writes) and `9231bd8` (Route Handler bypass + admin bootstrap on callback).

---

## Verdict: **PASS with warnings**

All Codex Critical and High findings from prior passes are closed. Two known items remain as deliberate follow-ups (Medium: `createUser` partial-success after auth-side success; Low: test mock predicate-blindness). Browser smoke covers the public surface, the durable layout gate (verified by injecting David — no team_member_roles, no admin role — and confirming all four `(app)/*` routes plus `/production/export` redirect to `/auth/auth-error`). Admin-side surface (`/admin/users` heading + Add-user dialog tabs + per-row Roles/Link contact + coach auto-filter) still couldn't be exercised — the harness blocked `pnpm dlx tsx scripts/promote-admin.ts` as an unauthorized RBAC grant. Per the rubric this is "PASS with caveat".

**Static checks:** tests PASS (81/81), tsc PASS, lint PASS (0 errors / 4 pre-existing warnings).
**Browser smoke:** Phase 1 PASS (5/5 confirmed), Phase 2 PASS (5/5 gate redirects). Admin-side checks SKIPPED — promote-admin step blocked.
**Codex review:** 0 new findings on the final pass; 6 confirmations.

---

## Static Checks

```
pnpm test       → 81/81 (8 files)
pnpm tsc        → no output (clean)
pnpm lint       → 0 errors, 4 warnings (all pre-existing in calendar-view.tsx + production-filters.tsx; predate this chunk)
```

All passed.

---

## Browser Smoke

Dev server: started by the eval (was not running; killed at end). Auth-inject succeeded for `david.hogan@networknode.ca` (currently a non-admin with no `team_member_roles` row in the dev DB).

### Phase 1 — Public surface (revalidated)

| Check | Result |
|-------|--------|
| `/login` renders Continue with Google + email + magic-link | PASS |
| `/share/coach/1` 200 + grid + ribbons | PASS |
| `/auth/auth-error?reason=Account+not+provisioned` renders the new copy | PASS |
| Public surface unaffected by the new layout gate | PASS |

### Phase 2 — Durable staff-app gate (the new code)

| Check | Result |
|-------|--------|
| `/calendar` for non-admin / no-roles → redirects to `/auth/auth-error?reason=Account%20not%20provisioned` | PASS |
| `/admin/users` for non-admin → redirects to `/auth/auth-error` (admin gate intact) | PASS |
| `/lists` for non-admin / no-roles → redirects to `/auth/auth-error` | PASS |
| `/production` for non-admin / no-roles → redirects to `/auth/auth-error` | PASS |
| `/production/export?...` for non-admin / no-roles → redirects to `/auth/auth-error` (was the prior new-Critical CSV exfil path) | PASS |

Screenshot: `/tmp/web-test-gate.png`.

### Smoke gaps (carried forward from the first pass)

- `/admin/users` page rendering as admin (heading "Users", table, Add-user dialog tabs, per-row Roles + Link contact buttons) — NOT exercised.
- Coach auto-filter — NOT exercised (no coach user in dev DB to inject as).
- Real failed callback for an unprovisioned auth user — NOT directly exercised.

To clear, run `pnpm dlx tsx scripts/promote-admin.ts david.hogan@networknode.ca David Hogan` (creates contacts row + email identifier + admin team_member_roles + sets `app_metadata.role = 'admin'`) and re-inject the session.

---

## Codex Adversarial Review (final pass)

Codex returned 0 new findings and 6 confirmations:

- The bypass is closed for the only `(app)/*/route.ts` (`/production/export`) — `requireStaffAccess()` runs before any domain data load.
- The other Route Handler under `src/app/**/route.ts` is `/auth/callback`, which intentionally runs its own gate after `exchangeCodeForSession` (admin short-circuit at line 39-42, role check at 44-47, fallthrough to auth-error at 49-54).
- `requireStaffAccess` shape is correct: no-user → `/login`, admins via `app_metadata.role` → pass, otherwise membership-required. Next 16.2.4 docs confirm `redirect()` works in Route Handlers and throws control flow.
- `cache(loadCurrentMembership)` correctly dedupes the layout gate's read with the calendar page's `viewerCoachId` fetch.
- `getUser()` uses `supabase.auth.getUser()` (validates against the auth server) rather than trusting local session state — no stale-session bypass.
- All 0018 auth Server Actions are correctly gated (`requireAdmin` for createUser/linkUserToContact/deactivateUser/setUserRoles; public-by-design for the login actions).
- TOCTOU fix verified: both `linkUserToContact` (line 266-281) and `createUser` pick-mode (line 170-185) use the conditional `UPDATE … WHERE id/unlinked/unarchived RETURNING` pattern with zero-row conflict handling.

The 4 lint warnings predate this chunk; none in new 0018 code.

---

## Cross-Reference

Both prior-pass blockers (Critical: post-callback URL bypass; new Critical: Route Handler bypass; High: TOCTOU) are confirmed closed in this pass. Doc drift Lows from the prior pass also closed: `auth.md:78` rewritten, `auth.md:89` admin-pass behavior now actually mirrors what the doc claims.

---

## Action Required

### Must Fix (blockers)
*None.*

### Should Fix (Codex critical/high)
*None.*

### Consider (carried forward, not in scope of this pass)
1. **`createUser` partial-success handling** (Medium from prior pass). Current behavior: auth user is created before contact linkage and role sync; later error returns leave a durable `auth.users` row that the admin sees as "creation failed". Either compensate via `auth.admin.deleteUser` or surface explicit "auth user created but link failed — attach manually".
2. **Test mock predicate awareness** (Low from prior pass). Extend the Drizzle mock in `actions.test.ts` and `route.test.ts` to capture the predicate the production code ran with so tests fail when guards disappear.

---

## Coverage Caveats

- **Admin-side smoke (`/admin/users` rendered surface)** — still SKIPPED. Harness blocks `promote-admin` invocation. User can run it themselves to clear.
- **Coach auto-filter** — still SKIPPED. No coach user in dev DB.
- **Codex test execution** — Codex's sandbox couldn't run vitest due to a `/var/folders` temp-dir restriction, so Codex's pass was static-review-only. Local `pnpm test` ran clean (81/81).
