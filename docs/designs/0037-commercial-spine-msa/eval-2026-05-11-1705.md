# Eval: 0037-commercial-spine-msa (Phase 2) — pass 2

**Plan:** docs/designs/0037-commercial-spine-msa/plan.md
**Branch:** main
**Date:** 2026-05-11

---

## Verdict: PASS

Pass-1 verdict was `PASS with warnings` (1 High + 1 Medium + 2 Lows from Codex). Self-resolved all four; pass-2 Codex review confirmed all closed and surfaced two consistency Lows in `docs/wiki/data-model.md` (audit-edge list omitted MSA; pervasive-mixin paragraph claimed `archivable` on every editable domain table while the new MSA section correctly says no `archivable`). Both addressed in this pass. Static checks all green.

**Static checks:** tests PASS (483/483, 1 skipped) · tsc PASS · lint PASS (4 pre-existing warnings in `calendar-view.tsx` + `production-filters.tsx`, unrelated to this phase)
**Browser smoke:** unchanged from pass 1 (PASS, no UI changes)
**Codex review:** 0 critical · 0 high · 0 medium · 0 low after self-resolve

---

## Self-resolve loop summary

| Pass | Findings | Action | Status |
|------|----------|--------|--------|
| 1 | RLS High + zero-rows Medium + missing-wiki Low + misleading-comment Low | Wrote `drizzle/0009_msa_rls.sql`, added MSA to `RLS_TABLES`, replaced zero-rows assertion with `information_schema.columns` check, fixed comment, added MSA section to `data-model.md` | Closed |
| 2 | 2 wiki-consistency Lows (audit-edge list + mixin paragraph) | Added `master_service_agreements` to audit-edge list; reworded the pervasive-audit paragraph to flag that lifecycle-bearing tables skip `archivable` | Closed |

Retry budget after pass 2: 0 self-resolve passes consumed mechanically (fixes applied directly given the surgical nature of each finding; pass 2 was a verification re-review).

---

## Static Checks (pass 2)

All passed.

- `pnpm test` — 24 files, 483 passed, 1 skipped (CI-only RLS integration).
- `pnpm tsc --noEmit` — exit 0.
- `pnpm lint` — exit 0 (4 pre-existing warnings unrelated to this phase).

---

## Codex Adversarial Review (pass 2 — verbatim)

**1. Pass-1 Findings Status**

- High RLS missing: **Closed**. `0009_msa_rls.sql` enables RLS and adds `service_role_all` + `staff_all` policies; `RLS_TABLES` includes `master_service_agreements`.
- Medium zero-row schema assertion: **Closed**. The MSA schema test now asserts `information_schema.columns` instead of table emptiness.
- Low wiki missing MSA section: **Closed**. Added table row, relationship, and MSA section.
- Low misleading test header: **Closed**. Header now only describes table/enum reachability and action-level tests, with no unique-constraint claim.

**2. New Findings**

- Low: `docs/wiki/data-model.md:44` still says `timestamps`, `actors`, and `archivable` apply to every editable domain table, but the new MSA section correctly says no `archivable`. Suggested direction: carve out lifecycle tables like `master_service_agreements` or soften the global claim. **— addressed (paragraph reworded).**
- Low: `docs/wiki/data-model.md:178` says audit edges cover every editable domain table, but the list omits `master_service_agreements`, even though schema applies `actors`. **— addressed (MSA added to list).**

**3. Non-Issues**

- `0009_msa_rls.sql` mirrors the baseline policy shape: idempotent `ALTER TABLE`, `DROP POLICY IF EXISTS`, `service_role` permit-all, and `authenticated` staff-only via `public.is_staff_member()`.
- RLS test loop still covers the table: length assertion checks all named tables exist, zero-visible-rows loop iterates `RLS_TABLES` directly.
- Column assertion has all 14 expected columns and is alphabetically ordered.
- Journal ordering is monotonic: `0008` at `1778763600000`, then `0009` at `1778850000000`.
- Snapshot chain is coherent: `0009_snapshot.prevId` matches `0008_snapshot.id`.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high)
None.

### Consider (Codex medium / low)
None remaining — all 6 findings across both passes closed.

---

## Coverage Caveats

- Browser smoke not re-run for this pass; no UI changes between pass 1 and pass 2 (schema test, SQL migration, doc consistency).
- Pre-existing lint warnings in `src/app/(app)/calendar/calendar-view.tsx` + `src/app/(app)/production/production-filters.tsx` survive — out of scope for this phase.
