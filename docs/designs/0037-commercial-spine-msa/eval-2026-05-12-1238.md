# Eval: 0037-commercial-spine-msa Phase 5 (wiki sweep + closing log entry)

**Plan:** docs/designs/0037-commercial-spine-msa/plan.md
**Branch:** main
**Date:** 2026-05-12

---

## Verdict: PASS

Phase 5 is doc-only — wiki sweep, closing log entry, and cross-plan reconciliation after Phase 4's narrowed-scope drop. Per the project's `feedback_docs_eval_single_pass.md` memory, doc-only diffs run a single Codex pass + apply findings directly + commit; the full static/web-test pipeline doesn't re-grade unchanged code. Codex pass-1 surfaced 2 Medium + 2 Low doc-drift findings — all resolved in-pass.

**Static checks:** carried forward from `eval-2026-05-12-1230.md` (Phase 4 eval — same code state; no source touched since).
**Browser smoke:** carried forward from `eval-2026-05-12-1230.md` (no UI/code change).
**Codex review:** 0 High / 2 Medium / 2 Low — all resolved.

---

## Codex Adversarial Review

### High

None.

### Medium (both resolved)

1. **`docs/wiki/log.md:16-25`** — The new 2026-05-12 "Commercial spine landed" entry replaced the prior `2026-05-12 — quotes table + outbound-mail row documented (0026 Phase 5)` heading instead of being appended above it; bullets from the 0026 entry were orphaned under the 0037 heading. **Fix applied:** restored the 0026 heading; the new 0037 entry now sits above it (reverse-chronological newest-first preserved). [log.md edit]

2. **`docs/designs/CURRENT.md:21`** — The Status line still claimed "active sub-plan flipped to 0037 Phase 4 (drop legacy commercial cols) — Phase 4 is destructive (column DROPs); next `/build` invocation will pause for confirmation." Stale — Phase 4 shipped at `b089d47`. **Fix applied:** Status rewritten to reflect Phase 4 shipped with narrowed scope, Phase 5 in flight, and the post-close sequence (close 0037 → 0035 P4 polish → 0025 leftovers).

### Low (both resolved)

3. **`docs/wiki/log.md:21`** — claim "`commercial-spine.md` re-read for drift — no edits needed" was false; the same diff edits `commercial-spine.md:100`. **Fix applied:** replaced the bullet with an explicit citation of the `commercial-spine.md` Phase 4 description edit + a fifth bullet citing the cross-plan edits to 0025/0026/0035.

4. **`docs/wiki/commercial-spine.md:112`** — "`master_service_agreements` section lands in 0037 Phase 2; `quotes` section lands when 0026 Phase 2 ships" was stale future-tense; both have shipped and `data-model.md` already carries the descriptions. **Fix applied:** rewritten to past-tense, citing the current MSA + quotes + updated campaigns walkthroughs in `data-model.md`.

### False positives Codex considered and ruled out

- `data-model.md:149` and `data-model.md:356` correctly retain `campaigns.audience_source_id` — schema still defines it, migration didn't drop it.
- `log.md:42` still has the 2026-05-11 six-column claim — acceptable because `log.md` is append-only and the new entry explicitly supersedes only the `audience_source_id` part.
- `0025-quote-to-payment/plan.md:38` is accurate — names only the five dropped cols and explicitly says `audienceSourceId` is still on `campaigns`.

---

## Action Required

### Must Fix (blockers)

None.

### Should Fix (Codex high/critical)

None.

### Consider

None remaining — all four Codex findings resolved in-pass per `feedback_docs_eval_single_pass.md` policy (single-pass; no pass-2 re-grade on doc diffs).

---

## Coverage Caveats

- Static + browser smoke not re-run on this commit — Phase 4 eval (`eval-2026-05-12-1230.md`) is the authoritative grade for the code state, which hasn't changed.
- Doc-only single-pass per project policy; if a follow-up touches code, the next eval grades the full pipeline normally.

---

## Chunk-close suggestion

All 5 phases of 0037 are now Done. Per CLAUDE.md → "Closing a chunk": next steps are (a) `mv docs/designs/0037-commercial-spine-msa docs/designs/closed/0037-commercial-spine-msa`; (b) sweep cross-references (the wiki pages link to `docs/designs/0037-…/` — those need `closed/` injected); (c) update `CURRENT.md` to flip Active to the next plan (0035 P4 polish per the queued sequence, or `_None — pick a new plan_`). The `/build` skill doesn't perform the move itself — that's an explicit user step. Flag for user.
