# Eval: 0035-quote-composer (Phase 2, pass 2)

**Plan:** docs/designs/0035-quote-composer/plan.md
**Branch:** main
**Date:** 2026-05-11

---

## Verdict: PASS

All 2 pass-1 Mediums + Low #1 + Low #3 verified closed by Codex pass-2. No new findings. Codex's one-line bottom line: **"Ship."**

**Static checks:** tests 591/591 PASS (+1 skipped) · tsc PASS · lint PASS (4 pre-existing warnings carry over)
**Browser smoke:** carried from pass-1 (UI did not change between passes — fixes were action-layer + small column conditional)
**Codex review:** 0 Critical, 0 High, 0 Medium, 0 Low — all pass-1 fixes verified

---

## Static Checks

All passed. Test count went 588 → 591 (3 new dealer-update edge-case tests for the new patch-omitted-when-absent semantics + the new not-found branch).

---

## Browser Smoke

UI rendering unchanged between passes — pass-2 fixes are: (a) action-layer (updateDealer's parsing + guard); (b) a single column conditional (hide Edit on archived rows). The pass-1 smoke (`eval-2026-05-11-2002.md`) covers the user-visible surface for this phase.

---

## Codex Adversarial Review (pass 2)

### Critical / High / Medium / Low
None.

### Fixes verified clean (Codex's wording)

- **Medium #1 (TOCTOU on updateDealer):** "Closed. `updateDealer` now does the dealer write atomically with `archivedAt IS NULL` in the `UPDATE` predicate and checks `.returning()` length before any contact/audit side effects. Other dealer updates are also guarded: archive, activate. No other `update(dealers)` writers found."
- **Medium #2 (Edit button on archived rows):** "Closed. Edit is hidden only when `d.archivedAt` is truthy, so active/prospect rows still render Edit."
- **Low #1 (acquiredVia not preserved):** "Closed. `formData.has('acquiredVia')` gates the patch, and an explicitly submitted empty value becomes `null`. JS `FormData` semantics confirm absent is `has=false`, empty submitted is `has=true` with `""`."
- **Low #3 (DealerForm.defaultStatus footgun):** "Closed. `defaultStatus` is create-only now; edit mode always renders the visible status select. `/dealerships` create passes no `defaultStatus`, so it also gets the visible select."

### Deferred (Low #2)

- **Low #2 — backfill discriminator-less:** still deferred. Codex pass-1 itself noted "likely consistent with stated v1/manual back-office funnel." No pre-sale prospects in dev/prod data set; if/when one is discovered, a one-shot correction script handles it.

---

## Cross-Reference

None.

---

## Action Required

None. Ready to commit. Codex pass-2 verdict: **"Ship."**

---

## Coverage Caveats

- Browser smoke carried from pass-1 (Phase 2 UI didn't change between passes).
- Archived dealer behavior couldn't be exercised live (no archived rows in dev DB) — verified by code review + unit tests.
- One Low (#2 backfill discriminator) intentionally deferred with documented rationale.
