# Eval: 0027-lists-to-dealers — Phase 1 (Rename + chrome simplify)

**Plan:** docs/designs/0027-lists-to-dealers/plan.md
**Branch:** main
**Date:** 2026-05-07

---

## Verdict: PASS

Phase 1's chrome reduction shipped cleanly: h1 renamed to "Dealers", subtitle + section-card chrome removed, nav label updated, behavior preserved. No findings from any evaluator.

**Static checks:** PASS — vitest 150/150, tsc clean, lint clean (4 pre-existing warnings, none in changed files)
**Browser smoke:** PASS — `/lists` renders new heading + nav label, dealer rows + Edit/✕ actions still wired
**Codex review:** Clean — no findings; 5 explicit non-issues confirmed (empty-state equivalence, action wiring, no stale label refs, no orphan helpers, plan-doc accuracy)

---

## Static Checks

All passed.

- `pnpm test --run` — 15 files, **150 / 150 tests** in 2.52s
- `pnpm tsc --noEmit` — exit 0
- `pnpm lint` — 0 errors, 4 warnings (all pre-existing in `calendar-view.tsx` + `production-filters.tsx`; none introduced by this phase)

---

## Browser Smoke

`inject-supabase`: success (cookies set for david.hogan@networknode.ca).

Plan Phase 1 has no smoke checks of its own — the full smoke table lives in Phase 5 and targets DataTable + Dialog work that hasn't shipped. For Phase 1's chrome-rename scope, the targeted check is: does `/lists` render with the new heading + nav label, and do the existing row actions still work?

| Check | Result |
|-------|--------|
| `goto /lists` returns 200 | PASS |
| Page h1 reads "Dealers" (was "Manage Lists") | PASS |
| No "Dealerships 26" section card header | PASS |
| No subtitle linking to `/admin/people` | PASS |
| Nav tab labelled "Dealers" → `/lists` | PASS |
| `+ Add Client` button rendered | PASS |
| Dealer rows render with `Edit` + `Remove <name>` actions | PASS |

Screenshot: `/tmp/web-test-lists-0027-phase1.png` — confirms chrome reduction matches the plan's intent (single h1, action button on the right, plain `<ul>` of dealers, no nested card).

Phase 2 (default repo-wide checklist) not run — Phase 1 is scoped to chrome-only; the rest of the gated surface is unchanged from prior eval baselines.

---

## Codex Adversarial Review

Verbatim from `codex:codex-rescue`:

> **Critical** — None.
> **High** — None.
> **Medium** — None.
> **Low** — None.
>
> **Explicitly not an issue (false-positive reduction)**
>
> *Empty state JSX is equivalent to the old helper call.* `src/app/(app)/lists/page.tsx:15` now renders the same icon/copy/classes that the diff removed from `EmptyState`: 🏢, "No dealers yet", and "Use + Add Client to get started." No fix needed.
>
> *Dealer actions are still wired.* `src/app/(app)/lists/page.tsx:2`, `:12`, and `:43` import and render `AddDealerButton` / `DealerRowActions`; `src/app/(app)/lists/list-actions.tsx:19` and `:38` still export them.
>
> *No live `src/` stale `Manage Lists` label found.* Grep hit for `/lists` nav is now `src/components/app/app-nav.tsx:11`: `label: 'Dealers'`. `rg "Manage Lists" src` produced no hits. Historical docs still mention the old label, but those are not live UI/test callers.
>
> *Removed local helpers have no remaining callers.* `rg "ListCard|EmptyState" src` produced no hits after the rewrite.
>
> *Verification claim is acceptable.* Plan checklist marks `pnpm tsc --noEmit && pnpm lint` complete; both ran clean (typecheck passed; lint exited 0 with pre-existing warnings outside this patch).

---

## Cross-Reference

No findings to cross-reference.

---

## Action Required

### Must Fix (blockers)

None.

### Should Fix (Codex high/critical)

None.

### Consider (Codex medium/low)

None.

---

## Coverage Caveats

- Phase 5's full smoke table (DataTable column headers, dialog field set, sort flip, search narrowing) is not exercised here — those checks target Phase 2 + Phase 3 work that ships later in the chunk.
- Pre-existing lint warnings in `calendar-view.tsx` (line 129) and `production-filters.tsx` (lines 30/33/36) remain unchanged; they are outside this phase's scope.
