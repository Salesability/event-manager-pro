# Eval Smoke: port-migration umbrella (re-run with auth-injected gated coverage)

**Plan:** [`docs/designs/0004-port-migration/plan.md`](plan.md) (umbrella; sub-plan slot vacated after 5.5 shipped)
**Recent sub-plan shipped:** [`docs/designs/closed/0011-email-send/plan.md`](../0011-email-send/plan.md) — Phase 5.5
**Branch:** `main`
**Date:** 2026-05-02 12:00

This is a re-run of yesterday's `eval-2026-05-01-1651.md` with the Auth-gated coverage gap closed via `inject-supabase`. Static + Codex results are unchanged from yesterday's run; the new content is Phase 2 of Browser Smoke and the shrunken caveats.

---

## Verdict: PASS with warnings

Static checks, public-surface, and auth-injected gated-surface smoke all green. The 4 Codex findings from yesterday's run still stand (1 High, 3 Medium, 2 Low) — unchanged because no remediation has shipped yet.

**Static checks:** PASS — vitest 35/35, `tsc --noEmit` clean, eslint 0 errors / 4 pre-existing warnings
**Browser smoke:** PASS — Phase 1 (public) 7/7 + Phase 2 (auth-injected) 6/6 = 13/13 checks verified
**Codex review:** unchanged from `eval-2026-05-01-1651.md` (0 critical, 1 high, 3 medium, 2 low)

---

## Static Checks

All passed (re-run today, identical to yesterday).

| Check | Result |
|---|---|
| `npm test` (vitest) | 35 / 35 |
| `npx tsc --noEmit` | clean |
| `npm run lint` | 0 errors, 4 pre-existing warnings |

---

## Browser Smoke

### Phase 1 — Public surface

| Check | Result |
|---|---|
| `goto /` → `/login?next=%2F` | ✓ |
| `/login` renders Google + email + magic link | ✓ |
| `/calendar` (gated) → `/login?next=%2Fcalendar` | ✓ |
| `/share/coach/1` 200, grid + ribbons | ✓ Shannon Tilley schedule, 4 ribbons |
| Month-nav `›` advances May → June 2026 | ✓ |
| `/auth/auth-error` 200 | ✓ |
| Console / network — no ERROR-level, no unexpected 4xx/5xx | ✓ |

Screenshot: `/tmp/eval-smoke-share.png`

### Phase 2 — Auth-injected gated surface

`inject-supabase` outcome: **Authenticated as `david.hogan@networknode.ca`. Cookies set: `sb-qppenapeguwevcheqwpz-auth-token.0`, `…1`.**

| Check | Result |
|---|---|
| `/calendar` — Master Schedule, stats (9 booked / 42 total / 4 coaches / 26 dealers), `Block Date` + `+ Book Event`, calendar grid + ribbons, coach filter | ✓ |
| `/production` — Production List, filters (Upcoming / Past / Show cancelled), search, `View` + `Edit` per row, ~42 rows | ✓ |
| `/lists` — Manage Lists, 26 dealerships + 5 sales coaches, `Edit` / `✕` per row, `Email link` per coach with email | ✓ |
| `/admin/lookups` — Lookup Admin, Event Styles (1) + Data Sources (4), `Add` / `Rename` / `✕` actions | ✓ |
| `/production` → click `View` on Capital Ford row → Event Detail dialog with full campaign data + `Email Client` / `Email Coach` / `Cancel Campaign` / `Edit` action buttons | ✓ — confirms Phase 5.5 wiring (`Email Client` / `Email Coach` are no longer disabled stubs) |
| `/calendar` → click `Block Date` → Block Out Dates dialog with `Start Date` / `End Date` / `Type` (Statutory Holiday / Company Closure / Coach Unavailable) / `Coach` / `Reason` / `Add Block` | ✓ — confirms Phase 5.4 wiring |
| Header consistent (`Calendar` / `Production List` / `Manage Lists` / `Lookups` + user avatar `DA` + email + `Sign out`) | ✓ |
| Console — no ERROR-level | ✓ |
| Network — no unexpected 4xx/5xx | ✓ (one expected 404 on `/admin/availability` confirmed Availability admin is dialog-only, not a route — see "Notes" below) |

Screenshots: `/tmp/eval-smoke-event-detail.png`, `/tmp/eval-smoke-block-date.png`

**Notes from this run:**
- Availability admin (Phase 5.4) is reached via `Block Date` button on `/calendar`, not via a `/admin/availability` URL. Initial probe returned 404; verified intentional in `src/app/(app)/calendar/calendar-view.tsx`. No action needed.
- `Email Client` / `Email Coach` / `Email link` buttons are visible and enabled but **not clicked** — those trigger a `confirm()` and a real Resend send, and `EMAIL_FORCE_DEV_REDIRECT` is unset by design (commit `1c22b66`). The render alone is what 5.5 ships; sending was already validated in 5.5 plan Phase 4.

---

## Codex Adversarial Review

Unchanged from yesterday's run (`eval-2026-05-01-1651.md`). Re-summarized for the action items below.

### Findings (0 critical / 1 high / 3 medium / 2 low)

- **High** — `/share/coach/[id]` enumeration via sequential `contacts.id` (`src/app/share/coach/[id]/page.tsx:15`).
- **Medium** — Dev email fail-open by default (`src/lib/email/send.ts:29`).
- **Medium** — Host-header forging emailed share URLs (`src/features/email/actions.ts:21`).
- **Medium** — Cancelled campaigns can still be edited via `updateCampaign` (`src/features/schedule/actions.ts:430`).
- **Low** — Email idempotency (best closed alongside the 5.5 deferred `messages_sent` audit table).
- **Low** — Month-boundary ribbon disappears (already parked as `0015-calendar-grid-clamp`).

Full Codex output verbatim is in `eval-2026-05-01-1651.md`.

---

## Cross-Reference

- Phase 2 confirmed the `Email Client` / `Email Coach` buttons render in Event Detail. This raises confidence in Codex's **Medium** dev-email fail-open finding: a user *can* trigger sends from the UI today, so the fail-open default is exploitable through normal use.
- Phase 2 confirmed `Email link` per coach renders on `/lists`. Same cross-reference for the host-header forging finding — these are the surfaces an attacker would use.

---

## Action Required

### Must Fix (blockers)
None — static, public smoke, and gated smoke all clean.

### Should Fix (Codex high/medium)
1. **High** — `/share/coach/[id]` enumeration. Switch to an unguessable token before Phase 6 cutover. Phase 2 confirmed the route renders coach name + dealer schedule with no auth.
2. **Medium** — Default `EMAIL_FORCE_DEV_REDIRECT=true` outside `APP_ENV=production`, or rename to `EMAIL_ALLOW_REAL_SEND` and require explicit opt-in. Phase 2 confirmed live send buttons.
3. **Medium** — Build emailed share URLs from a configured `APP_BASE_URL`, not the request `Host:` header.
4. **Medium** — Guard `updateCampaign` with a status predicate.

### Consider (Codex low)
1. **Low** — Email idempotency, deferred until `messages_sent` audit table lands.
2. **Low** — Month-boundary ribbon clamp — already parked.

---

## Coverage Caveats

- Email-send buttons (`Email Client` / `Email Coach` / `Email link`) were verified as *rendered*, not *clicked*. Smoke is read-only on the gated surface.
- Destructive flows not exercised: `Cancel Campaign`, dealer/coach `✕` deletes, `Add Block` form submit, lookup `✕` deletes. Out of scope for smoke.
- Booking modal (`+ Book Event`) was not opened in this run. Worth adding to the Phase 2 checklist on the next eval-smoke iteration.
