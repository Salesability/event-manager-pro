# Eval: 0040-quote-send-receipts — chunk-end

**Plan:** docs/designs/0040-quote-send-receipts/plan.md
**Branch:** main
**Date:** 2026-05-12

---

## Verdict: PASS with caveat — Codex review SKIPPED (no output)

All 5 phases shipped. Static checks clean; browser smoke confirms the send-receipt panel renders end-to-end on `/quotes/1` with a working signed-URL download link. Codex adversarial review returned no output from the subagent — treated as SKIPPED per the eval contract, not FAIL.

**Static checks:** test PASS (689 passed, 1 skipped) / tsc PASS (clean) / lint PASS (0 errors, 6 pre-existing warnings unrelated to this chunk)
**Browser smoke:** PASS — sent-quote panel renders, /quotes list + /calendar regression both 200
**Codex review:** SKIPPED (subagent returned no output; no findings to fold in)

---

## Static Checks

- `pnpm test` — `Test Files 29 passed (29) | Tests 689 passed | 1 skipped (690)` (Phase 5 added 2 new tests for `loadQuoteSendReceipt` + extended the `sendQuote` happy-path test + extended `loadQuotes` round-trip test for the denorm fields)
- `npx tsc --noEmit` — clean
- `pnpm lint` — `0 errors, 6 warnings` (all 6 pre-existing in `calendar-view.tsx` / `production-filters.tsx` / `quotes-filters.tsx`, untouched by this chunk)

All passed.

---

## Browser Smoke

Phase 1 public surface skipped (auth cookies already injected from earlier turn in this session; cookie eviction would have also wiped the gated session needed for the chunk's actual user-visible surface).

Phase 2 gated, focused on the chunk's user-visible surface:

| Check | Result |
|-------|--------|
| `/quotes/1` (existing sent quote, predates denorm) — heading "Quote #1" + status pill "sent" | PASS |
| `/quotes/1` — Send-receipt panel: heading + 4 term/definition rows (Sent / Sent to (recipient unknown fallback) / Resend ID / PDF link) | PASS |
| `/quotes/1` — Download sent PDF link points to a real GCS V4 signed URL with `X-Goog-Expires=300` | PASS |
| `/quotes` list — 200, renders | PASS |
| `/calendar` regression — 200 | PASS |

Screenshot: `/tmp/web-test-quotes-1-p4.png`.

**Caveats from web-test:**
- Draft-quote no-panel smoke skipped (no draft fixture in dev DB; JSX gate `quote.status !== 'draft' && (` verified by inspection at `src/app/(app)/quotes/[id]/page.tsx`).
- The "(recipient unknown)" fallback path was exercised since the dev DB's only sent quote predates this chunk's denorm — useful side-product, validates the pre-0040-data path.

---

## Codex Adversarial Review

**SKIPPED — subagent returned no output**

The Codex rescue Agent dispatched successfully (`agentId: a7f656c85f2dac5c0`, 18.9k tokens consumed, ~142s runtime, 1 tool use) but the subagent's terminal return was empty. No findings were emitted. Per the eval skill's contract, this is **SKIPPED — not FAIL**.

Likely cause: a transient runtime quirk in the Codex companion or an empty-response edge case after the Codex review concluded "no findings observed" but did not stream a finding-list block.

If this matters, the user can re-run `/eval` to retry the Codex pass; the static + browser smoke is sufficient to declare the chunk shippable with the caveat.

---

## Cross-Reference

No Codex findings to cross-reference. The chunk-end build observation that I want to flag for the next eval:

- **Migration not applied on first `db:migrate` run.** During Phase 4 smoke, `/quotes/1` returned 500 with `column quotes.sent_to_email does not exist`. The first `db:migrate` invocation in Phase 1 reported "migrations applied successfully!" but the runtime DB didn't have the column. A second `pnpm db:migrate` invocation (with `.env.local` sourced) landed it. Could be a transient connection issue (pooled vs direct) or a Drizzle CLI quirk; flagged in the plan body for a future investigation. No reproducible test today.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high/critical)
None (no Codex output).

### Consider (Codex medium/low)
None (no Codex output). One self-flagged item:

1. **Audit reader `orderBy` deferred.** `loadQuoteSendReceipt` filters with `.limit(1)` without `orderBy(desc(occurredAt))`. The v1 invariant (one row per quote) makes this safe today; when 0026 follow-up (a) "degraded-send retry" ships, add the orderBy to take the latest. Noted inline in `src/features/quotes/queries.ts`.

---

## Coverage Caveats

- **Codex SKIPPED** — empty subagent output; no adversarial review folded into the verdict. Re-run `/eval` to retry.
- **Draft-no-panel smoke SKIPPED** — no draft fixture in the dev DB; JSX gate verified by inspection.
- **Phase 1 public-surface smoke SKIPPED** — session reuse from earlier in the build chunk.
- **Send-receipt panel with denorm populated** — dev DB only has a pre-0040 sent quote; the populated-denorm path (showing `Pat <buyer@dealer.test>` instead of "(recipient unknown)") is covered by the `sendQuote` unit test asserting the `patch.sentToEmail` + `patch.sentToFirstName` fields land in the UPDATE. A live send would exercise the full panel-with-denorm path once GCS+Resend creds are wired in a sandbox (the 0026 follow-up (d) "live-send dev smoke" parked item).
