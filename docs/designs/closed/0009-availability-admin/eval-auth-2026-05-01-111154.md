# Authenticated Smoke: availability-admin

**Plan:** `docs/designs/closed/0009-availability-admin/plan.md`
**Date:** 2026-05-01

## Result: PASS

Authenticated browser session was injected with `inject-supabase` for `david.hogan@networknode.ca`.

Verified:

- `/calendar` loaded as signed-in user.
- `Block Date` opened the availability admin dialog.
- Added a `company_closure` block for May 6, 2026.
- Added a `statutory_holiday` block for May 7, 2026.
- Added a `coach_unavailable` block for Adam Godin on May 8, 2026.
- Confirmed global blocks render in the All Coaches calendar view.
- Confirmed the coach-unavailable block does not globally block All Coaches.
- Confirmed the coach-unavailable block appears when filtering to Adam Godin.
- Archived the temporary smoke blocks and confirmed the dialog returned to `No blocked dates.`
- Confirmed the calendar no longer displayed the temporary blocked labels after cleanup.

Notes:

- The first two rows were filled with parallel browser-tool commands and rendered with fallback reason `Blocked`; the sequential coach-unavailable row rendered the entered reason. This appears consistent with browser-command timing rather than a confirmed app bug.
