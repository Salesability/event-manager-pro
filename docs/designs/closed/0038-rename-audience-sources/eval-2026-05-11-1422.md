# Eval: 0038-rename-audience-sources (Phases 1+2)

**Plan:** docs/designs/closed/0038-rename-audience-sources/plan.md
**Branch:** main
**Date:** 2026-05-11

---

## Verdict: PASS with caveat — Codex review skipped

Phase 1 schema rename + Phase 2 call-site sweep are clean across static checks and browser smoke. Codex re-review was started but stalled in the "verifying" phase for 12 minutes without returning a final assistant message — cancelled. The Phase 1 eval at 14:04 already produced a complete Codex review (corroborated the tsc errors that drove the Phase 2 sweep); the diff added in Phase 2 is a mechanical sed across 20 files (3 unambiguous regex patterns, zero overlap, zero remaining occurrences). Risk is bounded.

**Static checks:** PASS (480 tests, 1 pre-existing skip / tsc clean / 4 pre-existing lint warnings)
**Browser smoke:** PASS (3 key surfaces — `/admin/lookups`, `/calendar`, `/production` — verified)
**Codex review:** SKIPPED — task stalled in verifying phase, cancelled at 12 min. Carry-forward from Phase 1 eval (`eval-2026-05-11-1404.md`).

---

## Static Checks

### `pnpm test` — PASS
```
Test Files  23 passed (23)
     Tests  480 passed | 1 skipped (481)
```

The previously-failing `tests/integration/rls.test.ts` is now green — `RLS_TABLES` was renamed `'sales_lead_sources'` → `'audience_sources'` in the Phase 2 sweep.

### `pnpm tsc --noEmit` — PASS (clean)

All 8 errors from the Phase 1 eval (5 TS2305 / 3 TS2339 across `schedule/queries.ts`, `schedule/actions.ts`, `scripts/import-from-sheets.ts`) are resolved by the call-site sweep.

### `pnpm lint` — PASS (4 pre-existing warnings, 0 errors)

Same 4 warnings as Phase 1 eval — not introduced by this work.

---

## Browser Smoke

### Phase 1 — Public surface

| Check | Result |
|-------|--------|
| `goto /` redirects to `/login?next=%2F` | PASS |
| `/login` renders Continue with Google + email + Send magic link | PASS |
| `goto /calendar` (gated) redirects to `/login?next=%2Fcalendar` | PASS |

### Phase 2 — Auth-injected gated surface

`inject-supabase`: success (cookies set as `david.hogan@networknode.ca`).

| Check | Result |
|-------|--------|
| `/admin/lookups` — "Lookup Admin" heading, Event Styles + **Data Sources** sections render | PASS |
| `/admin/lookups` Data Sources lists 4 seeded rows (Dealer Database / PBS / Third Party List / Previous Buyers) | PASS — confirms `loadAudienceSources` query works against the renamed table |
| `/calendar` — Master Schedule heading + Block Date + + Book Event buttons | PASS |
| `/production` — Production List heading + columnheaders including "Data Source" | PASS — confirms `audienceSourceLabel` join projection renders the column |

Screenshot: `/tmp/web-test-0038-lookups.png`.

**Note on user-facing labels:** `/admin/lookups` section heading is "Data Sources" and `/production` column is "Data Source" — both unchanged. The plan assumed the heading was "Sales Lead Sources" but it was already neutral. Renaming the UI label to "Audience Sources" is a separate UX decision; not in scope here.

---

## Codex Adversarial Review

**SKIPPED.** Codex task `task-mp1imae5-ypunq6` started normally, completed its file-reading + grep sweep, then stalled in the `verifying` phase for 12 minutes without producing a final assistant message. The progress log shows it completed its last command (`find src scripts tests …`) but never wrote a summary. Cancelled at the 11m39s status check.

Per `/eval` rules, Codex unavailability returns SKIPPED (not FAIL). The Phase 1 eval at 14:04 includes a complete Codex review of the schema/migration work; the diff added in Phase 2 is a 20-file mechanical sed with three non-overlapping regex patterns, all surfaced by the pre-sweep grep. No new logic.

Carry-forward from Phase 1 Codex (still valid):
- Medium: RLS policy names still carry the old table name (`sales_lead_sources_service_role_all`, `sales_lead_sources_staff_all` in `drizzle/0003_enable_rls.sql:188-198`). Policies stay attached by OID — behavior is correct — but pg_dump-visible names are stale. Optional `ALTER POLICY … RENAME TO` could fold into 0007 or land in a follow-up.
- Low: 0007 is not safely re-runnable. Standard for forward-only Drizzle; the migrator's `__drizzle_migrations.hash` tracking ensures it won't re-apply.
- Low: Out-of-band Supabase objects (Studio-created policies/publications/grants) can't be ruled out from repo alone. Low risk for the dev DB.

---

## Cross-Reference

Phase 2's sweep was the exact fix Phase 1's Codex review (High finding) predicted. The corroboration between Codex and the tsc errors in the first eval gave high confidence in the scope of the sweep, and the sweep cleanly resolved both.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix
None new this pass.

### Consider
1. (Carry-forward) `ALTER POLICY` rename for `sales_lead_sources_*` → `audience_sources_*` in a follow-up migration, or accept the cosmetic name drift. Behavior unaffected.
2. (UX, separate chunk) If "Audience Sources" should be the canonical UI name going forward, file a UX-polish chunk to rename `/admin/lookups` section heading "Data Sources" → "Audience Sources" and `/production` column "Data Source" → "Audience Source". Out of scope for the schema rename.

---

## Coverage Caveats

- Codex review SKIPPED — task stalled in verifying phase, cancelled. Phase 1 eval covers the schema/migration risk surface; Phase 2 diff is mechanical sed.
- Strict-mode click collisions on `/production` View buttons — not exercised. The columnheader presence is sufficient to confirm the `audienceSourceLabel` query path renders.
- Browser smoke did not re-screenshot `/calendar` or `/production` (just `/admin/lookups`) — the snapshot a11y trees provided the needed evidence.
