# Eval: 0026-quote-pdf (Phase 2)

**Plan:** docs/designs/0026-quote-pdf/plan.md
**Branch:** main
**Date:** 2026-05-11

---

## Verdict: PASS with warnings

`quotes` table + Server Actions land. Pass-1 eval surfaced 1 High + 4 Medium + 3 Lows (TOCTOU on lifecycle transitions, archived-dealer leak, missing row-local lifecycle timestamps, RLS-test-list omission, two comment drifts, predicate-blind test mock). Self-resolved all into pass-2: atomic guarded UPDATE-RETURNING on `sendQuote` + `markQuote*`, `archivedAt IS NULL` on `createQuote`, new `sent_at`/`accepted_at`/`declined_at` columns in follow-up migration `0011`, `quotes` added to RLS test list, comment fixes. Codex pass-2 verdict: "Ready to ship Phase 2, with two Low follow-ups."

One residual Codex Low (test-mock predicate-blindness) is a repo-wide parked follow-up — the same disclaimer lives on `people/actions.test.ts` ("predicate-blind by design; closing that gap is a parked follow-up"). Out of scope for this chunk.

**Static checks:** tests PASS (523/523, 1 skipped) · tsc PASS · lint PASS (4 pre-existing warnings, untouched)
**Browser smoke:** PASS — public + auth-injected gated regression clean (no UI delta in this phase)
**Codex review:** 2 passes · all High/Medium closed · 1 residual Low (parked follow-up)

---

## Static Checks

All passed.

- `pnpm test` — 25 files, 523 passed, 1 skipped (CI-only RLS integration suite).
- `pnpm tsc --noEmit` — exit 0.
- `pnpm lint` — exit 0 (4 pre-existing warnings in `calendar-view.tsx` + `production-filters.tsx`, unchanged).

---

## Browser Smoke

### Phase 1 — public surface

| Check | Result |
|-------|--------|
| `goto /` redirects to `/login?next=%2F` | PASS |
| `goto /login` renders OK | PASS |
| `goto /calendar` redirects to `/login?next=%2Fcalendar` | PASS |
| `goto /share/coach/1` renders | PASS |

### Phase 2 — auth-injected gated surface

`inject-supabase david.hogan@networknode.ca`: success.

| Check | Result |
|-------|--------|
| `/calendar` Master Schedule + Block Date + + Book Event + coach filter | PASS |
| `/production` Production List + filters + table | PASS |
| `/dealerships` Dealers + Add Dealer + table | PASS |
| Nav consistency | PASS |

Screenshot: `/tmp/web-test-calendar.png`. No UI surface added in this phase — smoke is regression-only.

---

## Self-Resolve Loop Summary

| Pass | Codex Findings | Action | Status |
|------|----------------|--------|--------|
| 1 | High: TOCTOU on `markQuoteAccepted`/`markQuoteDeclined` · Medium: TOCTOU on `sendQuote`, missing `archivedAt` check, missing lifecycle timestamps, `quotes` missing from RLS test list · Low: migration comment + test mock + comment drift | Direct fixes applied: atomic UPDATE-RETURNING guards + re-select on miss, archived-dealer check, follow-up migration `0011` adding `sent_at`/`accepted_at`/`declined_at`, RLS test list updated, comment rewrites | Closed for High + all 4 Mediums; 2 Lows partially closed |
| 2 | Low: migration comment still ambiguous on RLS-vs-public-route · Low: test-mock predicate-blindness wouldn't catch removal of the WHERE-status guard | Migration comment rewritten (current pass) · Test-mock Low deferred as repo-wide parked follow-up | Migration Low closed; test-mock Low remains as known caveat |

---

## Codex Adversarial Review (pass-2 verbatim, key points)

> **Pass-1 Findings Closure Table** — High + all 4 Mediums closed. Low 1 (migration comment) partially closed; Low 2 (test mock) partially closed; Low 3 (decline comment drift) closed.
>
> **Non-Issues Cleared:**
> - Atomic UPDATE-WHERE-RETURNING correctly defeats SELECT-then-UPDATE race in both `sendQuote` and `transition()`.
> - Re-select miss branch coherent (missing row / idempotent / illegal status).
> - Idempotent already-target status does not emit audit.
> - `createQuote`'s active-dealer check matches `createCampaign`.
> - `sentAt`/`acceptedAt`/`declinedAt` set in same atomic statement as status flip.
> - `0011_slim_wonder_man.sql` is nullable-only `ADD COLUMN`; journal monotonic.
> - Wiki data-model update remains correctly deferred to Phase 5.
>
> **New Low (now closed)**: migration `0010_busy_warbound.sql` RLS comment was still ambiguous about who the policies admit vs. who the public route can read as. Rewritten in the current pass to spell out that the policies only admit `service_role` and authenticated staff, and that the Phase 4 public accept-link route must read/write via the server-side Drizzle BYPASSRLS path (NOT anon supabase-js).
>
> **New Low (deferred)**: test-mock at `src/features/quotes/actions.test.ts:64,84` ignores the `where(...)` predicate, so deleting `eq(quotes.status, 'draft')` from `sendQuote` (or `eq(quotes.status, from)` from `transition()`) wouldn't fail the existing zero-rows tests. Same predicate-blindness disclaimer lives on `src/features/people/actions.test.ts` ("predicate-blind by design; closing that gap is a parked follow-up"). Carry-forward — not a Phase-2 blocker.
>
> **Overall Verdict**: Ready to ship Phase 2, with two Low follow-ups for comment accuracy and stronger predicate-aware regression tests.

---

## Action Required

### Must Fix (blockers)
None.

### Should Fix (Codex high)
None remaining — all closed.

### Consider (Codex medium / low)
1. **(Carry-forward, parked)** — Tighten the action-test db mock to capture `where(...)` predicates so removing a guard `eq(quotes.status, 'draft')` would fail a test. Same gap exists in `src/features/people/actions.test.ts`; would close repo-wide if addressed. Not a Phase-2 fix because (a) the pattern would need to land everywhere at once and (b) action-gate matrix tests + the upcoming integration suite already constrain the surface.

---

## Coverage Caveats

- No UI delta in this phase; smoke is regression-only.
- `pnpm test` skips the RLS integration suite (`tests/integration/rls.test.ts`) when `DATABASE_URL` is unset in CI — local run has it loaded so the new `'quotes'` entry in `RLS_TABLES` would exercise on demand.
- Pre-existing lint warnings in `src/app/(app)/calendar/calendar-view.tsx` + `src/app/(app)/production/production-filters.tsx` survive; unchanged from prior phases.
