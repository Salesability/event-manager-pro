# Active design

**Plan:** [`docs/designs/0044-quote-expiry-enforcement/plan.md`](0044-quote-expiry-enforcement/plan.md) — un-parked 2026-05-13 via user `/build 0044` (explicit focus-switch from 0043). Honor `quotes.quote_valid_days` (default 30) end-to-end: (1) `acceptQuote` expiry guard, (2) PDF + email validity surface, (3) lifecycle decision (recommend derived-field Option B), (4) conditional nightly sweep (skipped on B), (5) chunk-end `/eval`.

**Active sub-plan:** _None — this plan is standalone, not a 0025 sub-plan._

**Parked:**
- [`0043-portal-shell-conventions`](0043-portal-shell-conventions/plan.md) — re-parked 2026-05-13 via user `/build 0044` focus-switch (had been Active since 0045 closed but 7 phases all Pending — zero in-flight state to preserve). Original un-park trigger from 0042 close still satisfied; sequencing is now a user-choice queue. Standalone follow-on to 0042: replace top-header `AppHeader` with shadcn Sidebar shell; introduce `<PageHeader title actions sticky?>` so primary actions move to a persistent top-right slot (fixes hidden-below-fold + hand-rolled-submit pain); detail-page key-value strip + sections convention (`/quotes/[id]`, `/dealerships/[id]`); list-page filter-bar shape; status `<Badge>` + relative timestamps; `docs/wiki/layout.md`.
- 0045 follow-up (a): **`createPerson` / `updatePerson` appAccess + empty-email guard scope-creep** (Codex Medium from [`closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md`](closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md)) — `src/features/people/actions.ts:560` only enforces "email required when granting app access" on the `current.userId == null` (off→on) branch; an admin re-submitting `appAccess=1` with `email=''` against an already-linked person bypasses the guard, then `swapPrimaryIdentifier(..., '')` archives the primary email identifier while the auth-side sign-in email is preserved. Pre-existing bug, surfaced during 0045's eval but predates the chunk. Fix shape: hoist the email-required check out of the `userId == null` conditional so it fires on every appAccess=1 path, OR refuse `email=''` at the schema layer when `appAccess === '1'` (cross-field `.superRefine` on `personFormSchema`).
- 0045 follow-up (b): **`safeparse-required` ESLint rule policy hardening** (Codex Medium from [`closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md`](closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md)) — the rule accepts any reachable `safeParse` call regardless of (i) whether the argument is `Object.fromEntries(formData)`, (ii) whether it runs before mutation, or (iii) whether it sits in a dead branch. An adversarial action could insert/update from raw `formData`, then call `someSchema.safeParse({})` later and pass lint. Fix shape: tighten `eslint-plugins/safeparse-required.mjs` to assert the safeParse argument starts with `Object.fromEntries(...)` AND no mutation call (`db.update`/`db.insert`/`db.delete`) appears before the first `safeParse` in the action body. Adds RuleTester test coverage at the same time.
- 0041 follow-ups (deferred from chunk-end eval 2026-05-12 — see [`closed/0041-msa-send-bundled-envelope/eval-2026-05-12-2229.md`](closed/0041-msa-send-bundled-envelope/eval-2026-05-12-2229.md)): (a) **concurrent-send race on `sendMsaEnvelope`** (Codex High) — two callers can both post envelopes before the DB-claim lands; loser's webhook has no MSA row to correlate to. Fix shape: claim the send before external side effects (a `send_attempted_at` schema add + guarded UPDATE, or per-MSA advisory lock); paired naturally with (c) below; (b) **shared env-normalize helper** (Codex Medium) — `dropbox-sign/client.ts:83` uses raw `process.env.APP_ENV !== 'production'` while `email/send.ts` trims+lowercases; could test-mode a production deploy and still flip MSA rows to `active`; (c) **resend-envelope button** on the pending-MSA panel state (Codex Medium) — when `pending && !dropboxSignDocumentId`, the existing-MSA guard refuses a second `createMsaDraft` but `sendMsaEnvelope` is already idempotent — just needs UI; pairs naturally with (a); (d) **coach-scoped first-draft-Quote picker** (Codex Medium) — `firstDraftQuoteIdForDealer` is dealer-scoped, could pick another coach's draft in multi-coach dealers; better solved by an explicit Quote-picker UI than a `createdById` filter; (e) **live Dropbox Sign sandbox smoke** — once sandbox creds wired, exercise full envelope-post + webhook-signed round-trip (sibling to 0026 follow-up (d)).
- 0033 follow-up (carry-forward): retire hand-rolled validators (`parseId`/`field`/`parseLookupLabel`/`parseCampaignInput`/`validateContactInputs`) by writing per-action Zod schemas; replaces the `formDataSchema = z.instanceof(FormData)` passthrough; revisit when 0016 booking-intake lands so the public-facing form drives the schema-first conversion. **Superseded 2026-05-13 by `0045-form-schema-as-contract`** — covers the same retirement (Phase 7) plus the broader schema-sharing doctrine across all forms.
- 0025-quote-to-payment umbrella (Phase 7 of 0004-port-migration): Quote → Contract → Invoice → Payment. Phase 7.1 → Phase 7.2 first-half shipped via closed/0026 + closed/0035 + closed/0037 + closed/0040 + closed/0041; remaining epic leftovers — Phase 7.2 second-half (MSA renewal + cancellation-fee invoicing), 7.3 Invoice, 7.4 Payment — sub-plans not yet scaffolded. Plan: slot now occupied by 0043 portal-shell refactor (0042 closed 2026-05-13); 0025 returns to Plan: after 0043 ships.
- CSV-injection mitigation + print-overlay leak (Codex medium + lows from 5.6) → not yet scaffolded; pull a small chunk if/when revisited.
- 0018 follow-ups (not blockers): `createUser` partial-success after auth-side success (Codex Medium); test-mock predicate-blindness (Codex Low). Not yet scaffolded; small chunk if/when revisited.
- 0019 follow-ups (not blockers): email rate-limit / replay-prevention (Codex High parked from 0011 — the v1 0026 design dropped the public accept route, but rate-limit still load-bearing for any future public surface from `0016` web intake or a v2 quote-confirmation page); auth-callback Host-header tightening (Codex Medium from Phase 7); MFA enablement (0019 Phase 6 parked); secrets-boundary smoke test (0019 Phase 5 parked); `adoptOrphanAuthUser` audit emission (Codex Low from Phase 4); `actorRole` denorm precision once a non-admin path gains an audited action (Codex Low from Phase 4).
- 0023 follow-ups (not blockers): dealer-role grant audit emission for `createDealer`/`updateDealer`/Phase 2 backfill (Codex Medium from Phase 4 — needs a SELECT-then-conditional helper to distinguish granted/un-archived/no-op); production data triage (re-run `scripts/backfill-dealer-role.ts --dry-run` against prod); dealer + admin/coach hybrid confirm dialog (plan's open question, deferred to v2); schedule-actions vitest fixture for cleaner wiring tests; real form-submission test that would catch React-19-migration regressions like the `4a4afbd` wire-format drop.
- 0024 follow-ups (not blockers): Radix Form adoption (Phase 4 deferred — revisit alongside the `0016` booking-intake form so both forms' validation surfaces migrate in one pass); bundle-size baseline measurement on the people-admin route once a build-comparison harness exists; Combobox/Select interactive flows verified by code-trace + Codex source-review only — a Playwright-style test that drives ref-less popovers would catch any future regression.
- 0026 follow-ups (deferred from Phase 5, 2026-05-12 — each chunk-sized): (a) **degraded-send retry path** — small schema add (`emailed_at` on `quotes`) + a repair Server Action that re-renders, re-uploads, and re-emails a Quote when the guarded `draft → sent` UPDATE succeeded but GCS upload or Resend delivery failed; the degraded-state error already surfaces "admin repair required" so the gap is loud, not silent; (b) **split `quote:send` from `quote:edit`** (Codex Medium 4 from Phase 3 eval 2026-05-12) — match the irreversible nature of the document-emitting transition with an admin-only `quote:send` capability; touches the capability matrix, role assignments, action gates, and tests; (c) **staff accept/decline UI affordance** — `acceptQuote` / `declineQuote` Server Actions exist with prospect→active promotion + audit emission, but no UI button surface today (coaches phone/reply-to-email to confirm and run the action via CLI or future UI); paired naturally with (a) for an "admin repair" surface; (d) **live-send dev smoke** once GCS bucket + Resend creds are wired in a sandboxed environment — create campaign → create quote → send → verify PDF attachment in inbox → run staff accept → verify status flip + audit row + dealer promotion. Each of these is small enough to ship in a half-day chunk.

**Future:**
- [`future/0016-book-your-event-intake/plan.md`](future/0016-book-your-event-intake/plan.md) — public-form + Squarespace-cutover work, deferred to v2 on 2026-05-11. v1 funnel uses manual in-app intake (existing `+ Book Event` modal + 0035 P2 inline-create dealer). Un-defer trigger: prospect volume justifies replacing manual-entry-from-Shannon, OR marketing-site CTA is ready to point at the app. See `docs/designs/future/` and CLAUDE.md `### Deferring a chunk`.

**Status:** 0044-quote-expiry-enforcement active 2026-05-13 via user `/build 0044` (un-parked, 0043 re-parked). 5 phases Pending: (1) `acceptQuote` expiry guard inside `markQuoteAccepted`, (2) PDF + email "Valid until" surface (use per-row `quoteValidDays`, not hardcoded 30), (3) resolve OQ #1 lifecycle representation — default Option B (derived `isExpired` projection from `loadQuote`), (4) nightly sweep (skipped under Option B), (5) chunk-end `/eval`. Sequence forward after 0044: 0043 portal-shell, 0045 follow-ups (a + b bundled), 0041 follow-ups (a + c bundled), 0026 follow-ups (a–d), 0025 epic leftovers (Phase 7.2 second-half + 7.3 Invoice + 7.4 Payment), 0037 `audience_source_id` drop follow-up.

---

This file is the pointer to the plan currently being worked on. It exists because saying "the plan" or "read the plan" is ambiguous when `docs/designs/` has many folders. See the **Tracking the active design** section in `CLAUDE.md` for the convention.

## History

For the raw "when was X active" — `git log -- docs/designs/CURRENT.md` shows every transition as a separate commit (since this file is tracked as of `bc450ad`). Reach for that first if you just need sequencing.

The bullets below are a *curated* layer over that log: same dates, but with the eval verdict, why the slot vacated, what got requeued, etc. — the context a commit message wouldn't carry. When the active plan or sub-plan changes, prepend a one-line entry (newest first) noting date, what changed, where it ended up (Done → `closed/`, Paused, Abandoned), and any non-obvious context.

- **2026-05-13** — Switched Active **Plan:** from `0043-portal-shell-conventions` → `0044-quote-expiry-enforcement`. User typed `/build 0044` immediately after 0045 closed and 0043 re-promoted to Active. 0043 demoted back to **Parked:** without ever starting any phase (zero in-flight state to preserve); un-park trigger from 0042 close still satisfied. 0044's original un-park trigger ("0043 ships OR user explicitly switches focus") fulfilled via the second clause. Plan structure (5 phases) per `0044-quote-expiry-enforcement/plan.md` — recommended path = Option B (derived `isExpired` projection, no migration, Phase 4 skipped).
- **2026-05-13** — Closed `0045-form-schema-as-contract` → `docs/designs/closed/0045-form-schema-as-contract/`. All 9 phases shipped same day via `/build` chunk loop (11 commits): P1 `25cc0f0` (forms.md schema-as-contract doctrine + log.md entry — codifies "one zod schema per form lives in a sibling `*-schema.ts` module, imported by both client `zodResolver` and Server Action `safeParse(Object.fromEntries(formData))`"; B-shape variant carved out for partial-RHF + single-field admin forms), P2 `2e9853d` (extracted `dealerFormSchema` into `src/features/dealers/dealer-schema.ts`; `quoteInputsSchema` stayed at `src/lib/quotes/pricing.ts` — already the right home; wired `safeParse` into `createDealer`/`updateDealer` + `createQuote`/`setQuoteInputs`; new "surfaces per-field errors on safeParse failure" tests in dealers + quotes), P3 `80e082e` (services-admin full A-shape RHF port — factored shared `<ServiceForm mode='create'|'edit'>` component, new `service-schema.ts` covers per-field validation, `normalizeMoney` helper handles wire→DB padding without IEEE-754 rounding, `toLegacyResult` extended to forward `fieldErrors`), P4 `9879c98` (availability-admin full A-shape RHF port — `availability-schema.ts` + 6-case schema test, factored shared `<AvailabilityForm mode='create'|'edit'>`, action `parseAvailabilityInput` rewritten to safeParse), P5 `d287c5f` (lookup-admin B-shape carve-out — `lookup-schema.ts` is single-field, native `<form action={action}>` keeps the existing UI; action `parseLookupLabel` now safeParses), P6 `2ef7f94` (B-shape backfill for booking-form + PersonForm — new `person-schema.ts` + `src/app/(app)/calendar/booking-schema.ts`; `parseCampaignInput` rewritten to safeParse `bookingFormSchema` + cross-field rules; `createPerson`/`updatePerson` safeParse scalar fields while roles/dealerLinks stay on `formData.getAll(...)`), P7 `1d71b3f` (retired `validateContactInputs`/`parseDate`/`parseOptionalInt` from `schedule/validators.ts` along with their test blocks — replaced by zod schemas + the action-layer `validateContactCross` helper; kept `field`, `parseId`, `parseOptionalId`, `EMAIL_RE`, `parseCampaignInput` since they have non-schema consumers), P8 `ef0474c` (new `eslint-plugins/safeparse-required.mjs` rule + `eslint.config.mjs` registration scoped to `src/features/**/actions.ts`; `wrapperNames: ['parseCampaignInput']` config trusts the cross-file booking-form wrapper; opt-out via `// validation: skip` comment with one-line rationale on each of ~20 id-only / payload-less actions). Chunk-end `/eval` at `4f41e5e` (report [`closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md`](closed/0045-form-schema-as-contract/eval-2026-05-13-0937.md)): verdict **PASS with warnings**. Static gate clean (tsc + 757 tests + lint 0 errors). Browser smoke: Phase 1 6/6 PASS + Phase 2 plan-targeted PASS on the chunk's touched surfaces (with /admin/lookups verifying the new ServicesAdmin RHF shape and /calendar Block Date verifying the AvailabilityAdmin RHF dialog; Edit-dealer dialog click skipped on strict-mode collision — A-shape form unchanged per Phase 2). Codex returned 3 Mediums: **M2 (in-chunk regression) fixed in-cycle at `7c535b4`** — `dealerFormSchema` status field rejected empty string after the Phase 2 preprocess was dropped; restored the "status='' as absent" semantics via a `normalizeDealerWire` wire-format pre-pass + new regression test. **M1 + M3 parked as 0045 follow-ups (a) and (b) above** — pre-existing appAccess+empty-email guard scope-creep in `people/actions.ts` (predates 0045, surfaced during the eval focus on cross-field logic) and ESLint rule policy-strength gap (doesn't inspect argument shape or call ordering). Bookkeeping: 11 feature commits + the in-cycle fix + 2 plan `docs(plan): mark Phase N commit` updates + the chunk-end eval-report commit. Side-effect: extended `toLegacyResult` to forward `fieldErrors` through the adapter (Phase 3) so future A-shape forms can route per-field via `setError`. Cross-refs swept: this History block + the **Status:** line + the **Plan:** flip back to 0043 (un-park trigger "0043 + 0044 ship OR user switch" was fulfilled mid-session by the user-switch clause; 0043 returns to Active without needing the original trigger). New auto-close + auto-park doctrine landed mid-session in CLAUDE.md → "Closing a chunk" and `.claude/skills/build/SKILL.md` → "Auto-close on clean eval" — this close-out manually mirrors what the next chunk-end PASS-with-warnings will trigger automatically.
- **2026-05-13** — Switched Active **Plan:** from `0043-portal-shell-conventions` → `0045-form-schema-as-contract`. User typed `/build 0045` immediately after scaffolding, explicitly switching focus before 0043 started. 0043 demoted back to **Parked:** (still standalone, un-park trigger from 0042 close still satisfied — sequencing is now a user-choice queue). 0044 stays Parked. Un-park trigger for 0045 (which named "0043 + 0044 ship" or "user explicitly switches focus") fulfilled via the second clause.
- **2026-05-13** — Scaffolded `0045-form-schema-as-contract/plan.md` and added it to **Parked:** (did *not* flip Active — 0043 is the freshly un-parked Active plan and hasn't started yet; 0044 is the older parked sibling). Surfaced from same-day audit conversation: user asked "are we using zod and shadcn forms across the board?" — audit found three coexisting patterns (full-RHF + zod, useActionState + native, hand-rolled `useTouched()`) but the bigger gap was that **zero Server Actions in the repo currently use zod** (`schedule/validators.ts` is hand-rolled; the existing `safeParse` grep hit in `quotes/actions.ts` is a `JSON.parse`, not a zod parse). `docs/wiki/forms.md` already documents the two acceptable form *shapes* (full RHF + partial RHF), so the doctrine question is settled — this chunk closes the **schema-sharing** loop. Plan structure: 8 phases — (1) extend `forms.md` with the "one zod schema, imported by both client and action" rule + B-shape variant clarification; (2) extract canonical schemas (dealer + quote) into shared `*-schema.ts` modules + wire `safeParse(Object.fromEntries(formData))` into existing actions; (3) `services-admin` A-shape port (currently has no validation at all — biggest bug-catching win); (4) `availability-admin` A-shape port; (5) `lookup-admin` carve-out (single-field B-shape, no RHF, but action still `safeParse`s); (6) B-shape backfill — `booking-form` + PersonForm keep `useActionState` UI per `forms.md`'s explicit carve-out but the action `safeParse`s the same schema; (7) retire superseded `schedule/validators.ts` helpers (this is the 0033 carry-forward parked item — now scaffolded); (8) smoke + `/eval`. Code Anchors: `src/lib/quotes/pricing.ts:quoteInputsSchema` (existing shared-schema precedent), `src/features/dealers/dealer-form.tsx:33` (existing A-shape — anchor for extracted schemas + new A-shape ports), `src/features/people/people-admin.tsx` (existing B-shape anchor), `src/features/schedule/validators.ts` (replaced hand-rolled helpers). Supersedes the parked "0033 follow-up (carry-forward): retire hand-rolled validators" line — Phase 7 covers that work. Un-park trigger: 0043 + 0044 ship, OR user explicitly switches focus.
- **2026-05-13** — Scaffolded `0044-quote-expiry-enforcement/plan.md` and added it to **Parked:** (did *not* flip Active — 0043 is the freshly un-parked Active plan and hasn't started yet). Surfaced from the same-session post-close 0042 conversation: user asked "do the quotes expire in 30 days" after the close-out commit; answer was "schema says yes via `quote_valid_days NOT NULL DEFAULT 30`, but no code reads/enforces/renders it — `acceptQuote` doesn't refuse stale, the PDF doesn't print 'Valid until', the email doesn't either, and `quote_status` has no `expired` value." User asked "can we fix it or should we make a chunk", answered with the three coupled questions (guard semantics, validity rendered to the dealer, enum-vs-derived-vs-timestamp lifecycle representation) and the recommendation to scaffold. Plan structure: 5 phases — (1) `acceptQuote` expiry guard inside `markQuoteAccepted` lifecycle helper (the minimum behavior fix, layered on top of the existing `status='sent'` atomic-UPDATE guard); (2) surface validity on PDF + email (extend `QuoteData` with `validUntilDate`, sibling line below the existing `Issued: ...` line in `render-quote.ts:192`; same for email template's `QuoteEmailFields`); (3) decision-and-implement on OQ #1 lifecycle representation — three options laid out (A enum extension + sweep — most semantically honest, matches MSA's `expired` enum value, largest scope; B derived `isExpired` projection at read time — no migration, no sweep, recommended v1 default; C nullable `expired_at` timestamp — compromise); (4) conditional nightly sweep — only runs if Phase 3 picked (A) or (C), skipped for (B); (5) tests + chunk-end `/eval`. Seven OQs captured: lifecycle representation (recommend B), declined-Quote-blocks-acceptance layering, rejection error message wording (recommend verbose + actionable), "Valid until" vs "Valid for X days" PDF phrasing (recommend absolute), email body wording (recommend "Please respond by"), pairing with 0026 follow-up (c) staff accept/decline UI (recommend keep separate), prod backfill concerns. Code Anchors: `lifecycle.ts:69` for `markQuoteAccepted` (guard goes inside), `actions.ts:825` for `acceptQuote` (no change — new error flows through existing pipe), `render-quote.ts:192` for the "Issued:" line (anchor for the new "Valid until:" sibling), `actions.test.ts:268` for the test block, `drizzle/0019_msa_audit_actions.sql` for the enum-extension precedent (Option A only), `scripts/0041-msa-smoke.ts` for the sweep script shape (Options A/C only). Un-park trigger: 0043 ships, OR user explicitly switches focus.
- **2026-05-13** — Closed `0042-shadcn-ui-sweep` → `docs/designs/closed/0042-shadcn-ui-sweep/`. All 7 phases shipped via `/build` chunk loop: P1 `fd2ad89` (shadcn init `base-nova` preset + `globals.css` token reconciliation, strategy-A semantic-alias to existing named tokens, DM-Serif-Display + DM-Sans dropped, switched sans to Inter via `next/font/google`), P2 `09ad506` (form stack — pivoted mid-phase from classic `<FormField>`/`useFormField` to shadcn 4.x `<Field>` + `<FieldGroup>` per the shadcn skill's authoritative rules; hand-written `form.tsx` wrapper + `form-bind.ts` setError-router deleted), P3 `2462b0c` (quote-composer port — `<Field>` + `<FieldGroup>` for inputs, `<FieldSet>` + `<FieldLegend>` + `<ToggleGroup>` for retrieval-bracket with the Base-UI-array-shape adapter), P4 `29fd30d` (dealer-form full RHF + zod port, booking-form partial-RHF kept `useActionState` because auto-fill UX doesn't fit RHF), P5 `b4df471` + `3c5c7a7` (primitive sweep — 9 dialog consumers + 2 combobox consumers + 1 tabs consumer ported to shadcn Base UI; `src/components/ui/command.tsx` deleted + deps `cmdk` / `@radix-ui/react-dialog` / `-popover` / `-tabs` removed; chunk-introduced Tabs layout regression on `/reports` caught + fixed by rewriting `data-horizontal:flex-col` to `data-[orientation=horizontal]:flex-col` since shadcn-shipped class only matches `[data-horizontal]` attribute-presence and the wrapper sets `data-orientation="horizontal"` instead), P6 `de4da59` (PersonForm in `people-admin.tsx` ported off `@radix-ui/react-form` — Phase 4's "only dealer-form left" grep audit was stale; `pnpm remove @radix-ui/react-form` clean afterward; `docs/wiki/forms.md` written + cross-linked from `index.md` + `conventions.md` + `log.md`), P7 `3a1a16e` (chunk-end `/eval` rescue — Codex Medium "dialog default-width regression" addressed in-cycle: old in-house `Dialog.Panel` defaulted to `max-w-[560px]` but shadcn ships `sm:max-w-sm` ~384px, fixed by overriding the shadcn default to `sm:max-w-[560px]` so the Book Event / Edit Campaign / Add Person dialogs aren't cramped; also dropped orphan `inputClass` const in `quote-composer.tsx` for lint-clean). Chunk-end `/eval` verdict **PASS with warnings** ([`eval-2026-05-13-0701.md`](closed/0042-shadcn-ui-sweep/eval-2026-05-13-0701.md)): static all green, browser smoke 12/12 PASS (Add Dealer, Book Event, /quotes/new Field+ToggleGroup, /reports Tabs orientation, /admin/people PersonForm post-port), Codex 1 Medium → addressed. Concurrent first Codex pass orphaned mid-run (PID died at ~18min elapsed with state still `running`); swept via eval skill's stall recipe; second pass returned the canonical findings. Cross-refs swept: `docs/wiki/forms.md` (link `docs/designs/0042-…` → `docs/designs/closed/0042-…`); inside moved folder, `eval-2026-05-13-0701.md`'s `../../../src/...` paths climbed to `../../../../src/...`; `[plan.md](plan.md)` siblings stayed valid. Active **Plan:** flipped to `0043-portal-shell-conventions` (un-park trigger "0042 closed" fulfilled this turn); Active sub-plan stays None (0043 is standalone, not a 0025 sub-plan). 0025 umbrella stays in **Parked:** — slot will return to it after 0043 ships.
- **2026-05-12** — Switched Active **Plan:** from `0025-quote-to-payment` umbrella → `0042-shadcn-ui-sweep` standalone. Un-park trigger fully cleared (0041 closed at `270f8e8`; 0035 quote-composer RHF carry-forward committed at `f540c46`; 0042 theme/style/font decisions committed at `bfbd80a`). 0025 umbrella displaced from Plan: temporarily — its epic leftovers (7.2 second-half, 7.3, 7.4) stay in **Parked:** until 0042 closes, then 0025 returns to the Plan: slot. 0042 is standalone — Active sub-plan field deliberately set to None (it's not a 0025 sub-plan). User invoked `/build` immediately after the focus switch.
- **2026-05-12** — Closed `0041-msa-send-bundled-envelope` → `docs/designs/closed/0041-msa-send-bundled-envelope/`. All 6 phases shipped same day via the `/build` chunk loop (14 commits + 1 lint-fix + 1 eval report): P1 `3e1b6bd` (Dropbox Sign SDK + `client.ts` lazy `SignatureRequestApi` singleton + `templates.ts` env-version helper + `.env.example` env-var documentation for `DROPBOX_SIGN_API_KEY` / `DROPBOX_SIGN_CLIENT_ID` / `DROPBOX_SIGN_WEBHOOK_SECRET` / `MSA_TEMPLATE_VERSION`; @hookform/resolvers + react-hook-form package.json + lockfile lines pulled in as tag-along from the uncommitted 0035 RHF refactor — those dep declarations now land in committed history but the 0035 code changes in `quote-composer.tsx` + `pricing.ts` remain uncommitted) + bookkeeping `04ff0e9`; P2 `f07a952` (`renderMsaPdf` mirroring `renderQuotePdf` shape — §-clauses 1/2/3/5/9 hardcoded with variables `clientName` / `terminationNoticeDays` / `governingLaw` / `termStart` / `termEnd` interpolated; multi-page renderer that paginates body + auto-pages-on signature-block overflow) + bookkeeping `98f1719`; P3 `22c6a0f` (capabilities `msa:edit` admin+coach / `msa:read` admin+coach+viewer + 14-case admit-set tests; `msa.created/sent/signed/declined` added to `auditAction` pgEnum via `drizzle/0019_msa_audit_actions.sql` + journal `when` bumped to `1779724800000` + `0019_snapshot.json` patched; `createMsaDraft` with FOR-UPDATE dealer lock + same-tx one-MSA-per-dealer guard + `templateVersion` from env; `sendMsaEnvelope` with pre-load → render-both-PDFs → upload-draft-to-GCS → post-to-Dropbox-Sign → atomic guarded UPDATE on `dropbox_sign_document_id IS NULL` → audit; `sendSignatureRequest` + `getSignedFileBytes` helpers added to client.ts; 14-case action.test.ts including idempotent re-send + cross-dealer + API-failure-no-mutation) + bookkeeping `f9fdbb7`; P4 `81f18b1` (webhook route at `/api/dropbox-sign/webhook` with HMAC-SHA256(event_time+event_type, DROPBOX_SIGN_WEBHOOK_SECRET) verify via constant-time `crypto.timingSafeEqual` in `webhook-verify.ts`, dispatch by `signature_request_all_signed` / `signature_request_declined`, `Hello API Event Received` ack body; `markMsaSigned` + `markMsaDeclined` lifecycle helpers — non-`'use server'` so the 0031 action-gate lint rule doesn't flag them — that emit audit rows directly with `actorUserId: null, actorRole: 'system'`; 11-case webhook route test + 7-case lifecycle test + 7-case webhook-verify test) + bookkeeping `cc73442`; P5 `784a080` (MSA panel block on `/dealerships/[id]/page.tsx` rendered **above** the Quotes section per the umbrella-contract framing in `commercial-spine.md`; `loadMsasByDealer` + `loadActiveOrPendingMsa` + `firstDraftQuoteIdForDealer` server-only queries; `MsaCreateDialog` client component + `MsaCreateTrigger` wrapper for `useState`-driven open; status-pill `MSA_STATUS_PILL_CLS` sibling to quote `STATUS_PILL_CLS`; disabled-state UX surfaces "create a draft Quote first" gating + "No customer contact with primary email" gating; 7-case queries test) + bookkeeping `574f212`; P6 `e20ffa4` (`scripts/0041-msa-smoke.ts insert|cleanup` fixture script — pending MSA + signed MSA both against dealer #1, signed PDF GCS key is dummy + 404s when clicked; audit-row purge included in cleanup; sibling pattern to `scripts/0040-cleanup-quote-1.ts`) + bookkeeping `c95641a`. Chunk-end `/eval` at `408e2c9`: initial run FAIL on lint — `// authz: public` marker was in the top-of-file comment block instead of adjacent to the `export async function POST` declaration, fixed at `b3663fc` (the 0031 action-gate lint rule scans for the marker in the function's leading comment, not anywhere in the file). Post-fix verdict PASS-with-warnings: static gate clean (tsc + 757/759 tests + lint 0 errors / 6 pre-existing warnings), browser smoke Phase 1 5/5 + Phase 2 8/8 PASS (MSA panel + dialog verified end-to-end on `/dealerships/1` no-MSA branch + "create one first" gating link; regression-clean on `/calendar` + `/quotes`), Codex 1 High + 3 Medium + 1 Low — all routed to **Parked:** as 0041 follow-ups (a–e). Eval report `closed/0041-msa-send-bundled-envelope/eval-2026-05-12-2229.md` covers the verbatim findings + explicitly-not-an-issue notes. Live Dropbox Sign integration NOT exercised — no sandbox creds in `.env.local`; the mocked-client + webhook-route + lifecycle tests are sufficient to declare 0041 shipped, but the live envelope-post + webhook-signed round-trip is a separate "credentials wired in sandbox" follow-up (0041 follow-up (e)). Active sub-plan flipped to **None** — sequence forward is queued in **Parked:** with 0041 follow-ups (a + c bundled — concurrent-send race + resend UI), 0042-shadcn-ui-sweep (un-park-eligible once 0035 RHF work commits), 0026 follow-ups (a–d), 0025 epic leftovers (7.3 Invoice / 7.4 Payment), 0037 `audience_source_id` drop follow-up. No external `../X` refs to sweep — only CURRENT.md referenced 0041 externally; the moved folder has no `../` links to siblings.
- **2026-05-12** — Scaffolded `0042-shadcn-ui-sweep/plan.md` and added it to **Parked:** (did *not* flip Active — 0041 is mid-flight, and the working tree has uncommitted RHF + zod refactor on `quote-composer.tsx` that this sweep's Phase 3 would re-touch). Surfaced from same-session conversation: user asked whether to adopt the RHF+Zod+shadcn baseline as a project convention after dropping in a forms-stack recommendation; only the RHF+Zod half is already in flight (in `quote-composer.tsx`, uncommitted), and shadcn isn't installed. Plan structure: 7 phases — (1) `shadcn init` with explicit choices (`new-york` style recommended; theme reconciliation via semantic-alias strategy A — `--primary: var(--color-navy)` etc. — to preserve the existing navy/accent/stone palette and Tailwind v4 `@theme` setup, since this repo has *no* `tailwind.config.*`); (2) Form stack (`<Form>` + helpers; optional `src/lib/actions/form-bind.ts` adapter mapping Server Action `{error, fieldErrors?}` results to RHF `setError`); (3) port `quote-composer.tsx` — primitive swap only since the file is already RHF + zod; (4) port `dealer-form.tsx` + `booking-form.tsx` off Radix Form + `useActionState` onto the shared pattern, keeping Server Actions as submission target (TanStack `useMutation` explicitly out of scope per CLAUDE.md's "Mutations go through Server Actions" rule); (5) primitive sweep — replace in-house `dialog`/`combobox`/`tabs` with shadcn equivalents, **keep** `data-table.tsx` (column-config conventions from 0023) and `toaster.tsx` (audit-log hooks from closed/0030); (6) `docs/wiki/forms.md` + remove `@radix-ui/react-form` from `package.json` once the last consumer is ported; (7) full `pnpm test` + browser smoke + chunk-end `/eval`. Un-park trigger: **0041 ships AND uncommitted 0035 quote-composer work is committed** — otherwise Phase 3 will collide with same-file in-flight changes. Seven open questions captured in plan: style choice, theme strategy (A semantic-alias vs B template-edit-on-add), `components.json` paths (Tailwind v4 special-case), Server-Action ↔ RHF helper (shared vs inline), Radix Form removal scope verification, composer-port timing, in-house primitive keep-list. Code Anchors: `quote-composer.tsx:131` (current RHF use, anchor for `<Form>` wrapper); `src/components/ui/dialog.tsx` (in-house primitive shape, anchor for shadcn-installed primitives' file structure); `src/lib/actions/legacy-result.ts:1` (`toLegacyResult` shape, anchor for `form-bind.ts`); `docs/wiki/conventions.md` (sibling wiki page).
- **2026-05-12** — Scaffolded `0041-msa-send-bundled-envelope/plan.md` and set as Active sub-plan under the 0025 umbrella. Surfaced from same-session conversation: user asked "how do we send a MSA" after the 0040 close; answer was "we don't — schema exists from closed/0037 P2, nothing else does." Plan implements 0025 epic Phase 7.2 (Contract). Six phases: (1) Dropbox Sign client `src/lib/dropbox-sign/` (anchored on `gcs.ts` lazy-singleton pattern); (2) `renderMsaPdf` (anchored on `renderQuotePdf` shape); (3) `createMsaDraft` + `sendMsaEnvelope` Server Actions (anchored on `createQuote` + `sendQuote` pair) + add `msa.created/sent/signed/declined` to `auditAction` enum (via `ALTER TYPE … ADD VALUE` precedent in `0016_flat_typhoid_mary.sql`); (4) **route handler** (the rare legitimate one per CLAUDE.md — external caller, Dropbox Sign webhook) at `/api/dropbox-sign/webhook` with HMAC signature verify before any mutation, dispatching into a non-`'use server'` `markMsaSigned`/`markMsaDeclined` helper in `src/features/msa/lifecycle.ts` (sibling of `src/features/quotes/lifecycle.ts`); (5) MSA panel on `/dealerships/[id]` (sibling of 0040's send-receipt panel on `/quotes/[id]`) + `MsaCreateDialog` (single-click create-draft-and-send flow); (6) tests + smoke (mocked Dropbox Sign client; fixture-driven UI smoke; **no live envelope-send** until sandbox creds wired — separate follow-up). v1 scope-locks: **MSA-sign and Quote-send are independent transitions** (an MSA-active gate on `acceptQuote` is in scope but split into a follow-up chunk per 0026 follow-up (b)); one MSA per dealer; no re-sends; no auto-renewal — when `expiresAt` arrives a banner surfaces, renewal is its own chunk. Eight open questions inherited from closed/0037: termination-notice days (recommend 30), renewal flow (out of scope v1), envelope shape (template-side vs upload-inline), `templateVersion` strategy (recommend env-var bump), MSA-gate-on-acceptQuote (recommend yes, separate chunk), cancellation-fee math (lives in 7.3 or 7.4), quotes-per-MSA cap (recommend none v1), webhook security (HMAC-SHA256 with `DROPBOX_SIGN_WEBHOOK_SECRET`). Phase 1 triages which OQs need answers before Phase 3 kicks off. Inherits the new `/build` cadence shipped during 0040 (fast `tsc + test` gate per phase, single full `/eval` at chunk-end).
- **2026-05-12** — Closed `0040-quote-send-receipts` → `docs/designs/closed/0040-quote-send-receipts/`. All 5 phases shipped same day via the **first invocation of the updated `/build` skill cadence** (fast gate `tsc + test` per phase, single full `/eval` at chunk-end — replaces the prior per-phase Codex+web-test). Commits: P1 `ba16de8` (schema add `sent_to_email` + `sent_to_first_name` on `quotes`, migration `drizzle/0018_acoustic_cannonball.sql`; Codex flagged a journal `when` non-monotonicity Low, fixed in-chunk) + bookkeeping `71f37f1`; P2 `3d0aed1` (`sendQuote` UPDATE writes the denorm in the same atomic `draft → sent` flip; `loadQuote` projection extended) + `eb75034`; P3 `023a7cc` (`loadQuoteSendReceipt` audit-row reader + `signedQuotePdfUrl` Server Action; first production caller of `gcs.ts signedUrl`) + `9296da4`; P4 `52a0ddd` (send-receipt panel between header and composer on `/quotes/[id]`, server-renders the GCS signed URL directly since `assertCan('quote:edit')` already gated the page — server-action `signedQuotePdfUrl` reserved for future client-side refresh paths; side-fix added `pdfStorageKey` to the `Quote` type/projection that wasn't carried before) + `50d9171`; P5 `a7ec213` (test coverage — extended `sendQuote` happy-path to assert `patch.sentToEmail`/`sentToFirstName`, extended `loadQuotes` round-trip baseRow + a "lifecycle timestamps" assertion to cover the new denorm + `pdfStorageKey`, added a 2-case `loadQuoteSendReceipt` suite). Chunk-end `/eval` at `ed0bef0` — static + browser smoke clean; Codex SKIPPED (subagent returned no output; treated as PASS with caveat per the eval skill contract). Browser smoke confirmed the panel renders on `/quotes/1` with the "(recipient unknown)" fallback (the existing sent quote predated this chunk's denorm) + a working GCS V4 signed URL link. Quote #1 + 2 audit rows + GCS object subsequently deleted via `scripts/0040-cleanup-quote-1.ts --apply` (user-authorized via `AskUserQuestion`; classifier blocked direct psql + the dry-run, user ran the script via `!`-prefix shell themselves) — `/quotes` index now `All (0)`. Side-effect during smoke: first `pnpm db:migrate` run reported success but the runtime DB didn't have the column (`/quotes/1` 500'd until a second `db:migrate` invocation landed it) — flagged in the eval report for future investigation; no reproducible test today. Active sub-plan flipped to **None** — sequence forward is 0026 follow-ups (a–d) → 0025 7.2/7.3/7.4. The `/build` skill itself was updated mid-chunk (between Phase 2 and 3) — old per-phase `/eval` cadence kept emitting Codex Low ceremony on tiny mechanical phases (e.g. the Phase 1 journal-`when` finding cost more than the fix), so it now runs `tsc + test` per phase and saves Codex + web-test + lint for the chunk-end. Skill change at `.claude/skills/build/SKILL.md` (untracked — `.claude/` gitignored).
- **2026-05-12** — Scaffolded `0040-quote-send-receipts/plan.md` and set as Active sub-plan under the 0025 umbrella. Switched away from `None` (slot had been idle since 0035 closed earlier the same day). Surfaced from same-session conversation: user asked "is the data available about the send we could surface to the quote?" while reviewing the RHF+zod refactor of `quote-composer.tsx` — answered with the inventory (sentAt + pdfStorageKey + acceptToken already on the row; audit_log has actor + Resend `emailId`; missing piece is recipient-email denorm since `resolveQuoteRecipient` runs against the dealer's *current* primary contact and would lie after a contact rotation). User said "no make a chunk to do it properly." Plan structure: 5 phases — (1) schema add `sent_to_email` + `sent_to_first_name` (anchor on `pdfStorageKey` shape + the `0011_slim_wonder_man.sql` ADD-COLUMN-to-quotes migration); (2) wire `sendQuote` to denormalize in the same atomic UPDATE as `sentAt`/`pdfStorageKey`, extend `loadQuote` projection (anchor on `actions.ts:683-704` + `queries.ts:50-52`); (3) audit-row reader (`loadQuoteSendReceipt`, first reader of `auditLog` so anchored on `loadQuote`'s query shape) + new `signedQuotePdfUrl` Server Action (first caller of `gcs.ts:94 signedUrl`; sibling of `previewQuotePdf`); (4) send-receipt panel rendered between header and composer on `/quotes/[id]/page.tsx` (anchor on the existing header+status-pill block lines 47-67); (5) integration tests + smoke. Denorm is **set-once on the `draft → sent` flip** — re-sends not a v1 thing; if 0026 follow-up (a) degraded-send retry lands later, that chunk owns the re-send semantics. Audit `payload` schema intentionally **not** extended (avoids a parallel source of truth). Touches the **0026 follow-ups (c) staff accept/decline UI affordance** queue lightly — the panel scaffolds the surface where Accept/Decline buttons could later attach, but (c) is not in scope for 0040.
- **2026-05-12** — Closed `0035-quote-composer` → `docs/designs/closed/0035-quote-composer/`. Phase 4 (PDF preview pane + Send wiring on `/quotes/[id]`) shipped via `/build` chunk loop: `feat(quotes): pdf preview + send wiring on composer (0035 Phase 4)` at `fea4d82` + bookkeeping `docs(plan): mark 0035 Phase 4 commit + eval report` at `6ebb235`. New `previewQuotePdf` Server Action (renders the PDF from the persisted snapshot, returns `data:application/pdf;base64,…` for an `<iframe src=…>`; no GCS, no email, no lifecycle change) + UI surface in `quote-composer.tsx` (Preview/Send buttons, two Radix dialogs, dirty-state Send gate, recipient resolved server-side via `resolveQuoteRecipient` and threaded as a `Recipient` prop). MSA-gate per the 2026-05-11 commercial-spine alignment **deferred** to 0025 P7.2 — both 7.2 and the in-app MSA-creation surface are unscaffolded, so route (a) (active-MSA → `sendQuote`) is the only path today; Send-without-MSA support comes back when 7.2 lands. Codex pass-1 surfaced 1 High + 1 Medium, both self-resolved in-pass (orchestrator didn't dispatch `codex:rescue` — fixes were mechanical UI tweaks): **High** = confirm-dialog totals came from live `computed` while `sendQuote` emits the persisted snapshot, fixed with an `isDirty` memo (Send disabled when dirty + dialog reads `initial.lineItems.length` / `initial.total`); **Medium** = Preview button sat inside `<fieldset disabled={isReadOnly}>` and cascaded-disabled on sent quotes, fixed by pulling the buttons row outside the fieldset. Browser smoke clean (Phase 1 3/3, Phase 2 7/7, plus a post-fix re-verify that Preview opens on a sent quote). Eval report at `closed/0035-quote-composer/eval-2026-05-12-1650.md`. **Two follow-up commits** (`7366bbd` then `87022c6`) shipped after the close-the-chunk commit landed, on a different bug surfaced during the live send: stranded quote #1 hit the "Quote cannot be sent from status 'draft'" fallthrough because the atomic UPDATE's `eq(quotes.updatedAt, draft.updatedAt)` compared a microsecond-precision Postgres `timestamptz` against a millisecond-precision JS Date roundtrip — only matters when the row's `updated_at` was written by non-Drizzle SQL (e.g. `now()` in a one-off `UPDATE` to un-strand a previous degraded send). Fix: `date_trunc('milliseconds', updated_at) = $1::timestamptz` with explicit `.toISOString()` binding because drizzle's `sql\`\`` template doesn't propagate column-type metadata to bound JS Dates (postgres-js throws `ERR_INVALID_ARG_TYPE`). The original separate-issue trap that *caused* the strand: `GCS_CREDENTIALS_JSON` in `.env.local` was a **file path** but `gcs.ts:39-46` only handles inline JSON; `JSON.parse(path)` throws → `putObject` returns `{ error: 'GCS_CREDENTIALS_JSON is not valid JSON.' }` → `sendQuote` returns the "Quote sent but PDF upload failed; admin repair required" degraded path after the UPDATE already flipped status to `sent`. User-side fix: rename env to `GOOGLE_APPLICATION_CREDENTIALS=/path` (ADC fallback at `gcs.ts:37-49` picks it up, no code change); restart `pnpm dev` to clear the module-level cached `Storage` client (`gcs.ts:22`). Live verification: real send to `shannon@salesability.ca` dev-redirected to `david.hogan@networknode.ca` with subject prefix `[DEV→…]`, Resend `emailId 28a24ca4-2ec8-440e-8978-a758ff00567e`, PDF at `quotes/1/1.pdf` in `event-pro` bucket, `quote.sent` audit emitted. Cross-refs swept: `CURRENT.md` (this entry); `closed/0026-quote-pdf/plan.md` (`../../0035-…` → `../0035-…`); `closed/0037-commercial-spine-msa/eval-2026-05-11-1637.md` (absolute path got `closed/` injected); `closed/0038-rename-audience-sources/plan.md` (same); `wiki/log.md` (same). Internal `../X` refs inside moved 0035/plan.md climbed: `../0025-…` → `../../0025-…`, `../closed/0026-…` → `../0026-…`, `../closed/0037-…` → `../0037-…`, `../../wiki/…` → `../../../wiki/…`. Eval reports inside the moved folder kept their historical `docs/designs/0035-quote-composer/...` Plan headers (snapshot convention). Active sub-plan is now **None** — sequence forward is 0026 follow-ups (a–d) → 0025 7.2/7.3/7.4. Two new feedback memories worth saving: (i) Drizzle `sql\`\`` template doesn't propagate column-type metadata to bound JS Dates → pass `.toISOString()` + explicit `::timestamptz` cast; (ii) `<fieldset disabled>` cascades through descendant `<button>`s — pull always-enabled buttons outside the fieldset.
- **2026-05-12** — Closed `0039-quote-visibility` → `docs/designs/closed/0039-quote-visibility/`. All 5 phases shipped same day via `/build` chunk loop: P1 query layer (`d04afeb`), P2 `/quotes` index + filter pills + nav entry (`4a772d2`; Codex pass-1 3 Medium + 2 Low self-resolved — stale-state `pushParams` closure, `searchParams` array shape, nav-tab capability gap leaking `Quotes` to non-coach/non-admin staff, unvalidated URL status, actions-column a11y; orchestrator caught Codex's first attempt at a rules-of-hooks violation `useCan` inside `.filter()` and replaced with `useContext(CapabilityContext)` + bound `can` predicate), P3 `/quotes/[id]` edit-mode + composer `initial` prop + redirect fix (`56006fb`; Codex 2 Medium self-resolved — archived-dealer `—` fallback fixed by threading `dealerName` through `initial`, read-only catalog drift fixed by surfacing persisted `lineItems`/`subtotal`/`tax`/`total` from `loadQuote`; pass-2 1 Low deferred per "trust internal writes"), P4 `/dealerships/[id]` detail + dealer-name link + `parsePositiveIntPathSegment` helper (`b484de4`; Codex 2 Medium self-resolved — archived-dealer dead link routed to plain text, lax `Number.isInteger` validation tightened to `/^\d+$/` regex on both dynamic routes; pass-2 Medium re: trailing-`\n` was a verified false positive — `/^\d+$/.test('1\n')` returns `false` in JS), P5 closeout (`5eded2d` + `036e4e7`). UI polish on top: `a2e71ae` (table fills card width with `w-full` + Dealer `<th>` absorbing slack with `w-full`; nav reorder placing Reports right of Quotes). Also cleared pre-existing tsc fallout from 0037 P4 in `scripts/import-from-sheets.ts` (five dead lines referencing dropped commercial columns) up-front so the Phase 1 static gate cleared. 0035 P4 promoted Parked → Active per the queued sequence — it adds the email-a-quote UI surface (PDF preview pane + Send button on `/quotes/new`), gated on GCS+Resend creds being wired in `.env.local`. **No new Server Action, no migration in 0039** — every save path reuses the existing `setQuoteInputs` / `setQuoteTax` / `setQuoteDealer` setters on draft rows; the lifecycle guard (`actions.ts:281-289`) keeps non-draft quotes immutable. Cross-refs swept: CURRENT.md (3 lines including this entry); internal `../0025-...` ref inside moved folder climbed to `../../`.
- **2026-05-12** — Scaffolded `0039-quote-visibility/plan.md` and set as Active sub-plan under the 0025 umbrella. Surfaced in same-session conversation immediately after 0037 closed: user asked "where in the UI is the start of the prospect-to-invoice flow" → answered with `/dealerships` + `+ Add Dealer` + per-row `Quote` action → user noticed composer saves dump to `/production` (a leftover placeholder from 0035 P3 ship `0a17124`, `quote-composer.tsx:96`) → confirmed there's no `/quotes` index, no `/dealerships/[id]` detail, no quote-history surface today (only `pnpm db:studio` against the `quotes` table can show them). User said "yes" to a small chunk to close the gap, then immediately followed with "we need to add the edit route too" — scope expanded same-session to 5 phases. Final shape: query layer (`loadQuotes` / `loadQuote` / `loadQuotesByDealer`, anchor on `loadCampaigns`) → `/quotes` index page (anchor on `production/page.tsx`) → `/quotes/[id]` edit-mode (composer accepts initial-values prop; save handler branches between `createQuote` and the existing draft-only `setQuoteInputs` setter — no new Server Action, no migration, lifecycle-guard already present at `actions.ts:281-289`) → `/dealerships/[id]` detail + dealer-name link → tests + smoke. Composer `/production` redirect fix folded into Phase 3 (post-create now lands on `/quotes/<id>` so subsequent saves are UPDATEs). 0035 P4 kicked from Active to Parked (was briefly Active immediately after 0037 close).
- **2026-05-12** — Closed `0037-commercial-spine-msa` → `docs/designs/closed/0037-commercial-spine-msa/`. All 5 phases shipped (Phases 1–3 on 2026-05-11; Phases 4 + 5 today via `/build` chunk loop). Phase 4 shipped at `b089d47` ("refactor(db): drop legacy commercial cols from campaigns (0037 Phase 4)") with **scope narrowed mid-flight**: the original plan dropped six columns; pre-drop audit found `audience_source_id` has real readers in the booking-form Data Source dropdown + calendar event-detail popover + production view + two CSV exports + the `Campaign` type / `loadCampaigns` join — none for "commercial purposes" but dropping the column would have stripped Data Source attribution from the direct-booking path on `/calendar`. User-confirmed via `AskUserQuestion` (Option A: drop 5, defer 6th; no backfill needed) — five strictly-commercial cols (`fee`/`travel`/`deposit_pct`/`tax_pct`/`quote_valid_days`) dropped via `drizzle/0017_tranquil_living_mummy.sql`; `audience_source_id` deferred to a follow-up chunk that rewires the booking-form/composer flow first. Drizzle journal `when` for 0017 hand-bumped (generator-default fell before 0016 → monotonic bump to 1779552000000, same pattern as 0026 P2 / 0038 P1). Phase 5 (`docs(wiki): post-drop sweep` at `035d5e6` + bookkeeping `8143c1c`) ran the wiki sweep doc-only: `data-model.md` campaigns row + walkthrough rewritten to "operational delivery"; `commercial-spine.md` Phase 4 description records the narrowing; new `log.md` 2026-05-12 entry "Commercial spine landed"; 0025 plan annotated; CURRENT.md updated; CRT cross-refs swept. Codex P5 pass-1 surfaced 2 Medium + 2 Low (log.md replace-vs-insert headerless-bullets bug; CURRENT.md stale Status line; "no edits needed" claim false; commercial-spine.md stale future-tense cross-link) — all resolved in-pass per `feedback_docs_eval_single_pass.md` (single-pass policy on doc-only diffs; no pass-2). Eval reports at `closed/0037-commercial-spine-msa/eval-2026-05-12-1230.md` (Phase 4) and `eval-2026-05-12-1238.md` (Phase 5). External cross-refs swept: `0025/plan.md` (2 refs), `0035/plan.md` (1 ref), `closed/0026-quote-pdf/plan.md` (1 ref climbed `../../` → `../`), `wiki/commercial-spine.md` (preamble link + Cross-links section past-tense). Browser smoke (Phase 4 eval): public + gated PASS; "Book Event" dialog still renders Data Source field reading the retained `campaigns.audience_source_id`. Active sub-plan flipped to **0035-quote-composer Phase 4** (PDF preview + Send wiring polish; low-urgency).
- **2026-05-12** — Closed `0026-quote-pdf` → `docs/designs/closed/0026-quote-pdf/`. Phase 5 (tests + smoke + wiki close-out) shipped via `/build` chunk loop: `docs(quotes): wiki + plan close-out (0026 Phase 5)` at `7842063` + bookkeeping `docs(plan): mark 0026 Phase 5 commit + eval report` at `ae17a33`. All 5 phases now Done — chunk fully shipped. Codex pass-1 surfaced 1 High + 4 Medium + 2 Low factual drifts in the new wiki prose (incorrect "inside `acceptQuote`'s transaction" claim; `MAX_LINE_ITEMS=25` should be 13; degraded-state audit-payload overstatement; predicate-blindness parking pointer mis-pointed at 0033 vs 0018; commercial-spine.md still described the dropped public-accept-link surface in three places; architecture.md:102 stale `sendQuote` stub claim; `audience_source_id` "v1 sets at composer-time" overstated; `toRendererLines` bad symbol pointer) — all corrected directly in same eval pass without a pass-2 verification round per new feedback memory `feedback_docs_eval_single_pass.md` (doc fact-check loops don't converge cheaply; single-pass is the policy). Eval report at `closed/0026-quote-pdf/eval-2026-05-12-1143.md`. Browser smoke clean (Phase 1 + Phase 2 + quote-composer page render verified at `/quotes/new?dealerId=1`); 673 vitest tests pass. Four 0026 follow-ups deferred to chunk-sized future work and captured under "0026 follow-ups" in Parked: (a) degraded-send retry path + `emailed_at` schema, (b) split `quote:send` from `quote:edit`, (c) staff accept/decline UI affordance, (d) live-send dev smoke once GCS+Resend creds wired in a sandboxed env. External cross-refs swept: `0025/plan.md` sub-plan link, `0035/plan.md` sibling link, `0037/plan.md` (5 inline path mentions), `wiki/architecture.md` (2 links), `wiki/log.md` (1 link). Internal `../X` refs in moved 0026/plan.md climbed to `../../X`. Eval reports inside the moved folder kept their historical `docs/designs/0026-quote-pdf/...` Plan headers (snapshot convention, same as 0036/0004 closes). Active sub-plan flipped to **0037-commercial-spine-msa Phase 4** — destructive column drops on `campaigns`, needs confirm-before-run.
- **2026-05-12** — `0035-quote-composer` Phase 5 (wiki bookkeeping pass) shipped via `/build` chunk loop: `docs(wiki): document service_items + dealers.status; quote-composer architecture (0035 Phase 5)` at `3f9edbe` + bookkeeping `docs(plan): mark 0035 Phase 5 commit + eval pass-1/2/3 reports` at `84a6760`. Three Codex review passes self-resolved 3 High + 4 Medium + 2 Low factual inaccuracies (composer-setter overclaim, /dealerships coach-gating misattribution, money-parse `MONEY_RE` cross-attribution, "Add new prospect" entry-point claimed shipped when deferred, MSA gate framed as current when planned, stale `blocked_dates` in audit-columns paragraph, quote-row precision overgeneralized as `numeric(12,2)`, log-entry MSA-gate framing inconsistent with corrected architecture.md). All review passes kept strictly read-only / findings-only per the new feedback memory `feedback_codex_review_invocations.md` — verdict is the orchestrator's call, not the reviewer's. Eval report at `0035-quote-composer/eval-2026-05-12-0841.md`. Plan now 80% (4/5 Done); Phase 4 stays Blocked on 0026 P3+P4 (which is itself blocked on GCS creds carry-forward). Phase 4 won't move until either (a) 0026 P3 lands (needing `GCS_PROJECT_ID` / `GCS_BUCKET` / `GCS_CREDENTIALS_JSON` in `.env.local`), or (b) the sequence is re-ordered. Sequence remains: **(pause)** → 0026 P3 → 0026 P4–P5 → 0037 P4 cleanup → 0037 P5 sweep → 0035 P4 → 0026 close.
- **2026-05-11** — `0035-quote-composer` Phases 1+2+3 all shipped in one `/build` chunk-loop invocation (six commits): P1 service-catalog data model + admin UI at `bfaeff7` (Codex pass-1 4 Mediums self-resolved → pass-2 clean — un-archive recovery, money decimal-precision, overflow caps, gate-matrix coverage), bookkeeping `4453b11`; P2 prospect status + acquired_via + filter pills at `dcc80bc` (Codex pass-1 2 Mediums + 1 Low self-resolved → pass-2 "Ship" — TOCTOU on `updateDealer` archive-race closed via guarded UPDATE inside transaction with patch-style omit-when-absent for `status`/`acquiredVia`, Edit-on-archived UX bug hidden), bookkeeping `9de58f3`; P3 pricing module + composer page at `0a17124` (Codex pass-1 1 High + 4 Mediums → pass-2 2 Mediums residual → pass-3 "Ship" — setter race silently-OK bug, arbitrary JSON keys persisted, record-retrieval range-fail-open, tax-cap inconsistency, dealer archive race; pass-3 cleared the residual range-fail-closed + tax-rounding canonicalization), bookkeeping `567415c`. Tests went 542 → 655 (+113). 11 Codex findings + 1 new-medium self-resolved across 3 phases. Audit-action enum gained `dealer.activated` via `ALTER TYPE ... ADD VALUE` (migration `0016`). Paused at Phase 4 per plan body's "Blocked on 0026/4" instruction; Phase 5 partial (wiki updates + inline-prospect smoke pending). New `MAX_DOLLARS` constant exported from `pricing.ts` to canonicalize tax cap across all three persistence paths. Combobox primitive's lack of inline-add support deferred the composer's "Add new prospect" affordance — DealerForm already accepts `defaultStatus='prospect'` for the eventual wire-up.
- **2026-05-11** — `0026-quote-pdf` Phase 2 shipped via `/build` chunk loop (`6c65c80`: `quotes` table + bare-bones Server Actions + RLS + follow-up migration `0011` adding `sent_at`/`accepted_at`/`declined_at` lifecycle timestamps; bookkeeping at `55dd03c`). Codex pass-1 surfaced 1 High (TOCTOU on `markQuoteAccepted`/`markQuoteDeclined` SELECT-then-UPDATE) + 4 Medium (same TOCTOU on `sendQuote`, missing `archivedAt IS NULL` check on `createQuote`, missing row-local lifecycle timestamps, `quotes` missing from RLS test list) + 3 Lows; direct self-resolve made transitions atomic via `UPDATE … WHERE id + status = from RETURNING id` (cancelCampaign pattern) with re-select on miss for not-found/idempotent/illegal-status; pass-2 Codex verified all High/Medium closed, 1 residual Low (test-mock predicate-blindness — repo-wide parked follow-up, same disclaimer on `people/actions.test.ts`). 19 new vitest cases, 523/523 green. Migration journal `when` bumps stay monotonic. Active sub-plan flipped to `0035-quote-composer` P1 because 0026 P3 is blocked on: (a) the `service_items` catalog 0035 P1 owns (needed for `lineItems` computation in the quote PDF), and (b) the Phase 1 GCS-creds carry-forward (`GCS_PROJECT_ID`/`GCS_BUCKET`/`GCS_CREDENTIALS_JSON` still empty in `.env.local`). Umbrella stays 0025.
- **2026-05-11** — `0037-commercial-spine-msa` Phases 1–3 shipped same session via the chunk-level `/build` loop: Phase 1 decision doc + reconciliation at `3b9b18e` (already in place before today's session); Phase 2 `master_service_agreements` schema + migration `0008_bright_lockjaw.sql` + RLS in `0009_msa_rls.sql` + reachability test + wiki MSA section at `da05c54` (`/build` loop cleared eval P1 verdict — Codex High RLS-missing + Medium zero-rows-assertion + 4 Lows — via in-skill self-resolve + pass-2 Codex verify); Phase 3 bookkeeping close-out at `46db02b` confirming all four Phase-3 checklist items were already executed during Phase 1's cross-plan reconciliation (Codex spot-checked the four citations to `0026/plan.md:69-71`, `:72`, `:108`, and `0035/plan.md:126`). Phases 4–5 paused 2026-05-11: P4 (drop commercial cols from `campaigns`) gated on 0026 P2 + 0035 P3 + destructive enough to need confirm-before-run; P5 (wiki sweep) is post-P4. Active plan flipped to `0025-quote-to-payment` umbrella with `0026-quote-pdf` resumed as Active sub-plan at Phase 2 (`quotes` data model + Server Actions), the next link in the chain. The MSA + commercial-spine prerequisites that justified parking 0026 on 2026-05-08 are now in place; 0026 P2 can ship cleanly with the locked-in column shape.
- **2026-05-11** — Closed `0038-rename-audience-sources` → `docs/designs/closed/0038-rename-audience-sources/`. All 4 phases shipped same day: Phases 1+2 atomic at `1b64d12` (schema rename `sales_lead_sources` → `audience_sources` + 20-file call-site sweep via `sed`), Phases 3+4 at `31f0ca2` (wiki + cross-plan reconciliation in `data-model.md`, `log.md`, 0035, 0037, CURRENT.md), bookkeeping at `63cd952`. Zero data migration (`ALTER TABLE … RENAME` preserved all 4 seeded rows + 13 campaign FK values). Notable deviations: (1) `pnpm db:generate` TTY-blocked in harness → used `--custom` + hand-written RENAME SQL + `sed`-patched snapshot + bumped journal `when`; (2) Codex re-review (post-Phase 2) stalled in `verifying` for 12 min → cancelled, SKIPPED per eval rules — Phase 1 Codex review (in `eval-2026-05-11-1404.md`) still covers the diff's risk surface; (3) plan assumed UI heading was "Sales Lead Sources" but it was already "Data Sources" (neutral) — left unchanged, flagged for a separate UX chunk if "Audience Sources" should be the canonical UI vocabulary. Carry-forward: `ALTER POLICY … RENAME TO` for `sales_lead_sources_*` policies in `drizzle/0003_enable_rls.sql:188-198` (cosmetic — bound by OID, not name). External refs were limited to `CURRENT.md`; internal absolute-path self-refs in the moved folder rewrote `docs/designs/0038-…` → `docs/designs/closed/0038-…`. Active plan flipped to `0037-commercial-spine-msa` per the queued sequence — its Phase 3 was the original driver for 0038 (now lands cleanly under `audienceSourceId`).
- **2026-05-11** — Closed `0036-capabilities-only` → `docs/designs/closed/0036-capabilities-only/`. Same bookkeeping drift class as 0004's 5.7 cell: all 4 phases shipped 2026-05-08/09 (last code commit `6253773` "retire require-role.ts; declare capabilities-only-at-gates" + bookkeeping commit `69f00f0` "mark Phase 4 commit + close chunk"); plan tracker correctly showed 4/4 Done; the folder simply never got moved and CURRENT.md kept claiming Active. End state per `wiki/log.md` 2026-05-09 entry: capabilities-only at gates declared the durable convention; 4 new capabilities (`app:access`/`admin:access`/`reports:view`/`availability:edit`); `roleListClient` factory + `require-role.ts` deleted; 480/481 vitest. No external refs to sweep (only CURRENT.md referenced 0036). Active plan flipped to `0038-rename-audience-sources` per the queued sequence.
- **2026-05-11** — Closed `0004-port-migration` umbrella → `docs/designs/closed/0004-port-migration/`. Phases 1–4 + sub-phases 5.1–5.6 had long since shipped; 5.7 (Booking summary & reports) shipped 2026-05-07 via the closed `0014-summary-reports/` but the umbrella tracker still showed `Pending` — corrected to `Done` (`155df20`). 5.8 (Full-calendar share link) dropped without shipping per user: per-coach share at `/share/coach/[id]` covers the real use case. Sub-plan `0010-calendar-share-full/` also moved to `closed/` with an abandoned header; body stays as a starting point if "shareable read-only link" returns per roadmap Phase 2. Cross-refs swept in `docs/designs/0025-quote-to-payment/plan.md`, `docs/strategy/roadmap.md`, `docs/wiki/log.md` (today's funnel entry's path updated), plus internal path-climb (`../X` → `../../X` for non-closed siblings) inside 0004's plan + open-questions. Active sub-plan stays at 0036 — no focus switch.
- **2026-05-11** — Scaffolded `0038-rename-audience-sources/plan.md` and added to Parked **ahead of 0037 Phase 3**. Surfaced from a data-model review of 0037 (same day): the `sales_lead_sources` lookup carried three overloaded meanings — (a) audience source for a dealer's marketing campaign (the only meaning actually in use; seeded values `Dealer Database / PBS / Third Party List / Previous Buyers`), (b) reserved-for-future per-`(campaign × contact)` target table per `docs/wiki/data-model.md` OQ #6 (never built), (c) acquisition source for dealerships (already split off onto `dealers.acquiredVia` per the same-day funnel review). 0037 Phase 3 would propagate the misleading `salesLeadSourceId` name onto the new `quotes` table; renaming first means `quotes.audienceSourceId` lands cleanly. Four phases (~half-day): schema rename + migration (RENAME TO, zero data migration); sweep call sites in `src/features/schedule/{actions,lookup-admin}.tsx` + tests + any reports; wiki + cross-plan reconciliation (kills `data-model.md` OQ #6 reserved-for claim, updates 0037 P3 checklist to `audienceSourceId`); tests + smoke. Recommended sequence updated to `0036 → 0038 → 0037 P1+P2 → 0026 P2 → 0035 → 0037 P4`. Active sub-plan stays at 0036 — no focus switch. Same review also locked in 30 days for 0037 OQ #1 (MSA §2.ii termination-notice gap).
- **2026-05-11** — Funnel review pass. Three changes: (1) introduced `docs/designs/future/` as a third bucket parallel to `closed/` for chunks that are scaffolded but explicitly waiting on a trigger event (e.g. "v2", "when web intake matters"). Distinct from CURRENT.md **Parked** which means "queued for soon." Convention captured in CLAUDE.md `### Deferring a chunk`. (2) Moved `0016-book-your-event-intake` → `future/0016-book-your-event-intake` after the user clarified v1 uses manual in-app intake (existing booking modal + 0035 P2 inline-create dealer flow); web intake is v2. Added deferral header to the plan; swept cross-refs in `docs/designs/0004-port-migration/open-questions.md`, `docs/strategy/vision.md`, `docs/wiki/security.md`. New **Future:** field in CURRENT.md captures the un-defer trigger. (3) Same review collapsed 0037 OQ #7 to "no lead entity in v1; `salesLeadSourceId` set at composer time, never propagated" — surfaced that `sales_lead_sources` was being overloaded between audience-source (consumer list) and acquisition-source (how the dealership found us); split the second concept off into a new `dealers.acquiredVia` text column added to 0035 Phase 2. Active sub-plan stays at 0036 — no focus switch.
- **2026-05-11** — Scaffolded `0037-commercial-spine-msa/plan.md` and added to Parked **ahead of 0026/0035** as a prerequisite to 0026 Phase 2 unparking. Surfaced from a sales-funnel discussion that worked upstream from the existing 0026/0035 plans: user asked "shouldn't a quote move to an order which then creates a campaign?" Schema inspection confirmed `campaigns` was carrying double-duty commercial+operational columns (`fee`, `travel`, `depositPct`, `taxPct`, `quoteValidDays`, `salesLeadSourceId`) on what should be a pure delivery record. User then supplied the full Salesability MSA text, which resolved the question definitively — §1.iii: *"Each Quote issued by Salesability and accepted by the Client shall constitute a separate distinct and independent agreement and contractual obligation of the Parties hereto."* So accepted Quote IS the contract — no separate `orders` table. Locked: (1) `master_service_agreements` table introduced — per-Client, 12-month term per §2.i, with `signedAt`, `expiresAt`, `signedPdfStorageKey`, `dropboxSignDocumentId`, status enum, `templateVersion`, termination cols (§2.ii); (2) commercial columns move off `campaigns` onto `quotes`; (3) FK direction flips to `campaigns.acceptedQuoteId` so the operational delivery (Event in MSA noun) hangs off the accepted Quote; (4) first-deal e-sig **bundles MSA + first Quote in one Dropbox Sign envelope** (user 2026-05-11 — single envelope with two documents, not merged PDF); (5) NS HST 15% confirmed as seller-side default per §9 governing law (Dartmouth NS — partially resolves 0026's tax OQ). Five phases scaffolded: decision doc + cross-plan reconciliation (edits 0025/0026/0035 plan docs); MSA schema + migration; quotes schema patch into 0026 Phase 2 sketch; drop commercial cols from `campaigns` (gated on 0026 P2 + 0035 P3); tests + wiki sweep. Eight open questions captured (termination-notice `XX days`, quote validity default, MSA renewal flow, cancellation-fee math, envelope shape, `templateVersion` strategy, lead → quote attribution propagation, quotes-per-MSA-term). Two related project memories saved: `project_coach_owned_business` (multi-tenant-by-coach: leads/campaigns/quotes coach-owned, dealers/contacts shared registry — drives upstream funnel design) and `project_msa_structure` (the MSA clauses driving the schema). Active sub-plan stays at 0036 — no focus switch.
- **2026-05-08** — Promoted `0036-capabilities-only` from Parked → Active per user request ("can we build 0036 now"). Paused `0026-quote-pdf` (Phase 7.1) mid-flight; Phase 1 already shipped at `3c4b5b9`. 0036 is a standalone plan (no umbrella). 0026 returns to Active sub-plan slot under 0025-quote-to-payment after 0036 Phase 4 ships. Rationale matches the original parking note: ahead of 0035 so the composer's `quote:edit` inherits the uniform convention.
- **2026-05-08** — Scaffolded `0036-capabilities-only/plan.md` and added to Parked **ahead of 0035** as a half-day authz cleanup. Surfaced from the 0035 Q3 discussion (composer authorization gate): inventory showed 22 capability sites + 3 `roleListClient` stragglers (availability blocks) + 6 page-level `requireRole` / `requireStaffAccess` calls + 1 reports/export route handler, so the codebase is *almost* unified. The chunk collapses every gate at the app layer onto `assertCan` / `capabilityClient`; adds 4 capabilities (`app:access`, `admin:access`, `reports:view`, `availability:edit`); deletes `roleListClient` from `action-client.ts`; 0034 pairing CI then covers every gated action with no role-list opt-outs. Edge middleware (`ADMIN_PATHS`) keeps its JWT-claim path-match as defense Layer 1 — explicitly an optimization, not the durable answer. Four phases: matrix rows + tests, page+layout migration, action migration + roleListClient delete, docs + smoke. Sequencing: parallelizable with 0035 Phases 1+2 (catalog + dealer status — pure data-model work, no authz overlap); 0035 Phase 3 ideally lands after 0036 so `quote:edit` inherits the uniform convention without re-establishing it. Original active sub-plan stays at 0026; 0036 + 0035 stay parked until the user picks which to promote next.
- **2026-05-08** — Scaffolded `0035-quote-composer/plan.md` and added to Parked (kept 0026-quote-pdf as Active sub-plan; its Phase 1 shipped earlier today at `3c4b5b9`). Surfaced when the user asked for "a way for coach to create a quote and send it" plus "a quote build system" — turned into a real chunk after the user supplied concrete v1 pricing (Base Event $6,900 incl. 500 records, BDC $2.25/call, Letter $2.50, Digital $0.59, Additional Day $995, Record Retrieval $100–$400, Travel pass-through, plus a per-contact "Additional Contacts" item for audience > 500). Decisions locked in same conversation: (1) DB-backed `service_items` table — admin-editable prices, surfaces under `/admin/lookups`; (2) `status: 'prospect'|'active'` flag on `dealers` (no separate prospects table; `archivedAt` from the existing `archivable` mixin stays the source of truth for archived state, independent of `status`); (3) multi-channel touches per record allowed (no cap enforcement). Five phases scaffolded: catalog data model + admin UI; dealer status + inline-create; pricing module + composer page at `/quotes/new`; PDF preview + send wiring (depends on 0026 Phases 3–4); tests + smoke. Sequencing: Phases 1+2 are independent of 0026 and can run in parallel with 0026 Phase 2; Phase 3 needs 0026 Phase 2 (`quotes` table) first; Phase 4 needs 0026 Phases 3+4. Six open questions captured (capability key, Additional Contact price TBD, travel sub-fields, campaign-detail entry point, quote↔campaign nullability, custom one-off lines v2). Two resolved same-turn: #1 (status='archived' vs archivedAt) → `archivedAt` is the source of truth (`status` enum becomes `'prospect'|'active'` only); #2 (audience-overage UX) → composer pivoted from line-item-picker to **structured-input calculator** (audience + days + per-channel counts as inputs; line items are computed read-only output) so the input snapshot becomes the quote→invoice contract — drift class of bug eliminated. Phase 3 of 0035 fully rewritten to the calculator shape; 0026 Phase 2's `quotes` schema updated to add the `inputs` jsonb column the invoice recomputes against.
- **2026-05-08** — `0033-next-safe-action` → 4/5 phases shipped (commit `0d2fe95`); Phase 4 (retire validators / write Zod schemas) deferred as carry-forward to revisit alongside 0016 booking-intake. Active plan switched back to `0025-quote-to-payment` with `0026-quote-pdf` as active sub-plan (resuming Phase 7.1 mid-flight from where it was paused 2026-05-08 AM). All four authz tooling/migration chunks now in place: 0031 lint catches gate-missing, 0032 test catches wrong-admit-set, 0034 script catches Can/assertCan-asymmetric, 0033 middleware makes auth-required the *default* invocation pattern. Migration touched: bootstrap `src/lib/actions/action-client.ts` (4 tiers — `baseClient`, `authedClient`, `capabilityClient(cap)`, `roleListClient(roles)` — built on `next-safe-action@8` + `zod@4`); 22 Server Actions across `people/`, `schedule/`, `email/`; 8 form callers wrapped with `toLegacyResult` adapter; production export Route Handler kept on imperative `assertCan` per Open Question 6. Test mocks updated for `getUser` + `loadCurrentMembership` + Next-redirect-shaped errors so `isNavigationError` recognises them. 0034 pairing script extended with `capabilityClient('X')` regex. 450/450 vitest still green (was 441 pre-0033 — added 9 action-client tier tests). The 0031 lint rule's coverage now scopes to legacy `export async function` shapes (the safe-action middleware enforces gate-presence by construction); documented in auth.md. Carry-forward Phase 4 (write 12+ Zod schemas, delete `validators.ts`, retire `toLegacyResult`) — revisit when 0016 forces public-input validation.
- **2026-05-08** — `0034-can-assertcan-pairing` → Done (commit `47df3f6`); active plan switched to `0033-next-safe-action` (last in the authz-tooling sequence; afterwards 0025/0026 resume). Three phases shipped same-session: extraction script (`scripts/check-capability-pairing.mjs` — Node ESM, no deps), set diff → exit non-zero on asymmetric gates, wire + document. **Real UI gaps surfaced**: the script's first run flagged 5 server-only capabilities, three of which were genuine `<Can>` omissions (the page-level `requireRole('admin')` was carrying the load). Wrapped `+ Add Person`, `+ Add Dealer`, and the orphan `Adopt` button with `<Can capability=...>`. Two legitimately server-only — `production:export` (Route Handler) and `coach-availability:edit-own` (row-relative server check) — opted out via per-line `// expected: server-only`. Sabotage smoke confirmed both directions of asymmetry are caught (UI-only + server-only). 14/14 capabilities now paired or opted out. 441/441 vitest still green; tsc + lint + new check all clean. **Three layers of structural enforcement now active**: 0031 lint catches gate-missing, 0032 test catches wrong-admit-set, 0034 script catches asymmetric. The bulk safe-action migration (0033) can proceed under all three safety nets.
- **2026-05-08** — `0032-action-matrix-test` → Done (commit `3648eec`); active plan switched to `0034-can-assertcan-pairing`. Three phases shipped same-session: matrix data file (24 rows), test harness (24 × 7 = 168 outcome assertions + drift detection), wire-in + close. Codex review caught two Highs (4-role matrix admit-set blind spot — fixed by extending `RoleKey` to all seven roles `unauth/admin/staff/coach/viewer/dealer/orphan`; drift-detection regex missed const-async / default-async exports — fixed with five export-shape patterns) and Medium 2 (file-discovery brittleness — extended to `*/actions/**/*.ts` shapes + comment-tolerant `'use server'` detection); all three fixed in-eval. Medium 1 (deny-not-tied-to-gate) parked as known caveat — no current Server Action issues post-gate redirects, but if one ever does, a wrong admit could be masked by a downstream `redirect:/` that the harness can't distinguish from a gate denial. AST-level gate-call telemetry is the carry-forward. 441/441 vitest (was 272 pre-0032; +169 new). Sabotage smoke confirmed: flipping `cancelCampaign` to admin+staff makes `staff → redirect:/` row fail, as designed.
- **2026-05-08** — `0031-action-gate-lint` → Done (commits `55203ca` + `ce40ece`); active plan switched to `0032-action-matrix-test`. Three phases shipped in the same session: custom ESLint rule + AST traversal (`eslint-plugins/action-gate.mjs`); wired into `eslint.config.mjs` against `src/features/**/actions.ts` + `src/app/**/route.ts`; sweep + auth.md/log.md updates. Codex review caught three Highs on the v1 rule (gate-name-spoof via member access; default + specifier exports invisible; gate calls inside nested closures count) — all three fixed in-eval before commit, plus three more residual evasion vectors (member-property spoofing, dead-code-after-return, swallowed-gate-via-try/catch) addressed by the same tightening (gate must be a direct `Identifier` callee resolved to an `@/lib/auth/*` import; walk skips into branching constructs and stops at top-level Return/Throw). 33 rule tests + 272/272 full vitest pass; `pnpm lint` clean against the existing surface (only 4 pre-existing warnings). Folder stays at top level for now; will move to `closed/` after the rest of the 0032/0033/0034 sequence ships and a single sweep moves all four.
- **2026-05-08** — Re-ordered the authz risk-reduction sequence after an ordering critique. Original switch promoted `0033-next-safe-action` to Active; the critique pointed out the bulk-migration of ~20 gated actions is the highest-risk hour of authz work this quarter and shouldn't run without CI safety nets already in place. Scaffolded three new tooling chunks — `0031-action-gate-lint` (catches no-gate-at-all), `0032-action-matrix-test` (catches wrong-admit-set), `0034-can-assertcan-pairing` (catches asymmetric gates) — and switched Active to `0031` as the first in the new sequence. New order: 0031 → 0032 → 0034 → 0033 → resume 0025/0026. 0033 reverts to Parked; 0025/0026 stay parked. Skipped serial 0033 in the tooling triplet because it was already taken by next-safe-action — explained inline in 0034's plan. The three tooling chunks are each ~half-day; sequencing them ahead means CI rejects gate omissions / admit-set drift / Can-assertCan asymmetry *during* the bulk migration, mitigating the failure modes the migration is most likely to introduce.
- **2026-05-08** — Active plan switched: `0025-quote-to-payment` (with active sub-plan `0026-quote-pdf` mid-Phase-7.1) → Parked; `0033-next-safe-action` promoted to Active as a standalone plan (no umbrella). User committed to the safe-action migration as a risk-reduction investment ahead of the `0016` public-intake work; doing it now means 0016 inherits the convention rather than seeding it. 0025/0026 resume after 0033 ships. Same-day promotion (the plan was scaffolded earlier in this turn — see entry below). **Superseded same-day** — see top entry: 0033 demoted back to Parked, 0031 promoted to Active.
- **2026-05-08** — Scaffolded `0033-next-safe-action/plan.md` and added to Parked. Surfaced from a "how do we make authz mistakes harder to ship" research pass that synthesised the modern Next 16 / React 19 server-action authz literature (next.js auth guide, makerkit security tutorial, next-safe-action + zsa comparison). Shape decision: stay on imperative `assertCan` today; migrate when 0016 lands OR when the team grows past one developer; user picked **migrate now** as a risk-reduction investment. Five phases: action-client bootstrap → dealer CRUD pilot → bulk migrate ~17 actions → retire hand-rolled validators → wiki + smoke. Capability strings unchanged (forward-compatible with 0029); only the *invocation pattern* shifts (line-1 imperative → `.use()` chain). Serial 0033 picked explicitly to leave 0031 + 0032 free for the parked-but-not-yet-scaffolded action-gate ESLint rule, action-matrix negative tests, and Can/assertCan pairing CI check (the three other risk-reduction levers floated in the same conversation). Active sub-plan stays at 0026 — no focus switch.
- **2026-05-08** — Tightened coach surface (`4f07995`) — five Server Actions still admitting coach via `requireRole(['admin','staff','coach'])` (createDealer/updateDealer/createCampaign/updateCampaign/email-send helper) migrated to admin-only `assertCan` per role-purpose intent. Added 4 capabilities (`campaign:create`, `campaign:edit`, `campaign:cancel`, `email:send`); reused existing `dealer:create`/`dealer:edit`. UI affordances tightened: `+ Book Event` + event-detail's Email Client / Email Coach / Cancel Campaign / Edit all wrapped in `<Can>` with the matching capability. End-state coach surface: Calendar (read-only views + own availability blocks via Block Date) + Reports. 239/239 vitest. Wiki updated (auth.md role-purpose rewrite + matrix table + per-action gate row updates; new wiki/log entry). 17 capabilities in v1.
- **2026-05-08** — `0029-capability-layer` → Done as a same-day Parked → ship side-task (active sub-plan stayed at 0026); chunk moved to `closed/0029-capability-layer/`. Five phases bundled into a single commit. Phase 1 added the pure PDP `src/lib/auth/capabilities.ts` (13-cap string-literal union, admin-shortcut on `app_metadata.role || roles.includes('admin')`, resource-relative branch for `coach-availability:edit-own`) + the server PEP `assert-can.ts` (mirrors `requireRole`'s redirect-on-fail control flow); 50 new tests (43 pure + 7 mock-driven). Phase 2 migrated 12 Server Action call sites: 4 person CRUD → `person:create`/`:edit`/`:archive`/`:adopt-orphan`, `archiveDealer` → `dealer:archive`, 6 lookup admin actions → `lookup:edit`, `production/export` Route Handler → `production:export`. Multi-role staff sends (`createDealer`/`updateDealer`/`createCampaign`/`updateCampaign`), `cancelCampaign`, and the auth flow stayed on `requireRole` per Open Question 5 (no semantic gain). `availability-authz.ts` refactored to delegate the row-ownership predicate to `can(profile, 'coach-availability:edit-own', facet)` — soft-error `{error}` return contract preserved (Open Question 6). Tests updated: `people/actions.test.ts` mocked `assertCan` instead of `requireRole`; `availability-authz.test.ts` admin-no-DB-hit assertion dropped (admin path now goes through can()-with-membership-load, cached). Phase 3 added `CapabilityProvider` + `<Can>` / `useCan` in `src/components/auth/`; provider mounted in `(app)/layout.tsx` fed from the existing cached `loadCurrentMembership` (no extra DB hit). Component test for `<Can>` deferred — vitest is `environment: 'node'` and `@testing-library/react` not installed; predicate test coverage in `capabilities.test.ts`/`assert-can.test.ts` is comprehensive (carry-forward: install `@testing-library/react` + `happy-dom` if a future component-rendering question lands). Phase 4 wrapped admin row-actions: dealer Edit + ✕ in `dealers-columns.tsx`, person Edit + Archive in `people-columns.tsx`, lookup Add form + Rename + ✕ in `lookup-admin.tsx`. `app-nav.tsx` deliberately stayed on `isAdmin` boolean (server-derived) rather than migrating to `<Can capability="…:view">` — nav is essentially-static and a hydration round-trip for the admin-dropdown reveal would be churn for no UX gain (documented in `auth.md` § Capability matrix). Phase 5 updated `auth.md` (3 layers → 4; new "Capability matrix" subsection; per-action gate matrix rewritten), `security.md` (layer 3 renamed + intent-vs-enforcement caveat inlined; availability special-case updated to mention `can()` delegation), `wiki/log.md` entry. 227/227 vitest (was 172 pre-0029), tsc + lint clean throughout. Browser-smoked all three row-action surfaces as admin: `/dealerships` → Edit + Remove buttons render per row; `/admin/people` → Edit + Archive render; `/admin/lookups` → Add form + Rename + Archive render. Coach-only smoke deferred (same fixture-not-wired carry-forward as 0028); verified by code-trace through `can()`. Capability count: 13 in v1; future expansion path documented (`campaign:cancel`, `email:send` if those gain coach/staff carve-outs; `coach-availability:edit-any` already in the union for symmetry though only admin holds it today).
- **2026-05-08** — `0028-role-route-scoping` → Done as a same-day Parked → ship side-task (active sub-plan stayed at 0026 throughout); chunk moved to `closed/0028-role-route-scoping/`. Three phases bundled into a single small commit (43 lines, 6 files): edge `ADMIN_PATHS` extended to `['/admin', '/production', '/dealerships']` with two new test cases pinning the admin-only assertion + a leftover non-admin assertion now covering `/reports`; page-level `requireRole('admin')` added to `production/page.tsx` + `dealerships/page.tsx`; `production/export/route.ts` tightened from `requireStaffAccess()` to `requireRole('admin')` (matches the page gate, closes the Route-Handler bypass that 0018's late Critical established the pattern for); `app-nav.tsx` Tab type extended with optional `requiresAdmin` flag, applied to Production List + Dealers, with a one-line `OPERATIONAL_TABS.filter` keeping the dropdown shape clean. Plan adapted on-the-fly: original Phase 3 instructions ("add `admin: true` to tabs, rely on existing filter") were stale post-2026-05-08-AM dropdown reshape (`8910a2b`) — replaced with the new `requiresAdmin` flag pattern; Production + Dealers stay top-level for admins (operational-shape, daily-use) rather than getting buried in the settings-shaped Admin dropdown. Reports left out of scope — already gated `['admin', 'coach']` and the plan's "coach = Calendar only" body claim contradicted that gate; updated `auth.md` to reflect actual coach surface (Calendar + Reports), closed the now-stale gap paragraph, kept Reports' multi-role gate alone. Smoke green: tsc/lint clean, 172/172 vitest (was 171; new middleware test cases brought it up by one), browser-verified `/production` + `/dealerships` render for admin and Admin-dropdown trigger persists across both. Coach-only smoke deferred (no coach-only test fixture wired today — verified via code-trace: middleware redirects non-admin off `ADMIN_PATHS` at line 59, page gate redirects on role mismatch as second layer, nav filter hides tabs as third layer). Closes 0027 Phase 2 Codex Medium implicitly (staff-visible `✕` on `/dealerships` is moot once non-admins can't reach the page).
- **2026-05-07** — `0030-nav-bar-improvements` → Done same-day side-task; chunk moved to `closed/0030-nav-bar-improvements/`. Four phases shipped + Phase 4 (responsive collapse) skipped per the plan's working assumption (no overflow at 1280px after the user-menu reclaimed bar width). Phase 1 contrast bump (`9718a4d`), Phase 2 Radix DropdownMenu user-menu (`ca8ba48`) — added `@radix-ui/react-dropdown-menu ^2.1.16`; collapsed the inline email + standalone Sign out behind a single avatar trigger; form-in-menu-item submission hardened to `onSelect` + `e.preventDefault()` + `requestSubmit()` (canonical Radix pattern) after Codex caught the unmount-vs-submit race. Phase 3 admin/operational tab separator (`edff423`) — driven off a per-iteration `tab.admin && !prev.admin` predicate so future non-contiguous admin tabs work too (closed Codex Concern in-eval). Phase 5 smoke + closure (`6589356`) — three reference screenshots archived in the chunk folder; verified menu open/close cycle via the browse tool's `click-name` / `press-name` subcommands (the workaround for the Radix Trigger MCP a11y-snapshot ref gap surfaced in Phase 2). 171/171 vitest, tsc + lint clean throughout. Active sub-plan slot returns to `0026-quote-pdf`. Carry-forward (tooling, not product): browse-tool keyboard-flow exercise needs richer Radix-Trigger handling for end-to-end ArrowDown/Escape sequences. Phase 4 (responsive collapse) revisitable when 0028 / 0029 push admin tab count past ~3 OR a 7th operational tab lands.
- **2026-05-07** — Scaffolded `0030-nav-bar-improvements/plan.md` and added to Parked. Surfaced when the user shared a screenshot of the staff-app top nav and asked "what do you think" — review flagged four issues (inactive-tab contrast at AA boundary, admin tabs visually identical to operational tabs, email + standalone Sign out button eating ~30% of bar width, no responsive plan). Plan captures all four as five phases: contrast bump (one-line), Radix DropdownMenu user-menu replacing the inline pill (largest chunk; introduces `@radix-ui/react-dropdown-menu` if Open Question 1 resolves that way over reusing Popover), `admin: true`-driven separator between operational and admin tabs (forward-compatible with 0028/0029 admin surface growth), responsive-overflow spike (likely parks until a 7th tab lands), smoke. Active sub-plan stays at 0026 — no focus switch.
- **2026-05-07** — `0014-summary-reports` → Done end-to-end same day; chunk moved to `closed/0014-summary-reports/`. Four phases shipped in sequence: Phase 1 aggregation queries (`3f052aa`), Phase 2 Reports UI with tabbed dealer/coach/month/full-production views (`ebfa63f`), Phase 3 per-tab CSV export + print (`3f4643f`), Phase 4 route-handler test coverage (`155df20`). Active sub-plan slot returns to 0026-quote-pdf; umbrella flips back to 0025-quote-to-payment. The chunk also closed the parked Codex Medium from 0013 Phase 5.6 by extracting `csvCell` / `buildCsv` / `csvResponse` to `src/lib/csv.ts` with a hardened formula-prefix mitigation (`/^\s*[=+\-@]/` — covers leading whitespace including NBSP/BOM bypasses) and retrofitting `production/export` onto the shared helper. Each phase's eval caught Codex findings that were fixed in-eval rather than parked: Phase 2 dropped a dead `coachRowKey` helper, consolidated a duplicated month-formatter into `src/lib/dates.ts`, and (per user request) lifted DataTable sort/page state to `ReportsTabs` so tab switches preserve sort; Phase 3 hardened the CSV regex against whitespace-prefix bypasses and fixed a print-pagination trap by toggling `<DataTable>` to `getFilteredRowModel().rows` on `beforeprint`; Phase 4 added gate-before-loader ordering invariant + rejection-path test to the new route handler. 171/171 vitest, tsc + lint clean throughout. Carry-forwards (none blocking): aggregation tests upgraded to real DB fixture (paired with parked 0023 follow-up); `::int` cast overflow ceiling on `count()`/`sum()` if production totals exceed 2^31; `csvResponse` filename sanitization if helper ever takes user-controlled values.
- **2026-05-07** — Active sub-plan switched: 0026-quote-pdf (Phase 7.1 of 0025) → Parked; 0014-summary-reports (Phase 5.7 of 0004) promoted to Active. User redirected focus mid-session to ship the parked summary-reports work; umbrella also flipped back from `0025-quote-to-payment` to `0004-port-migration` to match the sub-plan's parent. 0014 Phase 1 (aggregation queries) shipped at `3f052aa` immediately before this switch — eval PASS with caveats (1 Codex Medium re test predicate-blindness, 2 Lows on `::int` cast ceiling and nullable `groupKey`; all carry-forward into Phases 2/4). Three remaining phases queued (UI / print + CSV / verification).
- **2026-05-07** — Scaffolded `0029-capability-layer/plan.md` and added to Parked. Surfaced from a "think hard about role access down to action buttons" research pass — synthesised the 2026 PDP/PEP literature (Aserto, Cerbos, Permit.io, AWS) and CASL/OpenFGA patterns into a project-fit shape: stay on RBAC at the floor (right scale for 3 roles), introduce a thin in-process PDP (`capabilities.ts`) so the role↔permission matrix lives in *one* file, and pair it with `assertCan` (server) + `<Can>`/`useCan` (client) as the two PEPs. The user's question was the prompt; the plan is the deferred follow-up to 0028's route-level tightening. Five phases: (1) `can()` + `assertCan` + tests, (2) selective Server Action migration (~10 sites where capability tightens intent), (3) `CapabilityProvider` + `<Can>` wrapper, (4) first UI adoption on row-actions, (5) wiki update + smoke. Open questions called out: capability naming (`subject:verb`), hide vs disable default for `<Can>` (hide), whether `availability-authz.ts` absorbs into `coach-availability:edit-own` (yes). Active sub-plan stays at 0026 — no focus switch.
- **2026-05-07** — `0027-lists-to-dealers` → Done; chunk moved to `closed/0027-lists-to-dealers/`. Five-phase chunk shipped over a single afternoon: Phase 1 chrome simplify (commit `539746f`) — dropped umbrella h1 + subtitle + redundant ListCard wrapper, renamed page header to "Dealers". Phase 2 DataTable + route move (commit `62de77d`) — TanStack DataTable mirroring `/admin/people` shape, plus mid-build route move `/lists` → `/dealerships` (user picked `/dealerships` over the plan's prior working assumption); 4 `revalidatePath` call sites + middleware test updated. Phase 3 DealerForm rewrite (commit `eadff5f`) — replaced bespoke `Field` helper with PersonForm-shape inline labels; deleted dead `list-actions.tsx`; archive-confirm copy fixed in-eval after Codex caught a misleading "contact links stay readable" claim. Phase 4 Radix Form adoption (commit `0564cb2`) — wrapped DealerForm + PersonForm standard text fields in `<Form.Root>` + `<Form.Field>` + `<Form.Message match="typeMismatch">` for email; user picked option (b) post-eval, added a 25-line `useTouched` hook inline in each form for blur-driven required messages (custom controls — Roles checkboxes + Combobox/Select dealer picker — coexist inside Form.Root without wrapping since they have no native `valueMissing` semantics). Phase 5 verified by cross-walk against the four prior phase evals (no separate eval). 150/150 vitest, tsc + lint clean throughout. Codex caught one Medium per phase that was either fixed in-eval (Phase 3 archive copy, Phase 4 blur-driven messages) or deferred to `0028-role-route-scoping` Phase 2 (Phase 2's staff-visible-but-admin-only `✕` button — pre-existing behavior, covered when 0028 page-gates `/dealerships` to admin). Folder cross-references swept: 0028 plan's Open Question 3 now points to `closed/0027-...`. Active sub-plan stays at 0026.
- **2026-05-07** — Scaffolded `0028-role-route-scoping/plan.md` and added to Parked. Surfaced from a discussion of role purpose: the user clarified "production is an admin concern, the coach goes to the store on the sales day." Captured the conceptual matrix in `docs/wiki/auth.md` § "What each role is for" (admin = ops/back-office; coach = field/event-day Calendar-only; dealer = customer-side, no staff access) and flagged the gap between intent and current state. Plan tightens three layers: edge `ADMIN_PATHS` middleware (`src/lib/supabase/middleware.ts:5`), page-level `requireRole('admin')` on `/production` + `/dealerships`, and nav `admin: true` flag on those two tabs (`src/components/app/app-nav.tsx`). Three phases. Has a soft dependency on 0027 Phase 2 (route move `/lists` → `/dealerships`) — plan adapts to whichever folder exists at start time. Open question called out: friendlier redirect target on rejection (today `requireRole` redirects to `/`).
- **2026-05-07** — Scaffolded `0027-lists-to-dealers/plan.md` and added to Parked. Surfaced when the user looked at the `/lists` screenshot and called out that "Manage Lists" is now really just the dealers (companies) — the page collapsed to one section after 0020 retired Sales Coaches, but kept the umbrella h1, the link-to-People-page subtitle, and the redundant `🏢 Dealerships 26` ListCard header. Plan migrates the page to mirror `/admin/people`: rename to "Dealers", drop the dead chrome, rebuild on the TanStack DataTable + Radix Dialog pattern that 0021 + 0024 established. Five phases: rename + chrome simplify, DataTable foundation, DealerForm rewrite to PersonForm shape, optional Radix Form adoption (carry-forward from 0024 Phase 4 — only run here if PersonForm migrates in the same pass), tests + smoke. Open questions: route move `/lists` → `/dealers` (working assumption: keep `/lists`); `loadDealers` co-location move from `schedule/` → `dealers/` (working assumption: re-export only, defer the move). Active sub-plan stays at 0026 (Phase 7.1 Quote PDF) — no focus switch without explicit user signal.
- **2026-05-07** — Promoted Phase 7 (Q→C→I→P loop) out of `0004-port-migration` into its own epic at `0025-quote-to-payment/`, and scaffolded sub-plan 7.1 at `0026-quote-pdf/`. Active umbrella switched from `0004` to `0025`; `0004` stays alive as a separate tracker for the remaining 5.7 + 5.8 parked work. Same edit dropped Phase 6 (domain cutover) and Phase 8 (legacy-secret rotation) from `0004` — both now tracked outside the port-migration umbrella. 7.1's frame: PDF templates for branded business documents stored in Google Cloud Storage, filled at runtime via `pdf-lib` (reconsidering the `@react-pdf/renderer` choice from `architecture.md`). Phase 1 of 0026 lays the GCS adapter + `pdf-lib` template loader as foundation for 7.2 (MSA) and 7.3 (invoice PDF) to reuse. Open questions: GCS vs. Supabase Storage for blobs; AcroForm-fill vs. coordinate-overlay; jsonb vs. normalized line items; cascade vs. manual gate from accept → contract send.
- **2026-05-07** — `0024-personform-radix-migration` → Done; sub-plan moved to `closed/0024-personform-radix-migration/`. Five-phase chunk shipped over an evening + the next morning: Phase 1 swapped the `src/components/ui/dialog.tsx` wrapper from `@headlessui/react` to `@radix-ui/react-dialog` (commit `891d569`) — Codex caught a real focus-restore High inline (no `Dialog.Trigger`, so Radix's auto-restore had no target); fixed via a `FocusContext` ref captured on open + restored in `Panel.onCloseAutoFocus`. Phase 2 replaced the three native role checkboxes with `<Checkbox.Root>` (commit `529be61`) — clean PASS, 10 Codex non-issues confirmed; the explicit hidden `<input name="roles">` rows that 0023 Phase 3 added became redundant since Radix Checkbox emits its own. Phase 3 introduced `src/components/ui/combobox.tsx` (cmdk + Radix Popover) for the dealer picker + Radix Select for the dealer-contact role (commit `3f07ddc`) — Codex caught a Medium inline: `Command.Item value={o.label}` would have made keyboard Arrow+Enter ambiguous on duplicate dealer names; fixed with `value={o.value}` (dealer ID) + `keywords={[o.label]}` for filter matching. Phase 4 deferred per the plan's decision tree (keep `useActionState` + toast; revisit when the `0016` booking-intake form lands so both forms migrate to Radix Form in one pass). Phase 5 ripped out `@headlessui/react` (commit `cb5da66`) — `grep -rn "@headlessui" src/` returns empty; both dialog call sites smoke-confirmed post-removal. Three in-eval Codex catches landed across the chunk; 149/149 vitest, tsc + lint clean throughout. The largest UX win is the typeable dealer-picker Combobox replacing the native `<select>` for the 50+-dealer list.
- **2026-05-06** — Picked `0024-personform-radix-migration` off Parked → in flight as Active sub-plan, immediately after 0023 closed. Five-phase chunk: dialog wrapper swap (Headless UI → Radix), Roles fieldset → Radix Checkbox, Dealers section → cmdk Combobox + Radix Select, optional Radix Form (deferred), tests + smoke. Surfaced after the user noticed the codebase was already on `@headlessui/react` and asked whether PersonForm could migrate to Radix. Going with the plan's working assumptions on all open questions: rip out HUI in Phase 5, Checkbox not RadioGroup, defer Radix Form. Largest UX win is the typeable dealer-picker Combobox (50+ dealers).
- **2026-05-06** — `0023-people-dealer-role` → Done; sub-plan moved to `closed/0023-people-dealer-role/`. Six-phase chunk shipped over a single afternoon: Phase 1 enum + parser whitelist (`drizzle/0005_team_member_role_add_dealer.sql` + `0006_is_staff_member_excludes_dealer.sql`; commit `d2a1344`) — Codex caught a real authz hole during eval (dealer aliasing to staff); fixed in-eval with `STAFF_APP_ROLES` whitelist. Phase 3 picked up before Phase 2 (commit `cdf554b`) to avoid the backfill-then-stale-UI race; the form-precondition discovery was that `4a4afbd refactor(forms)` had silently broken role-submission for hours. Phase 2 backfill (commit `c8c586e`) — 23 dealer-side contacts, zero truly-orphan. Phase 4 auto-assign (commit `ea3f522`) — Codex caught a re-grant-after-archive bug, fixed in-eval with `onConflictDoUpdate` un-archive. Phase 5 app-level invariant (commit `3056603`) — chose app-level over DB trigger for simpler rollout + cleaner carve-outs. Phase 6 verification (commit `3bfc37a`) — backfilled contacts render correctly in /admin/people; edit-dialog open hit a strict-mode collision (23+ Edit buttons), verified via snapshot + code trace instead. Six dated eval reports captured. 149/149 vitest, tsc + lint clean. Carry-forwards listed under Parked: dealer-role grant audit emission, production data triage, hybrid `dealer+admin/coach` confirm, schedule-actions vitest fixture, real form-submission test.
- **2026-05-06** — Picked `0023-people-dealer-role` off Parked → in flight as Active sub-plan, immediately after 0019 closed. Six-phase chunk: enum + parser whitelist for `dealer`, backfill of existing roleless dealer-side contacts, PersonForm Dealer checkbox + section gate + require ≥1 role guard, `createDealer`/`updateDealer` auto-assigning the role, DB-or-app-level "every contact has a role" invariant, integration tests + smoke. Surfaced when the user noticed dealer staff exist as roleless contacts and asked to gate the Dealers section on a new role. Migration numbering needs bumping (plan assumes `0004_*.sql` but `0004_worthless_titanium_man.sql` already shipped under 0019 — actual filename will be `0005_*`).
- **2026-05-06** — `0019-security-architecture` → Done; sub-plan moved to `closed/0019-security-architecture/`. Eight-phase chunk shipped over a single day: Phase 1 RLS baseline (`drizzle/0003_enable_rls.sql` — 11 tables, 22 policies, helper `is_staff_member()`; commit `ae3dbe3`), Phase 2 `requireRole(role|role[])` per-action audit retiring `requireUserId`/`requireAdmin` async wrapper (commit `04ad552`), Phase 3 coach availability row-level ownership check with TOCTOU-hardened WHERE (commit `278c429`), Phase 4 `audit_log` + `recordAudit()` best-effort helper wired into 6 emit points (commit `7a187cd`), Phase 7 email-send hardening: SITE_URL-only origin + booked-only status gate + inverted dev-redirect with case-normalised APP_ENV (commit `e7c497a`), Phase 8 wiki updates: rewrote `auth.md` RBAC section + new `Defence in depth` section + per-action gate matrix; updated `conventions.md` Drizzle/`supabase-js` section with the BYPASSRLS posture; new `docs/wiki/security.md` is the five-layer defence-in-depth map + threat models + grep cheat-sheet (commit `854dc8a`). Phases 5 (boundary-discipline `secrets-boundary.test.ts`) and 6 (MFA enablement) left parked with documented rationale. Each phase paired Codex adversarial review + in-eval inline fixes (Codex highs/mediums fixed before commit on every phase). Six dated eval reports captured in the chunk folder. 144/144 vitest, tsc + lint clean. Manual security walk-through verified RLS enforcement (3 tables × forged authenticated JWT → 0 rows) + Server Action gates (3 admin-required actions → redirect for non-admin via `require-role.test.ts` 8-case suite). Carry-forwards listed under Parked.
- **2026-05-06** — Scaffolded `0024-personform-radix-migration/plan.md` and added to Parked. Surfaced after the user noticed the codebase is already on `@headlessui/react` (not raw HTML) and asked whether the PersonForm could migrate to Radix. Plan frames it as a Headless-UI → Radix swap with `cmdk` Combobox for the dealer picker as the largest UX win. Five phases (dialog wrapper swap, Roles fieldset, Dealers section, optional Radix Form for inline validation, smoke verification). Strong sequencing preference: 0023 ships first so the Dealer checkbox lands before the Radix migration absorbs all three role checkboxes in one pass. Open questions: HUI rip-out vs. coexistence; Checkbox vs. RadioGroup (tied to 0023's mutual-exclusion question); Radix Form adopt vs. defer; future form-state library choice.
- **2026-05-06** — Scaffolded `0023-people-dealer-role/plan.md` and added to Parked (kept 0019-security-architecture as Active sub-plan; its Phase 1 shipped earlier today, remaining 5 phases in flight). Surfaced when the user noticed dealer staff exist as roleless contacts and asked to gate the Dealers section in the Person edit dialog on a new `dealer` role. Six phases: enum + parser whitelist, backfill of existing roleless dealer contacts, PersonForm Dealer checkbox + section gate + require-role guard, `createDealer`/`updateDealer` auto-assigning the role, DB-or-app-level "every contact has a role" invariant, integration tests + smoke. Open questions (mutual exclusion with admin/coach; whether `customer` is a sibling addition; trigger vs. app-level for the invariant; App-access semantics for dealer-only) called out in the plan.
- **2026-05-06** — Picked `0019-security-architecture` off Parked → in flight as Active sub-plan. Unblocked since `0018-user-system` shipped 2026-05-05 (role taxonomy + `requireAdmin` foundations). Phase 4 of the plan (boundary-discipline checks / secrets-in-bundle smoke test) was parked earlier today as low-marginal-value vs. cost; remaining 6 phases active.
- **2026-05-06** — `0022-saledayevents-logo-refresh` → Done; sub-plan moved to `closed/0022-saledayevents-logo-refresh/`. Three-phase chunk: dropped the new mark (`TILL SDE logo colour RGB-100.jpg`, 246×155) into `public/saledayevents-logo.jpg`, renamed off the old `saleday-logo.jpg`, swapped the three call sites (app-header, login, share-coach), and `git rm`'d the old asset. Browser smoke confirmed the new mark renders on `/login`, `/calendar`, and `/share/coach/1`. Pre-commit eval (eval-2026-05-05-1908.md) PASS — tests 100/100, tsc + lint clean (4 pre-existing warnings), Codex 0 findings. Bundled into commit `91fd228` as a single rename + swap. Active sub-plan slot vacated; the staff-app form-migration to React 19 primitives that ran in parallel landed as `4a4afbd refactor(forms)` outside any plan slot.
- **2026-05-05** — `0021-people-polish` → Done; sub-plan moved to `closed/0021-people-polish/`. Despite the mid-day pre-emption by 0022, the user picked up 0021 anyway and ran all five phases end-to-end. End state: `/admin/people` adopts TanStack Table (sortable headers + pagination + faceted filters) as the admin-table foundation; `firstName`/`lastName` surfaced on `AdminPersonRow` (no more `displayName.split` heuristic); page subtitle + "Team & contacts" subhead dropped; the `App access` checkbox dropped in favour of *implicit* sign-in (Admin/Coach role implies app access — convention now: "everyone who needs APP gets it by default, except dealer staff"); the previously-planned `app` chip reversed in the same review pass; `updatePerson` now lifts any active ban on Save when granting/maintaining roles (closes the in-app restore loop after Adam Godin got accidentally banned during the session); pre-submit confirm guards the role-removal-bans-auth-user transition; per-row archive confirm composed from facets (`drop coach role, end relationship with X (customer), and ban sign-in for …`); search box + Coach/Admin/Customer-side pills land on the table. Codex 1 Low (search-input aria-label) fixed in-eval. Two carry-forward items deferred for the future Production+Lookups port (keyboard-accessible sortable headers; Last-sign-in column visibility on filtered rows). Commits: `d0c6f04` → `cc480d5` → `aab1755` → `646e16a` → `0b0bb4b` → `6b4e96a`.
- **2026-05-05** — Scaffolded `0022-saledayevents-logo-refresh/plan.md` and picked it up as the active sub-plan. Pre-empts `0021-people-polish` (scaffolded earlier the same day, phases not yet started) — moved to Parked. Surfaced when the user dropped a fresh `TILL SDE logo colour RGB-100.jpg` (246×155, same dims as existing `public/saleday-logo.jpg`) and asked to rename the public asset off `saleday-logo.jpg` to match the new wordmark. Three call sites identified: `src/components/app/app-header.tsx:18`, `src/app/login/page.tsx:25`, `src/app/share/coach/[id]/page.tsx:44`. favicon + `<title>` metadata explicitly out of scope.
- **2026-05-05** — Scaffolded `closed/0021-people-polish/plan.md` and picked it up as the active sub-plan. Five phases: TanStack Table foundation on `/admin/people` (sortable headers + pagination + global/faceted filters), then the 0020 carry-forward UX polish (drop "Team & contacts" subhead, drop the four-sentence page subtitle, `app` chip, raw firstName/lastName from the query, per-row archive-confirm preview, confirm-on-disable-app-access, auto-uncheck role boxes when App access flips off, `Last sign-in` column visibility tied to population). User reversed my "stay native" recommendation by calling out muscle memory for grid behaviors and the desire to apply the same pattern to Production + Lookups in future polish chunks. User also called out the page subtitle as documentation-in-chrome that doesn't earn its place on every visit; folded into Phase 2 alongside the card subhead drop.
- **2026-05-05** — `0020-people-unification` → Done; sub-plan moved to `closed/0020-people-unification/`. End-to-end: `/admin/people` is the single entry point for managing humans (Add/Edit/Archive Person + adopt orphan auth users); `/admin/users` redirects there; Sales Coaches section gone from `/lists`; legacy Server Actions deleted (`createUser`, `linkUserToContact`, `setUserRoles`, `deactivateUser`, `createCoach`, `updateCoach`, `archiveCoach`); auth.users abstracted as a *facet* of a person via the App access toggle. Codex flagged blocker pairs each phase (TOCTOU + roles=[] coercion in Phase 2; lifecycle helper + partial-success contract in Phase 3; UUID validation + atomic per-orphan tx in Phase 4); all six fixed pre-commit. Final eval PASS with caveats (Phase 5 was docs-only; Codex + smoke skipped). Carry-forward Phase 3 UI follow-ups remain (auto-uncheck role boxes on App-access flip, confirm dialog on disable, raw firstName/lastName from query). Commits: `82e3564` → `1053257` → `33bfc51` → `3dc6ec0` → `a5c0ab0`.
- **2026-05-05** — Scaffolded `0020-people-unification/plan.md` and picked it up as the active sub-plan. Folds `/admin/users` + Manage Lists Sales Coaches into a single `/admin/people` flow; the data model already supports the unification (one `contacts` table, role facets) but the UI fragments the human across three pages. Surfaced while walking through the Tilley Shaye provisioning UX with the user — "yuck, the supabase user should be abstracted away."
- **2026-05-05** — `0018-user-system` → Done; sub-plan moved to `closed/0018-user-system/`. End-to-end: in-app `/admin/users` provisioning with three-tab Add User dialog (Create new / Pick existing / No link); `linkUserToContact` Server Action with TOCTOU-safe conditional UPDATE; auto-link trigger on `auth.users` insert; coach auto-filter on `/calendar` for signed-in coaches; role-aware `/auth/callback` routing PLUS a durable `requireStaffAccess()` gate in `(app)/layout.tsx` and `/production/export` that re-runs the same decision tree on every request. Two follow-up commits (`b4e3b6a`, `9231bd8`) closed Codex Critical + High + a new Critical (Route Handler bypass on `/production/export`); final eval PASS with warnings (81/81 tests, tsc + lint clean). 0017-user-admin moved to `closed/` as superseded. Two known follow-ups remain (Medium: `createUser` partial-success; Low: test-mock predicate-blindness) — not blockers.
- **2026-05-05** — Scaffolded `0019-security-architecture/plan.md` (RLS defence-in-depth + sensitive-op audit log + per-action role audit + email-send hardening + MFA enablement). Parked behind 0018 since it depends on the role taxonomy + `requireRole` helper that 0018 establishes. Surfaced after a discussion of the Drizzle-direct-vs-PostgREST trade-off — the staff app's drift from the original "Drizzle owns SQL, supabase-js owns RLS-bound reads" intent (`0002-nextjs-scaffold/decision.md:10`) is now an explicit re-alignment plan.
- **2026-05-05** — Scaffolded `0018-user-system/plan.md` and picked it up as the active sub-plan. Subsumes the parked `0017-user-admin` (which was unstarted) and extends to RBAC enforcement, contact-user linkage, coach auto-filter, role-aware login routing. Surfaced while provisioning Tilley Shaye and discussing the legacy `Users!A:E` parity gap. 0017 stays in Parked as `subsumed`; will be moved to `closed/` with a supersession note when 0018's Phase 6 ships.
- **2026-05-04** — `0015-calendar-grid-clamp` → Done; sub-plan moved to `closed/0015-calendar-grid-clamp/`. Eval-smoke PASS with warnings on 2026-05-04: 44/44 tests, tsc clean, lint clean (4 pre-existing warnings); browser smoke green across both phases plus cross-month visual smoke run on dev DB (throwaway fixture inserted via `scripts/calendar-clamp-smoke.ts`, both leading and trailing clamped ribbons rendered correctly across April/May/June 2026, cleanup verified). Codex Low (test gap for prior-month-strip-only and next-month-strip-only overlaps) closed in the same commit. Throwaway smoke script deleted with ship. Active sub-plan slot vacated.
- **2026-05-03** — Picked `0015-calendar-grid-clamp` off Parked → in flight. Code + unit tests landed in working tree; visual smoke pending (needs a cross-month-boundary fixture in Supabase, deferred until user confirms it's OK to insert + remove).
- **2026-05-03** — Scaffolded `0017-user-admin/plan.md` for the in-app User Admin page (replaces dashboard-only provisioning). Parked. Surfaced while manually provisioning Shannon — clear UX win for adding the rest of the team.
- **2026-05-03** — Scaffolded `0016-book-your-event-intake/plan.md` for the public-intake + marketing-site cutover work. Parked outside the port-migration umbrella (new entry-point work, not a 5.x phase).
- **2026-05-03** — Deployed HEAD `5825971` to Cloud Run revision `event-manager-pro-00003-drf` (100% traffic). URL: https://event-manager-pro-248904507231.northamerica-northeast1.run.app. Bundle includes Phase 5.6 export+print, SaleDay logo + favicon (RGBA-fixed), Drizzle HMR cache, docs/ tracking change.
- **2026-05-03** — Phase 5.6 (Production export + print) → Done in commit `2a42c93`; pushed to `origin/main`; sub-plan moved to `closed/0013-production-export/`. Eval-smoke PASS with warnings (1 medium CSV-injection + 3 low — captured in eval-2026-05-03-1024.md, parked for follow-up). Active sub-plan slot vacated; queued options remain 5.7 / 5.8 / slot-pack clamp.
- **2026-05-03** — Phase 5.6 (Production export + print) picked from Parked → in flight at `0013-production-export/plan.md`.
- **2026-05-01** — Phase 5.5 (Email send, Resend) → Done in commit `f963da7`; sub-plan moved to `closed/0011-email-send/`. Removed from Parked. Active sub-plan slot remains vacated; queued options now 5.6 / 5.7 / 5.8 / slot-pack clamp.
- **2026-05-01** — Phases 5.3 (Lookup admin) and 5.4 (Availability admin) → Done in commit `023ee01`; both sub-plans moved to `closed/`. Removed from Parked. Active sub-plan slot remains vacated; queued options updated.
- **2026-05-01** — Phase 5.2 (Campaign CRUD) → Done; sub-plan moved to `closed/0008-campaign-crud/`. Active sub-plan slot vacated; queued options listed under Parked.
