# Eval: 0039-quote-visibility Phase 5 (Tests + smoke verification — chunk closeout)

**Plan:** docs/designs/0039-quote-visibility/plan.md
**Branch:** main
**Date:** 2026-05-12

---

## Verdict: PASS

Phase 5 is the chunk closeout — verification phase, no code change. All Phase-1-through-4 deliverables are smoke-verified through the dev app, static gates clean, and Codex passes from each phase closed out on pass-2 verify (Phase 1 / Phase 2 / Phase 3 / Phase 4 all PASS in their dated eval reports). The chunk delivers the quote-visibility surface end-to-end: `/quotes` index → `/quotes/[id]` edit-mode → `/dealerships/[id]` per-dealer history → composer redirect-fix.

**Static checks:** tests PASS (680 + 1 skipped) / tsc PASS (exit 0) / lint PASS (exit 0)
**Browser smoke:** 11/11 plan-specific checks PASS, 2 skipped-by-design (sent-quote read-only, end-to-end create-save)
**Codex review:** No new Codex pass on Phase 5 (no code diff to review); Phases 1-4 Codex states are clean (each pass-2 returned 0 actionable findings).

---

## Static Checks

`pnpm test` — exit 0 (680/680 + 1 skipped; +7 new tests from Phase 1's `queries.test.ts`)
`pnpm tsc --noEmit` — exit 0
`pnpm lint` — exit 0 (6 pre-existing stylistic warnings; same set across the chunk)

All passed.

---

## Browser Smoke (consolidated, all phases)

`inject-supabase david.hogan@networknode.ca` — success.

| Check | Phase | Result |
|-------|-------|--------|
| `/quotes` heading + 5 pills (counts: All 1 / Draft 1 / Sent 0 / Accepted 0 / Declined 0) + search + table | 2 | PASS |
| `/quotes?status=accepted` empty-state ("No quotes match" + clipboard glyph) | 2 | PASS |
| `/quotes/1` from View click: header "← Quotes / Quote #1 / Draft pill", composer hydrated (audience 512, BDC 150, Letters 456, Digital 567, subtotal $8,748.03, total $8,748.03), `Save Quote` button | 3 | PASS |
| `/quotes/9999` → 404 via `notFound()` | 3 | PASS |
| `/quotes/1.0` → 404 (post-Phase-4 id-validation fix; pre-fix would have loaded id=1) | 4 | PASS |
| `/dealerships/1` header (← Dealers / abc motors / ACTIVE pill / address + email + phone subhead) + Quotes section + table | 4 | PASS |
| `/dealerships/2` (no quotes) → empty state + `Create the first quote →` link | 4 | PASS |
| `/dealerships/9999` → 404 | 4 | PASS |
| `/dealerships/1e10` / `/dealerships/0x10` → 404 (id-validation) | 4 | PASS |
| `/dealerships` dealer name now links to `/dealerships/<id>` (non-archived rows) | 4 | PASS |
| `/quotes/new?dealerId=1` regression-clean (heading "New Quote", Dealer Combobox, "Save Draft" button) | 3 | PASS |
| `/quotes/<sentId>` read-only banner — no fixture available | — | SKIPPED (documented) |
| End-to-end `/quotes/new` → save → `/quotes/<newId>` redirect | — | SKIPPED (dev-data hygiene; code-trace verified) |

Screenshots: `/tmp/web-test-quotes.png`, `/tmp/web-test-quotes-empty.png`, `/tmp/web-test-quote-edit.png`, `/tmp/web-test-quote-edit-v2.png`, `/tmp/web-test-dealer-detail.png`.

---

## Codex Adversarial Review

No Codex pass on Phase 5 — there's no new code diff to review. Phase-by-phase Codex states (per the dated eval reports in this folder):

| Phase | Pass 1 | Pass 2 | Closed |
|-------|--------|--------|--------|
| 1 (queries) | 0 High / 0 Medium / 1 Low (test-mock SQL-shape note — same as anchor file) | — | Yes |
| 2 (/quotes index) | 3 Medium + 2 Low | 0 findings | Yes |
| 3 (/quotes/[id]) | 0 High / 2 Medium / 0 Low | 1 Low (deferred per "trust internal writes" convention) | Yes |
| 4 (/dealerships/[id]) | 2 Medium | 1 Medium (verified false positive — `/^\d+$/.test('1\n')` is `false` in JS) | Yes |

Net: 7 Mediums total across the chunk, all self-resolved or verified false-positive. 0 outstanding actionable findings.

---

## Cross-Reference

No Codex finding overlapped with a static-check failure or browser anomaly across any phase. All correctness fixes (stale-state pushParams closure, searchParams array shape, nav-tab capability gap, archived-dealer dead link, lax id parsing, archived-dealer label mis-render, read-only catalog drift) were caught by Codex, not by the other gates.

---

## Action Required

### Must Fix (blockers)

None.

### Should Fix (Codex high/critical)

None.

### Consider (Codex medium/low)

1. **Defensive jsonb guard for `lineItems`** (Phase 3 Low, deferred). The `row.lineItems as unknown as ComputedLine[]` cast trusts the write path. Per CLAUDE.md → "Don't add error handling … for scenarios that can't happen." Promote only if a malformed legacy row ever surfaces.
2. **`pushParams` stale-state pattern** still lives in `src/app/(app)/production/production-filters.tsx` — only debounces `q`, blast radius is narrower than the original /quotes case. Same shape of fix; track if a production-filter regression is reported.
3. **Shared `parsePositiveIntPathSegment` util** — current 3-line dup across `/quotes/[id]/page.tsx` and `/dealerships/[id]/page.tsx`; promote to a shared util if a third dynamic-id route ever appears.

---

## Coverage Caveats

- Sent/accepted/declined read-only render path is verified by Codex reading the `display = isReadOnly && initial ? persisted : computed` discriminant in `quote-composer.tsx` — not by smoke (no non-draft fixture in dev).
- End-to-end save-and-redirect on `/quotes/new` is verified by code-trace of `quote-composer.tsx:152-159` → `router.push('/quotes/${result.quoteId}')` — not exercised live to avoid mutating dev fixture data.
- Coach-role visibility of `/quotes` + `/quotes/[id]` + `/dealerships/[id]` couldn't be exercised (auth-injected user is admin). Coach-gate matrix verified by reading `src/lib/auth/capabilities.ts:77-84` (`quote:edit` = admin || coach; `admin:access` = admin only); nav-tab filter at `app-nav.tsx:42-48` uses `ctx.can(t.capability)` which returns true for coach on `quote:edit`.
- Archived-dealer linkage from `/dealerships` table: verified by reading the cell branch at `dealers-columns.tsx:34-55`; no archived dealer in current fixture to click through.

---

## Chunk closeout

All 5 phases of `0039-quote-visibility` are **Done**. Next step per CLAUDE.md → "Closing a chunk": move `docs/designs/0039-quote-visibility/` → `docs/designs/closed/0039-quote-visibility/`, sweep cross-refs, update `CURRENT.md` to the next plan or `_None — pick a new plan_`.

The next plan in the recommended sequence (per `CURRENT.md` status line) is `0035-quote-composer` Phase 4 (PDF preview + Send wiring polish — low urgency, parked).

The chunk-close action is left for the next `/build` invocation (or the user's explicit decision) per the build skill's "Doesn't move folders to `closed/`" boundary.
